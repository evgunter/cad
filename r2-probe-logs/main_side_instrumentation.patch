diff --git a/crates/sweep/tests/all.rs b/crates/sweep/tests/all.rs
index 75e20f88..8cf127ce 100644
--- a/crates/sweep/tests/all.rs
+++ b/crates/sweep/tests/all.rs
@@ -399,6 +399,8 @@ mod m9_3_wall_door;
 
 #[path = "m9_3_zip.rs"]
 mod m9_3_zip;
+#[path = "mate2_cyl_rest.rs"]
+mod mate2_cyl_rest;
 
 #[path = "review_probes_m8_4.rs"]
 mod review_probes_m8_4;
diff --git a/crates/topo/src/boolean/reduce.rs b/crates/topo/src/boolean/reduce.rs
index bf3b779d..b4825ca8 100644
--- a/crates/topo/src/boolean/reduce.rs
+++ b/crates/topo/src/boolean/reduce.rs
@@ -1041,6 +1041,10 @@ fn curved_face_arm<T: Decide>(
             .flatten()
             .any(|f| declared.class_of(x_is, f, x_is.other(), face).is_some())
     };
+    // R2 REVIEW TRACE (main-side instrumentation; not for merge).
+    if std::env::var_os("MATE2_R2_TRACE").is_some() {
+        eprintln!("R2-TRACE curved_face_arm: x_is={x_is:?} edge={edge_key:?} face={face:?} covered={covered}");
+    }
     // NURBS walls (shape (iii)'s substrate): the SECTION arm is
     // certified since PR 7b (geom_brep::intersect::route says so),
     // but the boolean's CROSSING layer for the kind — edge×NURBS-face
@@ -1176,6 +1180,9 @@ fn curved_face_arm<T: Decide>(
                                 if !vertex_on_curved_face(
                                     x_is, y, w, pw, face, contacts, band, tol,
                                 )? {
+                                    if std::env::var_os("MATE2_R2_TRACE").is_some() {
+                                        eprintln!("R2-TRACE FRONTIER at the circle rung's endpoint loop (main reduce.rs:1179): edge={edge_key:?} face={face:?}");
+                                    }
                                     return Err(frontier());
                                 }
                                 any = true;
@@ -1625,9 +1632,13 @@ fn vertex_on_curved_face<T: Decide>(
     band: Band,
     tol: Tol,
 ) -> Result<bool, BooleanError> {
-    match super::contain::curved_face_containment(y, face, px, band)
-        .map_err(|e| esc(e, x_is.other()))?
-    {
+    let verdict = super::contain::curved_face_containment(y, face, px, band)
+        .map_err(|e| esc(e, x_is.other()))?;
+    // R2 REVIEW TRACE (main-side instrumentation; not for merge).
+    if std::env::var_os("MATE2_R2_TRACE").is_some() {
+        eprintln!("R2-TRACE vertex_on_curved_face: face={face:?} verdict={verdict:?}");
+    }
+    match verdict {
         Some(FaceContainment::OnVertex(vy)) => {
             push_vv(contacts, x_is, vx, vy);
             return Ok(true);
diff --git a/demos/tour/src/main.rs b/demos/tour/src/main.rs
index 2eb21a77..116f6569 100644
--- a/demos/tour/src/main.rs
+++ b/demos/tour/src/main.rs
@@ -753,6 +753,11 @@ fn main() {
     // document-authored scene saved as a `.pncad` the viewer can open.
     // Not behind a feature — it authors the same documents the ordinary
     // run does and links nothing extra.
+    // R2 REVIEW DISPATCH (main-side; not for merge).
+    if outdir == "lily-walls" {
+        lily::wall_probes::<f64>(tol);
+        return;
+    }
     if outdir == "gallery" {
         gallery::run(std::env::args().nth(2), tol);
         return;
