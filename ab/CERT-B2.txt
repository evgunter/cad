CERT-B2 (drawn 2026-08-30, v4 block-of-4 rule per MODEL-AB-LOG read
on main at dispatch: {opus, opus, opus, fable}, byte mod 4 = fable
position, reject >= 252)
byte=107 (od /dev/urandom, reject >=252), 107 mod 4 = 3 = fable position
slots: 0=CERT-5 (difficulty M/L, logged PRE-DRAW at spec
docs/CERT-5-SPEC.md, committed to cert/orchestrator at 3d7c8aeb
before this draw), 1..3 unassigned.
Arm for slot 0 = OPUS.
Branch-side block record per the v6 convention; reviewers are never
pointed here.
slot 1 = CERT-7 (difficulty M, logged at spec docs/CERT-7-SPEC.md on
cert/orchestrator before this assignment; block drawn in full at
creation, same recorded posture). Arm for slot 1 = OPUS (byte 107,
fable at position 3). Sequencing note (orchestrator decision, logged
in docs/S-CERT-LOG.md at the next seam): CERT-7 dispatches AHEAD of
CERT-6 — CERT-6 calibrates the A2 gauge on the area lanes CERT-5's
unmerged PR rewrites, so it waits for CERT-5 to land; CERT-7's
offset_fit.rs is disjoint from everything in flight.
slot 2 = CERT-6 (difficulty S/M, logged at spec docs/CERT-6-SPEC.md
on cert/orchestrator before this assignment; block drawn in full at
creation, same recorded posture). Arm for slot 2 = OPUS (byte 107,
fable at position 3). Dispatched after CERT-5's merge per the
recorded sequencing (the A2 gauge calibrates on the post-CERT-5
area lanes).
