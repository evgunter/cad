CERT-B3 (drawn 2026-08-31, v4 block-of-4 rule per MODEL-AB-LOG read
on main at dispatch: {opus, opus, opus, fable}, byte mod 4 = fable
position, reject >= 252)
byte=27 (od /dev/urandom, reject >=252), 27 mod 4 = 3 = fable position
Both difficulties logged PRE-DRAW at spec (docs/CERT-8-SPEC.md M/L,
docs/CERT-10-SPEC.md M/L, committed to cert/orchestrator at d90e7441
before this draw).
slots: 0=CERT-8 (chart-stretch honesty, issues 501+528),
1=CERT-10 (patch-hull consolidation, issue 1006 under Q2),
2..3 unassigned (CERT-M/CERT-N track lanes are the expected
claimants if they run as implementation lanes).
Arm for slot 0 = OPUS (byte 27, fable at position 3).
Arm for slot 1 = OPUS (byte 27, fable at position 3).
Sequencing note: CERT-8 and CERT-10 dispatch CONCURRENTLY — their
edit sets are file-disjoint (CERT-8: topo/pcurves.rs,
topo/chart_region.rs, geom-brep/pcurve_cache.rs; CERT-10:
geom_core/spline, geom-brep/patch_bound.rs + props/quad.rs +
offset_fit.rs call sites, mesh/nurbs_cert.rs), with explicit
cross-lane fences written into both specs; the orchestrator owns
any collision (CERT-8's export deliberately kept out of
geom_core::spline).
Branch-side block record per the v6 convention; reviewers are never
pointed here.
slot 2 = CERT-M1 (Track M's trait-ground rows: H3+H4, H10+S211, D78,
D221 — H5/S90-impl/S213 deferred to CERT-M2/M3; difficulty M, logged at
spec docs/CERT-M1-SPEC.md on cert/orchestrator at bacc06e27 BEFORE this
assignment; block drawn in full at creation, same recorded posture).
Arm for slot 2 = OPUS (byte 27, fable at position 3).
slot 3 = CERT-N1 (Track N's scalar-lift lane: D240+D241 with D242,
D243, C24 — H2 (ADV) and S235/D98/D31 deferred to CERT-N2/N3;
difficulty M, logged at spec docs/CERT-N1-SPEC.md at bacc06e27 before this
assignment). Arm for slot 3 = FABLE (byte 27, fable at position 3 — the
block's fable slot). Block CERT-B3 is now fully assigned.
Sequencing note: CERT-M1 dispatches immediately (its fence is disjoint
from CERT-10's in-flight files); CERT-N1 dispatches after CERT-10 merges
(D243 sits in geom-core/src/spline/, beside CERT-10's TensorNet home).
