CERT-10 dual (v6), recorded BEFORE R1 dispatch, 2026-08-31
ordinal: TO BE CLAIMED on main at actual R1 dispatch (S-CERT band;
CERT-8's claim lands first if its dual dispatches first — take the
next free ordinal at claim time and record which).
slot byte 242, parity 0 => R1 OPUS + R2 FABLE (cross-model).
METHOD NOTE (pre-recorded so both arms run identically; AMENDED
once, pre-R1, per its own rule): reviews run SEQUENTIALLY on the
frozen head f5ab8bab — the delivered head a33926e5 redded BOTH
clippy jobs on 21 excessive_precision test literals in
nurbs_cert.rs (the lane's local clippy skipped -p mesh, a
verification-claim gap reviewers should weigh alongside the body's
"clippy clean" line); the tip is a one-file orchestrator-direct
truncation pass, each pair verified bit-identical, mesh 182/182
green after. Dispatch CONDITIONAL on
f5ab8bab's gate concluding green on PR 1403 (budget-refreshed; the
render-lane drift lands via the rebaseline convention and is NOT a
red for this purpose — the neutral-check derivation is PR body §4).
CERT-8's dual may run concurrently on ITS unit; the two duals'
grounds are file-disjoint mirrors of the implementation fences.
R2 after R1 with the IDENTICAL stored brief, isolated. Probe
branches: cert/10r1-probes, cert/10r2-probes (local until
close-out). Implementer difficulty M/L (logged at spec pre-draw);
impl ~319k tokens, ~142m wall, single lane, no restarts.

COMMON BRIEF (stored verbatim; lane plumbing differs per lane):
---
You are a blinded adversarial reviewer for unit CERT-10 of the
S-CERT program (repo evgunter/cad). Review PR 1403 on its FROZEN
head f5ab8bab — do not review any later commit; if the branch has
moved, say so and still review f5ab8bab. The tip commit past the
delivered head a33926e5 is a one-file ORCHESTRATOR-DIRECT clippy
conformance fix (21 pinned test literals in nurbs_cert.rs truncated
to f64's own precision — excessive_precision denies digits beyond
what the type holds; each truncation verified pairwise
bit-identical) — verify that delta yourself, including the
bit-identity claim.

Read first, in order: docs/prompts/reviewer-style-lane.md (binding
style-lane brief; carry its structural questions and confidence
vocabulary), the unit's binding spec (git show
origin/cert/orchestrator:docs/CERT-10-SPEC.md — note the Q2 ruling's
decisions are RULED, so your target is execution fidelity and
soundness, not the decisions themselves), GitHub issue 1006 (and
1322 for the ride-along), memories/output-stability-as-justification.md,
the D2 addendum in docs/DESIGN.md, then the PR 1403 diff and body.

CLAIMS TO FALSIFY (each gets a verdict with evidence; execution
beats inspection; every number is the PR body's claim, not a fact):
1. THE SIGNED READING'S SOUNDNESS (highest-value item). The
   magnitude retirement rests on the signed quotient-rule
   evaluation being a genuine ENCLOSURE that is merely tighter —
   an 11x tightening on muv (1.266376e0 -> 1.152573e-1 on the
   quarter cylinder) is exactly where wrong-but-tighter would hide.
   Re-derive the rational arm's quotient-rule recurrence in the
   ring: does every step preserve containment (the true minus
   signs, the whole weight hull as divisor, the recentred
   A-tilde = A - c.w hull)? Construct an adversarial rational
   patch (extreme weight ratios, sign-alternating nets) and check
   the signed bound against a dense sample of the true second
   partials — a single sampled point above the bound falsifies the
   unit.
2. The coarser grids the tighter bound buys (8.4x fewer cells on
   the quarter cylinder at delta_s = 4e-3): does the tessellation
   still meet its chord tolerance on the coarser grid? Measure the
   realized chord error against delta_s on at least one re-sized
   rational face — the re-baseline is only owned if the coarser
   grid still delivers the certificate.
3. The fold's tighter-or-equal: the premise "the cell windows
   COVER the net" (is it true at every knot multiplicity and
   degree, including the refined rational grid?); the property
   test's coverage floor (360 nets, 640 strict — does the
   generator actually reach staggered-peak nets?); the whole-net
   counterfactual's faithfulness (reproduces pre-collapse digits
   EXACTLY — verify on one fixture against the pre-collapse
   commit's own output).
4. The cost table: reproduce at least the 4-cell and 625-cell
   rows' cost ratios in release; the disclosed earlier flattering
   reading (0.65-0.80x) is owned in the body — check the honest
   column is the one the decision cites, and weigh whether the
   trimmed.rs sharing (NurbsCellGrid::patch, the deviation) really
   removes a whole assembly per shipped face.
5. Behaviour-neutrality of the rehoming: 0 corpus columns moved
   merge-base -> shared-home (re-run the sweep or spot-check);
   PatchGrid's zero-fill for missing spans (is zero REALLY the
   containment-safe answer for its de Boor collapse and Dir::Const
   ladder, or can an enlarged hull cross a comparison somewhere?);
   patch_bound's poison-fill; the raw_deriv parameterization (the
   interior-multiplicity-equals-degree case — does the shared home
   reproduce its digits bit-for-bit?).
6. The re-baseline attribution: the four-sweep table (57 columns =
   main drift, 158 fold-moved with ZERO grew, retirement zero);
   worst cert/delta 0.124994 in BOTH cuts; the claim the corpus
   has no rational faces (64 nurbs rows, max cells 14). Check the
   csv diff against the table's totals (triangles -0.10% etc.).
7. Issue 1322's row as landed: does the pinned HullSup +
   bound.is_infinite() actually discriminate (would the pre-fix
   empty-body row have passed a wrong-limb regression this one
   reds on?).
8. The D2 classifications: the two row-3 poisons (shape mismatch
   over the DECLARED extent; out-of-range read), the row-0
   arguments, and the PatchGrid::base entrywise restoration (the
   disclosed first-draft semantics change — verify the shipped
   spelling matches the OLD reachability, not the draft's).
9. The sweep table: spot-verify three "not this unit" rows
   (mesh/chords.rs curve differencing; compose/patch.rs:135 basis
   conversion; pcurve_cache.rs:2676 — fenced, CERT-8's concurrent
   ground) and the stated blind spots' honesty.
10. The eps/lane posture: trailer premise vs scripts/ci-filter.py;
    the per-band premise split (net rows tolerance-free by
    construction vs cert10 rows riding Tol::witness()) — run one
    of each at all three bands yourself.
Also: Q3 (do the rows exercise the new arithmetic on constructed
inputs?), Q7 (TensorNet's API shape — the parameter-heavy signature
vs two thin wrappers), Q8 (how much of nurbs_cert.rs's 878-line
diff did you read?).

RULES: work in your own worktree from the frozen head; do NOT push
to cert/10-patch-hull; put probes on your assigned probe branch
(local commits; the orchestrator collects them); no GitHub writes of
any kind; no Co-Authored-By; serialize heavy builds through
/home/user/cad/local-scripts/with-build-slot.sh; foreground
discipline (never wait on a background notifier — poll); check
df -h / before big builds and clean your own target dir if free
space nears 8G. Verdict ladder: APPROVE / APPROVE-WITH-FIXES /
NEEDS-MAJOR-ATTENTION-ITEM / REJECT, with per-item MAJOR/MIN/NOTE
severities, executed-vs-inspected marked per item, and the rubric
scores per the style lane. Report <=150 lines.
