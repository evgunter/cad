# Dual Opus review — reviewer concordance log

**This is process data (an experiment log), not a design
reference** — nothing here binds kernel design; it moves out of
`docs/` when the experiment concludes.

Standing experiment (Ev, in-chat, 2026-09-23), opened the day the
model A/B protocol was suspended (`docs/MODEL-AB-LOG.md`, the
suspension entry). **The protocol in force lives in
`docs/DUAL-REVIEW-PROTOCOL.md`.** This log holds the rows; each names
the protocol it was recorded under by commit hash, so `git show
<hash>:docs/DUAL-REVIEW-PROTOCOL.md` reads that version.

## Rows

Columns: **protocol** (commit hash of the protocol in force at merge — the last
commit on main that touched `docs/DUAL-REVIEW-PROTOCOL.md`); **unit** (program, PR); **class** (difficulty S/M/L and task
class, logged at spec time) and the **triage reason**; **head** (the
frozen commit); **R1** and **R2** (verdict, MAJ/MIN/NOTE, one line of
prose per MAJOR, tokens, wall-clock); **correspondence** (bilateral
headline; unilateral findings R1→ and R2→; severity divergence);
**tally** (candidates, with the 6(a)–(e) disposition of each);
**fair** (yes, or the relaxation / interruption that excludes it);
**fix pass** (size, and who executed it).

| # | date | protocol | unit | class · triage reason | head | R1 | R2 | correspondence | tally | fair | fix pass |
|---|------|----------|------|-----------------------|------|----|----|----------------|-------|------|----------|
| DR-1 | 2026-09-24 | `c3129311b` | TOPO, PR #3148 — the fan `mev`'s re-basing gate and null edges (`a-null-edge-can-be-re-based-onto-a-distinct-point` + `the-re-basing-gate-refuses-m7-8-where-nothing-moves`) | M / STRUCTURAL · the unit decides what a kernel gate may ask about "nothing moved" with no point-identity door at `T: Real`; one shape was a structural-identity door on `geom-core`'s ratified surface — an architectural decision hard to change later (recorded at spec time) | `36c7d0f36` | APPROVE-WITH-FIXES, 0/6/6. No MAJOR. 268,769 tokens, 31 min (harness) | APPROVE-WITH-FIXES, 0/6/4. No MAJOR. 229,973 tokens, 52 min (harness) | Coded attribution-stripped, byte 233. BILATERAL, all executed: the both-halves refusal's stated reason false and the one-half mutant surviving; the null-vs-carrier run order unpinned; `Display`/variant doc claiming what the gate cannot know; the M7-8 no-move spelling oversold (its row asserts tier 1 only); the filed `kev` over-refusal hypothetical (zero measured instances); the stale-curve-key drive-by unpinned (MINOR vs NOTE, severity divergent); plus `program.md` rot, `19032e09f` authorship read as ratification, `needs_ev` unscheduled, a `NullScaffoldCurve` sibling, the rationale restated in five or six homes, and a structural both-halves test preferred (style/notes). UNILATERAL R1→: the production bit compare `topo::query::same_point_bits` (in `rim_of`, unratified, invisible to the bit-identity gate) — MINOR, claim class, orchestrator-verified; it contradicts R2's "no door exists, shape 1 forced"; zip's 58 ulp-distinct kills (inside MINOR); the `mev_null` control's F9 attribute (unsure). UNILATERAL R2→: the two-call spelling is not atomic (inside MINOR, executed); "bit for bit" spelled two ways (style); the "wherever" row's second point pins nothing current (style; R1 judged it fine) | No candidate: neither review raised a MAJOR (6(a)). Running: 0 | yes — identical briefs modulo lane paths (sha256 stored), no relaxation, neither interrupted; both disclosed seeing the head commit's co-author trailer (a model name; not a finding of the other review) | ten items, all taken, none refuted, by one fresh Opus lane (299,225 tokens, 77 min harness): the refusal refined to one-half-in-the-run, run order and drive-by pinned, rationale given one home and corrected for `query.rs` (filed on TQUERY), M7-8 asserted through `Body::mev` with tier-3 reading and non-atomicity rowed, `kev` evidence corrected and its probe a test; head `1baa7735e` run 35964734053 green, full matrix. The implementer (Opus): 262,570 tokens, 151 min (harness) |
| DR-2 | 2026-09-24 | `c3129311bd` | SCALAR RING-3, #3153 (`RingInterval` dissolves into `Interval`) | L, architectural · broad (92 files), hard to reverse, refusal surface invisible to the corpora | `4a29d75258` | APPROVE-WITH-FIXES, 0/4/6 (+10 style); 339,501 tokens, 42 min (harness) | APPROVE-WITH-FIXES, 0/3/4 (+style); 367,406 tokens, 26 min (harness) | Bilateral (11 groups): census re-key narrows coverage (MIN/MIN, both by execution: a plant in a no-door file reds the base census, not the head's); stale ring prose incl. ratified C2 (MIN/MIN, inspection); PR numbers stale (NOTE/NOTE). R1→ (8): `ssi/certify.rs` certifies from a `Trv` pcurve via NaN→first span, pre-existing (MIN, exec); `nurbs.rs:456` pinnable white-box (MIN, exec); `Bounds::hi(x)` UFCS escapes the census (MIN, exec); differential cannot see the doors, `from_certified` doc false at ∞, shared target dir hazard (NOTE). R2→ (6): empty bracket reaches `TensorNet` hull through public API — the PR's claim false, R1 answered it the other way (MIN, exec); unreachability reason not at the `nurbs.rs` site, 727 not 726, `from_certified`'s unstated `Bounds`/`certified_bracket` agreement (NOTE). Divergence: hull guard unit-only (R1 style / R2 MIN); `is_poison` with `Real` in scope silently passes (R1 NOTE / R2 style); transcendental-free rule a convention (R1 style / R2 NOTE). Coded attribution-stripped by a separate Opus coder (82,381 tokens), byte 232. | none (no MAJOR on either; every group fails 6(a)) | **no** — method divergence: R2's mutation reverts were refused by the session's permission classifier ("Security Test Removal") and R2 substituted compile probes and differential runs; R1 ran its reverts (not an orchestrator relaxation; items 4, 6(e)). R2 disclosed a glimpse of R1's scratch path names in `git worktree list` (no contents). | nine items, all taken, none refuted, by one fresh Opus lane (395,515 tokens, 141 min harness; 20 files, +498/−79 over the merged main): census population = door-callers ∪ the base's 26 holders (a vanished holder reds); `ssi/certify.rs`'s chart tube refuses a refused span hull (a false certification now refuses; R1's probe the row; SSI corpora unchanged); a `TensorNet` consumer row for the `hull` guard; the `nurbs.rs` white-box pin; prose in the extended fence incl. C2 (naming-only, for Ev); `Interval` docs; hygiene filed. One hosted red on its first head was main's (`f5_insert_refine_elevate…` fuzz, seed `0x827507a3dc90eac7`, bit-identical on main): tolerance scaled by the knot-gap conditioning (measured bound ×17), kernel conditioning filed on NURBS. Head `32a0a34c6c` run 35982519765 green, full matrix (37 + 2 cache-prime skips). main (RING-4, #3154) merged in at `a0cef634a9` (five conflicts, docs/tests; one stale `interval` cfg dropped), run 36004282466 green. The implementer (Opus): 609,737 tokens, 314 min (harness) |
| DR-3 | 2026-09-25 | `c3129311b` | DECIDE, PR #3192 into `props/sign-hull` — rule D past the unit bulge: what stands at bulge ≠ 1 attributed per decision, rule G's exact quotient (`rule-d-reaches-the-unit-bulge-only`) | M / NUMERIC · the unit changes what the tier decides across a family (a third of the tour), and a value-free rewrite at the root door is where the record has found unsound folds before (recorded at spec time, `work/decide/log.md` 2026-09-24) | `334bb2aa2` | APPROVE-WITH-FIXES, 0/7/4 (+style). No MAJOR. 286,693 tokens, 52 min (harness) | APPROVE-WITH-FIXES, 0/5/5 (+8 style). No MAJOR. 291,913 tokens, 51 min (harness) | Coded attribution-stripped by a separate Opus coder (72,688 tokens), byte 19. BILATERAL (10 groups), all but G6 executed: the quotient re-keys a root so it no longer meets rule G's split spelling (MIN/MIN); the `Q·D == N` check an equivalent mutant (NOTE vs MIN — R1 adds it refuses a correct quotient over the 4096-pair budget); the `QUOTIENT_STEPS` justification false (NOTE vs MIN); the leaf-cost rises single-take noise (+22–32% committed, ~+3–7% best-of-3; MIN/MIN); "every class re-checkable from the renders" false (the renders cut at 420 chars; MIN/MIN); stale dispatcher premises (NOTE/NOTE); plus the monomial helpers copying `signed.rs`, two meanings of "leading", the conjunction unspelled, deviations 4/5 unscheduled (style/style). UNILATERAL R1→: the multivariate order untested in-tree (MIN, mutants); the renders file the base tree's (MIN, insp); the step fires far beyond "a root" (NOTE, exec); the gated line redundant (NOTE, exec); the four-source list incomplete (NOTE); stale paths and more (style). UNILATERAL R2→: the negative row's t<0 half vacuous (MIN, exec); pin prose contradicted by the PR's own attribution (MIN, insp); the 2b recommendation absent from the PR (NOTE, unsure — it is on #3186); route A's counts predate 2a (NOTE, exec); four style | No candidate: neither review raised a MAJOR (6(a)). Running: 0 | yes — identical briefs modulo lane paths (sha256 stored), no relaxation, neither interrupted; R1 disclosed a name-only glimpse (`git branch -a` printed the other lane's branch name; nothing read) | the union A–L plus a second pass, all taken but one declined with its reason (the redundant `gated` line kept as defence in depth), none refuted, by the implementer lane resumed (171k tokens, 100 min, then 39k, 35 min, harness): the monomial order, division and leading term given one home in `form.rs` (rules C and E moved onto it, bit-identical per R1's delta); the product check dropped for the loop invariant; the trade disclosed, pinned and filed (`the-exact-quotient-re-keys-a-root-the-split-met`, P2); the renders re-taken uncut at the head; best-of-3 costs (+0–3%); the ceilings at 1e-6/1e-12. A delta by R1 on `bfb059b7f` (87,622 tokens, 32 min): APPROVE-WITH-FIXES, 0/3/2 — the budget-sized step cap made one declined division cost 0.78 s at the drive's budget (measured); fixed at `de02c07a1` (two necessary monomial conditions before any step, a step linear in `|d|`: 0.3 ms), run 36079428012 green. The implementer (Opus), first pass: 481,212 tokens, 250 min (harness) |
| DR-4 | 2026-09-27 | `c3129311b` | ATREST, PR #3191 — tier 3′ decides check 7 at SIGN level through one `plus_v_by_sign` (per solid, assembled), and the tier-3′ certificate doors return the `SignCertificate` the check decided on (`tier3-prime-still-couples-plus-v-to-the-reporting-target` + `the-multi-solid-reporting-quadrature-is-unscheduled`) | L / STRUCTURAL · a public return-type change across several crates, and the decision of what every tier-3 door promises — broad and hard to reverse (recorded at spec time) | `97b51065e` | APPROVE-WITH-FIXES, 0/4/4 (+style). No MAJOR. 226,055 tokens, ~42 min (harness). Executed claims 1–3 at f64 incl. parts stopped at DIFFERENT rounds, assembled and continued bitwise; measured an assembly's double check-7 read (8 vs 4) | APPROVE-WITH-FIXES, 0/4/5 (+style). No MAJOR. 224,523 tokens, ~26 min (harness). Executed claim 1 over offset/thin/exhausting pairs, claims 2–3 at f64, D-C at a `Dual`; could not build a round-split part | Coded attribution-stripped by a separate Opus coder (42,608 tokens), byte 186 (parity 0). BILATERAL: `EnclosureUncomputable` an import-side refusal of an admitted body, unscheduled (MIN/MIN); stale prose citing removed names (MIN/MIN on step-import; Style vs MIN on the k-lint/K-REPORT `sign_certified` citations); `plus_v_by_sign`'s "same scalar" overclaim, true per lane (NOTE vs MIN, both executed); an assembly derives check 7 twice (MIN executed vs NOTE read); near-twin hooks; `assembled`'s shared-parameters invariant in prose; the `(Vec, Option)` pair; the relaxed-impl continuation unpinned; the module header's certifying list. UNILATERAL R1→: the round-splitting path pinned by no committed row (MIN, test-gap); `import_step` `# Errors`; the per-instance gate's reason gone since ATREST-1 (NOTE, an `[ev]` question); cost wording right only for round-0 settlers; a refusal can name a different face; a `mass_properties` PANIC on a translated arc prism (`quad.rs` A2 tripwire, NOTE, EXECUTED, out of fence). UNILATERAL R2→: `gate3` throws away the certified bracket and conflates budget with escalation — the filed ENCL defect in a public type (MIN, read); the stale `validate.rs` 240–245 tier-3′ sentence; the per-window setup cost unpriced; an undisclosed behaviour change (a hard refusal after the sign settled now admitted); the enclosure doc at a `Dual`; a stale `work/perf` path; old test-file names | No candidate: neither review raised a MAJOR (6(a)). Nearest miss recorded: R1's executed panic, raised as an out-of-fence NOTE (filed on QUAD). Running: 0 | yes — identical briefs modulo lane paths (sha256 stored), no relaxation, neither interrupted; R1 saw build-slot pid lines only. The coder read orchestrator-condensed transcriptions of both reports, applied to both alike | the union, all taken, none refuted, by the implementer lane (resumed): import no longer refuses an admitted body — `EnclosureUncomputable` removed and `StepImport::Solid::enclosure` carries a `Result`, honouring DESIGN import step 4, with ONE classifier (`SignCertificate::measure` → `TargetUnreached { refusal, bracket }`) shared by `gate3`, `pncad-py` and the tour; `gate3`'s one-read claim corrected and the double read filed on EXCH as an `[ev]` question; round-split and `Dual` continuation rows committed; overclaims and stale citations swept by sentence; both behaviour changes disclosed; hooks unified, `assembled` asserts its shared parameters; the panic filed on QUAD. Head `fa9789d39`, run 36111203641 green (six `test`, five `k-lint`, step level). The implementer (Opus): 507,419 tokens across build and fix pass, one usage-limit death resumed |
| DR-5 | 2026-09-25 | `c3129311b` | PATHS, PR #3224 — lowering unit 1: verbatim vertices + one canonical `Segment` (`Line` \| `Arc{centre, radius, Δθ}`) per edge, byte-identical (`canonical-segment-type-in-profile`) | L / STRUCTURAL · the storage form every later lowering unit stands on, broad (profile, sweep, editor-core, viewer), with a byte-identity claim (recorded at spec time, `work/paths/canonical-segment-type-in-profile.md`) | `3342d3704` | APPROVE-WITH-FIXES, 1/2/3 (+13 style). MAJOR: an arc verb at b = 0 stores `Segment::Arc` with a poisoned carrier — `lift` emits `ArcTo(b:0)`, the anchor's area NaN, `replay_naming` None (executed vs base). 207,967 tokens, 93 min (harness) | NOT-MERGEABLE-AS-IS, 1/3/7 (+11 style). MAJOR: the same defect, reached also through a collinear `tangent_arc_to` (executed vs base). 211,113 tokens, 71 min (harness) | Coded attribution-stripped by a separate Opus coder (52,822 tokens), byte 115. BILATERAL (9 groups): the poisoned-carrier MAJOR (MAJOR/MAJOR, both executed; A's `chain_form` MINOR dedups into it); the undisclosed sub-tolerance loft change (MIN/MIN, B executed); the reversal test's 4ε cannot pin its documented choice (style vs MIN); two readings of "line" disagree; "one private constructor" false; several 4·atan spellings; the `is_exact_zero` trick; unmeasured extra lowering arithmetic (NOTE/NOTE, unsure); dead arms (probable). UNILATERAL R2→: the `chain_form` Bulge writer unscheduled and lossy to retire (MIN, insp); bulge stays stored across units 2–4 (NOTE); `derive_naming` doc; parallel Vecs; stale prose; `arc_span` = \|sweep\| f64-only; `embed` blind to the MAJOR (style). UNILATERAL R1→: pre-existing NaN area on a tiny bulge (NOTE, filed on WIRE); spec-listed unit-3 readers unmentioned (NOTE); poison via the public `Segment`; a doc forbidding what `arc_span` does; "verified at validation" unbacked; the hidden kind flag; two bulge routes in the viewer; `sweep`+`turn`; an unguarded justification (style). The condensed transcription of R2 dropped its verdict line (the coder could not compare verdicts; recorded here from the report) | No candidate: the one MAJOR is bilateral (6(a)). Running: 0 | yes — identical briefs, no relaxation, neither interrupted, neither disclosed a glimpse | the union, all taken but one reverted with its reason (`fillets_carry_their_tangency` reading the stored sweep is not byte-identical at Interval: `atan([0,0])` ≠ `4·[0,0]`), none refuted, by one fresh Opus lane (236,703 tokens, 103 min harness): the hidden kind flag removed so the stored kind is the exact-zero read at every lowering (`lower_to` / `lower_arc`), the reviewers' rows committed (b = ±0 and collinear tangent arcs lift as lines and keep their anchor; exact-zero across scalars), the loft change pinned and disclosed, the reversal pinned bit-for-bit incl. Interval, `lift::rotated` through the lowering, dead arms removed, prose corrected, residue scheduled on units 2 and 5, the tiny-bulge NaN filed on WIRE. Head `250cd5ee2` (main merged in), run 36134020925 green, full matrix. The implementer (Opus): 407,289 tokens, 76 min (harness); one earlier probe-feature compile red fixed by the orchestrator at `8a42d0517` |
| DR-6 | 2026-09-28 | `c3129311b` | ATREST, PR #3204 — `point_in_solid`'s planar arm read every arc-bounded face as its vertex polygon (a revolved half-disc cap: three collinear vertices, zero area), giving false `In`/`Out` under ordinary rotations; an arc-aware in-face parity walk (`point-in-solid-reads-out-from-inside-a-re-posed-torus-barrel`) | M / NUMERIC · numeric soundness of a certified containment walk the boolean trusts (recorded at spec time) | `af79c39bca` | APPROVE-WITH-FIXES, 0/3/3 (+style). No MAJOR. 190,838 tokens, ~2 h 10 min (harness, mostly slot waits). Executed ~12,000 adversarial probes (notch >π, lens, lune, tube, drilled box) at 8 poses, 0 wrong; mutants A–D; the cut-cylinder refusal regression; re-baselined volumes by execution | APPROVE-WITH-FIXES, 0/2/3 (+style). No MAJOR. 222,728 tokens, ~3 h 09 min (harness, mostly slot waits). Executed ~31,000 probes over 8 new plates/solids at 13 poses at 1e-6 body size, 0 wrong, old walk 2,931 wrong; mutants oneroot/noarcbound/endmiss; the cut-cylinder 296/296 refusal; the revolved-tube 88% escalation | Coded attribution-stripped by a separate Opus coder (44,021 tokens), byte 225 (parity 1). BILATERAL: `PartialPlaneFace` fires on every plane hit at any distance and turns a working boolean into an error (MIN/MIN, both executed); the committed grid cannot see the pre-pass, rings, full circles, the lune, near-boundary crossings — mutants survive (MIN/MIN, executed; surviving mutants partly overlap); `contfp` still walks the half-disc cap as a polygon (R2 MIN / R1 NOTE, read); the K roster not updated (R2 style / R1 MIN); the chord arm copies `ray_verdict`; ray×arc spelled twice with `profile::seg`; carrier classification duplicated and disagreeing on ellipses; the cancelling discriminant (both unsure); a missing curve read as a chord; dead `ContainError` mapping; the period guard asked twice. UNILATERAL R2→: the schedule loop copies `point_in_loop`; per-hit allocations; the module doc's "ONLY place" claim; the variant name; a revolved TUBE escalates 88% (NOTE, EXECUTED, pre-existing). UNILATERAL R1→: `join.rs` tiers 3–4 abort the join on a refusal contrary to their doc (NOTE, read); `on_boundary` tests each chord twice; the discriminant re-spells the sphere arm; two error names for one condition; test names contradicting their assertions; the repro's door (disclosed) | No candidate: neither review raised a MAJOR (6(a)). Running: 0 | yes — identical briefs modulo lane paths (sha256 stored), no relaxation, neither interrupted. Disclosed: R1 ran `pkill -f` on a pattern matching any lane's `sweep/tests/all.rs` compile, which could have killed another lane's build — R2 reports no build of its killed; both saw build-slot pid lines only; R1 piped one `-n` slot call through `tail` (exit 75). The coder read orchestrator-condensed transcriptions of both reports | the union, all taken, none refuted, by the implementer lane (resumed): ellipse arcs answered in the conic arm (one affine-circle quadratic) and the remaining spiric/spline refusal made local to hits inside a ball holding the loop (`EdgeCarrierUnsupported`); the walk moved to one home `splitting::containment::point_in_carrier_loop` sharing `walk_schedule` and `ray_parity::ray_crossings`; direct `point_in_face` rows for every arm the grid could not see; the K roster; the `1 − h²` discriminant and single metering; `join.rs` discards rather than aborts; the revolved tube filed on CONTACT. It also found — with the ellipse face now read right — 62 false `In` from the cylinder WALL arm on a tilted section (pre-existing, P0, filed on CONTACT, pinned `#[ignore]`). Head `d30f32a5`, run 36154124441 green (six `test`, five `k-lint`, step level). The implementer (Opus): 489,980 tokens across diagnosis, build and fix pass, one usage-limit death resumed |
| DR-7 | 2026-09-25 | `c3129311b` | VNEWS, PR #3235 — rank 1 no longer drops a notice nothing will say again: `frame::Retold { Again, Never }` on every `Message`, `Message::new` with no default, typed doors answering per arm (`rank-one-discards-the-frames-other-news`, P0) | L / STRUCTURAL · a classification every status-line producer must answer, on a type every producer builds — broad and hard to reverse. **Tier chosen after spec, not at it**: the unit first had a single style review; that review found the rule tested the wrong property, and the fix pass made the rule broad enough to earn a dual | `612a9aa6d` | NOT-MERGEABLE-AS-IS, 1/2/3 (+7 style). MAJOR: a `Strand` on a `Declare` carrier is answered `Again`, but a `Declare` never resolves its names, so no tree row ever says it and beside a refusal it is lost (executed: `declared_union` probe, every row `None` after pump). 180,947 tokens, 77 min (harness) | APPROVE-WITH-FIXES, 0/3/5 (+7 style). No MAJOR. 178,287 tokens, 113 min (harness) | Coded attribution-stripped by a separate Opus coder (43,030 tokens), byte 120 (parity 0). BILATERAL: the `Strand`/`Declare` misclassification (MAJOR vs MINOR, **severity divergent**, both executed by independent probes with identical results — the verdict split tracks it exactly); `store_refusal` answered `Again` though a repeated act never re-raises it (MIN/MIN, inspection); README's "no evaluation reports" false for `StrandedAppearance` (MIN vs NOTE, doc); `joined`'s `Retold` pinned or not (NOTE/NOTE — contradiction resolved by execution scope: one ran integration rows only and saw it unpinned, the other ran `--lib` and it went red in a `frame::tests` row, so it is pinned by a unit test, not a policy row); doors outside the rows' reach unpinned (NOTE inferred vs MIN executed — four typed-refusal mutants survive `--lib --test all`; borderline dedup, tally unaffected); "the frame's losses" vocabulary; a doubled `///`; the `# The ranking` sort a hand-written census restating the per-arm answers (style/style). UNILATERAL R1→: an `EdgesLost` spelled `Again` in a test; the startup comment's wrong reason; Clear sweeping a `Never` notice (all style). UNILATERAL R2→: `Retold` meaning two things; all-`Never` arms as ceremony; unwrapped lines (all style) | No candidate: the one MAJOR is bilateral (R2 raised it as MINOR), fails 6(a). Running: 0 | yes — identical briefs modulo lane label, `CARGO_TARGET_DIR` and scratchpad prefix (checked by the orchestrator at dispatch; **no sha256 stored**), no relaxation, neither interrupted, neither disclosed a glimpse; PR thread empty at briefing. R1 did not run `--lib` and R2 did — each reviewer's own choice within one brief, not an orchestrator relaxation | the union, all taken, one decided differently from the brief with its reason, none refuted, by the implementer lane (resumed; one session-limit death): `Strand` answered `Never` for every carrier rather than per carrier — the brief's shape could not be written, since the classifying door sees neither the carrier's kind nor its evaluation, and a carrier poisoned upstream defeats every kind — under a new stated burden (*a door answers `Again` only when it can show the retelling from what it holds*); every typed-refusal door's answer pinned; `store_refusal` → `Never`; the census replaced by pointers to the doors; prose fixed. 24 mutants planted on committed trees, all red. `Maintenance::Rebound` deleted on main meanwhile, its arm removed. Head `a036e82ca`, run 36212415371 green. The implementer (Opus): ~401k tokens across build and two fix passes (harness) |

**Tally: 0 of 8. Fair pairs toward twelve: 6.** (DR-2 excluded: method divergence.)
