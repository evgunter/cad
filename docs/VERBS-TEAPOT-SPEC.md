# VERBS-TEAPOT — the designated demo (one PR)

The Q8 arc's demo unit, unblocked at shell's merge (#1048). One
PR, branch `verbs/teapot`. Difficulty logged pre-dispatch: **M**.
A DEMO unit: the kernel is NOT changed — every door that refuses
is a finding pinned wall-style, and the teapot's walls join the
lily's as the register's demand signals. The scene ships with
whatever composes; an honest partial teapot with typed walls
beats a smoothed one (the awkwardness-is-a-finding rule, and the
lily precedent — its exit shape was ratified as finding-bearing).

## The body (expected to compose end to end)

1. **The vessel**: one revolved profile (belly + foot), shelled
   **opened** at the mouth — `shell(body, t, open_faces)`'s first
   consumer beyond its own acceptance: the annular mouth rim on a
   revolved body exercises the chart-group rim lift (#1048's
   MIN-1 fix) on a real scene. Wall thickness stated; the
   per-face reach margins all definite (pick t comfortably under
   the belly's reach). The planar-pair clearance gate does not
   bind (no antiparallel planar pair at 2t); say so in the scene.
2. **The lid**: a second revolved body (dome + knob), the knob
   rim filleted through the annulus door (a second bud-class
   consumer). No assembly mate is authored — the lid renders
   lifted above the mouth (the exploded-view idiom); declared
   contact is M9-territory and NOT this unit.
3. Census + mass properties against closed forms for both bodies;
   the vessel's opened-shell rim tier-3 valid; baseline rows per
   the TESS-BUDGET runbook (pure-additive; per-scene counts in
   the PR body); renders through the standard lanes; STEP
   frontier pinned self-retiring wherever a curved two-shell body
   exports (the hollow-torus precedent).

## The spout and the handle (expected to WALL — pin, don't force)

4. **The handle**: `tube_along_arc` (the natural authoring — a
   partial-torus tube). The union handle ∪ vessel is a
   curved×curved pair at the operand gate; whatever door refuses,
   pin it typed wall-style (self-retiring: a DIFFERENT refusal or
   a success fails the tour), citing the germ-lane program and
   #1057's arms as the schedule. Do not widen any route.
5. **The spout**: author through existing doors only — a revolved
   cone-frustum tube (or a straight cylinder tube set at an
   angle) is acceptable; a lofted/canal spout is NOT available
   and its absence is recorded as a register note, not hacked
   around. Its union with the vessel walls like the handle's —
   same pin discipline.
6. If either union DOES compose (the substrate has moved since
   the last measurement — PIERCE is in flight), take the
   composition and pin its census/mass properties instead; the
   scene text states which way it went and why. Measure, never
   assert (the wall-7/#1059 lesson: a refusal's text is not
   evidence of its cause — the payload and raising site are).
7. The montage cell shows the ensemble (vessel + lid lifted +
   spout/handle bodies present, unioned or adjacent per item 6's
   measurement); the cell's caption text states plainly what is
   one solid and what is not. KERNEL-VERBS gains the teapot rows:
   what composed, what walled, each wall's door and schedule.

## Fences

- No kernel code changes. No route widening, no gate softening.
- No assembly mates (M9 territory), no variable-radius/canal
  machinery, no loft authoring beyond what exists.
- The #1044/#1048 conservatism and clearance rows untouched.

## Lane obligations

`docs/prompts/implementer-discipline.md` binds. No Co-Authored-By
trailer (blinding). Lane-private PR draft. Targeted local runs
over full batteries. Merge origin/main before opening; confirm CI
jobs are actually RUNNING (both silent-run shapes are on record);
note the drawn point; watch to completion; cancel detached timers
before the final report. Do not merge.
