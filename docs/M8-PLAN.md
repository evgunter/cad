# M8 — kernel residuals (plan)

Ratified by construction: the milestone and its firm slate were set
by Evan's 👍 on PR #300's final closure affordance (comment
5234714306, 2026-08-09) — M7's walk ruling and this restructure are
the same ratification. **M8 = the misc kernel residuals the demos
raised** (Evan's directive, 2026-08-09: iron these out before the
larger programs). **M9 = C7** (declared-contact join lane, pulled
forward as its own logical unit; the lily FULL rebuild rides it).
**M10 = error propagation** (signed clearance; the C7 co-design
records stay cross-referenced).

## The firm units (ratified)

1. **M8-1 — #284 Newell chart-frame re-anchor** (mesh lane derives
   the planar chart frame from the boundary itself; the two wild
   tessellation refusals flip into montage cells 6→8). At
   ratification: PR #301 in fix pass (R1 APPROVE-WITH-FIXES — the
   anchor judgment needs in-CI coverage; doc claim narrowed to the
   axis-noise class actually closed).
2. **M8-2 — rational-carrier `speed_lower_bound`** (quotient-rule
   bound assembled from the existing hull machinery; retires the
   span-meter half of the rational bank; two conscious pin flips;
   the calochortus CRESCENT restoration rides as demo evidence).
   At ratification: implementing.
3. **M8-3 — rational-patch-flux quadrature** (the M7-3 Arm B bank:
   volume certification for rational walls). Flips: the arc-prism
   waypoint (#288 — seam certifies, body currently refuses the
   banked quadrature), the arc-loft round-trip (#276's honestly
   refused class), tier-3 on rational walls.
4. **M8-4 — the `nurbs_iso_derive` Intersection arm** (the second
   gap #288 measured: pcurve minting for declared plane×NURBS
   intersection edges on integral walls). With M8-3 it makes mixed
   plane×NURBS bodies first-class END-TO-END — the #288 walk row's
   full retirement.

5. **M8-5 — mesh rational deviation certificate** (plan amendment,
   2026-08-10, discovered by M8-2: the restored crescent blade
   BUILDS AND CERTIFIES kernel-side but cannot tessellate —
   `mesh/src/nurbs_cert.rs:151` refuses rational NURBS faces, no
   certified Hessian bound; the THIRD rational bank). The working
   crescent restoration is parked at
   cad-work/span-meter-crescent.patch and rides this unit; the
   #218 empirical per-triangle falsifier guards whatever bound
   lands. Amendment class: a demo-raised kernel residual joining
   the residuals milestone — recorded for retroactive review.

## Candidates — ADOPTED (all four; Evan's 👍 on PR #302's
affordance, comment 5234756324, 2026-08-10). Sequenced after
M8-1..M8-4 unless a lull opens earlier; each gets its own
substrate → spec → dispatch cycle.

- **#186 assembly instancing** (dm1's blocker — the M7-6 walk row's
  named carried item; flips dm1 first-class, wild corpus 9/13 →
  10/13).
- **#214 dimensional-debt riders** (F6/F7 typed-margin fold-in —
  M8-2 touches F7's predicate and deliberately leaves the redesign;
  the natural moment per docs/predicate-dimension-audit.md).
- **#222 long-turn sweep frontier** (the ≥0.5-turn helix chord
  collapse — integral class, distinct from M8-2's rational work).
- **The stale large-K lint floor** item from the M6 register if any
  residue remains post-#239/#253 (verify before slating).

## Exit shape

Every demo-raised refusal this milestone owns either FLIPS (the
crescent sweeps, the arc prism imports first-class end-to-end, the
wild montage holds 8 cells) or is re-banked explicitly with a named
owner and flip condition. No silent survivals: each of M7's
flip-when-fixed pins touched by these units executes its own
retirement text. Hosted CI green on every merge; the walk convention
(criteria vs evidence, honesty rows) applies at exit.

## Process (unchanged)

The standing process applies verbatim: one implementer + one blinded
adversarial reviewer + one fix pass; binding specs before dispatch
(substrate exploration first); A/B v3 triples with rows at merge
(the block series keeps its running index — the next block after
M7-12 opens as **M8-13**); dual-review every 3rd merged blinded row,
ordinal fixed at R1 dispatch (shared counter with the LIB series);
hosted CI the only gate; demo-class units orchestrator-reviewed.
M8-LOG is this milestone's narrative record.
