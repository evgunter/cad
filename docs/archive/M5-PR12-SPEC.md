# M5 PR 12 — fillets: the validity battery + analytic blends + the die (binding spec)

Branch `ev/m5-pr12-fillets` from current main (post-S13). Plan
line 12 (M5-PLAN :293-307); CURVED-DESIGN C8 (:462+), OQ6 as
decided (scope = closed smooth chains + the three-convex-edge
sphere-octant corner; run-out vocabulary as refusal-payload names
only — :842-868). Acceptance shape (v): the die-with-pips
upgrade — EXIT-GATING.

**Scope call, recorded**: the general-spine canal-surface fitted
blend (C8's approximating-surface lane) is NOT in this unit — the
exit criteria demand the die demo + the predicate battery + the
pinned refusal vocabulary, and the die is analytic end-to-end
(straight spines → cylinders, arc spines → tori, corners →
sphere octants). The canal lane banks as its own reviewed unit
(the kernel's first approximating SURFACE deserves undivided
review attention); `FilletCornerUnsupported`/`FilletSpineUnsupported`
name it as the missing front door for general spines.

## 1. The validity-predicate battery FIRST (C8's order is binding)

Each a named Q1 trilean over the INPUTS, evaluated BEFORE any
construction, each with a fixture firing it as a typed
pre-construction error (exit criterion):
- radius vs curvature headroom (r < 1/κ_max along the chain, the
  lever-arm discipline for the margin);
- face consumption (the blend must not consume a face entirely);
- spine regularity; chain G1 closure (closed chains) /
  termination classification (open chains);
- convexity-sign consistency along the chain;
- corner configuration (three-convex-edge octant vs everything
  else).
K-funnel from birth (fillet3_* family or extend the S2 fillet_*
naming — pick ONE convention and say why); k-lint clean;
telemetry verdict-log row.

## 2. Analytic blends (the die's whole diet)

- plane–plane edge → cylinder blend; straight spine → cylinder;
  arc spine → torus (the pip rims); vertex ball → sphere;
  the cone cases per configuration (C8's list — implement the
  arms the die + fixtures reach, refuse typed with the OQ6
  payload for the rest, enumerate in the report).
- Trimlines stored `TangentIntersection` FROM BIRTH
  (prefer-intrinsic, D7; the S10/S12 sense machinery and PR 9's
  jet certificates are live — the blend walls' tangency to their
  support faces is jet-certified, and the mark/must-carry rules
  apply).
- The sphere-octant corner patch (three-convex-edge case).
- Blend faces mint with correct sense (S11 discipline — a
  concave blend's chart normal vs material: derive from stored
  structure, never sampled normals).

## 3. `FilletCornerUnsupported` (OQ6 vocabulary, refusal-only)

Payload names: `RunOutStopAtVertex`, `RunOutFeather`, plus
corner-configuration tags for N-edge/mixed-convexity vertices —
zero constructor surface, FullRevolveHoles-precedent honest text.
Every payload variant pinned by a fixture that reaches it.

## 4. The die (shape (v)) — the acceptance headline

Cube, edge fillets (open chains terminating in sphere-octant
corners on the cube edges), pips as S13 subtractions, pip rims
filleted (closed chains, torus blends). The demo:
- builds, tier-3 certifies, tessellates WATERTIGHT (admesh),
  meters certified volume (closed form where derivable — the
  pip-rim tori and corner octants have exact volumes; else
  certified enclosure), exports STEP, imports FreeCAD (the
  check_step.sh row + the PR 13 fixture corpus grows the die);
- joins the tour (the M5 showcase per Evan's ruling — the demo
  stop renders in BOTH montages if the dual-montage unit has
  landed);
- Band-4 corpus + persistence/latency rows.
Multi-ε honesty throughout; two-tolerance on every arm INCLUDING
definite arms; both lanes + bit-replay.

## 5. Out of scope

The canal-surface general blend (banked unit — named in refusal
text); variable radius; chamfers; blend-on-blend; anything
touching the marcher. Frontier errors name the real blocker.

## 6. Process

One implementer + one blinded adversarial reviewer + one fix
pass. Review charter musts: independent verification that every
C8 predicate fires pre-construction (attack: a fixture that
passes the battery but would fail in construction = MAJOR
ordering defect); the pip-rim torus tangency jet-certificates
re-derived; watertightness attack at blend-support seams; the
die's FreeCAD import; sense audit on concave blends; rubric.
Local scope by iteration-speed: touched crates default ε + new
rows at Interval + the tour battery; CI proves the matrix. Push
per unit; foreground; OUTPUT DISCIPLINE per standing process.
