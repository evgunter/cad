# MATE-7a — issue 968 items 1–2 + the π arm: the torus declared-Rest lane's first unit

**Binding at dispatch** (S-MATE program, `docs/S-MATE-PLAN.md`;
difficulty pre-logged at this spec: **L**). Read
`docs/prompts/implementer-discipline.md` in full before starting.
The primary specification is the RULING CHAIN:
`docs/MATE-7-TANGENCY-DESIGN.md` (RATIFIED 2026-09-01 — the
material-wedge table routes a shared-rim contact: π → the
smooth-seam zip, built HERE; 0/2π → the declared-Tangent cusp
family, DEFINED-BUT-UNBUILT, typed refusal citing the ruling;
in-band → escalate) with issue 968's measured needs as the map.
The kissing arm and the `tangent_locus` torus×torus witness are
NOT this unit's (they bank together with the recorded demo rider).

## Situation

Lily wall 1 — the stem glue — refuses
`CurvedBooleanUnsupported{Torus}` at the operand gate
(`reduce.rs:184`-era pin), and stays a named residue through M9-5;
issue 968 is its durable record, ceded by VERBS with the LILYWELD
context (the weld's declared contact measured plane×plane, so the
gate admission had nothing to consult and the `carrier_eq` rung
sat consumer-less). The three measured needs: (1) torus through
the operand gate under covered declarations; (2) a torus rung in
`carrier_eq` so the declared-Rest descent (M9-3 PR-A's pattern:
door → reduction rung → carrier lump) has a verdict on torus
pairs; (3) the rim disposition — now RULED, with only the π arm in
scope.

## Deliverables

1. **The operand gate** (issue 968 item 1): torus admitted under
   covered declarations, per the posture VERBS-GATE ratified —
   read that unit's landed shape first and follow it, do not
   re-derive; klein walls 3/4 pin the current per-face-kind gate
   and re-bless onto the widened admission with reasoning at the
   sites (VERBS-authored pins — the flip is flagged prominently in
   the PR and the orchestrator coordinates it on the away channel).
2. **The `carrier_eq` torus rung** (item 2): certified predicate
   growth (metered NUMERIC rows), the same descent shape the
   cylinder rung carries; declared torus×torus Rest pairs get a
   verdict, everything outside stays typed.
3. **The π arm** (the ruling's built half), MEASUREMENT-FIRST:
   price the join-side wedge classification before designing it —
   the table's machinery runs at tier-3 today, and consuming it at
   join time is the ruling's unpriced piece. If the price is fair,
   the join routes a declared shared-rim torus pair whose wedge
   classifies π into the smooth-seam zip (the tube-chain treatment
   reached via the ratified routing). If the price is heavier than
   this unit bears, the honest fallback is EXPLICITLY permitted:
   land the π reconciliation via the existing tube-chain path with
   the routing's classification deferred, the deferral cited to
   the ruling, and the price measurement in the PR — the ruling is
   about what the design IS; this unit reports what it costs.
4. **The 0/2π typed refusal**: a shared-rim pair whose wedge does
   not classify π refuses typed CITING the ruling (the
   defined-but-unbuilt arm); a row pins it.
5. **The demo — lily wall 1 retires**: the stem glues through the
   public doors; the wall probe re-blesses onto the working union
   with real assertions (census/volume per the scene's own oracle
   style), the retirement reasoning at the site. `lily.rs` is
   VERBS-measured ground — same coordination posture as item 1's
   pins.
6. **Class sweep** (discipline §5): the genus is "a per-face-KIND
   gate arm whose admission posture predates covered
   declarations" — sweep the operand gate's other kinds for the
   same widening question; hit list with dispositions (expected:
   mostly not-this-unit with the posture named), blind spots
   stated.

## Acceptance

- Lily wall 1's probe re-blessed and the scene green; klein pins
  re-blessed; the 0/2π refusal row green; existing
  boolean/sweep/topo suites green (MATE-2's `Placement` rows and
  the germ-arm pins are near ground — merge main frequently; VERBS
  PR #1353 is OPEN on `vertex_on_curved_face` and its unification
  debt is theirs, do NOT touch that function).
- Refusal-reach moves classified against the D2 addendum in the PR
  body (row 2: the torus operand over-refusal retires under
  covered declarations).
- ε posture (issue-1356): the carrier rung and gate margins are
  band-sensitive — argue the band story; pin lanes as warranted
  (`lane=both` on the final head is the recent precedent for units
  whose arms live in both compile modes).

## Hard rules

- NO `Co-Authored-By` trailer and no model names in lane commits.
- Keyword hygiene: write "issue 968" spelled out; never a closing
  keyword before a `#`-reference. The unit does NOT close issue
  968 (the kissing arm remains); say so in the PR.
- Scope fence: `crates/topo/src/boolean/` (the gate and the join's
  π-arm wiring; NOT `vertex_on_curved_face`),
  `crates/topo/src/boolean/carrier_eq.rs` (or wherever the rung's
  home is — read first), `crates/topo` and `crates/sweep` TEST
  files, `demos/tour/src/lily.rs` (the wall-1 probe and what its
  retirement requires, nothing structural), and the klein wall
  pins. Nothing else — no `geom-brep` (VERBS' germ ground), no
  `census.rs`/`chart_region.rs` (MATE-8's live ground), no
  `tangent_locus`, no `editor-core`, no `docs/MODEL-AB-LOG.md`,
  no `docs/S-MATE-*.md`.
- Work only inside your worktree (the shared session scratchpad is
  off-limits); merge main before opening the PR and whenever it
  moves.
- Commit and push after every coherent unit of work (branch
  `mate/7a-torus-rest`).
