# VERBS-OFF-D — shell: the verb (two PRs)

Wave 3's final substrate unit, per the ratified
`docs/OFFSET-DESIGN.md` O4 (as amended through both of Evan's
rounds: the sealed shell is the DEGENERATE NO-CROSSING ARM through
the shared void-insertion door; the open-faces shell is a closed
thin solid via rim surgery; "every cavity is born through the
shared door" is the ratified invariant). A TWO-PR unit: **PR-1 the
face-replacement primitive; PR-2 the shell verb.** Branches
`verbs/offd-1`, `verbs/offd-2`. Difficulty logged pre-dispatch:
PR-1 **M**, PR-2 **L**.

## Inheritance (measured facts that bind, from OFF-C's cycle)

- The face-replacement primitive owes its edges the new chart's
  SPLINE SPACE: **knot refinement AND degree elevation** (the
  ordinal-76 reviewer measured elevation alone insufficient on
  curved fits — the fit refines past the seed grid even at
  d = 5e-10).
- The pulled-back-base coherence budget is `d·|n − n₀|` against
  the run band (measured: d = 2e-9 escalates at 2.3e-9).
- Tier 3 re-derives Approx certificates against the RUN's ε (the
  re-ruled posture); #1019's perf box is open — PR-2's shell body
  is its named measurement fixture.
- **The apex-window predicate lands in PR-1** (re-pointed here from
  OFF-A/OFF-C): where a replacement mints a CONE from an offset
  description context, decide `inf(v-window) + d·cot α > 0` before
  treating the mint as the face's offset — a shifted window
  crossing the apex silently reads mirror-nappe geometry
  (ordinal-73's measured trap).

## PR-1 — `replace_face_offset`: the face-replacement primitive

1. A door (home: `topo`, beside the attach layer) that replaces one
   face's surface with its certified offset — analytic kinds via
   OFF-A's exact mints; NURBS via OFF-B/OFF-C's `Approx` — and
   re-describes the face's boundary per the M6-1 ordering
   (surface → edge descriptions → whole-body `mint_pcurves`),
   with the spline-space obligation (elevation + refinement into
   the fit's interior knots) executed for Approx charts. The
   neighbors' surfaces are UNTOUCHED: the replaced face's edges
   become intersection/mapped descriptions against the moved
   surface per the existing vocabulary — where a carrier cannot be
   described (the neighbor pair has no route arm), refuse typed
   naming the pair (the C5 table is the honest boundary).
2. Validity pre-construction (the ratified stance): the offset
   refusals propagate (radius floor, torus ring, the collapse
   meter for Approx); the apex-window predicate; a
   decided-then-mutated clone with one validation at the end.
3. Acceptance: a planar face offset (trivial); a cylinder wall
   offset inward/outward with the neighbors re-described and
   tier-3 valid; an Approx replacement on a curved fit (the OFF-C
   consumer's successor — now through the DOOR, with the seam
   carriers landing in the refined spline space); planted reds
   (undescribable neighbor pair; apex-window violation; collapse).
   Bit-identical everywhere the door is not called.

## PR-2 — `shell`: sealed and opened

1. **Sealed**: `shell(body, t)` = every boundary face replaced by
   its inward offset (PR-1 per face, one clone, one validation),
   the offset boundary assembled as the cavity body, inserted
   through `topo::boolean::voids::insert_void` with
   `Carried { Positive }` evidence derived from the d-vs-reach
   margins (the collapse meter's own decides — by-construction
   containment, the RING pattern; the door never derives it). NO
   SSI, NO crossing pipeline — pinned structurally (the RING
   verdict-log pin's shape).
2. **Opened**: `shell(body, t, open_faces)` — the O4 ratified
   composition: the sealed construction minus the designated
   faces, with annular rim faces minted where the wall thickness
   shows (the rim surgery: the M6-1 discipline, the ARMS-1
   annulus precedent for ring-topology bands). The result is a
   CLOSED thin solid — genus rises, nothing opens (O4's argument;
   D1 untouched).
3. Validity: t vs the reach (per-face collapse margins — the
   sealed case's evidence and the refusal are the same decides);
   open-face designation validated (a face must exist and its
   removal leave a connected boundary — refuse typed otherwise).
4. Acceptance: **the Klein bottle's walls re-authored**: at least
   one of klein's hand-built `r ± t/2` wall pairs replaced by a
   `shell` call with BIT-COMPARISON against the hand construction
   (the register's "paid once per wall" debt starts retiring — a
   demo edit, byte-identity NOT required where naturalness
   improves, per the demo rule; say which contract applies);
   a sealed shelled box (trivial closed form); a sealed shelled
   solid of revolution (the vessel shape — #1019's perf fixture);
   an opened shell with rim faces tier-3 valid and mass properties
   against closed forms; every refusal planted red; the
   `CurvedShellClassification` STEP gate recorded per the standing
   pattern where curved two-shell bodies export.
5. **The teapot demo is NOT this unit** — it follows as the demo
   unit once shell exists (the designated demo, with the
   spout/handle needing loft/sweep composition beyond shell — its
   own authoring exercise).

## Fences

- No draft, no vertex-move pass (DR2's territory).
- #1005/#1008's capabilities not built ahead (rational fits still
  refuse; small-d wall per the documented limit).
- The mesh-tolerance widening (#1018) not wired.
- PR-2 STOPS for adjudication if the opened variant's rim surgery
  needs machinery beyond the annulus/ladder precedents.

## Lane obligations (both PRs)

`docs/prompts/implementer-discipline.md` binds. No Co-Authored-By
trailer (blinding). Lane-private PR drafts. Merge origin/main
before opening each PR; confirm CI runs STARTED; note the drawn
point + coverage; watch to completion. Do not merge.
