# LIB-PYG5 spec — audit gap G5 + register R3: the detect/declare protocol from Python, and the refusal-menu wiring (binding)

Mandate: close the audit's last two-stop gap. G5 = declared flush
contact from Python (the SEL2-ratified detect/declare protocol —
find_flush_candidates → declare — is entirely unbound; PYBUNDLE
landed only the declare= DATA field). R3 = the register's
UndeclaredContact refusal-menu wiring: the finding payload crosses
into the boolean's refusal so the refusal NAMES the candidate
declarations (the shape is recorded in the SEL2 report,
~/.local/share/cad-work/lib-sel2-report.md — read it).

## 0. Discipline (absolute)

docs/LIB-PYG1-SPEC.md §0 verbatim and binding
(local-scripts/with-build-slot.sh, foreground one-at-a-time,
kill-your-own-waiter, commit+push per chunk, NO Co-Authored-By,
no model names, merge-main-before-open + re-merge, checks
STARTED, cold clippy CI scope incl. `-p pncad-py --features
python`, k-lint discipline, invariant comments). PLUS: shard any
battery row that would exceed ~15 minutes; prefer the express
lane for sharded rows.

## 1. Deliverables

1. **R3 first (Rust, editor-core/topo per the SEL2 shape)**: the
   UndeclaredContact-class boolean refusal carries the DETECTOR'S
   FINDINGS as typed payload — the refusal names the flush-pair
   candidates (their StableNames + relation) so the recourse is
   in the error, not a doc. No new detection machinery: the C4
   verifier in candidate-gen mode (find_flush_candidates) is the
   one source. Tags updated; refusal rows both directions
   (undeclared → menu payload; declared → proceeds).
2. **G5 binding**: find_flush_candidates + declare/declare_all
   cross to Python per the house pattern (typed values, no
   name-text parsing — results feed Node.boolean's declare= and
   Node.declare); stubs + ty fixtures per new door.
3. **Audit flips, honest**: table (21) and crosslap glued (28)
   become executed YES rows against their exact oracles, built
   through detect → declare → boolean from Python; counts
   script-re-derived; absence rows flip; G5 → Closed gaps. If
   either scene hits a residue, re-partition honestly.
4. **Guide**: one detect-declare-glue Python block (executed),
   showing the refusal-menu recourse (author the undeclared
   boolean, show the typed menu, declare, succeed).
5. Numbered findings per the demos' purpose.

## 2. Fence

OUT: crates/profile and the PATHS/step vocabulary (RESPELL PR-1
is concurrently in flight THERE — your editor-core work is the
names/detect/boolean-refusal neighborhood, disjoint; if a
collision appears, STOP and report), the audit page's G12/G13
rows beyond arithmetic (PR-2 owns broader audit churn — keep
your audit edit minimal: your two rows + counts), geometric
selector atoms (GS-Q2), schema, CI structure. Anything missing:
REPORT.

## 3. Acceptance

Python suite green (delta stated); cargo test -p editor-core -p
topo -p pncad -p pncad-py; cold clippy CI scope all lanes;
hosted CI green; zero new [[test]] binaries; stub + ty green.
Pre-draw fields at dispatch: difficulty M, task-class STRUCTURAL.

## 4. PR discipline

One PR. Report ≤150 lines to
~/.local/share/cad-work/lib-pyg5-report.md, per-phase figures.
Open, do NOT merge. Final message: PR number + report path +
≤10-line summary. Forks: report, smallest faithful reading, flag.
