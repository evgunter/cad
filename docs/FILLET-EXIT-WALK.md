# FILLET exit walk — criteria vs evidence

**STATUS: PROPOSED — awaiting Ev's ratification (a 👍 on this PR's `[ev]`
thread ratifies it AS WRITTEN; a comment amends it in place).** FILLET =
the blend-completion second pass (`work/fillet/plan.md`, opened 2026-09-03
from `docs/WORK-TRACKS-2026-09.md`; orchestrated 2026-09-04/05 on a remote
box). Every unit on the slate is merged: the plan's seven, the two its
rulings grew (RIM, ATTR), Track T's riders as one unit (T), and the
`surgery.rs` split Ev's ruling on PR 1916 opened (SPLIT) — eight A/B rows,
samples #126, #131, #132, #134, #135, #136, #139, #141, ordinals 2000–2007.
Criteria are quoted **verbatim** from `work/fillet/plan.md` — the three E
openers, the four H units, the four D rulings, Track T, and the exit-shape
paragraph — with the units the program grew in their own rows.
Dispositions per the M5–M9 convention: MET / MET-WITH-RECORDED-HONESTY /
CARRIED (named owner). On ratification this file is deleted with
`work/fillet/` and recorded in `docs/DOC-LEDGER.md` at the SHA it is
recoverable at; the residue table at the end names where every open item
goes.

## The walk

| # | Criterion (verbatim from plan.md) | Disposition | Evidence / honesty note |
|---|---|---|---|
| 1 | E1: "`fillet-nonpositive-radius-false-fact-refusal` — chamfer's `NonpositiveSize` door check applied to `fillet_edges`; flip the characterisation probe to pin the typed refusal." | MET | PR 1743 (2026-09-04), single style review per the charter. The door check applied; the probe flipped to the typed refusal. Filed from the review: `blend-size-gate-unmetered-under-epsilon`, `fillet-escalation-site-has-no-producer`. |
| 2 | E2: "`recourse-sentences-owe-followability-pin` — a composed pin that EXECUTES every `FILLET*_RECOURSE` sentence and asserts it builds, or records the gap at the site; it also inventories which recourses lie, which is input to the H units." | MET | PR 1753 (2026-09-04). `blend_recourse_followability` executes every recourse; the inventory fed H5 (the assembly recourse's closed clause was the one that lied at H5's shape, and H5 rewrote it — then H5's dual caught the rewrite over-promising at the ringed host, fixed at H5's fix pass). Filed: `blend-recourses-under-describe-their-doors`, `path-fillet-door-validator-tangency-disagree`. |
| 3 | E3: "`bare-f64-margin-payload-family` — named payload shapes (`Option`/`Indeterminate` carried whole) in the fillet battery; the `edge_nurbs.rs` twin is TRIM's ground and filed there." | MET | PR 1763 (2026-09-04). `ClassifiedMargin` family; the twin filed to TRIM (`work/trim/fitted-magnitude-nan-schedule-parameter`). Filed: `blend-payloads-outside-the-margin-family`. |
| 4 | H4: "`concave-closed-rim-has-no-band` — relax `resolve_rim`'s convexity gate, make the excise/merge walk orientation-agnostic, a concave fixture with a closed form; L." | MET (re-scoped at Phase 1, the fold into the unit) | PR 1752 at `fc38f753b`, sample #126, ordinal 2000. Phase 1 found the arms folding sense bits, not convexity; the fold homed on `Convexity`; the gate retired; the fourth quadrant built; dump identical. Dual: R1's setback-sign mutant a tally candidate. |
| 5 | H5: "`repaired-pole-rim-serves-no-closed-door` — the annulus closed-rim arm learns that one planar host face may carry several rim arcs of one circle in one loop; the p4 probe is the acceptance row." | MET (re-scoped at Phase 1: no native instance) | PR 1824 at `91e6d4309`, sample #132, ordinal 2002. `HostFoot::{Seam,Strut}`, six repaired fixtures on both sides, `p4` flipped. Dual: the rewritten recourse over-promised at the ringed host (bilateral) — conditioned at fix; filed `hostless-rim-on-a-ringed-host-refuses`, `curved-single-host-rim-refuses-at-the-half-band-gate`, `plane-hosted-rim-has-no-native-instance`, `ring-clearance-refuses-a-nested-trim-circle`. |
| 6 | H6: "`extrude-cap-rim-smooth-arm-noop` — construct an input that reaches the no-op `Smooth` arm, or state the true unreachability argument; S but H in kind." | MET-WITH-RECORDED-HONESTY | PR 1891 at `195460c7a`, sample #135, ordinal 2003. The arm is unreachable AT THE SHIPPED K; the dual found it reachable at admitted K ≤ 1.272 (bilateral MAJOR), so the argument is K-conditional and the door now refuses typed (`SmoothCapRim`) below the crossover. The must-carry rule homed as `tangent_second_order`. Filed: `ambiguity-k-below-the-cap-rim-crossover`, `smooth-arm-siblings-disagree-on-the-in-band-case`. |
| 7 | H7: "`fillet-ruled-spine-arms-no-surgery` — curved-support trimline descriptions and chain terminations at caps and seams; blocked on the OQ6 run-out taxonomy, so last." | MET (the transverse cut-off; the mid-curve run-outs stay A3-3's) | PR 1897, sample #136, ordinal 2005; vocabulary ratified on `[ev]` PR 1819. The ruled band with the transverse cut-off; the rod at its prism closed form; the concave ruled band pinned through the extrude door (the dual's find). Dual: the new predicate's lever not red-capable at its call site — a tally candidate; pinned at fix. Filed: four (coaxiality recourse arm, ladder dead key, certifier gap, `CornerArc` name). Deviation 1 (the carve in `surgery.rs`) → `[ev]` PR 1916. |
| 8 | D: "`corner-config-tag-all-concave-trihedron` (code-quality ruling, already on Ev) — whether `ThreeConcaveEdges` is minted at all." | CARRIED (code-quality's `kind: ruling` file, where it already lives; re-asked with its moved premise on `[ev]` PR 1935) | Its premise moved under the program: both verbs carve the all-concave trihedron (BLEND-4), so the "mixed" label is minted nowhere, and carved configurations now carry tags (`ThreeConvexEdges`; H7's `TransverseCap`) — the question left is symmetry versus a tag without a reader. A 👍 on PR 1935 closes the ruling with no tag; "mint" makes a small execution unit that would take block FILLET-B3's banked slot 2. Either answer amends this row in place before or after ratification; unanswered, the ruling stays code-quality's open item and the program does not wait on it. |
| 9 | D: "`nocornersidecandidate-has-no-producer` — delete the variant or keep it with a stated reason; a mined witness or an argument none exists." | MET (as ruled) | Ev's 👍 on PR 1733 (2026-09-04): option 1 — the variant stays as a stated defensive arm, its doc stating the invariant; item closed. |
| 10 | D: "`fillet-refusal-describes-unbracketed-crossing` — an attribution rule for the arc-carrier refusal channel, or name the crossing in the payload." | MET (ruled on PR 1734 → the ATTR unit) | Ev: report every refusing crossing nearest-anchors-first. ATTR: PR 1895 at `aa5384288`, sample #134, ordinal 2004 — `NoCornerOfPair`; Phase 1 confirmed 8.2 % mis-attribution at the base. Dual: the header's false count (bilateral MAJOR), the silent precedence inversion; both fixed. Filed: `overrun-attribution-picks-the-first-candidate`. |
| 11 | D: "`no-public-rim-arc-selector` — the consumer door: key shape (carrier circle versus named entity), co-surface seam exclusion built in, 'not one rim' refused typed." | MET (ruled on PR 1735 → the RIM unit) | Ev: `rim_of(body, edge)` in `topo::query`. RIM: PR 1821 at `40d50f272`, sample #131, ordinal 2001 — the EXACT door, four typed refusals, eleven scans made calls. Dual: the unrowed unused-arcs guard a tally candidate. Filed: `rim-door-admits-a-double-cover`, `rim-seed-finders-disagree-on-at-this-radius`. |
| 12 | Track T: "Track T's rows `D320`–`D325` land as riders on the units that touch their files; the park on #1360 is stale and is cleared at the first dispatch." | MET (as re-shaped: the two rows every lane contended landed as their own unit once the file was free) | `D322` closed as H4's rider; `D323`/`D324` closed by code-quality (PR 1783) before any FILLET lane reached them; `D325` and `D326` (filed onto the track after opening) were held while every lane contended `surgery.rs` and landed as FILLET-T — PR 1943, sample #139, ordinal 2006: the corner fusion's first arc a value, one `kef` door over a snapshot of the input body's faces (the dual's MAJOR: the first cut refused a per-phase list, not a source face). The #1360 park cleared at the first dispatch. |
| 13 | Exit shape: "The seven land, the four rulings are answered, Track T is empty; the walk convention applies." | MET-WITH-RECORDED-HONESTY (one D ruling put to Ev again at exit — row 8) | The seven landed (rows 1–7) plus the two the rulings grew (rows 10–11); three of the four D rulings answered (rows 9–11) and the fourth re-asked with its moved premise (row 8); H7's vocabulary a fifth ruling (PR 1819); Track T empty (row 12). Two units the program grew at its exit — FILLET-SPLIT (Ev's ruling on PR 1916, a move) and the possible `ThreeConcaveEdges` mint — are recorded in the honesty rows, not as criteria. |

## Honesty rows

1. **Two of the seven stopped at Phase 1 and were re-scoped by the
   orchestrator** (H4: the fold; H5: no native instance). Both re-scopes
   are recorded in the specs' §Re-scope and the A/B rows; neither unit
   proceeded on a false premise.
2. **Three tally candidates in six duals**, all test-gap class on the
   unit's own new code, all found by one lane only and demonstrated by
   execution: H4 (R1/OPUS: the setback sign), RIM (R2/OPUS: the
   unused-arcs guard; a second flagged for the blinded adjudication), H7
   (R2/OPUS: the lever at the call site). Recorded per row for the
   blinded adjudication; the running v6 tally across the stream is left
   to that adjudication (`ab-log-v6-stream-is-past-its-stopping-rule-unadjudicated`).
3. **The CI trailer died under the program.** `ci.yml` retired the
   `CI-Config` trailer and the k-lint draw on 2026-09-04; every PR run
   since gates the whole matrix. Briefs written before that carried the
   trailer as prose; the A/B rows say which runs were drawn points and
   which the full matrix. One push produced no run at all for twenty
   minutes — "CI is green" and "CI ran" are different questions, and
   every state-sync verified the run exists on the head.
4. **Fix-pass commits of one unit (H4) carry a harness attribution
   trailer naming a model**, pushed after both reviews were delivered;
   disclosed in the row, history not rewritten.
5. **Ev's two open threads at exit**: `[ev]` PR 1916 (the `surgery.rs`
   split behind a re-scoped compound-bound allowlist) — the program's
   Q8 finding, not a shortfall; and the residue slate (below), re-homed
   before the sweep.

6. **Two units the program grew at its exit are recorded here, not as
   criteria.** FILLET-T (Track T's two riders, sample #139, PR 1943) and
   FILLET-SPLIT (the open bands leaving `surgery.rs`, sample #140, PR
   1964; Ev's ruling on PR 1916 that a move with no design implication
   needs no ask) — both
   S / STRUCTURAL, both dualled under v6. Block FILLET-B3 holds them in
   slots 0 and 1; slot 2 was banked for the `ThreeConcaveEdges` mint and
   closes EMPTY unless Ev answers PR 1935 "mint" — a block closed short,
   recorded on its branch-side record.
7. **One sentence staled three times under this program.**
   `docs/KERNEL-VERBS.md`'s ruled-band row named `blend/ruled.rs` (a
   file that never existed — H7's dual), then `surgery.rs` (H7's fix),
   then was re-staled by the move (SPLIT's dual). Every token-keyed sweep
   missed the paraphrase class ("the surgery's cap plan"); the lesson
   the reviewers wrote is the one the walk repeats: sweep by SHAPE
   (who cites the moved thing), not by filename.
8. **The `kef` door's first cut claimed more than it enforced.** FILLET-T
   shipped a door refusing the running phase's hand-built list while
   three docs said "a source face"; one reviewer killed a source face
   through it. The fix pass made the claim true (a snapshot of the input
   body's faces, one carrier, one door) — the pattern this program's
   reviews found most often: a guard whose prose is stronger than its
   check.

## Residue re-homed before the sweep

Every open item on the slate at exit, with its new home (moved by `git mv`
with its header's program edited, ids kept):

| item | home | why |
|---|---|---|
| `tangent-parallel-certifier-passes-a-transverse-arc` | `work/cert/` | the certifier's `TangentParallel` margin — S-CERT's ground |
| `rim-door-admits-a-double-cover` | `work/seat/` | `topo::query::rim_of` is SEAT's by announced seam |
| `cut-off-arc-persists-as-a-corner-arc` | `work/docm/` | a persisted `RoleSeg` name — the document model's vocabulary |
| `debug-in-prose-at-blend-and-step-import` | `work/fix/` | a Display-through-Debug panic at the Python façade, fix in hand |
| `blend-size-gate-unmetered-under-epsilon`, `blend-payloads-outside-the-margin-family` | `work/props/` | metering and the margin family — PROPS' interval-honesty ground |
| `anchor-span-sole-bracket-bound-unrostered`, `anchor-span-sole-bracket-door-missing-roster-line` | CLOSED at exit if H7's roster line stands (verify), else `work/props/` | the roster line landed in PR 1897's fix pass; the duplicate-listing follow-up is already in `work/issues/` |
| `plane-hosted-rim-has-no-native-instance` | CLOSED at exit as a record | Phase 1's census; nothing is owed — the walk cites it |
| `surgery-module-holds-four-surgeries` | closes with FILLET-SPLIT | the unit |
| `ambiguity-k-below-the-cap-rim-crossover`, `blend-recourses-under-describe-their-doors`, `curved-single-host-rim-refuses-at-the-half-band-gate`, `escalated-recourse-dispatch-has-no-coaxiality-arm`, `fillet-escalation-site-has-no-producer`, `hostless-rim-on-a-ringed-host-refuses`, `ladder-rim-phase-may-retire-a-new-split-key`, `overrun-attribution-picks-the-first-candidate`, `path-fillet-door-validator-tangency-disagree`, `rim-seed-finders-disagree-on-at-this-radius`, `ring-clearance-refuses-a-nested-trim-circle`, `smooth-arm-siblings-disagree-on-the-in-band-case`, `sweep-top-field-docs-make-the-spatial-claim-capend-shed` | `work/issues/` | the blend kernel and the profile fillet door have no live program after FILLET; owner genuinely undecided — the next blend program's opening slate |
