# M8 exit walk — criteria vs evidence

**STATUS: RATIFIED — M8 CLOSED (Ev's 👍 on PR #508's closure
affordance, comment 5302962744, confirmed directly in-session,
2026-08-15). This document is M8's done-state of record. M9 (C7
declared-contact join lane + the lily FULL rebuild) opens per the
standing #300 ratification, with the D1/D2 disposition and #496's
ratified A′ shape as the seam's carried items.** M8 =
the misc kernel residuals the demos raised (ratified with M7's
close, PR #300 comment 5234714306). Criteria are quoted **verbatim**
from `docs/M8-PLAN.md` — the five firm units, the four adopted
candidates, and the exit-shape paragraph — with the units the
milestone grew en route (#327, #382 half-1) dispositioned in their
own rows. Dispositions per the M5/M6/M7 convention: MET /
MET-WITH-RECORDED-HONESTY / CARRIED (named owner).

## The walk

| # | Criterion (verbatim from M8-PLAN) | Disposition | Evidence / honesty note |
|---|---|---|---|
| 1 | M8-1: "#284 Newell chart-frame re-anchor (mesh lane derives the planar chart frame from the boundary itself; the two wild tessellation refusals flip into montage cells 6→8)." | MET | **#301** (2026-08-10): both Adafruit wild files tessellate (204 / 12 triangles, volumes to 0.015% / 0.000% of exact); the montage holds 8 cells — and hosted CI has RENDERED AND GATED all four lanes on every push since, so the flip is continuously re-proven. Both review MAJORs earned honestly: the position-noise ~ν² class is a PINNED typed posture with the doc claim narrowed, and the anchor judgment carries an in-suite falsifier that reds under the centroid mutation via a 1-ulp assert. #303 filed register-class from the review's large-coordinate probing. |
| 2 | M8-2: "rational-carrier `speed_lower_bound` (quotient-rule bound assembled from the existing hull machinery; retires the span-meter half of the rational bank; two conscious pin flips; the calochortus CRESCENT restoration rides as demo evidence)." | MET (the demo evidence re-routed, recorded) | **#306** (2026-08-10): the per-span quotient-rule bound survived the full adversarial battery with ZERO unsound cases (worst bound/truth 0.9987); the ruled deviation (arc-length contract, not injectivity) verified consumer-by-consumer; the sweep-crate pin flipped to its positive statement. **The recorded honesty**: the crescent could not ride as demo evidence HERE — it builds and certifies kernel-side but could not tessellate, which DISCOVERED the third rational bank (mesh Hessian) and became M8-5 by plan amendment; the restoration landed there (row 5). |
| 3 | M8-3: "rational-patch-flux quadrature (the M7-3 Arm B bank: volume certification for rational walls). Flips: the arc-prism waypoint (#288 — seam certifies, body currently refuses the banked quadrature), the arc-loft round-trip (#276's honestly refused class), tier-3 on rational walls." | MET-WITH-RECORDED-HONESTY | **#309 + #353** (two-PR unit, 2026-08-10/11): the flux enclosure engine (PR-1, through the project's third NOT-MERGEABLE-AS-IS — the reviewer executed a REAL soundness hole, deriv_kv's silent Dir::Const fallback, and proved it latent in MAIN's integral lane (16/3 vs true 20/3), healed as #313 in the same fix pass) + the IsoArc chart-map pcurve mint (PR-2). The arc prism and arc loft certify at default ε; the arc loft round-trips STEP bit-identically — #276's honestly-refused class RETIRED; tier-3 runs on rational walls. **The honesty**: the flips are ε-conditional — at 1e-12 the same geometry refuses with honest Budget refusals, pinned as postures, nothing widened. |
| 4 | M8-4: "the `nurbs_iso_derive` Intersection arm (the second gap #288 measured: pcurve minting for declared plane×NURBS intersection edges). *Scope amended at spec time (2026-08-14, docs/M8-4-SPEC.md, binding): #353 collapsed the integral/rational wall split … the #288 walk row retires with its ε cells stated, boundary-iso carriers only, interior/diagonal carriers keep their typed refusal with a named follow-up.*" | MET (as amended) | **#499** (2026-08-15): the arm mints boundary-column Intersection carriers through the EXISTING seam class (no new certification class — as the spec predicted), carrier-keyed (reads neither s1 nor s2), payload-domain boundaries, fixed 4-candidate schedule with a metres-metered interior probe. **Both flip rows executed their own retirement text**: the integral twin and the rational arc prism import first-class END-TO-END at default/1e-6 — the #288 walk row's full retirement — with the 1e-12 cells staying pinned refusals carrying their measured sups (6.22e-12 / 6.32e-12 class). Review 0 MAJ / 2 MIN with every constructed pick attack refused typed, and the one re-pinned pre-existing row (deviation #5) proven honest by the reviewer's own merge-base measurement. Interior/diagonal carriers refuse typed and permanently — the named follow-up is **#498**. Riders: adopt.rs writes knot-domain ends (with a red-then-green reversion proof), the IsoUnsupported doc truthed. |
| 5 | M8-5 (plan amendment, 2026-08-10): "mesh rational deviation certificate … `mesh/src/nurbs_cert.rs:151` refuses rational NURBS faces, no certified Hessian bound; the THIRD rational bank. The working crescent restoration … rides this unit; the #218 empirical per-triangle falsifier guards whatever bound lands." | MET | **#322** (2026-08-10): both mesh gates open (Hessian + sagitta, quotient rule over the homogeneous nets); the crescent restoration LANDED — the calochortus crescent builds, certifies, and tessellates (the M8-2 demo evidence completing one unit later); the #218 falsifier guards the bound; the review's one MAJOR was test-strength (a 1500-patch sweep caught a cross-term mutation the suite missed — adopted red/green). |
| 6 | Candidate (adopted): "Assembly instancing, now tracked as #317 (dm1's blocker — the M7-6 walk row's named carried item; flips dm1 first-class, wild corpus 9/13 → 10/13)." | MET-WITH-RECORDED-HONESTY (the flip claim was optimistic; the refusal ADVANCED twice and now rests at a named bank) | **#325** (2026-08-10): instancing is LIVE (rep-graph composition, fresh-id materialization, graft_disjoint, the ratified A7 record shape) through the project's fourth NOT-MERGEABLE-AS-IS (nested outer transforms silently dropped — refuse→silently-wrong, answered with COMPOSE + three typed refusals, delta-verified against hand oracles). **The honesty**: dm1 did NOT flip — the placement layer had masked deeper ladder gaps. Edge #668 closed in-unit; edge #685 became #327 (row 8); after #327, dm1 rests on **#390** (rational flux at its quadrature budget) with nine ε cells pinned. The wild corpus honestly stands 9/13, every refusal naming its class — the S9 waypoint pattern, again. |
| 7 | Candidate (adopted): "#214 dimensional-debt riders (F6/F7 typed-margin fold-in — M8-2 touches F7's predicate and deliberately leaves the redesign; the natural moment per docs/predicate-dimension-audit.md)." | MET | **#502** (M8-F67, 2026-08-15): F7's bare rate and both census F6 sites proved to be ONE quantity (`speed_lower_bound`) handled three inconsistent ways — now one metred gate (knot-domain length × certified speed bound, collapsed-arm at every lane), the cone azimuth headroom on its real lever, decide_flagged census 12→8, the audit doc truth-passed (incl. its formerly-FALSE trim_containment row). The review executed everything: the arc-length bound re-derived by hand both arms, the reparametrization twin (t→2t invariant where the bare rate splits by exactly 2), three comparand mutants red, and the ~1.82M-row K-stream diff reproduced byte-for-byte — ZERO outcome changes, k-lint gate green at all three ε, NO baseline re-cut (smallest new margin 0.312 m, ~4 decades above the floor). The excluded topo::pcurves arm residue is **#501**. |
| 8 | Candidate (adopted): "#222 long-turn sweep frontier (the ≥0.5-turn helix chord collapse — integral class, distinct from M8-2's rational work)." | MET | **#369** (2026-08-11): integral `speed_lower_bound` = JOIN of the verbatim old global-chord arm with a per-span scan (provably never below the old arm; poison lattice with abstention). Dual sample #9's calibration point earned (the same coverage gap MINOR-by-inspection to R1, MAJOR-by-demonstration to R2 — two unsound mutants at +2.3 overshoot), all four union mutants red-then-green. Helices to 2 turns certify with second-order Pappus convergence; the ≥π planar-spine wall honestly filed as **#368** (ReversedStacking) per the inclusion-or-follow-up rule. |
| 9 | Candidate (adopted, then retired pre-unit): "The stale large-K lint floor — RETIRED (verified 2026-08-10, cad-work/kfloor-check-report.md): fully consumed by #239 + #253 … No unit scoped." | MET (verification in lieu of a unit — the plan's own text) | The verification run confirmed the floor work fully consumed: lint code matches the K-REPORT addendum, the #250 register row closed, the latest hosted gate 0-flag at all three ε on a corpus grown +31k samples/row. The adjacent ε=1e-6 blind-window row stays open BY DESIGN with its own trigger. |
| 10 | (Grew en route — #325's honest advance, Ev's excluded-kinds rule binding the spec): #327 stage-1 CURVE recognition. | MET-WITH-RECORDED-HONESTY | **#391** (M8-14b, 2026-08-12): circles promote with an EXACT certificate AND a measurably-refusing coverage gate — the unit survived the project's fourth and fifth NOT-MERGEABLE-AS-IS (the FIRST cross-model dual, sample #10: both reviewers independently executed on-locus carriers silently promoting through the old turning witness; the fix's structural certificate was then itself caught right-in-ℝ-wrong-in-f64 and closed with a salient-cone margin, δ=π/6, whose removal is a 3-RED mutation). **The honesty**: excluded kinds carry NAMED follow-ups per Ev's rule — #388 (degree-1 promotion + the ExtrudedPoint rung), #389 (open arcs/ellipse/helix + the gate's measured 150° tightness scope); dm1 advanced to #390. |
| 11 | (Grew en route — ASM-2K's finding, filed as #382): doc-truth half of the overlap-detection gap. | Half-1 MET; half-2 CARRIED (named, design-scoped) | **#491** (2026-08-15): `topo::instance` + three further sites now state what `validate_geometric` PROVES (per-entity local battery + a summed signed volume — overlapping solids pass undetected by construction) and point at the tier-3′/A5/C6 design. Half-2 (detection itself) stays OPEN on #382 by the issue's own scoping — contact/interference is M9/M10-era census work, not an ad-hoc graft-door check. |
| 12 | Exit shape: "Every demo-raised refusal this milestone owns either FLIPS (the crescent sweeps, the arc prism imports first-class end-to-end, the wild montage holds 8 cells) or is re-banked explicitly with a named owner and flip condition. No silent survivals: each of M7's flip-when-fixed pins touched by these units executes its own retirement text. Hosted CI green on every merge; the walk convention (criteria vs evidence, honesty rows) applies at exit." | MET-WITH-RECORDED-HONESTY | The three named flips: **crescent sweeps** (rows 2+5 — builds, certifies, tessellates); **arc prism first-class end-to-end** (rows 3+4 — TRUE at default/1e-6, the ε cells stated and pinned at 1e-12; the ε-conditionality is the plan-amendment honesty, recorded before the unit ran); **montage holds 8 cells** (row 1, re-gated every push). Every touched M7 flip-when-fixed pin executed its own retirement text (#276's arc-loft class at row 3; #288's walk row at row 4; the seam-orphan lineage closed end-to-end). Re-banked with NAMED owners + flip conditions: #390 (dm1/rational budget), #498 (interior/diagonal carriers), #501 (pcurves arm residue), #388/#389 (recognition exclusions), #368 (≥π spines), #382 half-2 (tier-3′/A5/C6), #497 (env-sensitivity class), #427 (pcurve unification, M9 design). Hosted CI: every unit merged only on its own fully-green head, and main's tip containing every M8 merge (132c0494) ran GREEN. |
| 13 | (Not a plan criterion — demo-raised AFTER the slate, die recipe conversion 2026-08-14): residuals D1 (revolve name emitter refuses all-on-axis loops — no sphere reaches `Node::Revolve`) and D2 (no group union in the recipe layer). | CARRIED (named, in-progress) | Raised post-ratification, so they are M8-CLASS but not M8-SLATE: **D2** is the OPEN design conversation **#496** (options argued, Ev engaged on-thread; schema-v9 coordination summoned to LIB); **D1** is the next candidate behind it (adoptable by the M8-5 amendment precedent, crate-disjoint). Neither blocks anything today; both workarounds are visible as workarounds with pointers back to the log entry. Disposition — carry into the next program alongside #496's outcome — is part of this walk's ruling. |

## Walk evidence beyond the criteria

- **The A/B record**: every M8 unit row recorded AT MERGE
  (M8-1..M8-5, M8-14a/#222, M8-14b/#327, M8-F67, M8-4 + the
  instancing row), spanning the v3→v4 protocol transition executed
  per the amendment; ordinals 37/38 claimed on main at their review
  dispatches per the main-is-authority rule; the dual stopping-rule
  tally stands 4-of-6 (both M8-side thirds fell to other programs'
  ordinals; the milestone's own duals were samples #9 and #10, both
  qualifying rows already counted).
- **Flaky-gate hygiene, fail-loud both times**: two latent hosted
  flakes surfaced during M8's endgame and were fixed ON MAIN with
  red-then-green seed proofs rather than retried away — #489→#490
  (the scratch-hull subnormal cap, re-derived from the ring's pad
  structure instead of a constant) and #503→#505 (the enclosure
  test's lattice corner landing outside its own box; the enclosure
  contract itself adjudicated INTACT). #497 filed for the adjacent
  ambient-env class. #415 closed as already-fixed with independent
  red/green verification at the pre-fix merge-base.
- **Process record**: the milestone crossed a full orchestrator
  pause/cold-resume (2026-08-12 → 2026-08-14) with nothing lost;
  Ev's small-change boundary ruling recorded (orchestrator-direct
  + merge-on-green below the unit threshold, full protocol above);
  the shared-scratchpad hazard caught live and converted to a
  standing lane rule (lane-private publish paths); the expired
  hooksPath migration shim retired on schedule.
- **Hosted state**: main's full-matrix run on the tip containing
  every M8 merge (132c0494) — GREEN at presentation; renders gated
  on all four lanes throughout.

## Tally

Thirteen rows: **6 MET** (rows 1, 4-as-amended, 5, 7, 8, 9), **5
MET-WITH-RECORDED-HONESTY** (rows 2, 3, 6, 10, 12 — each honesty
enumerated, pinned, and carrying a named flip condition), **2
CARRIED with named owners** (row 11's half-2 by its own design
scoping; row 13's post-slate residuals with #496 live). Zero
criteria unmet, zero honesty items unpinned, every re-banked
refusal names its owner and flip condition.

**Closure is Ev's call.** Presented via a docs-only PR; his
sign-off on the closure affordance ratifies M8 CLOSED — M9 (C7
declared-contact join lane + the lily FULL rebuild) opens per the
standing #300 ratification, with the D1/D2 disposition and #496's
outcome as the seam's carried items.
