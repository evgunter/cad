# M9-5 — the lily FULL rebuild (spec)

**STATUS: RATIFIED (Evan, #966 comment 5388118595, 2026-08-23) —
M9-5 = SEAM D ONLY.** The K-ruling, as answered: **Q1 — wall 1
DEFERRED as a named residue**, recorded for later pickup as issue
**#968** (the torus declared-Rest lane: gate admission, the
carrier_eq torus rung, the torus×torus rim-tangency vocabulary);
its probe stays, its text truthed to name #968. **Q2 — wall 7
HANDED to the VERBS breadth program** ("write it into their
plan"): its needs map exactly onto VERBS-PLAN Wave 2 items 6
(VERBS-GATE, the per-face-kind gate re-scope) and 9
(VERBS-SPHSPH, the sphere×sphere germ lane) — annotated there
with the wall-7 demand signal at this ratification; wall 7's
disposition is dependency-stated like wall 8's, naming those
items. **Q3/Q4 — MOOT** (they only applied if a K-seam was
bought; none is — the operand gates do not open in this unit and
the klein pins are untouched). **Q5 — seam D carries #782's
arming** (Evan asked what the stale comment is — answered on the
PR: ci.yml's klint-row prose still says two finding-13 rows "are
red on main today", which #782's decision has since falsified,
and the widening-is-owed obligation it records is now due).
**Q6 — FULL content latitude** ("go crazy — you can even change
the geometry"): the rebuild may re-author the lily's geometry
where the new doors give a more natural spelling; demo-purpose
discipline still governs (real usage through public doors,
awkwardness reported). The substrate finding that forced the
conversation stands recorded: **no lily wall flips from M9-3**
(dedicated exploration 2026-08-23 against main @ 6cda1dd0;
anchors below). Dispatch waits on M9-3 PR-B (the demo cell needs
its zip).

## Dependencies (all measured, none assumed)

- **M9-3 PR-B (the generalized zip)**: the two-peg demo cell's
  union completes only with it; K1/K2 touch
  boolean/{reduce,rest,ops,mod,carrier_eq}.rs — the join lane's
  own files. HARD serialization after PR-B's merge.
- **The tessellation baseline pointer (EXISTS)**: ASM-LOG tail —
  the shipped selection is `split_steps` + the fixpoint snap,
  baseline re-cut at 31f052d2 (re-cut commit 00cbdc5a;
  tess-budget-baseline.csv carries 90 `lily/*` rows of 1075, cut
  WITHOUT --sizing-only). The pause-entry's wait-or-derive fork is
  moot.
- **#554 is CLOSED** (PR #910, 2026-08-22) — the plan's
  rides-or-banks question is MOOT; wall 6's pin survived #910
  unchanged (n_a×n_b ≡ 0 on a co-surface seam regardless of
  lever).
- In-flight VERBS work (#962/#960, crates/sweep) is file-disjoint
  except demos/README prose.

## Wall inventory (current main; the honesty table)

| # | probe | refuses today | site | disposition |
|---|-------|---------------|------|-------------|
| 1 | lily.rs:1422 | CurvedBooleanUnsupported{Torus} | operand gate, reduce.rs:184 | stays — NAMED RESIDUE, banked as #968 (ruled) |
| 2 | lily.rs:1444 | CurvedBooleanUnsupported{Cone} | same gate | DEFERRED by ruling (germ-chord lane stays banked) |
| 3 | lily.rs:1492 | ObliqueExtrusion | extrude | stays |
| 4 | lily.rs:1508 | NotRigid{col2_unit} | transform | stays (#250 triage) |
| 5 | lily.rs:1529 | NotRigid{det_plus_one} | transform | stays (#250 triage) |
| 6 | lily.rs:1544 | TangentialEdge{margin==0.0} | fillet battery | stays (post-#910 genuinely the co-surface seam) |
| 7 | lily.rs:1563 | CurvedOpUnsupported{Subtract} | ops.rs:415-427; no sphere×sphere lane (ops.rs:1560-1568) | stays — dependency-stated on VERBS Wave 2 items 6+9 (ruled) |
| 8 | lily.rs:1620 | CurvedEdgeUnsupported | reduce.rs:206 | stays — flip rides the post-M9 pcurve migration (ratified) |

The wall harness (demos/tour/src/walls.rs:23-43) panics on ANY
move, and the tour runs per merge in ci.yml's tess-budget sweep —
whatever K-seam lands, the walls it does not open must keep their
texts VERBATIM, and every flip re-writes its probe deliberately.

## Seam D — the rebuild, the cell, the baselines (BINDING)

1. **The rebuild executes what the join lane enables and
   dispositions the rest honestly** (the plan's exit-shape
   sentence governs, not the optimistic item-7 clause): the
   declared-contact joins the lily can now spell (the tube-chain
   Tangent rims are M9-3 PR-B fixture (ii)'s shape) are authored
   through the public doors the way a real user would; walls that
   still refuse keep probes with texts truthed to the
   post-M9-3 sites. The lily's CONTENT stays Evan's (Q6 asks the
   latitude); the rebuild is the ratified plan its own wall-probes
   state.
2. **The two-peg plate cell** — the join lane's canonical new demo
   cell (settled twice: M9-PLAN.md item 7; M9-3-SPEC PR-B fixture
   (i) keeps the CELL here). Scene module with assembled+apart
   framings, three declared contacts (one planar + two cylindrical
   Rest), volume exactly additive against the closed-form oracle;
   the sheet consolidation folds the crosslap/plate pairs
   (sheet-only); manifest/README/baseline rows. The demos/README
   revisit anchor is :598-613 on current main (the M9-3 spec's
   :569-585 has drifted). Consolidate only after PR-B — crosslap
   is pinned byte-identical during M9-3.
3. **Re-baseline, with the diagnose-first discipline**: the
   tess-budget CSV via scripts/tess_budget_sweep.sh (read the
   `vanished` findings; TESS-BUDGET.md:320-336); the finding-13
   table (lily.rs:1897-1910 — leaf_b 468 / leaf_c 414, Pappus band
   (5e-5, 4e-4)) under #782's diagnose-before-re-pin rule; the
   renders (lily.png, montage/manifest, main.rs:763-792
   arithmetic; uv self-re-baselines). LEAVE: the split machinery
   and A=16; non-lily rows (regenerated but must not move);
   rebuild-latency history (append-only; carries no lily rows —
   verified).
4. **#782's CI arming rides here** (proposed, Q5): the
   ci.yml:2226-2239 arming obligation is unblocked and lily-
   adjacent — the finding-13 pin joins a lane that actually runs,
   and the stale ci.yml comment (which still claims two rows red)
   is corrected by the same change.

## Seam K1 — wall 1, the stem glue (RULED: deferred → #968)

What the flip needs, measured: torus through the operand gate
(reduce.rs:171-198 refuses ANY torus face at :184 before sweep or
zip — a verified planar-disc declaration cannot route past it); a
torus rung in `carrier_eq` (carrier_eq.rs:167) so the declared
Rest descent has a verdict to consume; M9-3 PR-A's rung/descent
pattern extended to the torus arm; and a disposition for the
cross-operand torus×torus tangency at the shared rim circle —
whose in-carrier-scope sibling is exactly M9-3 PR-B's fixture
(ii). The DEV-1 witness loci (plane×cylinder, parallel cylinders)
do NOT cover torus×torus, so the tangency either gets a
CurveContact-granularity treatment, a wedge=π smooth-seam
treatment, or an honest STOP clause (Q1). Blast radius: klein
walls 3/4 pin the same gates — opening must be CONDITIONAL on
covered declarations or those pins panic (Q3).

## Seam K2 — wall 7, the tepal seam (RULED: handed to VERBS)

What the flip needs, measured: per-arm relaxation of the
non-union kind gate (ops.rs:415-427 — the lantern's cone pucker
takes `CurvedOpUnsupported` before the cut is attempted) and a
sphere×sphere germ arm (today a typed refusal, ops.rs:1560-1568).
This is an UNDECLARED TRANSVERSE cut — curved-boolean BREADTH,
not declared contact; KERNEL-VERBS.md:43 registers this exact
row. It consumes M9 implementer budget without touching the
milestone's C7 subject — which is why Q2 asks whether it is M9-5
scope at all.

## Acceptance (unit, seam D; K-seams add rows if ruled in)

1. The rebuilt lily authors its joins through the public doors
   (demo-purpose discipline — awkwardness is a library finding,
   never hidden); every still-refusing wall's probe text is true
   of current sites; the wall harness stays green through every
   intermediate commit.
2. The two-peg cell renders assembled+apart with the
   exactly-additive volume statement in the cell's own narration;
   crosslap unregressed (byte-identical where mechanical).
3. Baselines re-cut with each move diagnosed before re-pin (#782
   discipline); non-lily rows unmoved; no baseline treated as a
   target to preserve.
4. Hosted CI green on every merge; the tess-budget sweep's
   `vanished` findings read and dispositioned in the PR body.

## Process

Dispatch AFTER M9-3 PR-B merges (the demo cell needs its zip;
seam D's file overlap with the join lane is the crosslap
behavioral pins and demos). The implementer arm comes from
**block M9-16** (drawn 2026-08-23, byte 80; slot 1 = fable,
consumed by M9-3; slots 2–4 all opus — slot 2 went to the census
rest-closure unit): **M9-5 takes the next open slot at dispatch =
OPUS**. Difficulty pre-logged: **seam D = M**, task-class
STRUCTURAL/demo-class. Demo-class units are orchestrator-reviewed
per the plan's process section. Standard brief lines apply as in
M9-3's spec.

## Questions for Evan (the K-ruling)

1. **Wall 1's tangency vocabulary**: the torus×torus rim tangency
   at the shared rim circle — declared Tangent at CurveContact
   granularity (DEV-1's witness loci don't cover it, so this
   grows the witness lane), a wedge=π smooth-seam treatment like
   PR-B's fixture (ii), or an honest STOP: wall 1 narrows to a
   named residue instead of retiring. My read: the STOP shape is
   acceptable and the plan's exit sentence ("dispositioned
   honestly") already licenses it; buying the witness-lane growth
   now is the only path to a true retirement.
2. **Wall 7's allocation**: the sphere×sphere germ arm +
   non-union gate is curved-boolean breadth (KERNEL-VERBS row),
   not declared contact. Keep it in M9-5 so the lily finishes its
   milestone with both headline walls retired, or hand it to the
   verbs/breadth slate and state wall 7 dependency-style like
   wall 8? My read: hand it off — M9's subject is C7, and wall
   7's honest disposition names the registered row it waits on.
3. **Gate opening posture** (if any K-seam is bought):
   conditional on covered declarations — klein walls 3/4 survive
   untouched — or unconditional with same-PR klein retirements?
   My read: conditional; C8's auditable-door principle extends to
   operand gates.
4. **PR shape if both K-seams are bought**: K1/K2/D as three
   serialized PRs of one unit, or shed K2 regardless of Q2? (Moot
   if Q2 hands wall 7 off.)
5. **#782's arming**: seam D claims the ci.yml arming + stale
   comment correction (proposed above). Veto if you want it
   elsewhere.
6. **Content latitude**: the FULL rebuild touches authored
   geometry (lanceolate arcs, lily.rs:72-77; #960's tube door may
   change natural spellings). The lily's content is yours — how
   much may the rebuild re-author where the new doors make a more
   natural spelling, vs. preserve verbatim?
