# S-BOOL exit walk — criteria vs evidence

**STATUS: PROPOSED (design conversation — awaiting Ev's
ratification; on sign-off this document is S-BOOL's done-state of
record, the program closes, and the successor PATHS opened in the same
PR stands).**
S-BOOL = boolean reach and containment (`work/bool/plan.md` /
`work/bool/log.md`; opened 2026-08-31 from the ratified stream cut in
`docs/WORK-STREAMS-2026-08.md` §S-BOOL by the S-MESH orchestrator;
A/B band 1100–1199). The plan's "Exit shape (proposed)" paragraph
supplies the criteria, quoted verbatim, one commitment per row,
dispositions per the S-MATE/S-CERT convention: MET /
MET-WITH-RECORDED-HONESTY / CARRIED (named owner).

Thirteen units merged: ordinals **1100–1112**, samples **#75, #84,
#91, #93, #100, #104, #156, #164, #212, #214, #215, #216** and BOOL-4's
at its merge (PR #2767 — see row 3). Three rulings ratified in-program
on the Q1 chain (the straight continuation, PR 1512's point-target
continuation, the declared arrival's joint token) plus Ev's option-C
schema ruling (BOOL-13) and the 2026-09-13 sixth-round ruling that
dropped BOOL-10's split form.

## The walk

| # | Criterion (verbatim from `work/bool/plan.md` §Exit shape) | Disposition | Evidence / honesty note |
|---|---|---|---|
| 1 | "`point_in_solid` answers cone and torus with the grazing posture escalating (#1011, both VERBS pins flipped)" | MET | **PR #1425** (BOOL-2, ordinal 1101, sample #84): the ray×cone arm (quadratic + nappe test + axial trim through the shared `chart_azimuth_margin`), the Cone `KindUnsupported` refusal retired as a D2-row-2 capability landing. **PR #1464** (BOOL-3, ordinal 1102, sample #91): ray×torus on a CERTIFIED root count (monic quartic; the count read off exact sign algebra, never off the roots; a disagreeing constructed count escalates `bool_ray_torus_count` typed), two chart-trim windows, tangential and grazing escalating; both `verbs_gate_r1_probes` pins flipped in that PR; issue 1011 CLOSED at merge. |
| 2 | "coplanar splits carry adjacent citations (#1152, probe un-ignored)" | MET | **PR #1378** (BOOL-1, ordinal 1100, sample #75): the section-boundary citations restated — root cause STALE, not absent (extrude-time operand edges whose wall pairs moved to the above product); the P-1b probe un-ignored; issue 1152 closed at merge. |
| 3 | "material containment separates box from material and the pocket/cavity refusal retires with S-MATE consuming the record (#750)" | MET-WITH-RECORDED-HONESTY (landing) | **PR #2767** (BOOL-4, ordinal 1112, sample at merge): the census's instance-containment arm keeps its six-margin box test as the GATE and decides material through a per-solid entry over `point_in_solid`'s one closest-hit core (the issue-1011 arms consumed, as the plan's first question asked); the L-bracket, pocket and cavity certify; a decided interference refuses typed (`InstanceInterference`) instead of "undecidable"; neither of issue 750's falsifications retried. **The honesty, threefold**: (1) the spec's literal gate could never reach its own embedded-cube fixture — flush is the box's IN-BAND case — so the gate was corrected at the implementer's measurement (both box outcomes go to the material test); (2) BOTH review arms falsified the first head's single-witness invariant with executed constructions (a crossing degraded to vertex-on-face touches; a container's void shell inside the part), bilateral, no tally — the fix pass redesigned the arm's soundness conditions (every vertex probed, both orderings always, touches admitted only as locally one-sided rests) and this row is finalised at the merge; (3) S-MATE has EXITED (`docs/DOC-LEDGER.md` sweep 6), so "S-MATE consuming the record" is discharged by the record living in the design page of record, `crates/editor-core/ASSEMBLY.md`, as present-tense description only — no forward-binding clause was added (the one the first head added to `topo/README.md` was withdrawn in the fix pass). Issue 750 closes at the merge; the `declared.faces` observation and the partial-overlap-at-the-gate hole are filed on CURVED. |
| 4 | "the rim-free wedge computes its volume (#542)" | MET | **PR #2748** (BOOL-5, ordinal 1109, sample #214): `sphere()`'s rimless branch gains the WEDGE arm (`props_wedge_azimuth`, the face's own arc read structurally off the sense bit and traversal, the closed form signed by the face side, the pole-to-pole premise checked on the pole helper's own margins); issue 542 CLOSED at merge. The dual's bilateral finding — the coplanar arm could not tell opposite from coincident meridians, reachable through `revolve` with a SILENT number — was closed in the fix pass (`props_band_opposite`); the wedge arm's missing lineage fold and the file's two D9 readings are filed on PROPS. |
| 5 | "#433 and #368 are ruled and their rulings implemented or recorded at both sites" | MET | **#433, both halves, five units on the ratified Q1 chain**: **PR #1508** (BOOL-8, ordinal 1103, sample #93 — the straight continuation, merged on Ev's §4 sign-off), **PR #1520** (BOOL-11, ordinal 1104, sample #100 — `continue_to`, the declared point-target continuation; three rulings out of one measurement), **PR #1573** (BOOL-12, ordinal 1106, sample #156 — the declared arrival at the seam, HELD for Ev through three addenda and merged on his release), **PR #2134** (BOOL-9, ordinal 1107, sample #164 — the RawLoop demotion: the vertex table a cache, authoring through the lattice only; issue 433 CLOSED at merge with both halves landed), **PR #2135** (BOOL-10, ordinal 1108, sample #212 — `arc_continue` retired; the declared-split arc form BUILT, dual-reviewed, fix-passed and then DROPPED by Ev's sixth-round ruling of 2026-09-13, preserved at `f79fa7081`). **#368**: **PR #2752** (BOOL-6, ordinal 1110, sample #215): the per-slab stacking fold under the Q2 ruling, bitwise for two sections, the klein wall retired in place, issue 368 CLOSED at merge; the honesty recorded in its row — curls past a full turn build SELF-OVERLAPPING bodies with every tier silent, disclosed and filed on BLEND, not presented as wins. |
| 6 | "#134 is routed per its ruling" | MET-WITH-RECORDED-HONESTY | **PR #2755** (BOOL-7, ordinal 1111, sample #216): the shadow-exec rung under Ev's option-(a) ruling as Q3 routed it — per partner, at the boolean's OPERAND, the emitter's own verdict door, calibrated against the record, typed declines, nothing spliced into any log. **The honesty**: the first head resolved partners at their first carrying node and probed the UNPLACED body (a fabricated flip — R1's unilateral executed MAJOR, tally +1) and pooled partner populations (R2's MAJOR, bilateral with R1's minor); the fix pass redesigned the second side, and the unit's own headline scenario now recovers NOTHING, honestly — pruning a pair and re-qualifying a name are different events. Two halves of the issue are recorded as limits rather than claimed: the OrderAlong half (the qualifier records no partner — `work/wire/`) and the collapse half (the group collapsed with the partners unmoved — `work/wire/`). Issue 134 closed at merge with both on record. |
| 7 | "track Q's topo rows are empty in §D with the carve-outs discharged or explicitly ceded (S83/D36 to whoever holds `ssi*` when P-2 lands; D283 ruled)" | CARRIED (CURVED ×6, PRED ×3, S-TINT ×2, PROPS ×1) | No Track Q row ran as a lane: the shared lane budget went, in order, to the seven defect units and the four Q1-chain units the rulings kept producing, and Ev's 2026-09-16 direction was to finish the units and close the program. BOOL-Q dissolved at this exit and its rows re-homed by FILE, not dropped: D280, D284, D95, G9, S173, S234 to CURVED (whose `paths` hold `census.rs`, `boolean/*`, `chord_join.rs`); D46, D57, D281 to PRED (the predicate-dimension ledger class); D287, D66 to S-TINT; H11 to PROPS. The carve-outs stand as the plan recorded them: D283 is Ev's question (not ruled in this program); S83 and D36 wait on PCURVE P-2 with `ssi*`. |
| 8 | "Every unit merged on its own green hosted head; the walk convention applies at exit" | MET | Thirteen unit merges, each on a hosted green run verified at the check-run level (39 checks, 12 `test (…)` rows and 5 `k-lint (gate, …)` rows, skips standing) before the merge, the A/B row riding the unit branch as its last commit under the docs-only-over-a-green-head convention — and where the landing merge of main carried code into the unit's crates (BOOL-5's and BOOL-6's did), CI was re-run on the merged head before the merge. This document is the walk. |
| 9 | Process (§Process, verbatim in substance): "v6 duals, blocks `BOOL-B<n>`, ordinals claimed on main at review dispatch from band 1100–1199, blinding, record-at-merge, hosted CI as the only gate, … discipline/reviewer prompts by path" | MET-WITH-RECORDED-HONESTY | Thirteen v6 duals, ordinals 1100–1112 all claimed on main at dispatch, rows at merge with per-phase tokens and wall-clock, briefs symmetric with HARD RULES written fresh per unit, blocks BOOL-B1–B5 drawn branch-side and quoted from the private draw file before an arm was named. **Tally**: BOOL-9 +1 (fable), BOOL-10 +1 (opus, on the split-form head later dropped by ruling — the findings stand as found), BOOL-7 +1 (fable); every other dual bilateral or clean. **The honesty, five notes**: (1) the band was renumbered twice at opening (900 lost to GAUTH, 1000 to SEAT) and fixed on main at BOOL-1's claim; (2) BOOL-12 was HELD for Ev through three in-chat addenda and merged by another session on his release, its state-sync landing four days later; (3) BOOL-7's first R1 reviewer HUNG after the permission classifier denied a plain file read and was relaunched from a clean worktree — the pair counts on the relaunched arm, disclosed in the row; (4) BOOL-7's two reviewers SHARED one target directory to save disk and cargo served one of them a stale library across worktrees (its lock serialises builds, it does not prevent clobbering) — every dual thereafter got its own target, recorded at BOOL-4's claim; (5) the BOOL-6 and BOOL-7 union fix passes were REDESIGNS driven by the duals (the fold's second reader, the rung's second side), taken implementer-inherited and recorded per row rather than as new units. |

## Walk evidence beyond the criteria

- **BOOL-13** (PR #1553, ordinal 1105, sample #104) executed Ev's
  option-C schema ruling — `SCHEMA_VERSION`, its ledger, the version
  door and the ratified-empty migration chain demolished — a unit the
  cut did not name and the program's rulings produced.
- **The Q1 chain as doctrine**: the sixth-round ruling ("every
  zero-turn joint is a declared tangent joint; the lattice never asks
  whether carriers are the same") is what BOOL-8/11/12/9/10 left in
  `docs/PATHS-DESIGN.md` §3/§4; the split form's rejection is the
  program's clearest example of a design measured, built, reviewed
  and then declined on its cost (a required wire field, three types,
  a leaky admissibility surface) — preserved in history, not on main.
- **The experiment's yield**: three tally candidates in thirteen
  duals (BOOL-9, BOOL-10, BOOL-7), two bilateral central findings at
  split labels (BOOL-6's pooled-population class on BOOL-7; BOOL-4's
  invariant), two duals where BOTH arms falsified the same claim by
  different constructions (BOOL-4; BOOL-5's revolve-reachable blind
  spot) — calibration data for the blinded coding.
- **Residue re-homed at this exit, by file** (`work/README.md`'s
  closing rule): the eight lattice items to the successor **PATHS**
  (`work/paths/`, band 5000–5099, opened in this PR); 29 boolean,
  containment, join and declaration items plus six Track Q rows to
  CURVED (its charter names this ground and "inherits at S-BOOL's
  exit"); the graft, the two `topo::split` items and the deferred
  void-birth marking to TOPO; the profile fillet door's three items,
  the two loft findings and the subdivided-side lowering to BLEND
  (`crates/sweep/src/loft.rs` passes to BLEND's `paths` here); the
  raw-door gate to GUARD; the Python pins to LIB; the collapse-half
  limit to WIRE; D46/D57/D281 to PRED; D287/D66 to S-TINT; H11 to
  PROPS; the two heat-sink demo items to `work/issues/` (demos/tour is
  in no program's `paths`). BOOL-Q closed as dissolved. Nothing was
  left in `work/bool/` but the program files, the closing unit and
  its issue.
- **Filed forward on other slates during the program** (each with
  its home): PROPS — the forgeable certificate types (BOOL-9), the
  sphere-wedge lineage fold and the two D9 readings (BOOL-5), the
  sphere-flux item re-scoped; BLEND — the skin's unbanded chord compare
  (BOOL-6, re-scoped with the measured hand-off); WIRE — the OrderAlong
  partner (BOOL-7); DOCM — the GUI-1 fuzz guard (MESH-12's landing);
  LIB — the north-star audit's `arc_continue` name (BOOL-10);
  `work/issues/` — the klein one-body scene (BOOL-6).
- **What opens with the sweep** (per `work/README.md`): `work/bool/`
  is deleted at ratification with the walk, recorded in
  `docs/DOC-LEDGER.md` with the SHA; `crates/topo/src/boolean/*` and
  `splitting/*` are CURVED's outright (the fence dissolves);
  `crates/editor-core/src/resolve/vdiff.rs` is EDIT's (resolve is
  EDIT's territory per `work.py territory`); `crates/profile/*` is
  PATHS'; `crates/sweep/src/loft.rs` is BLEND's.
- **Open with Ev at exit** (nothing blocks ratification): this walk's
  sign-off and, with it, the PATHS opening; D283 (Track Q's carve-out
  the plan called "Ev's question", now CURVED's to raise); the
  `#[non_exhaustive]` question on `Diagnosis` that BOOL-7 left to the
  edit lane; the blinded tally coding whenever the analysis runs
  (three candidates queued from this band).
