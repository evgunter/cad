# QA-6 PR 2 — the measured-claim sweep, remaining legs (issue 681)

Unit spec, S-QA program (`docs/S-QA-PLAN.md` §QA-6; PR 2 of 2 —
PR 1 was #1311, the uncontested legs). Binding alongside
`docs/prompts/implementer-discipline.md`. **Read issue 681 in full
first**, then PR #1311's body (the instrument's contract, its
variants and blind spots, and the per-leg tally format live there).
The instrument is committed: `local-scripts/measured-claim-sweep.py`
— extend it with each new leg's variant; it must not mint undated
numbers of its own (PR 1's lesson, twice over).

## Legs

1. **`.github/workflows/` + `scripts/`** — every numeric/measured
   claim in ci.yml + nightly.yml comments and in `scripts/*`
   headers/docstrings, Q6-bucketed AT the site: dated + derivation,
   register-backed, guarded, or a written reason. Two standing
   facts shape this leg: `docs/CI-MINUTES-2026-08.md` is a
   deliberate hand-maintained register (its own paragraph says why)
   — claims that point there are REGISTERED, do not re-litigate its
   figures; and the tree is fresh with dated claims from qa/7 and
   qa/8 (this week) — verify bucket compliance, do not re-measure
   what carries a date and a derivation. The QA-6 lesson binds:
   before calling any figure dated, check for the live register
   that re-takes it.
2. **`tools/` lint-threshold provenance** — k-lint, tess-lint,
   tess-meter: every threshold, count, and cost claim gets its
   provenance stated (measured when, by what, refreshed where).
   PROVENANCE ONLY: change no threshold, no rule, no behavior.
   tess-lint's module docs are the budget-rule roster of record —
   point, never copy. KEEP OUT of Track K's contested
   bounds-allowlist rows (D102/D103/D106/D109) entirely.

## Deferred legs — disposition in this PR, not silence

3. **`crates/*/tests`**: Track W's fence (`docs/SMELL-SCAN-2026-08.md`
   §Track W owns test-side mechanisms, live rows). Write the
   disposition into the PR body and at the instrument's blind-spot
   list: the leg is W's ground, the class home is issue 651, and a
   W-lane picking it up finds the instrument ready. Do not sweep it.
4. **`docs/` prose**: stays deferred per the plan (highest
   over-match, lowest yield) — restate the reason where the
   instrument names its blind spots.

Issue 681 closes at merge on PR 1 + PR 2 + these two recorded
dispositions (orchestrator closes; keyword-adjacency rule: write
"issue 681", never a closing keyword).

## Premises to verify at merge base

The tallies are taken AT THE MERGE BASE (PR 1's irreproducibility
lesson — a head count that includes your own diff is not a tally);
re-derive every claim set rather than phrase-grepping for it (QA-3's
lesson: a phrase-grep finds true sentences and misses false ones).
Live-lane check at start: list open PRs; any file another live lane
is editing gets a deferral note, not an edit.

## Out of scope / fences

Thresholds/rules/behavior anywhere; Track K's contested rows;
`crates/` (all of it, tests included); `docs/` prose beyond the
disposition notes; `review/`; `memories/`; the k-lint row mapping
(QA-3's `_selftest_klint_workflow` guards it — if a claim edit
would touch ci.yml near it, run the selftest).

## Verification

The instrument's selftest + both new leg variants run at base and
head with reproducible tallies (commands in the PR body); ruff per
repo pin if scripts change; hosted CI green — your diff touches
ci.yml comments + scripts docstrings, so expect code tier and say
what ran. Own CARGO_TARGET_DIR if cargo is ever needed (it should
not be), deleted at the end.

## Lane discipline

Branch `qa/6-measured-claims-2` (created, from main, own spec only);
push after every coherent step; NO Co-Authored-By or model
identifier in lane commits (the spec commit carries one by
orchestrator convention — note in the PR body); foreground only,
setsid-detach + poll anything >600 s; notes in your worktree;
keyword-adjacency scan on every commit message and the PR body; PR
non-draft, body ends with a --- rule then
_Generated by [Claude Code](https://claude.ai/code)_; confirm a run
starts and poll to conclusion. Final report ≤100 lines: per-leg
tally (with found-by-reading column), the two deferral dispositions
quoted, run ID + drawn config, CLASS findings called out.
