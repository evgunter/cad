# MATE-9 — issue 973 part (b), stage 1: the crossing rung as the unified strength's first instance

**Binding at dispatch** (S-MATE program, `docs/S-MATE-PLAN.md`;
difficulty pre-logged at this spec: **M-L**). Read
`docs/prompts/implementer-discipline.md` in full before starting.
The primary specification is the RULING CHAIN:
`docs/MATE-4B-CROSSING-DESIGN.md` (RATIFIED 2026-09-01 — option A
under the UNIFIED end state: every backing rung's strength is
"a declared pair answers exactly for its verified interface";
the crossing rung lands as the FIRST instance; the legacy
region-unconfined rungs are GRANDFATHERED BY NAME and migrated one
at a time, each migration MEASURED; the three-valued side verdict
is the declared-interpenetration hook; the C3/C4 doctrine edit
rides THIS unit), with issue 973's part (b) as the map. Stage 0
(issue 1435) LANDED as MATE-8 (PR #1472): `interior_witness` is
now complete-or-honest — the precondition the design named is
paid.

## Situation

`EdgeEdgeCross` and `EdgeFacePierce` are pushed as "categorically
undeclarable" on an interpenetration premise MATE-4a restated to
at-rest; with the ef rung landed, the overhang class's remaining
hard findings are exactly the crossings. The ratified geometric
fork: an in-contact-plane crossing at a declared seat is LEGAL
(material on opposite sides of the shared carrier); a TRANSVERSE
crossing is interpenetration. Distinguishing them needs the region
test (door 2's machinery + MATE-8's completed witness layer) and
the side test (the outward-normal sense algebra the tier-3 wedge
pass carries — `classify_material_pairing`'s family). The MATE-4a
dual demonstrated the unconfined strength's anomaly (an unrelated
declared pair backing the ef bound); the unified strength is the
ratified repair, delivered by measured migrations, not a blanket
switch.

## Deliverables

1. **The `EdgeEdgeCross` backing rung** (planar-first): a declared
   face pair backs a crossing of two coplanar boundary edges iff
   (a) the crossing point lies within the pair's VERIFIED overlap
   region, and (b) the side test answers OPPOSITE-SIDES. The side
   verdict is a named THREE-VALUED type (opposite-sides /
   same-side / undecided) — the declared-interpenetration hook: a
   future C6 class consumes SAME-SIDE as admission evidence, so no
   bool may foreclose it. Undecided ESCALATES typed; same-side is
   a refusal that NAMES the verdict. Consume the existing sense
   algebra — no new numerics, no re-derivation; certified region
   decision through the existing door-2/witness machinery.
2. **`EdgeFacePierce` stays categorical** — stage 2 is deferred to
   the C6 interference-fit era by name. A row pins the categorical
   refusal unchanged, its comment citing the ruling's staging.
3. **The C3/C4 doctrine edit** (`docs/CONTACT-DESIGN.md`, marked
   post-ratification annotation style): the ONE unified strength
   sentence plus the GRANDFATHER NOTE naming the legacy
   region-unconfined rungs by name (the roster is deliverable 4's
   list). Cite the 4b design doc. No other doc changes.
4. **The grandfather roster + ONE measured migration.** Enumerate
   every backing rung at the legacy strength by name (expected:
   the `declared` consults landed by #969/#1063/MATE-4a/MATE-5 —
   read the code, do not trust this list). Then pick the FIRST
   migration candidate — recommend `ef_bound_backed`, the
   demonstrated anomaly's home — implement its region-confined
   variant, and MEASURE: every oracle suite
   (#969/#1063/MATE-4a/MATE-5/MATE-8 rows) plus the corpus, before
   vs after, every currently-certifying seat accounted for. If the
   measurement is clean, land the migration and shrink the
   grandfather note; if any seat regresses to the frontier, the
   honest fallback is EXPLICITLY permitted — the migration stays
   grandfathered, the measurement lands in the PR and at the
   grandfather note. Either way the anomaly row (the unrelated
   declared pair backing an ef bound) is pinned: red-first if the
   migration lands, as a documented-anomaly pin citing the
   grandfather if it does not.
5. **Red-first from the ratified fork**: an overhang seat whose
   boundary edges CROSS inside the declared region — today a hard
   `EdgeEdgeCross` (or `Uncertified` residue) — certifies after;
   a TRANSVERSE crossing (material same-side) refuses with the
   verdict named, red-first both; the MATE-4a (b) fence rows
   re-blessed onto the new outcomes with reasoning at the sites;
   frame invariance pinned (run the crossing pair both ways).
6. **Class sweep** (discipline §5): the genus is "a backing rung
   whose strength predates the unified sentence" — the grandfather
   roster IS the hit list; per-rung disposition (migrated /
   grandfathered-with-measurement / grandfathered-unmeasured and
   why), blind spots stated.

## Acceptance

- The crossing rows green both ways; the pierce pin green; all
  oracle suites green; the migration measurement in the PR body
  whichever way it lands.
- Refusal-reach moves classified against the D2 addendum in the PR
  body (row 2: the crossing over-refusal retires at declared,
  in-region, opposite-sides seats).
- ε posture (issue-1356): the crossing point, the region decision
  and the side test all decide under the band — argue the story;
  `lane=both` on the final head (the rung has certifying rows and
  the side algebra runs in both compile modes).

## Hard rules

- NO `Co-Authored-By` trailer and no model names in lane commits.
- Keyword hygiene: write "issue 973" spelled out; never a closing
  keyword before a `#`-reference. The unit does NOT close issue
  973 (the pierce arm is deferred by name); say so in the PR.
- Scope fence: `crates/topo/src/census.rs` (the rung + consult
  sites), `crates/topo/src/chart_region.rs` (CONSUMER additions
  only — a small entry point if the region test needs one; the
  witness schedule itself is landed ground, do not redesign it),
  `docs/CONTACT-DESIGN.md` (deliverable 3 only),
  `crates/topo/tests/`. Nothing else — no `boolean/` (MATE-7a's
  live ground), no `geom-brep` (consume `classify_material_pairing`'s
  family, never edit it), no `editor-core`, no demos, no
  `docs/MODEL-AB-LOG.md`, no `docs/S-MATE-*.md`.
- Work only inside your worktree (the shared session scratchpad is
  off-limits); merge main before opening the PR and whenever it
  moves.
- Commit and push after every coherent unit of work (branch
  `mate/9-crossing-rung`).
