# PORT-DIMS-1-SPEC.md

PORT-DIMS-1, the load door's structured refusal and the vacated name:
`PersistError::Dimension` carries the document layer's `DimensionError`
whole, and the Python quantity class takes its Rust type's own name
(#2702).

Deleted at the unit's merge, 2026-09-23.
Recover with `git show ef0c7ef60:docs/PORT-DIMS-1-SPEC.md`.

Three of its statements are corrected by the unit; the corrections are
in `work/port/PORT-DIMS-1.md`'s Closed section, which is the record
that survives.
