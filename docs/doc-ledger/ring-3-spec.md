# RING-3-SPEC.md

RING-3, `RingInterval` dissolves into `Interval`: the ring's refusals become named `Interval` doors, `Enclosure` and `crossing_bracket` go, C9 and DL4 say what is true (#3153)

Deleted at the unit's merge, 2026-09-24.
Recover with `git show 32a0a34c6c:docs/RING-3-SPEC.md`.
