# PROPS-VERDICT-SHAPES-SPEC.md

PROPS verdict-shapes, the two derived per-node verdict forms in one
module (`resolve/vdiff.rs`); `ReplayOutcome` folded into `RunStatus`
with `Absent` kept distinct (#1920).

Deleted at the unit's merge, 2026-09-05.
Recover with `git show 9f52d8df89ba4d45197ee0654c2837f67ebeae21:docs/PROPS-VERDICT-SHAPES-SPEC.md`.
