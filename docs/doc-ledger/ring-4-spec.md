# RING-4-SPEC.md

RING-4, the `interval` feature is dropped: the certified code compiles in every build, CI's lane axis collapses, Q1 says what is true (#3154)

Deleted at the unit's merge, 2026-09-24.
Recover with `git show c9b4e232e7:docs/RING-4-SPEC.md`.
