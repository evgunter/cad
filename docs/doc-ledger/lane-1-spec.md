# LANE-1-SPEC.md

LANE-1, `PropsQuadLane` deleted: the quadrature door is a parameter, the certified name keeps its quadrature, a `_structural` twin carries the `None` (#3010)

Deleted at the unit's merge, 2026-09-21.
Recover with `git show 97d8ffe54d:docs/LANE-1-SPEC.md`.
