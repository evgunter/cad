# Sweep 19 — 2026-09-21: DOOR leaves the tracker

DOOR — the doors whose fix is already written — opened 2026-09-11 in the
eleven-program cut of that day (`docs/WORK-TRACKS-2026-09.md` addendum 3)
and closed 2026-09-21 on Ev's in-chat ruling, quoted in the walk. Sweep
5's rule: `work/door/` whole, with no row re-homed at the sweep — the slate
was emptied first. Twenty-two rows closed from the directory and
twenty-seven passed through it; five left rather than closed (to AUTHOR,
PROPS ×2, CENSUS and TINT). The last wave merged #2984, #2985, #2986 and
#2989. Band **3800–3899** was claimed at the cut and never drew an
ordinal, so it is claimed and empty in `docs/MODEL-AB-LOG.md`.

DOOR claimed **no paths at all**, so every row it held was on another
program's ground by construction; the successor practice is the same as
FIX's, and it is in `work/README.md` and
`docs/prompts/implementer-discipline.md` §6.

Two rule repairs landed with it and are in force where they belong:
`docs/prompts/implementer-discipline.md` §6 now agrees with
`work/README.md` about filing on another program's slate (Ev's ruling on
PR #2421, against the orchestrator's own proposal), and **CLAUDE.md's
approval rule states a test instead of a list** — *"the exception is text
that binds future work rather than describing this change"*. The
guard-reading finding is `work/census/all-census-idiom-forces-the-visit-not-the-update`.
Two further practices the walk recorded — *route from the instrument* and
*a negative result by deletion is a fact about the tree* — have no home
outside this note and are carried to Ev on
`work/meta/four-lane-practices-have-no-home-outside-the-ledger.md`.

Sweep SHA `284ca7e9889f6d86366e696e41157916133f4cb9`.

    git show 284ca7e9889f6d86366e696e41157916133f4cb9:work/door/<FILE>
    git show 284ca7e9889f6d86366e696e41157916133f4cb9:docs/DOOR-EXIT-WALK.md

Done-state of record: this note, the walk at the SHA above, and the code it
left — `topo::BooleanOp::ALL` and `editor_core::Dimension::ALL` published
with their hand-written mirrors retired, `grid_pitch`'s non-finite fallback
replaced by a refusing door, `endpoint_params`' conic and LINE arms
refusing where the angle is derived, `ders1_in_span` collapsing
`loop_area`'s two basis passes, `PartFault::PartProduct` carrying
`ProductErrorKind`, `eval::phrase::DATUM_AXIS` as the one home of the
datum-axis phrase, and `VectorSlot::slots` deleted.
