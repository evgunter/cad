# FIX leaves the tracker — 2026-09-21

FIX — kernel and facade doors with the fix written — opened 2026-09-03 in
the tracker-wide cut of that day (`docs/WORK-TRACKS-2026-09.md` §FIX) and
closed 2026-09-21 on Ev's in-chat ruling, quoted in the walk. The closed-program rule
(`five-closed-programs-leave-the-tracker`): `work/fix/` whole, with no row re-homed at the sweep — the slate was
emptied first. Forty-eight rows closed, forty-four carrying a PR, none
larger than class E and none reviewed adversarially; the last wave merged
#2943, #2944, #2945, #2946 and #2948. Band **1700–1799** was claimed at the
joint opening and never drew an ordinal, under the A/B exemption (Ev,
2026-09-04), so it is claimed and empty in `docs/MODEL-AB-LOG.md`.

`docs/FIX-EXIT-WALK.md` was ratified in advance in chat on 2026-09-21 and
merged with this sweep.

**The successor practice is not a successor program**: file the row on the
slate of the program whose ground it lands on the day it is found, take it
in passing when you are already in the file, and let `work/issues/` be the
last resort `work/README.md` says it is. That rule is in `work/README.md`
and in `docs/prompts/implementer-discipline.md` §6. Two standing practices
the walk recorded — *route from the instrument* and *the pin question* —
have no home outside this note and are carried to Ev on
`work/meta/four-lane-practices-have-no-home-outside-the-ledger.md`.

Sweep SHA `6f0e04ce1534f1c7d25e5dfbe09751b992f8492e`.

    git show 6f0e04ce1534f1c7d25e5dfbe09751b992f8492e:work/fix/<FILE>
    git show 6f0e04ce1534f1c7d25e5dfbe09751b992f8492e:docs/FIX-EXIT-WALK.md

Done-state of record: this note, the walk at the SHA above, and the code it
left — `checks::Subject::refused` routing on
`ProductErrorKind::means_no_body`, `refactor.rs`'s three name-shaped
refusals carrying the node the rewrite stopped at, `path::num` as
`crates/profile`'s one rounding grid, `quantity`'s display row on
`test_utils::f6`'s census weld, and enforcement rows at `SplineError`,
`KnotVectorIssue`, `MeterError`, `PatchBoundError` and `FitError`.
