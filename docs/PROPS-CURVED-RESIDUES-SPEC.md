# PROPS curved-residues — the three the sphere unit measured and did not serve

**Binding at dispatch** (PROPS program; items
`work/props/cone-apex-cap-refuses-degenerateface.md`,
`work/props/the-shape-door-could-take-the-sense-free-rim-side-residue.md`
and `work/props/rim-level-rule-manufactures-its-error-by-feeding-nan-into-classify.md`
— read all three in full; difficulty logged at spec: **M / NUMERIC**,
dual review). Read `docs/prompts/implementer-discipline.md` in full.
Branch `props/curved-residues`, cut from `main`.

## Read this first: the unit whose residues these are

PR #2741 (sphere-pole-side, merged 2026-09-16) served the sphere's
rim-only polar cap and refused the L-shaped complement, and its dual
found that the first landing had pushed a pole without deciding the rim
CLOSES — a half rim measured half the cap. **That defect is the
template for two of the three items below**, so read the three closed
items it left behind (`rimless-polar-cap-refuses-degenerateface`,
`two-face-sphere-split-measures-zero-volume`,
`rim-stores-its-traversal-direction-twice`) and the ledger's
`## Per-merge deletion — PROPS sphere-pole-side's spec` before writing
anything. The precedent is not decoration: the cone item exists
BECAUSE that unit refused to repeat the defect by serving a sibling it
had not guarded.

## 1. The cone apex cap — serve it, guarded

A cone face bounded by one rim and nothing else, the apex interior,
refuses `DegenerateFace` by the same `lo == hi` path the sphere's did;
the item carries the executed rows for both traversals. The fold is
three lines — push level `0` when a generator-free cone boundary's
levels collapse — and **needs no σ**, because the apex is the only
candidate for the missing extreme where the sphere had two poles.

**It needs the closure guard, and that is the whole risk.** An
unguarded fold answers the UNBOUNDED complement with the apex cap's
area, which is precisely the sphere unit's own MAJOR repeated on the
other chart. `props_rim_only_closed` is the sibling written for the
sphere (`Δu − τ` levered at the radius, through `require_zero`); mint
the cone's counterpart at the cone's own arm, and **red-first the four
shapes the sphere unit's row uses** — a partial rim, a doubled rim,
and whatever the cone's chart makes of them. The inverted traversal
(the complement, not a finite face of any solid) is caught by check 6
through the gate arm's `linear_rim_side` inside a body, per the item;
verify that by execution rather than inheriting it.

## 2. The shape door and the sense-free residue — measure, then rule

`require_iso_rectangle` asks one predicate; the sphere's flux lane asks
two, and the door cannot take the second (σ carries the face's sense
bit and the door is handed a loop with no face, deliberately). It CAN
take the sense-free residue — `unanimous_rim_side`, already written —
and today the door answers `Ok(())` for a face the flux lane refuses.

**Ruled: measure the blast radius first, and take it only if the
measurement is clean.** The door gates `mesh`'s walk and is cited by
`topo`, so adding a refusal changes which bodies mesh. Run the corpora:
if no body that legitimately meshes today stops meshing, take it, with
the door's doc saying what it now guarantees. **If anything legitimate
stops meshing, STOP and report** — do not widen a meshing gate on a
props unit's judgement, and do not soften the predicate to make the
corpus pass. A seam note goes to whoever owns the walk either way.

## 3. The rim-level rule's manufactured error

`props/curved.rs` has a structurally-impossible arm that throws by
feeding `f64::NAN` into `classify` and letting the funnel escalate — a
decision predicate used as a `throw` — and `unreachable_zero` returns a
four-tuple of NaNs into live flux arithmetic. The citation is from
2026-08-18 and the tree has moved twice under it (`same_level` is gone,
unified into `level_coincides`), so **re-read before you fix**: name
the sites as they are now, and say whether the idiom survives anywhere
else in the file.

This is D2 territory — a kernel-bug-only state answered two ways — and
`props-curved-carries-two-readings-of-d9-unreachable-vs-poison` is its
sibling row, still open. **Ruled: fix the manufactured escalation here,
and do NOT unify the two readings of the unreachable state** — that is
the sibling's question and it wants the whole file's census, not a
decision taken at one site. Say at the site which reading you left and
why.

## Posture

- ε posture: none expected — the cone fold reuses the extent's
  comparand and the closure guard reuses the sphere's. Say so, or say
  what moved.
- Golden movement: the cone fold ADMITS inputs that refused, so any
  recorded-verdict population, census digest or tess baseline carrying
  a cone rim-only face moves. Re-baseline with its digits and its
  reason (discipline §3).
- **This machine has a contended build mutex** — read
  `memories/agent-lane-operations.md` §Build concurrency, wrap every
  heavy cargo call in `local-scripts/with-build-slot.sh`, pass no `-j`,
  expect to lose the slot, and let hosted CI be the record.
- Review: dual, in the experiment.
- **Landing: the items get `pr:` and `status: review`. DO NOT MERGE.**
  No `Co-Authored-By`, no `CI-Config:` trailer, no empty commits.

## Acceptance

The cone apex cap measures both traversals against its closed form with
the closure guard red-first on the partial and doubled shapes; the
shape-door question measured and either taken or reported with the body
that stops meshing named; the manufactured escalation gone with the
sibling reading left explicitly; every moved population re-baselined
with digits; hosted CI green.
