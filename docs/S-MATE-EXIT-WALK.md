# S-MATE exit walk — criteria vs evidence

**STATUS: PROPOSED (design conversation — awaiting Evan's
ratification; on sign-off this document is S-MATE's done-state of
record and the program closes).**
S-MATE = contacts, rest, and assembly composition
(`docs/S-MATE-PLAN.md` / `docs/S-MATE-LOG.md`; opened 2026-08-31
from the ratified cut in `docs/WORK-STREAMS-2026-08.md` §S-MATE as
its last unclaimed stream; A/B band 1300–1399). The plan's "Exit
shape (proposed)" paragraph supplies the criteria, quoted verbatim,
one commitment per row, dispositions per the
M5–M8/ASM/S-QA/S-BLEND convention: MET /
MET-WITH-RECORDED-HONESTY / CARRIED (named owner).

Nine units merged (the planned seven, with MATE-4 and MATE-7 each
splitting into a ratified design conversation + implementation, and
the 4b design funding two staged units): ordinals **1300–1308**,
samples **#78, #80, #81, #83, #85, #87, #89, #90, #94**. Two design
conversations ratified in-program (PR #1440 — the unified backing
strength; PR #1469 — the shared-rim tangency routing).

## The walk

| # | Criterion (verbatim from S-MATE-PLAN §Exit shape) | Disposition | Evidence / honesty note |
|---|---|---|---|
| 1 | "Mates compose with patterns per the ratified rider (#945 closed)" | MET | **PR #1400** (MATE-1, ordinal 1300, sample #78): `head_of`'s member vocabulary (`InstantiatePart` OR pattern-placed `Instance(i)`), the solve composing the derived offset into the member frame (`F⁻¹·O_c⁻¹·O_p·F`), the two ratified pins as their own rows; issue 945 closed at merge. Lane note for the record: the first implementer worktree was reclaimed prematurely at report — the fix pass ran as a FRESH same-arm lane on pushed work (the lesson recorded in the log; worktrees kept through fix passes thereafter). |
| 2 | "a shaft seats in a bore with no plate beside it, or the requirement refuses typed at the door (#1032)" | MET | **PR #1417** (MATE-2, ordinal 1301, sample #80): the measurement ran to the line, `curved_face_arm`'s coverage widened so a declared shared-carrier incidence is covered regardless of which incident face the declaration names (main's `Placement` unification settling the #1353 collision toward VERBS' lane, recorded on their thread as their debt); the four measured spellings rows; issue 1032 closed at merge. |
| 3 | "declared cusps have their verdict table and their authoring door, with the boolean-routing and emission halves handed off by name (#941 items 1–2)" | MET | **PR #1423** (MATE-3, ordinal 1304, sample #85): D1's tier-3 material-wedge verdict table (`classify_material_pairing` / `MaterialArmOutcome`, revert involution bit-identical) and the PATHS `.cusp()` door; three corpus verdicts moved with two documented "lone flip certifies" residuals caught as orientation defects; items 3–5 recorded as handoffs at landing (items 3–4 VERBS-lane ground; item 5 cross-program); issue 941 stays open on them by design; issue 1439 filed (the fold's hand-rolled siblings). |
| 4 | "the at-rest census answers for exactly the configurations Q2 ratifies, with the remainder's justification restated on an at-rest premise (#973)" | MET-WITH-RECORDED-HONESTY | Part (a): **PR #1432** (MATE-4a, ordinal 1303, sample #83) — `ef_bound_backed`'s interior arm at the existing rung strength, licences restated at-rest; its dual's schedule-bifurcation finding became issue 1435 (stage 0's demand). Part (b): ratified as the 4b design (PR #1440, the UNIFIED strength end state) and executed as **PR #1496** (MATE-9, ordinal 1308, sample #94) — the `EdgeEdgeCross` rung born unified (point + both edges on-carrier + region-verified + the Smooth-preconditioned three-valued side test; same-side refuses NAMING the verdict — the declared-interpenetration hook Q2(b)'s constraint demanded), C3/C4 carrying the ratified sentence + the grandfather roster by name. **The honesty**: the first migration (`ef_bound_backed`) was implemented, MEASURED NOT CLEAN, and grandfathered with the measurement recorded — the ruling's own fallback; its blocker is scheduled as issue 1500. `EdgeFacePierce` stays categorical, C6-era by name; issue 973 stays open on exactly that arm. |
| 5 | "the cross-instance curved seat certifies through the sanctioned ε-rung, or its residue is re-recorded with Q3's ruling (#943 closed or re-scoped)" | MET | **PR #1443** (MATE-5, ordinal 1305, sample #87): Door 2's certified everywhere-within-ε overlap enclosure for declared cylinder pairs — with the gate REDESIGNED at review to measure the per-vertex transfer residual and price the radius (the dual's bilateral MAJOR, both arms independently); sphere/cone/torus restated per kind at their refusal sites; issue 943 CLOSED at merge. The witness layer both doors lean on was completed by **PR #1472** (MATE-8, ordinal 1306, sample #89 — the 4b design's funded stage 0): `interior_witness`'s two-stage schedule, complete up to f64 rounding, declines-only degradation; issue 1435 closed, issue 1478 filed (the typed budget decline). |
| 6 | "sub-assembly instantiation carries its mates per Q1's ruling (#946)" | MET | **PR #1420** (MATE-6, ordinal 1302, sample #81): minting moved into the product gather as pass 4 (`assemble` = product + tier-3′, verification once at the outermost gate — the drift-closure against A3's own ratified sentence), mint total over live mates, the ×3-stands red-first row; issue 946 closed at merge; issue 1429 filed (carried-refutation attribution). The dual produced the program's first tally candidate (the unguarded mint-totality). |
| 7 | "#968 is landed or re-banked per Q4" | MET-WITH-RECORDED-HONESTY | The tangency disposition returned to Evan first as ruled (PR #1469, RATIFIED: the wedge table routes a shared rim; π → the smooth-seam zip, BUILT; 0/2π → declared-Tangent, DEFINED-BUT-UNBUILT, banking with the `tangent_locus` witness + demo). **PR #1477** (MATE-7a, ordinal 1307, sample #90) landed items 1–2 + both routing arms, the π classification built at a measured fair price (53 rows including the tangency screen its dual forced). **The honesty, twofold**: (1) the spec's demo premise REVERSED by measurement, confirmed independently by both review arms — lily wall 1 does NOT retire here: its refusal is the whole-ring torus box artifact (exact loci 2.08 m apart) and the weld has NO torus contact to declare (a boundary-tight box retires it with no declaration at all, min true separation 0.008 m) → issues 1488 (box tightening, the real path) and 1489 (`circle_residual_harmonics`' torus arm, VERBS' germ ground — the declared-Rest torus lane's last blocker, one function away). (2) No pin flipped anywhere — the spec expected re-blessing; the widened admission is reachable only via declared covered pairs, stated at the sites. Issue 968 stays open on the kissing arm, annotated with the corrected diagnosis. |
| 8 | "the `m9/census-xid` strand is dispositioned on #943" | MET | Verdict recorded on issue 943 (2026-08-31): SUPERSEDED by the landed gap-2 unit (#1063) — the branch's own scope is what #1063 shipped reviewed, and #1063 additionally carries what ratification corrected AFTER the branch died (the frame-invariance lemma, the Door-1 Zero honesty, the re-derived probes). Nothing salvaged; the branch stays as history. |
| 9 | "#750's landed record is consumed when BOOL-4 lands" | CARRIED (S-BOOL → S-MATE successor) | BOOL-4 has not landed (issue 750 open, no closing PR at this walk). The gate held: no S-MATE unit touched containment ground; consumption remains recorded in both plans. |
| 10 | "#944 is taken iff LIB hands it off" | CARRIED (LIB) | No hand-off arrived; not taken; the gate held. |
| 11 | "Every unit merged on its own green hosted head; the walk convention applies at exit" | MET | Nine unit merges, each on a hosted green run verified at step level, gate points recorded per row (`lane=both` asked on every final head from MATE-5 on — the both-compile-modes precedent this program set); two docs-only ratification merges besides. This document is the walk. |
| 12 | Process (§Process, verbatim in substance): "standard v6 — substrate → binding spec → one implementer + the cross-model dual review + union fix pass; … ordinals claimed on main at review dispatch from band 1300–1399; record-at-merge with per-phase tokens/wall; blinding discipline verbatim" | MET-WITH-RECORDED-HONESTY | Nine v6 duals, ordinals 1300–1308 all claimed on main at dispatch, rows at merge, briefs symmetric (MATE-2/MATE-6 SEQUENTIAL same-head under pre-R1 method notes when the 3-lane budget bound; all others concurrent), blocks MATE-B1/B2/B3 drawn branch-side and FULLY CONSUMED (nine slots — no close-short needed; every arm restated in its merged row). **The honesty, four notes**: (1) the band was RENUMBERED 1200→1300 at opening per main-is-authority (no ordinal had been claimed; recorded in plan+log). (2) MATE-9's fix pass was INTERRUPTED mid-edit by the account's Fable usage limit (~75m gap, ANNOTATED in the row); its state was preserved by an orchestrator WIP snapshot commit (`dec34c02`) that carries a model-name attribution trailer under a harness attribution mandate — a deviation from the blanket no-trailer lane rule, taken POST-VERDICT only (the arm becomes public in the merged row regardless); the lane resumed same-arm. (3) Sample numbers were assigned at merge under contention twice (MESH-3 took #88 mid-write; #94 followed #93 the same way) — main's merge order ruled, no renumber needed. (4) The MATE-8 implementer's phase tokens are UNRECORDED (orchestrator-side compaction), disclosed in the row rather than backfilled. |

## Walk evidence beyond the criteria

- **The experiment's yield — FOUR tally candidates in nine duals,
  coding at the blinded adjudication**: MATE-6 (R1's unguarded
  mint-totality), MATE-7a (R2's missing tangency screen), and
  MATE-9's pair — ONE EACH ARM in a single dual (R1's diving-edge
  soundness break; R2's unguarded region conjunct), the program's
  richest. Two verdict SPLITS on converged substance (MATE-5,
  MATE-8 — both bilateral central findings at MAJ-vs-MIN labels)
  are calibration data for the coding. The MATE-7a/MATE-9 briefs'
  explicit flag of the just-caught dropped-preconditions class was
  VINDICATED one unit later — a dispatch-shaping datum.
- **The doctrine this program leaves behind**: the UNIFIED backing
  strength is ratified doctrine with one instance landed and the
  grandfather roster named (C3/C4); the material-wedge verdict
  table runs in tier 3; the shared-rim routing is ruled with one
  arm built; minting lives in the product gather. Each carries its
  measured-migration or defined-but-unbuilt discipline rather than
  an overpromise.
- **Handoffs ledger at exit** (every one with a home): issue 1478
  (typed witness-budget decline), 1488 (torus operand-box
  tightening), 1489 (`circle_residual_harmonics` torus arm —
  VERBS), 1500 (D3 boundary-crossing cuts — the first migration's
  blocker), 1429 (carried-refutation attribution), 1439 (the
  fold's siblings), 941 items 3–5 (VERBS-lane + cross-program,
  recorded at MATE-3's landing), 968's kissing arm (banked with
  witness + demo per the ratified routing), 973's pierce arm
  (C6-era by name), #944 (LIB gate, intact), #750/BOOL-4
  (S-BOOL gate, intact), VERBS' #1353 unification debt (their
  thread). Orchestrator-direct repairs during the program: the
  spending-limit silent-CI face recorded in
  `memories/agent-lane-operations.md`; the inherited-red merge
  ruling recorded there likewise ("the lane that caused it owes
  the fix").
- **Demand signals the program surfaced without absorbing**: the
  story-suites datapoint on issue 943 (edge-level declaration
  vocabulary through the GUI) was answered in part by MATE-9's
  rung — the windmill story's badge moved Refused →
  `Certified{minted:3}` at its merge — with the vocabulary
  question itself left where it was filed.
- **Open with Evan at exit** (nothing blocks ratification): this
  walk's sign-off; the blinded tally coding whenever the analysis
  runs (four candidates queued); and the standing design
  conversations already in his court (issue 1429's A11-rule-4
  drift note from MATE-6's era, if still wanted).
