# M9 — C7: the declared-contact join lane (plan)

**STATUS: RATIFIED (Evan's 👍 on PR #509's affordance, comment
5303293943, confirmed directly in-session, 2026-08-15 — with the
in-conversation rulings folded: A5 ownership, schema posture,
`Fit{gap}` deferral, lily wall-2 deferral, A′ → LIB).** Scope was
fixed by the #300 closure ratification
(M9 = C7 pulled forward as its own logical unit; the lily FULL
rebuild rides it) and CONTACT-DESIGN's sibling-deliverable
paragraph ("the M9 spec adopts both doors deliberately"):
**M9 = the join lane + ASSEMBLY-DESIGN A5's at-rest census door.**
M10 = error propagation (C5's contract items bind M10, not M9 —
M9 uses the signed gap in VERIFICATION only and grows no
Measures). CONTACT-DESIGN C1–C6 are ratified (#178) and not
re-litigated here; this plan sequences their implementation.

## Ownership ruling — RULED (Evan, in-session 2026-08-15)

The A5 at-rest door is **built in M9, consumed by ASM** (R2-b/R3
take it without adapter, per A3's records-are-the-same-currency
rule). Evan restarted the ASM line and informed it directly; the
ASM orchestrator's #509 subscription confirms R2 stays strictly
planar meanwhile. M9-1/M9-2 specs cite R2-b's consumption points
so the door's shape is negotiated in the spec, once.

## Schema posture — RULED (Evan, in-session 2026-08-15)

Schema changes are CLEAN BREAKS while nothing is released — no
migrations, no overhead, so bump COUNT is not optimized; the only
discipline that remains is one-meaning-per-version (the
double-claim rule) and the claim-on-main-at-dispatch corollary.
Consequence taken below: `Fit{gap}` defers to its first consumer
rather than riding M9-1 unconsumed.

## The firm units

1. **M9-1 — contact vocabulary: records + declaration classes
   (C3 + C4, scoped `Rest` + `Tangent`).** `ContactRecords` grows
   the two ratified granularities (`CurveContact` /
   `PatchContact`, per-locus and chart-space-area certification);
   `BooleanDeclarations` / `DeclaredPairs` grow the class payload
   with C4's per-class must-verify/contradict/bridged tables and
   the typed failure modes; the recipe (`Node::Declare`) and LIB
   (`ContactClass`) homes grow TOGETHER with the kernel's — the
   vocabulary-breadth mismatch is this unit's subject.
   **`Fit{gap}` DEFERS to its first consumer** (C6-era) as its own
   clean break, per the schema ruling — M9 exercises `Rest` and
   `Tangent` only. Persistence per C4's rule (class payload ships
   with the implementation; claim on main at dispatch). AQ6 rides
   here (the value-equal-by-authoring trilean + recourse text —
   its `Fit{g₀}` steering text lands with the vocabulary even
   though `Fit` itself defers, so the recourse is never a dead
   pointer: it names the deferral). **Sprawl escape, stated**: if
   the three-home growth outgrows one unit, the sanctioned split
   is kernel-first with an immediate recipe/LIB fast-follow —
   STOP and report, never a silent scope shed.

2. **M9-2 — the A5 at-rest door** (census + verification, NO
   boolean, NO zip): tier-3′ census growth beyond
   exact-on-planar (curved census arms certified per C3),
   `UndeclaredContact` reach extended accordingly, the
   import-side declaration channel (D7 step 4's named residue).
   **#382 half-2 executes here** — inter-instance overlap is
   undeclared contact / C6's recorded-gate-skips posture, not an
   ad-hoc graft check. Acceptance includes the KISS-ASSEMBLY pin
   executing its own retirement text
   (review_r1_tier_gate_probes.rs:601 — the touching import
   certifies at 3′ with declarations or refuses under the SHARED
   gate) and the boss_union "refuses HERE" pin likewise.

3. **M9-0 — the #459 refusal migration** (text-only,
   orchestrator-direct class, no behavior change): lands
   IMMEDIATELY BEFORE M9-3 dispatches — sequenced, not folded in,
   so the join lane's reviewer reads behavior diffs unmixed with
   text moves, and the merge collision is avoided by ordering
   rather than by riding.

4. **M9-3 — the join lane proper (C7 items 1 + 2), AFTER M9-2**
   (the dependency is real and stated: the census arms M9-2
   builds are exactly what this unit's verification consumes —
   the two units SERIALIZE). The curved
   coplanar-lump arm (the vtxfac.rs:118 wall: with a verified
   `Tangent`/`Rest` on the face pair, classification descends to
   the second-order sector trilean, lump verdict from
   declared-backed relative-curvature sign); the zip generalized
   by carrier kind (S1's planar declared-REST zip → cosurface
   patch removal + seam mint on a shared carrier of any kind,
   structural/declared rungs ONLY).
   C8's invariant (undeclared touching refuses FOREVER;
   osculating/in-band escalates) is the unit's auditable boundary.
   crosslap (the shipped planar precedent) must not regress.
   (#459's text rides ahead as M9-0, above.)

5. **M9-D — the #427 pcurve unification DESIGN PASS** (design
   before code): **opens IMMEDIATELY at ratification, in parallel
   with M9-1's implementation** (a design conversation consumes
   no implementer lane) — before M9-3's seam minting hardens new
   edge descriptions and before M9-4, which its ruling shapes
   (and may collapse into M9-3). No code until ratified; the
   #388/#498 pcurve banks cite it but stay excluded.

6. **M9-4 — COLLAPSED INTO M9-3 (M9-D's ratified consequence,
   2026-08-15).** Under U2 the marks and their enforcement
   predicates already exist — the join lane wires them
   (determinate-jet G1 rims carry `TangentIntersection`; isolated
   κ_rel-zero rims conservatively unenforced; wedge = π legal),
   and M9-3's emission shapes map 1:1 onto (surface, pcurve) so
   the migration unit inherits them cleanly.

7. **M9-5 — the lily FULL rebuild** (the demo moment; the lily's
   CONTENT stays Evan's — the rebuild executes the ratified plan
   its own wall-probes state). Walls 1 (stem glue) and 7 (tepal
   seam) execute their retirement texts. **Wall 2 is DEFERRED by
   ruling** (Evan, in-session 2026-08-15): its other half is the
   banked germ-chord lane (transverse curved×curved SSI, not
   contact) — the wall stays probed and refusing with its named
   residue; the germ-chord lane stays banked. **Wall 8 carries a
   stated dependency**: its refusal is `CurvedEdgeUnsupported` —
   an edge-vocabulary gap whose flip rides the post-M9 pcurve
   MIGRATION unit (M9-D's ratified sequencing), so its
   disposition is stated with that dependency rather than
   promised unconditionally. The
   internal-tangency fixture row (#250/#161 §2c) rides as
   evidence; #368 (≥π spines) only if the rebuilt spines demand
   it. The two-peg plate lands as the join lane's canonical new
   demo cell (three PatchContacts, volume exactly additive —
   demos/README's named revisit).

## Candidates (adjacent, own substrate → spec → dispatch cycles)

- **D1 — the revolve name emitter discriminator** (all-on-axis
  loops; no sphere reaches `Node::Revolve`): cheap,
  crate-disjoint, and the lily rebuild benefits directly (the
  tan(π/8) detour deletes as its acceptance). Sequence EARLY.
- **A′ PlacedUnion** (docs/GROUP-BOOLEAN-DESIGN.md, shape
  ratified): **LIB TOOK IT** (right-of-first-refusal accepted on
  the #510 thread, 2026-08-15 — it closes their G8/diecomposed/
  heatsink register items; dispatches after their RESPELL PR-1).
  Off M9's slate; its schema claim is LIB's, its own clean break.

## Excluded, named

#390 (dm1 quadrature budget), #388/#389 (D7 recognition banks),
#498 (trimmed-NURBS/cut-loft pcurve lane), #501 (metering
residue), #497 (env class) — import/pcurve banks, not contact.
`min_clearance` and every E-numbered Measure — M10's, per C5.
Thermal/handbook fits — LONGTERM consumers, leave-room only.
Stale pre-#300 labels ("M8's C7", tier_gate.rs:46, the
demos/README M6 pointer) get truthed by whichever unit touches
each file; historical walk documents stay as written.

## Exit shape

The kiss assembly and boss_union pins execute their retirement
texts under the SHARED gate; undeclared touching still refuses —
forever — and osculating/in-band escalates (C8's invariant holds
at every seam); crosslap unregressed; the two-peg plate cell
shows three certified contacts with exactly-additive volume; the
lily's C7 walls dispositioned honestly (wall 2's germ-chord half
named if it survives); every new row ε-row three-outcome honest;
hosted CI green on every merge; the walk convention (criteria vs
evidence, honesty rows) applies at exit.

## Process (unchanged)

One implementer + one blinded adversarial reviewer + fix pass;
binding specs before dispatch (substrate exploration first); rows
recorded AT MERGE with per-phase figures; demo-class units
orchestrator-reviewed; hosted CI the only gate.

The A/B protocol and **all of its live state** — block, slot,
ordinal, dual-review tally — belong to `docs/MODEL-AB-LOG.md`, and
are claimed from that ledger ON MAIN at dispatch. No copy of any
of those numbers lives in this plan. M9-LOG is the live tail.
