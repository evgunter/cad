# Demo-local build/venv/output artifacts. Only the tour sources,
# render scripts, and the rendered PNGs under renders/ are tracked.
tour/target/
out/
.venv/
target-demo/
renders/.freecad_ok
renders-freecad/*.fail.txt
out/freecad-logs/
