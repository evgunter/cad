# GUI-4 review — R2 (PR #1113, frozen head `75b0babf`)

**Verdict: APPROVE-WITH-FIXES** (no MAJOR; the MINORs below are recordable fixes; hosted CI of record already green at the frozen head — verdict conditional on it staying green through merge).

## Findings

**MAJOR** — none.

**MINOR-1 (disclosure inaccuracy).** The PR body says "the interactive half of the acceptance runs on the real gallery through the app (screenshots below)" — but `docs/gui-shots/2026-08-28/README.md` says the document rendered is "the GUI-4 suites' gallery-shaped workspace … the exit-walk fixture as saved by the walk row." Nothing in this unit — walk, screenshots, or CI — ever opened the tour's actual `gallery/assembly` output. The fixture deviation itself is disclosed and fine (below); the body's sentence overstates what the screenshots pin and should be corrected.

**MINOR-2 (app-gated chrome defect).** `app.rs::instance_ui`: the free-move mm `DragValue`s map the gesture triple by hand and omit the typed-input arm — `began|previewed|stopped` all stay false when a user TYPES a value (`changed() && !dragged()`), so a typed probe value is silently dropped. `drag_ops` (same file) exists precisely to own this mapping and its doc says "any future dragged number in this file calls this"; this PR added dragged numbers that don't (Q4: the comment's invariant was invalidated by the same PR). App-gated, inside the disclosed not-row-tested surface, but it is a concrete dead control, not just missing coverage.

**MINOR-3 (unverified mates render Ok — scope-consistent but worth banking).** The viewer path never runs `assemble`/the C2 verification tables: a committed mate's node status reflects only the constructive solve (`wire.rs`: `Node::Mate → poses.fault`), so a Rest mate whose declared class does not geometrically hold (e.g. Aligned sense driving the post through the shelf) shows an all-green tree, and A5's undeclared-contact hard error never fires on the draw path. Disclosed for Tangent only; it applies to every class. Spec-consistent (verification is A5's door, not this unit's), but the walk reads "every row Ok" as acceptance evidence on a path where declaration verification cannot fail. Suggest a banked follow-up: run at-rest verification (or a lint-grade subset) against the landed product and badge it.

**MINOR-4 (save-a-copy bricks the store).** Discovered by a failing probe of mine: saving the open document under a NEW name into its OWN directory creates a second file claiming the same document id, so the next resolution refuses `DuplicateId` for the whole store (both sessions). Typed and file-naming, consistent with fail-loud — but "save a copy beside the original" is an ordinary act with a surprising blast radius, and it is disclosed nowhere. At minimum a doc sentence; possibly a save-door warning.

**NOTE-1 (memo absorbs resolver changes — the save-as re-evaluate rationale is half-real).** Pinned by my rows: deleting a part file mid-session then `Reevaluate` stays green (pin-verified memo reuse — safe, and the PR's memo-safety argument covers it), and save-as into a partless directory does NOT turn a green session red for the same reason; the rebind's "references may resolve differently" bites only for previously-FAILED resolutions (which do recover — also pinned). The directory rule is enforced where resolution actually happens (fresh open), which my rows confirm; the PR's save-as paragraph could state the memo asymmetry.

**NOTE-2 (walk assertions that cannot go red).** Stage 10's "layer-3 state gone" asserts on a freshly constructed session whose `DisplayState` is empty by construction, and the persistence schema has no field that could carry display state — the guarantee is structural unrepresentability (stronger than a test), but as evidence rows these assertions cannot fail. Stage 9's "the two picked frames coincide" asserts ORIGINS only (not axes/references), and is solver-vs-authored — circular with respect to the frame pullback (the solve enforces exactly what the proposal authored). My suite adds the independent teeth (below).

## Style (per the style-lane brief)

Questions exercised: Q1 (prose + constants sweeps run over the touched files), Q2, Q3, Q4, Q5, Q6, Q7, Q8 (whole-file read: `app.rs`, 1373 lines, end to end). Q1's disclosed-copy grep: clean; constants sweep found one.

- `gpu.rs` WGSL `(in.flag & 1u)` restates `SceneMesh::FLAG_PROBE = 1` — two spellings of one constant across the Rust/WGSL boundary, held together by a reconciling comment ("The FLAG itself is `SceneMesh::FLAG_PROBE`"). Unenforceable across the string boundary, but it is the Q2 shape. **sure**.
- `evalseam.rs::run_once` doc: "The one place `evaluate` is called in this crate" is false — `scene.rs::product_body` also calls it (pre-existing drift, not this PR's, but the sentence anchors the memo discipline and should be scoped to the seam). **sure**.
- `matetool.rs::admitted_classes` — "the table is read, never restated" is true of the VERDICTS but the class LIST `[Rest, Tangent]` is a hand-kept enumeration (`ContactClass` is `#[non_exhaustive]`, so no automatic enumeration exists). Fail-safe direction (under-offers, never over-offers); my suite adds a tripwire row that goes red when the kernel enum grows a nameable class. **likely** worth a comment naming the hand-kept list.
- `docio.rs::DirResolver::resolve` re-scans the whole directory (`Workspace::open`) on EVERY resolution — O(dir entries) per instantiate node per non-memoized evaluation. Memoization hides most of it; a large messy directory pays repeatedly on first evaluation. No measurement taken. **unsure** (perf shape, not correctness).
- `app.rs::run` sets `fit_on_scene = true` unconditionally after a CLI open, duplicating `perform_batch`'s success-only arm — a REFUSED CLI open still re-frames on the next scene rebuild. Trivial. **unsure**.
- MINOR-2 doubles as the Q4 instance (invalidated `drag_ops` comment). **sure**.
- Pick `t`-comparison doc (`pick.rs::pick_for`): the comparability argument leans on rigidity, but `t` is a ray PARAMETER (units of |dir|, per `PickHit` docs) and is exactly invariant under any invertible affine conjugation since the direction is transformed, not renormalized — the rigidity rule is needed for the honesty of drawn lengths, not for `t` comparability. Doc nuance only. **likely**.

## Claims disposition

1. **Resolver/directory rule** — HOLDS. Rule enforced at resolution (my fresh-open rows); broken store refuses typed naming `junk.pncad` (unit row, re-run); scan-at-resolution revision traded away only eager health-checking, and the memo asymmetry (NOTE-1) is the honest residue; save-as rebind sound with NOTE-1's caveat; `PartResolver: Send + Sync` (trait bound) and my threaded-seam row exercises the Arc across the worker; absent-resolver behavior unchanged (default `EvalOptions`, unit row).
2. **Hide** — HOLDS. Scene+pick drop / tree+doc keep / no history / never persisted all pinned by unit rows that can go red; id windows walked over hidden parts so ids keep meaning.
3. **Free-move** — HOLDS. Document-derived eligibility (typed `MateConstrained` listing mates); `NonRigidFrame` typed (scale/mirror/NaN rows); distinctness is a value (`probe`/`FLAG_PROBE`/`probe_parts`, exact-extent row); **inverse-ray math verified**: re-derived (t invariant under conjugation), and my rotating-probe row pins the linear half with hand-computed world coordinates plus the vacated-position miss; supersession discards (None, not identity — unit row + my hide/gesture variants) and the unrepresentable-state argument is sound (legality derived from the document).
4. **Mate tool** — HOLDS. **Frame composition verified against an independent oracle**: my row compares `proposal.alignment` to the standalone part documents' own `face_frame` (never placed, never inverted) — a forward or identity pullback differs by 0.12–0.16 m and fails; my seat-geometry row then checks the SOLVED placement by ray against fixture constants (post bottom at z = −0.05 under the shelf). Admission verdicts are kernel-read (divergence tripwire planted); one committed edit through the one door; `PickLost{side,pick,resolution}` two-stage degradation row-tested.
5. **Exit walk** — driven, read. Readable as an acceptance document; ten stages real; 1e-9/1e-12 assertions have teeth for solver+persistence determinism but are circular for the pullback and vacuous for layer-3-gone (NOTE-2 — the latter because the property is structurally unrepresentable, which is the stronger guarantee). **Adjudication input on the fixture deviation**: acceptable as CI evidence — the fixture is authored through the same public doors and same store shape, and the tour is deliberately outside the workspace — BUT (a) the PR body's screenshot sentence overstates (MINOR-1): nothing anywhere opened the tour's real gallery output; (b) "unbuildable in-lane" is true of the test binary yet hosted CI's render lane already builds the tour, so a cheap render-lane step (run `demo-tour gallery`, open `assembly/*.pncad` headlessly, assert rows Ok) is feasible and I recommend banking it — until then the plan's literal exit demo ("the app opens the gallery assembly") has been executed only on a replica.
6. **Tangent commit** — adjudication input: commit-with-verdict is RIGHT for v1. The document doors accept Tangent (solve admits it); refusing at the viewer would make the GUI stricter than the API it fronts, contra G1's operations-are-API parity, and the verdict is shown at the commit. ASSEMBLY-DESIGN's "declarations are verified, never trusted" cuts the other way only at the at-rest door, which is not on the draw path for ANY class — that broader gap is MINOR-3, and the right fix is verification surfacing, not a viewer-side refusal.
7. **Façade** — HOLDS. `CLASS_DEFERRAL` root re-export; pncad mates stanza carries the table with an argued comment; census 78→76 with the family doc updated; the first CI red was exactly and only that census row (log verified) and the fix is honest.
8. **CI** — VERIFIED AT STEP LEVEL. Run 33128016123 ({interval, default}): sole failure `pncad::all every_document_layer_root_export_is_carried_or_listed` naming exactly `ClassAdmission`/`class_admission`. Run 33128806185 on `75b0babf` ({interval, 1e-6}, both shards): GREEN; fmt job steps 10–11 show the three `RUN:` verdict lines and `cargo clippy -p viewer --features app --all-targets -- -D warnings` finishing clean.
9. **Mechanical sweep** — VERIFIED with a differently-shaped sweep (type-name occurrence scan rather than field-literal grep): construction sites are exactly the disclosed 15 `resolver: None` + 2 `probe: None` + this unit's new code; only helper is `ScenePart::plain`, added by this unit; `build_parts` callers all accounted.
10. **CLI arg** — HOLDS: `bin/viewer.rs` → `run(tol, path)` → `SessionOp::Open`, no second open path. `ClassRefused` unreachable-today claim TRUE (`ContactClass` has exactly Rest/Tangent, neither `NotAdmitted`). App-gated untested surfaces honestly listed — with MINOR-2 showing the cost of the exemption.

## E2E exercise — scope and ergonomics

Drove the whole flow headlessly as a consumer: authored a workspace through public doors, typed Open (resolver wired), hide, free-move (translation and rotation), both mate-tool picks through the real display-aware ray path, one committed edit, supersession, save/reopen; plus my adversarial set — contradictory second mate (fails typed at evaluation, undo recovers), mate landing over an in-flight gesture (gesture dies, preview refuses `NoFreeMove`), free-move→hide→mate (hide survives, probe discarded, hidden at solved placement), missing part file (fresh open refuses typed; mid-session absorbed by memo — pinned), save-as both directions, threaded seam with resolver, two-different-faces self-mate. Ergonomics: the layer-3 API is genuinely pleasant to drive — every step is a value in, a typed outcome out; `OpOutcome::superseded` reporting rather than inferring is exactly right; the one friction point is that `MateTool` lives beside the session rather than in it, so a consumer must remember to `reconcile` (the app does it in `sync_scene`; a headless consumer can forget silently). **Not exercised**: the GPU pass (lavapipe screenshots reviewed, not reproduced), egui widget chrome (`instance_ui`/`mate_tool_ui`/`fit_on_scene` — where MINOR-2 lives), the rfd dialog, real hardware (#1097). Local runs unique-signal only; CI-covered suites ride the gate.

**Consumer suite**: `crates/viewer/tests/review_gui4_r2.rs` — 13 rows, all static fixtures, all green; committed on `gui/gui-4-review-r2` and aggregated in `tests/all.rs` (promotable as-is).

## Code quality report

Deviations: 6 reported (translation-only chrome; document-not-probe mate frames; tangent commit-with-verdict; PlanarRest offset 0; app-gated untested surfaces; fixture-not-gallery walk) vs 2 silent (screenshots-on-fixture contradicting the body's "real gallery"; the bypassed `drag_ops` invariant / dead typed-input path). Reported-to-silent ratio is good; both silent ones are small.

- **Idiom/structure: 5** — one home per state (DisplayState, tool state), typed refusals end-to-end, the one commit door and one submit door actually are one; the display-view plumb through scene/pick is clean and additive.
- **Test quality: 4** — refusal rows and value assertions can genuinely go red, both pick directions covered for translation; docked one point for the circular coincidence assertion, origins-only "frames coincide", and the cannot-fail reopen assertions (NOTE-2).
- **Doc/comment honesty: 4** — module docs state boundaries and name their own unenforced invariants; docked for the PR-body screenshot sentence (MINOR-1) and the stale "one place evaluate is called" claim.

## Isolation / disclosure

Did not read `docs/MODEL-AB-LOG.md`, any A/B material, or `gui/gui-4-review-r1`; older review branches untouched. **Accidental glimpse, disclosed**: `docs/GUI-LOG.md` (binding reading) ends its GUI-2 entry with "GUI-4 dispatches now (fable, block GUI-B2 slot 1)" — the arm of the unit under review, leaked by the log itself (the same defect class the log records against GUI-2/GUI-3). I read it before recognizing it; nothing in this review keys on it and no model identifier appears in anything pushed. Also disclosed: container clock shows the session spanning 2026-08-27→28 midnight UTC; no interruption occurred.

Branch `gui/gui-4-review-r2` (frozen head + suite + this report), pushed. No PR comments posted. Detached waits: none outstanding.

Tokens ≈ 350K. Wall-clock ≈ 45 minutes.
