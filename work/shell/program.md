---
id: shell
kind: program
title: SHELL — shell, offset and transform
status: open
opened: 2026-09-03
area: kernel
prefix: shell/
tag: (SHELL orchestrator)
ab_band: 2300-2399
paths: [crates/topo/src/shell.rs, crates/topo/src/replace_face.rs, crates/topo/src/transform.rs, crates/topo/src/offset_together.rs, crates/geom-brep/src/offset.rs, crates/geom-brep/src/offset_meters.rs, crates/sweep/tests/verbs_shell*.rs, crates/editor-core/src/clearance.rs]
keep_out: [crates/topo/src/offset_axial.rs stays VERBS' while VERBS-RIMCAP is open (its PR-1 rewrites the file) and joins the paths above at RIMCAP's merge, geom-brep/src/offset_fit.rs is S-CERT's then PROPS' — the Approx transform's certifier is filed as a row there, the curved wall-clearance certificate is the E7 engine (M10's deliverable) — where the gate lives is a joint [ev] question with M10 and PROPS, LIB-G17 (Node::Shell) consumes the ShellNaming record and SEAT's shell VerbRecord migration must agree with it, the tess-budget re-baseline of the hollow-tube scene coordinates with S-CERT then PROPS, VERBS' remaining Wave-2 units and the boolean germ lanes are not this program's]
---

VERBS' Wave-3 leftovers in `topo/{shell,replace_face,transform}.rs`
and `geom-brep/offset*`: the `ShellNaming` birth channel LIB-G17 waits
on, the ruled shell-of-hollow-body semantics, the cone nappe home, the
`Approx` transform door, the curved wall-clearance gate, and the tour's
hollow-tube scene. Class H with two E openers. Opened ahead of VERBS'
exit on Ev's direction (2026-09-04); VERBS' shell items are re-homed
here and the shell files are this program's territory. Charter and
unit order: `work/shell/plan.md`; narrative in `work/shell/log.md`.
