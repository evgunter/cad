---
id: shell
kind: program
title: SHELL — shell, offset and transform
status: open
opened: 2026-09-03
area: kernel
prefix: shell/
tag: (SHELL orchestrator)
ab_band: 2300-2399
paths: [crates/editor-core/src/clearance.rs]
keep_out: [a SUCCESSOR — dispatches nothing until VERBS' exit walk is ratified, topo/src/shell.rs and replace_face.rs and offset_*.rs and geom-brep/src/offset*.rs are VERBS' until its exit and join the paths above then, geom-brep/src/offset_fit.rs is S-CERT's then PROPS' — the Approx transform's certifier is filed as a row there, the curved wall-clearance certificate is the E7 engine (M10's deliverable) — where the gate lives is a joint [ev] question with PROPS, LIB-G17 (Node::Shell) consumes the ShellNaming record and SEAT's shell VerbRecord migration must agree with it, the tess-budget re-baseline of the hollow-tube scene coordinates with S-CERT then PROPS]
---

VERBS' Wave-3 leftovers in `topo/{shell,replace_face,transform}.rs`
and `geom-brep/offset*`: the `ShellNaming` birth channel LIB-G17 waits
on, the ruled shell-of-hollow-body semantics, the cone nappe home, the
`Approx` transform door, the curved wall-clearance gate, and the tour's
hollow-tube scene. Class H with two E openers. A successor: holds the
one re-homed item now and inherits VERBS' shell items at its exit.
Charter and unit order: `work/shell/plan.md`; narrative in
`work/shell/log.md`.
