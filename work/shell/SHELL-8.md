---
id: SHELL-8
kind: unit
title: shell is per solid, on every solid — a multi-solid operand shells solid by solid, designations on any solid
status: closed
opened: 2026-09-08
branch: shell/8-multi-solid
refs: [shell-open-on-a-multi-solid-body, SHELL-5, SHELL-7]
pr: 2207
closed: 2026-09-08
---


`shell` / `shell_open` refuse a body with more than one solid; the
verb is per solid by definition, so on a body of `N` solids it applies
to every one — each solid's own gates, door choice, moves, evidence,
insertion and partition, exactly SHELL-5's construction solid by
solid — and a designation names a face on any solid. Closes
`shell-open-on-a-multi-solid-body` ("hollow, hollow, open" becomes
three verbs). Spec `docs/SHELL-8-SPEC.md`. Pre-draw difficulty **M**,
task class **STRUCTURAL** (three whole-body reads made per-solid, one
additive N-ary void door, the doors' coverage precondition relaxed to
the solid) — logged AFTER block SHELL-B2's byte was drawn, so the
covariate is contaminated for this row and is disclosed as such.
