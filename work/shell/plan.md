# SHELL — shell, offset and transform (plan)

**STATUS: OPEN (2026-09-03).** Opened 2026-09-03 from `docs/WORK-TRACKS-2026-09.md` (SHELL section), which is this
program's charter until this plan supersedes it. Live state is
`work/shell/log.md`'s tail and the item files beside this plan, never
this file.

Branch prefix (the #396 convention): **`shell/`** — unit branches
`shell/<unit>-<slug>`, orchestrator branch `shell/orchestrator`.
Away-channel tag `(SHELL orchestrator)`. A/B ordinal band
**SHELL = 2300–2399**, claimed in `docs/MODEL-AB-LOG.md`'s banding
entry in the opening commit, per that entry's rule.

**Opens at VERBS' exit.** See §Opening condition.

## Charter

Finish the shell and offset verbs to the semantics Ev ruled and give
them the naming record and the clearance gate their consumers wait
on. Every item here is VERBS' "open shell residue this wave has not
scheduled" plus the M10-5 clearance engine's first kernel consumer.

## Opening condition

A successor: dispatches when VERBS' exit walk is ratified and its
shell items re-home here. Until then this directory holds
`shell-curved-clearance-consumer` (unowned) and the plan.

## Unit order (at opening)

1. `shell-needs-shellnaming-birth-channel` — E, M; the `ShellNaming`
   record in the `FilletNaming` shape; unblocks LIB-G17 and agrees
   with SEAT's shell `VerbRecord`.
2. `shell-offset-three-followups` items 2–3 and
   `mint-offset-ignores-cone-mirror-nappe` — one nappe home both
   consumers read; predicate names for the winding uses.
3. `shell-of-hollow-body-thicken-every-boundary` — Ev's ruling
   verbatim in the body; retire `OperandAlreadyHollow`.
4. `transform-rigid-refuses-approx-face` — shape 1 (a certifier at the
   transform door) or 2 (a per-scalar lane re-deriving the certificate);
   the composition law is pinned.
5. `shell-offset-three-followups` item 1 — the props inventory's
   curved-face-with-ring reading.
6. `shell-curved-clearance-consumer` (D→H, joint with PROPS: a
   verb-layer report above editor-core versus a `Body<Interval>` engine
   in topo; then a cheaper pair filter) and
   `shell-curved-wall-clearance-window` (its build; M10-5 landed, the
   park is stale).
7. `tour-hollow-tube-scene` — E; the tess-budget re-baseline by the
   runbook.
8. `tier3-approx-regrid-per-face-cost` (PERF's) stays parked on an
   `Approx`-heavy fixture this program produces.

## Exit shape

The verbs' README states the ruled semantics and every item above has
landed; the walk convention applies.
