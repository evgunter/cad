---
id: SHELL-9
kind: unit
title: shell runs the closing pcurve mint — the void door transfers rows, the producer re-derives them
status: closed
opened: 2026-09-08
branch: shell/9-closing-mint
refs: [void-insertion-refuses-a-cavity-with-a-same-surface-latitude-seam, SHELL-7, SHELL-8]
pr: 2223
closed: 2026-09-08
---



`shell` / `shell_open` end with `validate_geometric` and never run the
pcurve mint pass the kernel's posture table says every producer runs
after a `Transfers` door: `insert_voids` reverts the cavity (rows
keep their keys and go stale in content) and grafts it, and "both
producers' final mint passes re-derive every row of the merged body"
(`crates/topo/src/pcurves.rs`, the posture table) — the boolean and
the revolve do, `shell` does not. Measured on a sphere authored as
two cocircular arcs: the door's cavity is tier-3 valid, the assembled
thin solid fails tier 3 with a pcurve `LoopDiscontinuity` on the
reverted lower zone (a one-period `u` wrap the forward walk parked at
the loop's closure lands mid-chain when the loop runs backwards), and
`topo::mint_pcurves` on the assembled body makes it tier-3 valid at
`4/3·π(r−t)³`. The unit adds the closing mint to the verb, flips that
row to its closed form, and keeps the drum half of the item
(TOPO's `revert-does-not-mirror-plane-chart-images`) refusing and
named. Spec `docs/SHELL-9-SPEC.md`. Pre-draw difficulty **S**, task
class **STRUCTURAL** — logged BEFORE block SHELL-B3's byte is drawn (a
clean covariate; this is the block's slot 0).
