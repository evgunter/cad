---
id: S93
kind: unit
title: #713's prose-held-invariant sweep minted two new prose-held caller obligations at mev's fan site and kev's fan merge
status: open
opened: 2026-08-21
track: P
refs: [713, kevs-fan-merge-needs-a-re-describing-kill-door, a-null-edge-can-be-re-based-onto-a-distinct-point, the-re-basing-gate-refuses-m7-8-where-nothing-moves, three-refusal-variants-nest-a-certification-error, set-face-surface-hands-the-caller-an-ordering-obligation-in-prose]
branch: topo/s93-rebased-carriers
pr: 2562
priority: P3
cost: E
---

## What

#713's prose-held-invariant sweep minted two new prose-held caller obligations, at `mev`'s fan site and `kev`'s fan merge.

Kind note: `issue`, because the row states the defect and shapes no fix.

## Was

unrowed.

## Finding

### S93. #713's prose-held-invariant sweep minted two new prose-held caller obligations

`crates/topo/src/euler.rs:1048-1059` (`mev`'s fan site) and `crates/topo/src/euler_kill.rs:522-535` (`kev`'s fan merge) each now carry a paragraph stating that re-based edges keep carriers certified against their *old* endpoint, instructing the caller: *"**Re-describe the moved run** (via `set_edge_curve`) whenever the two points differ."* Nothing enforces it — tier 1 does not constrain it, no operator re-checks it, and the paragraph says so.

That is the exact "the caller must / kept in step by hand" shape S15 was raised to retire, minted by S15's own fix pass. `seqgen.rs:562-575` already works around the resulting state (*"an edge's stored curve is routinely stale against its own endpoints"*) with a re-certification filter that admits it costs coverage.

**Verdict:** (blank — unruled.)

## Fence

Track P — `topo`'s Euler surgery, liveness and the generator. **Fence:** `crates/topo/src/{euler.rs,euler_ring.rs,euler_kill.rs,split.rs,attach.rs,movefac.rs,revert.rs,live.rs,merge_faces.rs,seqgen.rs,validate.rs}`, plus `crates/topo/src/{review_d18.rs,review_d18_probes.rs,fixtures.rs,source_walk.rs}` (added 2026-08-29, because they belonged to no track). **Block:** `D260`–`D279` / `S330`–`S349`.

## Claimed by TOPO (2026-09-04)

Moved from `work/code-quality/` to `work/topo/` when TOPO opened and
claimed **Track P whole**, per `work/code-quality/program.md` ("a row
leaves the moment a program claims it, by moving into that program's
directory keeping its id, `track:` letter and body") and the 2026-09
cut's rule that a program taking a code-quality letter takes all of it
so the schedule stays single-owner. Id, `track: P` and body unchanged.

Track P had **never had a lane** — no `smell/p-*` branch has ever
existed, while K, X and T lanes have run — and its fence
(`euler*.rs`, `validate.rs`, `live.rs`, `seqgen.rs`, `merge_faces.rs`
and the review-and-fixture readers) is exactly the `crates/topo/src`
territory that was in no program's `paths`. The three sub-lanes
`work/code-quality/plan.md` defined for P are inherited unchanged and
are restated in `work/topo/plan.md`.

## Brief (TOPO, 2026-09-14) — block TOPO-B3 slot 2, dual at review

**The answer to give.** `mev`'s fan site and `kev`'s fan merge re-base
a run of half-edges onto a different vertex; today each carries a
paragraph telling the caller to re-describe the moved run's carriers
"whenever the two points differ", and nothing enforces it — an edge's
stored curve is routinely stale against its own endpoints, `seqgen`
filters around it, and `split_edge` refuses on it. After this unit no
public operator leaves a re-based edge with a carrier certified
against a point that is no longer its endpoint: the operator either
re-certifies every re-based edge's carrier against the new endpoint in
its plan phase (through `EdgeCurve::recertify`, the door `split_edge`
already certifies through — `T: Decide`, no bound moved) and REFUSES
typed, body untouched, where a carrier does not pass through the new
vertex; or, where the two points coincide within band, carries the
certificate as it is. Phase 1 decides whether the re-certification is
exact or a re-fit per carrier kind and says why; a re-description the
operator cannot make honestly is a refusal, never a stale carrier.

**Rows.** Red-first: `mev` at a fan site with a moved run whose
carriers do not pass through the new vertex — on the merge base `Ok`
with stale carriers (assert `split_edge` then refuses on one of them,
the `seqgen` symptom); at the head a typed refusal naming the edge.
`kev`'s fan merge the same way. Control: the coincident-point case
carries every certificate byte for byte. `seqgen.rs`'s
re-certification filter ("an edge's stored curve is routinely stale")
retires with the reason it existed, and its proptest/roundtrip rows
stay green.

**Receipt.** Every site that re-bases a half-edge run (`mev` fan,
`kev` fan merge, `kemr`?, `split_edge`'s children, the merge door's
absorption) with whether it re-certifies, carries or refuses; the two
prose obligations deleted with the mechanism that replaces them. Both
files are TOPO's.

Branch `topo/s93-rebased-carriers`. PR title: "TOPO: a re-based edge
never keeps a carrier its endpoint left". Do not close the item; the
dual runs at review.

## Where it stands (2026-09-14)

**`mev`'s half landed in PR 2562.** `Body::mev`'s fan site re-certifies
the moved run in its plan phase, through
`Body::certify_rebased_run` and `geom_brep::EdgeCurve::recertify`, and
refuses `EulerOpError::RebasedCarrier { edge, error }` with the body
untouched where the move is what makes a carrier false. The brief's
prose obligation is gone from that door, replaced by the mechanism; the
`Ok` arm is pinned per carrier kind (`Line`/`Circle` under `Scaffold`,
`Chart`, `Intersection`) and the refusal is fuzzed on every `MevFan`
step of the generator's walk.

Two things the brief assumed turned out to be false and are recorded
where they are caused rather than here: a carrier can be within band of
the old point and still have moved (so the gate's coincident arm is
exact, never a band decision), and a certified `mev` CAN reach a
run-moving fan site — through a closed carrier at the old vertex's own
point (`euler::tests::a_certified_mev_reaches_a_run_moving_fan_site_through_a_closed_carrier`).

**`kev`'s half is open**, and this item stays open with it. The
measurement that says why, the four coordinates the cost lands at, and
the three landing shapes with what the generator does to each are
`work/topo/kevs-fan-merge-needs-a-re-describing-kill-door.md`. The
residues the `mev` half left are their own rows:
`a-null-edge-can-be-re-based-onto-a-distinct-point`,
`the-re-basing-gate-refuses-m7-8-where-nothing-moves`. Two findings
outside the unit's fence went onto TOPO's slate with it:
`set-face-surface-hands-the-caller-an-ordering-obligation-in-prose` —
the class's remaining instance, which `mev`'s old paragraph used to
point at — and `three-refusal-variants-nest-a-certification-error`.
