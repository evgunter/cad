---
id: probe
kind: program
title: PROBE — topo's own guards, probes and generators that cannot go red
status: ready
opened: 2026-09-20
area: kernel
prefix: probe/
tag: (PROBE orchestrator)
ab_band: 6700-6799
paths: [crates/topo/src/seqgen.rs, crates/topo/src/seqgen/*, crates/topo/src/review_d18.rs, crates/topo/src/review_d18_probes.rs]
keep_out: [opened by TOPO's 2026-09-20 priority-seam cut (Ev, in chat) per work/README.md Track size - TOPO keeps the Euler operators proper and its band 2700-2799, the sibling cuts are ORIGIN ATREST TQUERY WALKS PROBE, a row here that has to edit another of the six announces the seam in the PR that lands it rather than drawing a second fence]
priority: P3
---
TINT's class, on TOPO's ground and left here rather than added to a
slate already three budgets over: a fuzz row that draws from entropy
and logs no seed, so a green run records nothing; a proptest corpus
orphaned by a file move and never replayed; a hammer that drives no
`mekr` though `mekr` reaches `link_half_edges` at twelve splices; a
`CensusSubject` `PartialEq` whose catch-all arm makes a new variant
compare unequal to ITSELF. Plus the prose that describes a tree which
has moved.

Cheap, almost entirely class `E`, and exactly the band the contract
says to weigh against cost rather than to starve: several of these are
one commit each and should be taken as drive-bys by whoever is next in
the file (`work/README.md`, "The tracker is not comprehensive").

Charter and order: `work/probe/plan.md`; narrative in `work/probe/log.md`.
