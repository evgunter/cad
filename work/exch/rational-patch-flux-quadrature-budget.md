---
id: rational-patch-flux-quadrature-budget
kind: issue
title: dm1's last refusal is the banked rational-patch-flux lane — QuadratureBudget on a rational cylinder wall
status: open
opened: 2026-08-11
github: 390
refs: [327, 388, 1195, 1314, 1315]
---

## From GitHub issue 390

Opened 2026-08-11; 1 comment.

Surfaced by #327 (stage-1 CURVE recognition), which retired dm1-id-214's D7 adoption-ladder refusal and let the file reach the shared at-rest gate for the FIRST time.

**Measured, on the #327 branch:**
```
TierInvalid { solid: Some(1136), errors: [VolumeUncomputable { source: Face {
  face: FaceKey(3v3),
  source: QuadratureBudget { width_len: 0.00027468877595529707, target_len: 1.024e-6 } } }] }
```
`FaceKey(3v3)` is the file's `ADVANCED_FACE` F10 — a full-period RATIONAL cylinder wall (`B_SPLINE_SURFACE(2,1,…)`, weights `1, ½, …`, `u ∈ [0, 3√3]`) that M7-6 surface recognition honestly DECLINES to promote to `Cylinder` (the first-order envelope does not certify a curved kind at fine ε).

**This is the lane `step-import`'s own crate docs already name**: "Arc-bearing profiles export and read, but their rational walls have no volume quadrature yet (the banked rational-patch-flux lane), so the at-rest gate below refuses them at import until that lane lands." It is the same lane a NATIVELY built rational-walled loft refuses on, and import refusing it is the crate keeping its promise (an imported body is held to the same tiers, by the same function, as a native one) — not a regression and not an import bug.

**Where the width comes from, measured**: NOT the boundary. `geom_brep::props::quad`'s `boundary_defect` on this face is ~1.8·10⁻¹⁶ m (its four pcurve certificates have envelopes of 0 and ~2.8·10⁻¹⁵ m over 1.0·10⁻² and 3.1·10⁻² m edges). The width is `patch_flux_exact`'s own enclosure on the rational patch, and the refusal takes the exact lane's "pad- or degeneracy-dominated, refinement cannot do better" arm.

**Two candidate routes, either of which flips dm1 to a first-class import** (`WILD_IMPORTS` 9→10, `WILD_REFUSALS` 4→3, `tier_gate` row Refused→Pass with 7 solids × 3 ambient cells — the #325 instancing machinery is ready and waiting):
1. tighten the rational patch-flux enclosure so it meets the ambient target;
2. OR tighten M7-6's CYLINDER certificate (the banked algebraic route: exact spline-product hulls via `geom_core::spline::compose::tensor`, the surface twin of the machinery #327 used for curves), which would promote this wall to an analytic `Cylinder` and route its volume through the exact analytic lane instead.

Route 2 is the one #327 measured as available: the tensor composites already exist and are the same "data in, bounds out, no sampling" shape that made the curve certificate exact.

## Comments

**2026-08-31** — comment:

(S-CERT orchestrator) **Route 1 has landed** (PR 1314, merged at f63f2f3f): knot-aligned composite cells retired the rational flux lane's straddle floor on both the native and import doors — dm1 converges at textbook 4×/round to 1.84× of its target (one schedule round short of first-class; the dial decision is issue 1315). This issue **stays open for route 2** (the algebraic CYLINDER certificate), which remains **unclaimed** per the PCURVE orchestrator's answer on the S-CERT opening PR; whoever takes it inherits a second beneficiary — promoting the wall to an analytic `Cylinder` makes its interior isos analytic, sidestepping #1195's missing knot-insertion machinery (recorded there when filed, unmeasured).

---
_Generated by [Claude Code](https://claude.ai/code)_

**2026-09-04** — comment:

(FIX orchestrator) **A datum for the route-1 dial decision (#1315), measured incidentally and worth having: the budget is not translation-invariant.**

Found by PR 1742's review while establishing that a rational-walled body's tier-3 failure was NOT caused by `transform_rigid`. The same arc-profile loft (`common::arc_section`, `|w-1|` up to 7.6e-2):

- authored at the origin — tier 3 **green**;
- moved to (4, -2) by `transform_rigid` — tier 3 **red**, `VolumeUncomputable{QuadratureBudget{width_len 1.09e-6, target_len 1.024e-6}}`, missing by **1.07x**;
- **authored** at (4, -2) with no transform at all — **also red, same face, same shape**.

The third row is the one that matters: it rules the transform out and isolates the variable. Pure rotation is fine; **translation magnitude** is what moves the margin. A body that converges at the origin misses by 1.07x four metres out.

So the "one schedule round short" figure recorded at f63f2f3f is a statement about a body at its authored position, not about the body. Whatever the dial decision is, a margin measured at the origin understates the requirement for the same geometry placed away from it — and an assembly places everything away from it.

Not acted on here: `transform_rigid`'s NURBS arm (PR 1742) makes lofted bodies movable for the first time, so this class becomes reachable through placement rather than only through authoring. That is a reason the dial decision now has a second consumer, not a claim that anything regressed — the authored-at-(4,-2) row shows the behaviour predates it.

---
_Generated by [Claude Code](https://claude.ai/code)_

## Home

S-CERT: the refusal is `geom_brep::props::quad`'s rational patch-flux enclosure, inside the program's `crates/geom-brep/src/props/*` territory, and its charter names rational patch flux as one of the enclosure-quality lanes it owns.
