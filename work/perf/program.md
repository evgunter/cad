---
id: perf
kind: program
title: PERF — the performance work still owed
status: open
opened: 2026-07-21
area: kernel
paths: [benches/*, docs/perf-data/*, docs/PERF-SCAN-2026-08.md]
keep_out: [the parallel subdivision driver is M10's, per-frame rendering and hover-picking are viewer ground, the evaluate memo door from Python is LIB's binding]
---

A standing register of unbuilt performance work, advisory beneath
`docs/DESIGN.md` (its ratified parts live there as the D9 addendum:
parallelism idioms and the GPU boundary). No orchestrator and no units: it
ranks open cost centers by payoff, confidence and effort, keeps
`benches/` and `docs/perf-data/` as the measurement record, and expires its
own claims — re-check a cited `file:line` before quoting a cost as current.
Detail: `plan.md` (last resurveyed 2026-08-26).
