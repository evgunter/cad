---
id: apex-crossing-branch-premise-has-no-body-level-witness
kind: issue
title: tess: the branch premise's cone arm is no longer reached through tessellate — the bow tie refuses at the shape door first
status: open
opened: 2026-09-20
---



Filed by the PROPS curved-residues unit (PR 2924) as the disclosed cost
of the change disclosed in
`shape-door-gained-the-rim-side-unanimity-premise`.

## The measurement

`crates/mesh/tests/mesh11_arc_branch.rs` carries MESH-11's two
π-rad witnesses, each a body that used to reach the walk and mis-mesh:
the pole-crossing half cap (sphere) and the apex-crossing bow tie
(cone). Both refused `NotOneChartBranch` through the public
`tessellate`, at every δ, which is what the rows pin.

The bow tie's SECOND face states two rims that encode different
material sides, so `require_iso_rectangle`'s new unanimity companion
refuses it `NotIsoRectangle { what: "props_rim_side" }` before
`require_one_chart_branch` is ever asked. Executed at every δ in the
row's schedule and at every one of CI's six lane/eps points; the row is
re-pointed and renamed `the_apex_crossing_bowtie_refuses_at_the_shape_door`.

**What is lost is a witness, not a premise.** The branch door's cone
arm still refuses the same boundary — `props_cone_apex`, pinned at the
props level by `an_apex_crossing_generator_is_not_one_chart_branch`
(`crates/geom-brep/tests/mesh11_arc_branch.rs`), on a hand-built bow
whose two rims are traversed opposite ways and which the shape door
still admits. What no body in the corpus does any more is reach that
arm THROUGH `tessellate`, so the walk-side claim "`mesh` cites the
branch door and gets it" has a sphere witness and no cone one.

## What it would take

A cone body whose apex-crossing generator sits on a face whose rims do
NOT contradict each other — the hand-built bow above is that boundary,
so the question is whether the Euler doors can state it as a face. If
they cannot, the honest disposition is to say so in the row's header
and let the props-level witness carry the cone arm, rather than leave a
sentence claiming a reach no fixture has.
