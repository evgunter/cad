---
id: tess
kind: program
title: TESS — the tessellation kernel: crates/mesh and the body-side coherence examination
status: active
opened: 2026-09-16
area: kernel
prefix: tess/
tag: (TESS orchestrator)
ab_band: 5100-5199
paths: [crates/mesh/*, crates/topo/src/coherence.rs]
keep_out: [props/quad.rs and patch_bound.rs and certify.rs are PROPS' (the quadrature rows C3 and D30 and the stored-span reads moved there at S-MESH's exit; the iso-rectangle and one-chart-branch doors are PROPS' shape predicates that mesh cites), the tess-budget document and tools/tess-meter are INSTR's, demos/tour pins are Track X conventions (re-pin in the moving PR with what moved and why), the guide page docs/guide/meshing.md is LIB's, the certificate-fuzz seed gate is S-TINT's, D9's byte-identical replay is ratified ground — a mesh byte that moves is re-baselined with the cause named and never weighed against the change]
priority: P0
---

S-MESH's successor for the tessellation kernel: `crates/mesh` (walk,
sizing, the three tessellation lanes, certificates, memos) and the
body-side coherence examination S-MESH relocated into
`topo::coherence` under its Q2 ruling. Opened by S-MESH's orchestrator
at S-MESH's exit (2026-09-16) with the fourteen mesh findings, the
parked MESH-9 and the seven Track R rows S-MESH left open as its slate
— one track on one territory, per `work/README.md`'s closing rule.
Charter, slate and process: `work/tess/plan.md`; narrative in
`work/tess/log.md`.
