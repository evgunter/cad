# TESS — the tessellation kernel (plan)

**STATUS: OPEN.** Opened 2026-09-16 at
S-MESH's exit (`docs/S-MESH-EXIT-WALK.md`) by S-MESH's orchestrator, as
the successor program for `crates/mesh` and `topo::coherence`. Live
state is `work/tess/log.md`'s tail and the item files beside this
plan, never this file.

Branch prefix (the #396 convention): **`tess/`** — unit branches
`tess/<unit>-<slug>`, orchestrator branch `tess/orchestrator`.
Away-channel tag `(TESS orchestrator)`. A/B ordinal band
**TESS = 5100–5199**, recorded in `docs/MODEL-AB-LOG.md`'s banding
entry in the commit that opens this program, per that entry's rule.

## Charter

What S-MESH's eleven units left the tessellation kernel with — every
guard fires where somebody looks and none lies about its ε; ε's
consumers are named operations; the iso-rectangle and one-chart-branch
premises are doors mesh cites; input-quality detectors live body-side
in `topo::coherence` — and what they did not fund: the findings below,
the parked rim-chord question (MESH-9, issue 950, its typed trigger
intact), and code-quality Track R's mesh rows as track lanes.

## Ratified ground (cited, not re-litigated)

- S-MESH's three rulings (Ev, in-chat, 2026-09-01; `work/mesh/plan.md`
  §Rulings sought, recoverable at the ledger's sweep SHA): Q1 — S65
  stays compiled out, the two uncovered cases as debug-profile census;
  Q2 — option (d), the detectors relocate body-side, non-gating; Q3 —
  explicit doors, no transitive floor, each door cites
  `props_rim_level`.
- D9's byte-identical replay for the mesh (the MESH-4 digest at three
  ε rows is the instrument); D2's typed refusals; the never-infer
  doctrine (structural rungs only).

## The slate

The item files beside this plan, and nothing else, are the slate
(`python3 scripts/work.py status --program tess`). TESS keeps the rows
where the mesh LIES or REFUSES; the arithmetic and certificates beneath
them — the register rows, the chart-frame and unit-direction rows, the
memo dumps, the writable `Mesh` — went to CHORD at the 2026-09-20
priority-seam cut (Ev, in chat), which shares `crates/mesh` whole with
this program: neither fences the other, and a unit crossing announces
the seam in its PR.

## Process

Review tiers (Ev, in-chat, 2026-09-18 — EDIT's tiers of 2026-09-16):
the full v6 dual on a kernel unit; a style review with a correctness
arm, and no A/B row, for a unit that is easy but not trivially so
(test-only rows included); no review and no row for a rename or prose
row, which merges on green CI and the orchestrator's read.

A kernel unit runs the v6 dual (`docs/MODEL-AB-LOG.md` normative): one
implementer plus the cross-model dual review and the union fix pass;
implementer blocks `TESS-B<n>` drawn branch-side; ordinals claimed on
main at review dispatch from 5100–5199; record-at-merge with per-phase
tokens and wall-clock; blinding discipline verbatim; hosted CI the only
gate; the #1356 ε-trailer practice where a unit is band-sensitive; the
implementer and reviewer prompts by path.

## Exit shape (proposed)

The patch bound encloses the described patch and the soundness sweep
is green on a bilinear-stratified census; every refusal the slate names
arrives at its door with its name; MESH-9 is either landed on its
trigger or still parked with the trigger intact. Every unit merged on
its own green hosted head; the walk convention applies at exit.
