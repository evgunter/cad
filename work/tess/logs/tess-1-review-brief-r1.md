You are a blinded REVIEWER (r1) of PR #2852, "TESS-1: a meridian-free curved face refuses typed", in the Rust B-rep CAD kernel at github.com/evgunter/cad. Frozen head: `7a5fe831ea2a3dcaac6df534ff880dd93a3d1e35` (branch `tess/1-meridian-free-refusal`). Review THAT commit, whatever the branch does later.

## Read first, in full
1. `docs/prompts/reviewer-style-lane.md` — the standing second half of every review; your report names which of its questions you exercised and every finding carries `sure`/`likely`/`unsure`.
2. `docs/TESS-1-SPEC.md` (the unit's binding spec) and `work/tess/rim-only-sphere-cap-panics-at-census.md`.
3. The PR body (`gh pr view 2852`) — the author's account, deviations and sweep. It is a hypothesis, like this brief: reviewers correcting the dispatcher or the author is a working lane, not a malfunction. Check framing against the tree before building on it.

## Isolation (protocol v6 item 5) — binding
Another reviewer is reviewing the same head concurrently. Until your report is delivered you must NOT fetch, check out, read or list any other review lane's branches, clones, scratch directories, targets or CI artifacts (anything named `tess-1-review-*` other than your own), and must not read PR comments posted after you start. Disclose any accidental glimpse in your report. Do not post anything to the PR; do not push to the PR branch.

## Setup
- Your own clone: from `/home/evan/projects/cad` run `bash local-scripts/new-lane.sh tess-1-review-r1`, then in it `git fetch origin tess/1-meridian-free-refusal && git checkout --detach 7a5fe831ea2a3dcaac6df534ff880dd93a3d1e35`. Work ONLY in `~/.local/share/cad-work/tess-1-review-r1/cad`; every Bash call starts with `cd ~/.local/share/cad-work/tess-1-review-r1/cad && ...` (cwd resets between calls). Probes and mutants you write live there; commit useful ones to a branch `tess/1-review-r1-probes` and push it ONLY AFTER your report is written (reviewer rows become ordinary tests).
- `CARGO_TARGET_DIR=/home/evan/.local/share/cad-work/tess-1-review-r1-target` on every cargo call (never shared; confirm a `Compiling mesh` line before trusting a run). Every cargo invocation wrapped in `local-scripts/with-build-slot.sh` (read its header; a machine-wide mutex another lane also needs — prefer `-n` grab-or-retry). Never pipe a slot-wrapped command through `| tail`/`| head`; redirect to a file under your private scratch `~/.local/share/cad-work/tess-1-review-r1/scratch/` (NOT the session scratchpad, NOT /tmp) and read that.
- **Method note, identical for both reviewers**: the box has ~16 G free. Use `CARGO_INCREMENTAL=0`, debug profile, and build only the packages you need (`-p mesh`, `-p topo`, `-p step-import`, `-p pncad-py` as your claims require) rather than the workspace. Check `df -h /home` before each new package build; if free space is under 8 G, stop building, say so in the report, and finish by reading. This narrows nothing you are asked to judge — hosted CI on this head already ran the full matrix green (run 35550649883; verify that at the step level yourself if you rely on it).
- Foreground rule, both halves: no background waiters or chains for your own builds/tests; anything that could outlive a 600 s call is launched `setsid`-detached to a file with its PID recorded and polled in the foreground; kill your own previous waiter by recorded PID before re-issuing a timed-out call; nothing left running when you report. `gh` calls on this box intermittently hang — give each a `timeout`.

## Claims to falsify (correctness lane: a finding here should be DEMONSTRATED — a red probe, a mutant, a measured value — wherever you can)
1. **The refusal is decided on structure and only structure**: `TessellateError::MeridianFreeCurvedFace` fires iff the face's loop classifies with no `TravKind::Meridian`, and no floating-point comparison can turn it on or off. Hunt for a face that SHOULD mesh and now refuses (a legitimate curved face whose traversal list has no meridian: think full-period walls, seam edges used twice, pole-to-pole bands, cone half-faces, tori, faces after booleans/fillets in the tour and wild corpora) and for a rim-only face that still escapes the refusal (a loop with a vanishing or mis-classified meridian; a classification that depends on ε). Try at the 1e-6 and 1e-12 ε rows as well as default.
2. **It arrives before anything is emitted, in every profile**, and in the right order relative to `EmptyLoop`, the shape doors (`UnsupportedCurvedShape`) and `RingOnCurvedFace`. With debug assertions OFF the old behaviour was `Ok` with a hole; show the new behaviour there too.
3. **Deviation 3 is not a regression**: the PR re-worked an in-crate row because the oblique lens loop ("two Rims") now refuses at the walk. Establish what that loop did before this PR in each profile, whether it is reachable through `mesh::tessellate` at all (the PR says it sits behind the shape door), and whether any documented behaviour or pinned row elsewhere depended on the old walk output. Same question for the `curved_walks` split.
4. **The rows can go red** (style Q3): with the guard removed, which of the new rows fail, in which profile, and do any pass vacuously? Do they assert the variant AND payload? Is the positive control really the same solid as its rim-only twin, and would it go red if the refusal over-fired?
5. **The torus disposition is right**: all-rims torus refused one door earlier with or without this PR (verify on main's code, not by reading the PR); the all-meridians torus is filed rather than fixed — is `work/tess/rim-free-loop-on-a-poleless-chart-meshes-as-a-hole.md` accurate, and is the PR's argument that "no rim" cannot be the refusal (the sphere's pole-to-pole band) correct?
6. **Every exhaustive consumer took the arm and none was papered over**: the python tag table, `tessellate_err`, the rosters; anything the compiler did not force that now lies (the PR names `pncad.pyi` and `docs/guide/meshing.md` as filed on LIB — is filing, not fixing, right for each, per `python3 scripts/work.py territory --files -`?).
7. **No mesh byte moved** on any tour/wild body, and no pin, golden or baseline was re-taken. Verify from the diff and the run, not from the PR body.
8. **The census message and the corrected prose are true**: `tessellate_impl`'s message, `unpaired_chord_segment`'s doc, `walk.rs`'s module-doc pole premise, `walk_anchor`'s doc, the `types.rs` variant doc (D2 addendum row 2; the stated recourse — does the seamed statement really mesh?), `curved.rs`'s "four questions", the `mesh12_rim_row_reach.rs` header. Comments must state invariants, not history.
9. **The sweep's unexecuted members are filed honestly**: `work/tess/trimmed-and-planar-lanes-answer-ok-on-an-empty-patch.md` rests on reading only — does it say so, and is its reading right? If you can cheaply construct the trimmed-lane zero-area case, do.
10. **The STEP rows and fixtures**: are `rimonly1.step`/`rimonly2.step` what the generator produces, are they valid statements of the cap, and does the row that asserts "adopted as stated" say (where a future reader will see it) that it is expected to change when import learns to normalize this face (`work/exch/import-normalizes-the-rim-only-cap.md`)?

Style-lane emphasis for this unit: Q1 (does `require_a_meridian` duplicate a predicate that already exists — props' private `sphere_rim_only_pole_level` family, `has_rim`/`no_rim` in the walk, the shape door's rim/meridian parse; which direction should the dependency run?), Q4 (this PR turns a documented premise into an enforced one — who else cited the old premise or the old lens row, including in prose?), Q6 (five disclosed deviations: each an improvement, or each with a filed follow-up?), and Q8 — read `crates/mesh/src/walk.rs` end to end once. Also the brief's standing trap: does the fix mint a fresh instance of the defect it closes (a new path that answers `Ok` with nothing emitted; a new unreachable-by-prose claim)?

## Report (your final message, ≤150 lines)
1. Verdict: APPROVE / APPROVE-WITH-FIXES / REQUEST-CHANGES.
2. Findings as MAJOR / MINOR / NOTE, each with file + symbol (a line number may ride along), confidence, and — for MAJORs — at least one line of prose saying what breaks and how you demonstrated it (or that it is by inspection).
3. `## Style` section per the style brief (no proposed fixes; a long tail is expected).
4. **CODE QUALITY REPORT, fixed rubric**: counts of MAJOR / MINOR / NOTE; spec deviations counted as reported vs SILENT (silent = a deviation from `docs/TESS-1-SPEC.md` the PR body does not disclose); ratings 1–5 with one line of evidence each for (a) idiom/structure, (b) test quality — do the tests pin the real contract?, (c) doc/comment honesty.
5. Which style questions you exercised; what you ran (commands) versus read; what you did NOT look at; any isolation glimpse; approximate wall-clock.
Do not name or guess what kind of author wrote the PR.
