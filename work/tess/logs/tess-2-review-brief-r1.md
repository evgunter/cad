You are a blinded REVIEWER (r1) of PR #3080, "TESS-2: the patch bound encloses the described patch — knot refinement inside the ring", in the Rust B-rep CAD kernel at github.com/evgunter/cad. Frozen head: `445257159069886c821ea537c6bcab16bcf42c6e` (branch `tess/2-refinement-in-the-ring`). Review THAT commit, whatever the branch does later.

## Read first, in full
1. `docs/prompts/reviewer-style-lane.md` — the standing second half of every review; your report names which of its questions you exercised and every finding carries `sure`/`likely`/`unsure`.
2. `docs/TESS-2-SPEC.md` (the binding spec, including "The allowance that now hides the defect") and `work/tess/rational-cells-hull-the-f64-refined-net-so-the-described-patch-escapes.md` (the exact-arithmetic evidence; the referee scripts are on `origin/tess/nurbs-bound-diag` and a copy ships in this PR).
3. The PR body (`gh pr view 3080`) — the author's account: schedule choice, the convex-vs-lerp decision, Phase 1 numbers, censuses, sampler-error measurement, widening/timing, re-pins, sweep, deviations. It is a hypothesis, like this brief: reviewers correcting the dispatcher or the author is a working lane. Check framing against the tree before building on it.

## Isolation (protocol v6 item 5) — binding
Another reviewer is reviewing the same head concurrently. Until your report is delivered you must NOT fetch, check out, read or list any other review lane's branches, clones, scratch directories, targets or CI artifacts (anything named `tess-2-review-*` other than your own), and must not read PR comments posted after you start. Disclose any accidental glimpse in your report. Do not post anything to the PR; do not push to the PR branch.

## Setup
- Own clone: from `/home/evan/projects/cad` run `bash local-scripts/new-lane.sh tess-2-review-r1`, then in it `git fetch origin tess/2-refinement-in-the-ring && git checkout --detach 445257159069886c821ea537c6bcab16bcf42c6e`. Work ONLY in `~/.local/share/cad-work/tess-2-review-r1/cad`; every Bash call starts with `cd ~/.local/share/cad-work/tess-2-review-r1/cad && ...` (cwd resets between calls). Probes and mutants live there; commit useful ones to a branch `tess/2-review-r1-probes` and push it ONLY AFTER your report is written (reviewer rows become ordinary tests).
- `CARGO_TARGET_DIR=/home/evan/.local/share/cad-work/tess-2-review-r1-target` on every cargo call (never shared; confirm a `Compiling` line before trusting a run). Every cargo invocation wrapped in `local-scripts/with-build-slot.sh` (read its header; machine-wide mutex another lane also needs — prefer `-n` grab-or-retry). Never pipe a slot-wrapped command through `| tail`/`| head`; redirect to a file under your private scratch `~/.local/share/cad-work/tess-2-review-r1/scratch/` (NOT the session scratchpad, NOT /tmp) and read that.
- **Method note, identical for both reviewers**: ~28 G free. `CARGO_INCREMENTAL=0`, debug profile for tests, release only for a census you need; build per package (`-p geom-core`, `-p geom-brep`, `-p mesh`) rather than the workspace. Check `df -h /home` before each build; under 8 G stop building, say so, and finish by reading. Hosted CI on this head ran the full matrix green (run 35745584307) — verify at the step level yourself if you rely on it. The exact referee (`crates/mesh/tests/nurbs_exact_referee.py`, Python `fractions`) is yours to run.
- Foreground rule, both halves: no background waiters/chains for your own builds/tests; anything that could outlive a 600 s call is launched `setsid`-detached to a file with its PID recorded and polled in the foreground; kill your own previous waiter by recorded PID before re-issuing; nothing left running when you report. Give every `gh` call a `timeout`.

## Claims to falsify (correctness lane — a finding here should be DEMONSTRATED: a red probe, a mutant, an exact-referee number)
1. **Every `PatchCell` now encloses the DESCRIBED patch's partial on its cell**, rational and integral arms, with no carve-out. The PR proves two bilinear corners; hunt beyond them: degree 2 and 3 with interior knots and multiplicities at the C¹ gate, extreme weight ratios (1e-2..1e2), higher split counts (`patch_cells_refined` as `offset_meters` calls it), points strictly inside a refined cell and on refined-knot lines, first derivatives (`s_u`, `s_v`) as well as second, the `w` enclosure itself. Use the exact referee to get truths; an escape of ANY size against a bare comparison is the finding.
2. **`CurvePlan::apply_ring` applies the SAME schedule as `apply_points`** (one schedule, two arithmetics), and every ratio it uses is outward-rounded from the knots — no `f64` quotient survives, including the plan's stored `lambda`. Construct a schedule where the two could diverge (removal/elevation steps where `Ratio` is `None`; repeated insertion at an existing knot; insertion at a knot one ulp from an existing one — see `work/nurbs/refine-dir-hairline-knot-insertion.md`) and show what happens. The unit-weight chain is claimed EXACT — check it, and reconcile it with the re-pinned `rational_uniform_weight_bilinear_dust` ceilings moving from 1e-100 to 1e-12 (`muu`/`mvv` measured 2.84e-13): where does a unit-weight bilinear's second partial stop being exactly zero, and is the author's explanation ("the old 1e-100 recorded the f64 refinement reproducing an exact cancellation") right?
3. **The convex form `β·x + α·y` is sound** (β and α each outward-rounded; is `β = 1 − α` enclosed or a second quotient? does the pair straddle 1 correctly?) and the measured width advantage over lerp is real; `the_ring_applier_stays_in_step_and_inside_the_hull` goes red under lerp (the PR says so — reproduce).
4. **Phase 1 is red before, green after**: rerun the exact-referee rows on the merge base with the fix reverted (or on `origin/main`) and on the head; check the 17-digit numbers and that the rows use `Domination::new` (allowance zero), not `sampled_under_certified`.
5. **Nothing got tighter** (0 of 22,500 components) — an enclosure that includes more roundings cannot shrink; a component that did would be a bug. And the widening's top 1 % exceeds the spec's ~1e-12 (max 5.36e-12): is the author's cause (a fold of ~16 single insertions) right, and does the widening change any grid count or mesh byte the digests would see at 1e-6 / 1e-12 ε (CI says no — read the digest rows to see what they cover)?
6. **The censuses**: the pre-fix rates (bilinear 2/6,000; general 1/1,500) contradict the row's recorded 2/30,000 and 0/90,000 — the author attributes the change to RING-2's tighter ring; check that, and check that the enumeration row `the_bilinear_rational_stratum_is_dominated_at_every_weight_decade` can go red (style Q3: revert the fix and run it).
7. **The sampler-error measurement** (median/p99/max in ulps per component) is computed the way the PR says and against the right truth; and `SAMPLER_ULPS` was left at 64 as the spec required.
8. **`RefinedWeightLostPositivity` on the enclosure's `lo` and on poison** is reachable and typed (a weight ratio wide enough that the enclosure reaches 0); the `rational_cells` centroid from enclosure midpoints keeps the recentring sound.
9. **No golden, digest, demo pin or baseline moved** except the two re-pins the PR names, each with its cause; `tess-budget-baseline.csv` untouched. Verify from the diff and the run, not the PR body.
10. **The sweep is honest**: the five filed sites in `work/props/f64-refinement-inside-an-enclosure-has-five-more-sites.md` are real instances (read each), `compose.rs` `insert_once_ring` is a hand-written Boehm loop the grep could not see, and the stated blind spots are the actual ones.

Style-lane emphasis: Q1 (does `apply_ring`/`refine_plan_homogeneous`/`TensorNet::refine_*`/`patch_bound::refine_chain` mint a second spelling of anything — `insert_once_ring` in `compose.rs` is the obvious near-twin; which direction should the dependency run?), Q2 (the convex-form argument, the unit-weight-exactness argument, "why 16 is affordable" — are these comments doing work the code or a test should do?), Q4 (`PatchCell`'s retired caveat: who cited it, and did `offset_meters::cell_normal`'s rewrite change what it computes?), Q6 (deviation 6 — the referee `.py` under `crates/mesh/tests/` because `scripts/` refused it; every other disclosed deviation), Q8 — read `crates/geom-brep/src/patch_bound.rs` end to end once. And the standing trap: does the fix mint a fresh instance of what it closes (an f64 step inside an enclosure anywhere in the new code — the centroid, the span extents, the recentring)?

## Report (your final message, ≤150 lines)
1. Verdict: APPROVE / APPROVE-WITH-FIXES / REQUEST-CHANGES.
2. Findings as MAJOR / MINOR / NOTE, each with file + symbol, confidence, and — for MAJORs — one line saying what breaks and how you demonstrated it (or that it is by inspection).
3. `## Style` section per the style brief (no proposed fixes; a long tail is expected).
4. **CODE QUALITY REPORT, fixed rubric**: counts MAJOR / MINOR / NOTE; spec deviations reported vs SILENT (silent = a deviation from `docs/TESS-2-SPEC.md` the PR body does not disclose); ratings 1–5 with one line of evidence each for (a) idiom/structure, (b) test quality — do the tests pin the real contract?, (c) doc/comment honesty.
5. Which style questions you exercised; what you ran versus read; what you did NOT look at; any isolation glimpse; approximate wall-clock.
Do not name or guess what kind of author wrote the PR.
