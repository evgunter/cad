---
id: fix
kind: program
title: FIX — kernel and façade doors with the fix written
status: open
opened: 2026-09-03
area: kernel
prefix: fix/
tag: (FIX orchestrator)
ab_band: 1700-1799
paths: [crates/editor-core/src/checks.rs, crates/editor-core/src/refactor.rs, crates/quantity/*]
keep_out: [crates/topo/src/transform.rs was this program's until SHELL opened 2026-09-03 and its glob claimed it — the NURBS arm is carried here by announcement and SHELL owns the Approx arm (transform-rigid-refuses-approx-face) in the same two matches, this program spans fences by nature — every PR is one item and names the fence it touches, crates/topo/src/census.rs is CURVED's — FIX edits it by recorded seam, and the seam is the FILE not a fixed list of rows: it was written for the two typed-decline items and has since carried pair-subject-witness-strings-unswept (comments only) and now census-witness-string-repeats-the-subject, whose fix reaches CURVED's chart-region predicate and so wants CURVED's assent rather than announcement; CURVED's keep_out does not name FIX, so the record is one-sided and the crossing is invisible from CURVED's side, crates/geom/src/* and crates/geom-core/src/* are PROPS's — S-CERT closed and PROPS's glob covers that ground now; the NURBS point-map helper landed as a row here (nurbs-net-point-map-helper, closed) and is-finite-length-homed-in-the-query-seat crossed into geom-core by announcement, crates/editor-core/src/eval/wire.rs is WIRE's as of 2026-09-11 — it was in no open program's paths when this clause was first written (LIB and SEAT claimed it by keep_out prose and SEAT is closed) and WIRE's opening glob took it; the one-line unit() fix landed under the old reading, and any further FIX unit on that file crosses WIRE by announcement, crates/mesh/* is S-MESH's — the one TessellateError Display impl is announced, pncad/src/workspace.rs is LIB's — the pin-mismatch fix edits it by note, boolean/mod.rs is S-BOOL's glob — BooleanErrorKind is off its slate and announced, a door whose shape is not yet chosen is not this program's — BooleanDeclarations' producer and the census/genus query wait on their decision]
---

One-PR items whose body already contains the fix, on ground no live
program is working: the unowned `topo/src` files, `census.rs` and the
mate/assembly files S-MATE leaves at exit, `editor-core`'s checks and
refactor modules, `crates/quantity`. Class E throughout, one small
ruling. Review posture: batched style review; an A/B row only where a
unit moves kernel logic. Charter and unit order: `work/fix/plan.md`;
narrative in `work/fix/log.md`.
