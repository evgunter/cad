---
id: fix
kind: program
title: FIX — kernel and façade doors with the fix written
status: open
opened: 2026-09-03
area: kernel
prefix: fix/
tag: (FIX orchestrator)
ab_band: 1700-1799
paths: [crates/editor-core/src/checks.rs, crates/editor-core/src/refactor.rs, crates/quantity/*]
keep_out: [THIS PROGRAM TAKES NO DESIGN DECISIONS — Ev in chat 2026-09-20; a row whose blocking question is a design decision goes to the track that owns the surface the decision is about, by `git mv` with a `## Re-homed` section and a note on that program's log, and fourteen rows left this way on 2026-09-20 (the sweep is in `log.md`). What stays is rows whose fix is written; what a lane may still take in seat is a judgement call on THIS program's own three paths, recorded on the item., THIS LIST IS NOT EXHAUSTIVE BY CONSTRUCTION: it records seams that needed a note, not every crossing ever made, so a crossing absent from it is not thereby unannounced and a lane should read `scripts/work.py territory --base origin/main` rather than this clause — two claims below were stale for weeks because the seat asserted a fence instead of running the instrument., crates/profile/* is PATHS's, not BOOL's and not its own fence — a wave-3 brief told a lane otherwise and territory corrected it once; territory now names PATHS, and the fillet-leg-carrier row crosses it by announcement., crates/test-utils/* is S-TCOST's and S-TINT's — the quantity row folds onto `test_utils::f6::assert_f6` and adds a dev-dependency edge, announced there., crates/geom-brep/*, crates/geom/* and crates/geom-core/* are CHART's, ENCL's and PROPS's respectively — the recourse-chain row reads five carriers across all three and crosses each by announcement., crates/*/tests/* is S-TCOST's and S-TINT's, the documented always-overlapping seam., crates/pncad-py/* and crates/pncad/src/document.rs are LIB's, forced wherever an exhaustive match or a census crosses to Python., crates/topo/src/census.rs is REACH's — it was recorded here as CURVED's until 2026-09-20 and CURVED's paths have not named it for some time; the rows that used that seam have left the program, so nothing here needs it now., crates/topo/src/transform.rs is SHELL's and crates/editor-core/src/eval/wire.rs is WIRE's — both were FIX's by an older reading, both rows have gone to their owners.]
---

One-PR items whose body already contains the fix: a typed decline whose
payload is named in the body, a `Display` impl the consumers already
need, a one-line gate, a rename. Class E throughout. This program exists
so that they land instead of accreting, and it spans fences by
construction — one item per PR, the fence named in the PR body, the
owning program told on the away channel.

**It carries no design decisions** (Ev, in chat, 2026-09-20). A row that
has to decide something before its fix can be written belongs to the
track that owns the surface being decided, not here; fourteen such rows
were re-homed on 2026-09-20. Review posture: orchestrator-review or
style-only, never a full adversarial lane. Charter and unit order:
`work/fix/plan.md`; narrative in `work/fix/log.md`.
