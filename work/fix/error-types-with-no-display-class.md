---
id: error-types-with-no-display-class
kind: issue
title: Error types that reach a user surface with no Display - the class, not the instance
status: open
opened: 2026-08-27
github: 1111
refs: [1103, 1106, 1161]
branch: fix/viewer-display
pr: 1741
---

## From GitHub issue 1111

Opened 2026-08-27; 1 comment.

Opened by the GUI-2 fix pass (PR #1106) after both reviewers converged on
"issue, not fix-now, **but the issue must exist and must be the class**".
#1103 is the shape: a disclosed gap with a number is scheduled; "worth an
issue" in a PR body is not.

## The rule this is about

This repo's standing convention is that **the layer that raised a failure
names it** — a consumer renders a payload through the payload's own
`Display`, and never composes a sentence about somebody else's refusal.
`viewer::session::Refusal` follows it: every arm forwards to its payload,
and the two arms that cannot (`ParseError`, now `HitTestError`) say so at
the site and point here.

Where a type has no `Display`, the consumer's only honest option is a
debug rendering — `{error:?}` — and that is what several user-visible
surfaces currently show. A `Debug` rendering in a status line is not a
message to a person; it is a struct dump that happens to be printable.

## The class

Reported as an instance in #1106 (`HitTestError`), swept by R2 into the
list below. Every one of these is public, is reachable by a consumer, and
has no `Display`:

**`editor-core`**
- `HitTestError` — reaches the viewer through `PickError::HitTest`, which
  debug-renders it.

**`viewer`**
- `CameraError` — reaches the status line through `PickError::Camera` and
  through `ViewerApp`'s projection/framing paths.
- `CameraOpError`
- `SceneError` — status line, `format!("scene: {error:?}")`.
- `SceneDocError`
- `StartupError` — the process's exit message.
- `IdMapError`, `PickIndexError` — added by GUI-2; `PickIndexError`
  reaches the status line as `format!("pick index: {error:?}")`.
- `ReplayError`

Not a complete census of the workspace — it is the set two reviewers'
sweeps over one unit's touched surface turned up. Whoever takes this
should re-sweep rather than trust the list: the pattern is a public error
enum with no `impl Display`, and the interesting subset is the one whose
values reach a string a person reads.

## What "done" looks like

`impl Display` on each, stating what happened in the vocabulary of the
layer that raised it, plus `impl core::error::Error` where it is missing.
Then delete the debug renderings at the consumer sites and the comments
that explain why they are there — those comments name this issue, so they
are the thread to pull.

Cited from:
- `crates/viewer/src/pick.rs` — `PickError`'s `Display`.
- `crates/viewer/src/app.rs` — the `scene:` / `pick index:` status lines.

## Why it was not done in #1106

`HitTestError` lives in `editor-core/src/resolve/`, which GUI-2's spec
fences: two extensions were already argued there and a third edit for a
`Display` impl is the wrong trade inside one unit. The viewer-side types
are the same shape of work and belong with it rather than scattered.

## Comments

**2026-08-29** — comment:

(LIB orchestrator) The editor-core slice is discharged by #1161: `HitTestError` now has the LIB-DOORS-F6-shape `Display` (+ `Error`), with a contract test pinning content-not-struct-dump. The viewer half stays open and GUI-owned, including the now-false comment at `crates/viewer/src/pick.rs:676` explaining why `HitTestError` was debug-rendered.

The re-sweep this issue asked for, run by that unit (façade-carried error-shaped names in `crates/pncad/src/**` pub use, cross-checked against every `Display for` impl in `crates/`; 52 carried, 13 flagged, `PathError` a false positive):

Still lacking `Display`, editor-core: `NodePickError` (wraps `HitTestError` — belongs with the viewer pass), `DeclareError`, `InterrogateError`, `SelectRefusal`, `ResolveFault`, `ParseError`, `MigrationError`.
New to this issue's list, other crates: `ContactRefusal` (`topo/src/contact.rs:146`), `ReadbackError` (`topo/src/readback.rs:129`), `TessellateError` (`mesh/src/types.rs:84`), `FmtQuantityError` (`quantity/src/fmt.rs:43`).

Stated blind spots of the sweep: error types without an error-shaped suffix; types reachable only via wholesale module re-export (exactly the R2 shape); viewer-owned types (out of façade scope); and the cross-check is a text grep — sound here only because the workspace has no derive-based Display crates (verified across all Cargo.toml).

---
_Generated by [Claude Code](https://claude.ai/code)_

## Home

What remains open is viewer-owned (GUI ground, and that program is closed) plus a handful of kernel-crate types no single open program's territory covers, so it lands under `work/issues/`.

## Cut 1 of 3 — viewer (landed)

PR #1741, branch `fix/viewer-display`. The item stays **open**: it
closes when cut 3 lands.

**The re-sweep inverted the unit.** All eight viewer types this issue
names already have `Display` + `Error` on main, as does
`editor_core::NodePickError` (2026-09-01, `4067a3ef7`). The list above
came from a text grep for `Display for`; these are spelled
`impl core::fmt::Display for …`. No impl was owed on this fence — and
zero `viewer` public types in an `Err` position lack a `Display`
(22 have one). What was owed is the other half of "done": the debug
renderings at the consumer sites, and the four comments explaining why
they were there, every one of which stated a gap that had since closed.

Cuts 2 and 3 also appear discharged on main already: the editor-core
stragglers landed 2026-09-01, and `ContactRefusal`, `ReadbackError`,
`TessellateError` and `FmtQuantityError` on 2026-08-29 (`03368cfd8`).
`MigrationError` is the one name from the 2026-08-29 comment with no
`Display` found.

**Fixed — eight sites, all in `crates/viewer/src/`:** the two
`HitTestError` arms (`pick.rs` `PickError::HitTest`,
`EdgeNameFault::Unnamed`), `PickIndexError::Node` (`NodePickError`),
`SceneError::NotTessellated` (`mesh::TessellateError`),
`MateToolError::Frame` (`InterrogateError`), the
indeterminate-resolution status line (`ResolveIndeterminate`), and
`WebStartupError::Runner` (`eframe::Error`). The last three were not on
this issue's list. Pinned by five containment tests in
`crates/viewer/tests/error_display.rs`, each followed by the file's
`prose()` helper — whose `" { "` field-brace check is this repo's
existing spelling of content-not-struct-dump.

**Two instances of the class found and NOT taken**, both needing a
routing decision rather than a mechanical fix:

- `profile::path::Verb` — a macro-generated public fieldless enum with
  no `Display`, reaching a user surface through
  `viewer::sketch::PreviewError::Transition` (`sketch.rs:651`). A
  **sixth** crate, outside all three cuts.
- `viewer::frame::Disagreement` (`frame.rs:460`) renders
  `StableName.path` through `Debug`. Forwarding is not obviously right:
  `StableName`'s `Display` deliberately omits the role path, so two
  names differing only in path would render identically and the
  disagreement this message exists to report would vanish.

Adjacent, out of class: `Dimension` is rendered through `Debug` in four
UI labels (`app.rs:2832`, `app.rs:4339`, `app.rs:4362`,
`session.rs:750`) though it has a `Display`. Same defect shape, but not
an error type.

**Blind spots of this sweep.** The type census matches
`pub enum|struct|union` at a line start, so it misses macro-generated
public types — exactly how the `Verb` hit is declared — and `pub type`
aliases (`viewer` has none; its one `macro_rules!` emits trait impls).
The reachability test is a regex for an `Err` position, so it misses an
error carried outside a `Result` or returned through a `Result` alias.
The debug-rendering sweep matches `{x:?}`/`{:?}`/`{self:?}` in format
strings, so it misses a `Debug` rendering routed through a helper or a
direct `Debug::fmt` call. It is a text grep over source: a
`#[derive(Display)]` would be invisible, sound here only because no
workspace `Cargo.toml` pulls a derive-`Display` crate (re-verified).
