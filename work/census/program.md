---
id: census
kind: program
title: CENSUS — one vocabulary, spelled by hand in several places
status: open
opened: 2026-09-11
area: api
prefix: census/
tag: (CENSUS orchestrator)
ab_band: 3900-3999
paths: []
keep_out: [this program claims NO paths - its subject is a class that crosses every crate by construction, and each row draws its fence in the PR that lands it, crates/pncad-py/* is LIB's - the prose census instrument lives there and its two rows are announced to LIB, crates/editor-core/src/* files are DOCM's and WIRE's by their own tables - the prose-word row reaches expr.rs and node.rs and mate.rs and edit.rs and announces each, crates/viewer/src/* is CHROME's and VIEW's - the prose-word row's viewer sites are announced there, crates/geom-core/src/spline/* and crates/geom/src/* are PROPS' - the Debug census row is announced there, crates/topo/src/chord_join.rs is CURVED's and crates/profile/src/path/* is S-BOOL's - S133's two halves are announced separately, S57's anti-re-fork guard lives in scripts/gates/* which is GUARD's - the guard half is filed on GUARD and the call sites are landed here]
---

One class, seven rows: **a vocabulary spelled by hand in more than one
place, and the census or instrument that cannot see one of the
spellings.** The `deny_unknown_fields` attributes that are inert on unit
enums, nine `Debug` impls listing their fields by hand, a prose census
that a bypassed renderer is invisible to, four spellings of the prose
word for a kind with only `Display` censused, and the counts that drift
in prose because nothing re-derives them. Class M at its core with two H
rows. Charter and unit order: `work/census/plan.md`.
