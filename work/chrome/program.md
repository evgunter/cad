---
id: chrome
kind: program
title: CHROME — viewer chrome and coverage
status: open
opened: 2026-09-03
area: gui
prefix: chrome/
tag: (CHROME orchestrator)
ab_band: 1600-1699
paths: [crates/viewer/src/*, crates/viewer/tests/*, crates/viewer/README.md]
keep_out: [the VIEW carve-out of 2026-09-15 — VIEW's split LANDED and VIEW is live in this crate daily, so 'CHROME goes first' is spent and the two programs divide the files instead: CHROME works datums.rs, bounds.rs, combine.rs::denotes_body, tree.rs::blamed_mates and crates/viewer/tests/{valid_range, combine_ops, tree_badges}.rs, and CEDES scene.rs and gpu.rs (VIEW's scene-mesh-carries-an-identity-index-buffer rewrites both), theme.rs and pane/features.rs (VIEW's tone-is-a-value-in-frame-and-a-comment-in-two-panes), marks.rs and blend.rs (VIEW works these files; the edge-cost row is re-homed to VIEW for that reason, NOT because a VIEW row claims its subject — three VIEW rows cite it only as a stale citation needing a repoint), and app.rs, session.rs, session/*, pane/*, frame.rs, pickindex.rs, display.rs, props.rs, forms.rs, sketch.rs, crates/viewer/tests/* is also S-TCOST's and S-TINT's and Track W's by declaration — S-TCOST split on 2026-09-11 and the integrity half is S-TINT's, so test-mechanism changes are announced to both, crates/editor-core is NOT DOCM's any more — DOCM exited 2026-09-14 (docs/DOC-LEDGER.md sweep 14) and its ground divided: mate.rs and mate/* are MSOLVE's, doc.rs and edit.rs and node.rs and resolve/* and REFERENCES.md are EDIT's, and crates/pncad-py is LIB's — a CHROME row reaching any of them is a hand-off, not a fence to work around, editor-core assembly.rs vocabulary is read and not edited — a badge fix that must thread provenance through the solve becomes a CURVED rider, the narrow mate.rs amendment of PR 1748 (lifting solve.rs's private head_of into a public member_of and its re-exports, no mate semantics) is spent and now reads as MSOLVE's to grant, the persist schema is contended ground — the one persisted field of doc-params is announced on the away channel]
priority: P1
---

The viewer chrome defects and coverage rows on the GUI/GAUTH residue
in `crates/viewer`. The nine units of the opening slate all landed on
2026-09-04; what this program holds now is the residue they filed plus
hand-offs from DOCM, re-read against the tree on 2026-09-15.

**Two things the opening charter said are no longer true, and the
correction matters more than the sentences did.** *"Every item is E"*
described the opening slate, not this one: of the rows audited on
2026-09-15, most are HARDER — they carry an undecided design fork, not
a written fix. And *"lands before VIEW's module split ratifies"* is
spent: the split landed 2026-09-04 and VIEW has been live in this crate
every day since, so the two programs now divide the files rather than
take turns. The division is in `keep_out` and in `work/chrome/plan.md`.

Review posture is unchanged and reaffirmed by Ev in-chat on 2026-09-15:
**style reviews, no A/B row, no protocol run**, with a correctness arm
added only where a unit's failure mode is a confident wrong answer
rather than a refusal. `docs/MODEL-AB-LOG.md`'s roster line states this
narrowly — *"infra-only and test-only units in CIW and CHROME record no
row"* — which understates it; the program-level posture has been no
duals since its opening plan, and the band 1600-1699 stays claimed and
empty.

**Cut on 2026-09-20** (Ev, in chat), when this slate measured 65
budget points against a 30-point ceiling: the ten rows naming a door
the viewer does not have went to AUTHOR, where they are the GUI half
of the standing goal, and six prose rows went to VDOC. What CHROME
keeps is what its charter has always described best — the viewer's
reported defects and the architecture that entrenches behind them.

Charter and unit order: `work/chrome/plan.md`; narrative in
`work/chrome/log.md`.

`paths` covers `crates/viewer/README.md` by a narrow amendment
(refused-mate-badges-every-instance-row, PR 1769): CLAUDE.md makes the
README beside the code the home for finished-work design, and that
unit made two of its clauses false — G4's "the failing feature marked
in the tree" and GQ2's "a failure poisons only its descendants" no
longer describe what the tree's POISONED badge means. The amendment
covers documenting the badge attribution and nothing else.
