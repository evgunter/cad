---
id: chrome
kind: program
title: CHROME — viewer chrome and coverage
status: open
opened: 2026-09-03
area: gui
prefix: chrome/
tag: (CHROME orchestrator)
ab_band: 1600-1699
paths: [crates/viewer/src/*, crates/viewer/tests/*]
keep_out: [VIEW edits the same files after this program's slate lands — CHROME goes first, crates/viewer/tests/* is also S-TCOST's and Track W's by declaration — announce test-mechanism changes, editor-core assembly.rs vocabulary is read and not edited — a badge fix that must thread provenance through the solve becomes a CURVED rider, editor-core mate.rs was read-only under this clause too until viewer-mate-tool-refuses-pattern-picks amended it (PR 1748) — the viewer's pick gate was a RESTATEMENT of A11's member rule, so honouring the fence would have meant keeping the defect rather than removing it; the amendment is narrow, covering only lifting solve.rs's private head_of into a public member_of and its re-exports, and no mate semantics are edited, the persist schema is contended ground — the one persisted field of doc-params is announced on the away channel]
---

The small viewer fixes and the coverage rows CI cannot see, on the
GUI/GAUTH residue in `crates/viewer` that no program has owned since
2026-08-31. Every item is E; review posture is the batched style
review with no A/B row. Lands before VIEW's module split ratifies.
Charter and unit order: `work/chrome/plan.md`; narrative in
`work/chrome/log.md`.
