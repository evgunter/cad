---
id: port
kind: program
title: PORT — the crate-boundary doors: Python, exchange and the façade refusals
status: open
opened: 2026-09-11
area: api
prefix: port/
tag: (PORT orchestrator)
ab_band: 4600-4699
paths: []
keep_out: [this program claims NO paths - crates/pncad-py/* and crates/pncad/* are LIB's territory and crates/step-import/* and step-export/* and stl/* are EXCH's, so every unit here is announced to the owner and LIB or EXCH may simply take a row instead, crates/editor-core/src/assembly.rs is DOCM's - the two assembly refusal rows are announced there and the kernel half of the attribution class is CURVED's census.rs, Cargo.toml's rust-version and rust-toolchain.toml and the CI rows that would compile the floor are CIW's and META's between them - the MSRV row is a question about what the repository promises before it is an edit, S107 is a ruling and no lane resolves it by implementing a rename, crates/editor-core/src/node.rs is DOCM's - D341's census reads its variants and does not edit them]
---

The doors at the crate boundaries, where the thing a user meets is a
refusal or a missing setter: an assembly door that raises only the head
of each refusal list, a display contract that answers a tie before kind
while the operand answers the reverse, three residues across the STEP
and STL crates, the Python surface that cannot set an options struct and
whose bindings census has no mold for `Node`, the unruled question of
what a Python error is called, and an MSRV floor the repository declares
and never compiles. Six of seven rows are `M`. Charter and unit order:
`work/port/plan.md`.
