---
id: port
kind: program
title: PORT — the crate-boundary doors: Python, exchange and the façade refusals
status: active
opened: 2026-09-11
area: api
prefix: port/
tag: (PORT orchestrator)
ab_band: 4600-4699
paths: []
keep_out: [this program claims NO paths - crates/pncad-py/* and crates/pncad/* are LIB's territory and crates/step-import/* and step-export/* and stl/* are EXCH's, so every unit here is announced to the owner and LIB or EXCH may simply take a row instead, crates/editor-core/src/assembly.rs is EDIT's since 2026-09-13 (DOCM's until it closed at sweep 14) - the two assembly refusal rows are announced there and EDIT's own keep_out names this program back and the kernel half of the attribution class is CURVED's census.rs, Cargo.toml's rust-version and rust-toolchain.toml are unowned by any program's globs and the CI rows that would compile the floor split two ways - scripts/gates/* is GUARD's and .github/workflows/ci.yml is CIW's (corrected 2026-09-15 from an earlier claim that both were CIW's and META's, which work.py territory does not bear out) - the MSRV row is a question about what the repository promises before it is an edit, S107 closed 2026-09-15 ruled a defect and its successor row is on crates/pncad-py/ which is LIB's, crates/editor-core/src/node.rs is EDIT's - D341's census reads its variants and does not edit them, crates/editor-core/src/persist/wire.rs is EDIT's - the load-path row's defect site is its Error::custom and EDIT's keep_out does not yet name that path so the record there is one-sided]
priority: P3
---

The doors at the crate boundaries, where the thing a user meets is a
refusal or a missing setter: an assembly door that raises only the head
of each refusal list, a display contract that answers a tie before kind
while the operand answers the reverse, three residues across the STEP
and STL crates, the Python surface that cannot set an options struct and
whose bindings census has no mold for `Node`, the unruled question of
what a Python error is called, and an MSRV floor the repository declares
and never compiles. Six of seven rows are `M`. Charter and unit order:
`work/port/plan.md`.
