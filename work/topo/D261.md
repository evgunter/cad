---
id: D261
kind: unit
title: Collapse topo/src's two remaining hand-rolled readers and two private blankers onto test_utils::source
status: dispatched
opened: 2026-08-21
track: P
refs: [D264, D287, D205]
branch: topo/d261-reader-collapse
---

## What

**`topo/src`'s two remaining hand-rolled readers and the two private blankers, on this track's newly-drawn ground.** `review_d18.rs` reads raw text plus a `\n    }\n` body carve (`code_only` plus a brace match); `review_d18_probes.rs` reads message text inside `unreachable!(…)` with line-leading `//` awareness only (`code_and_literals`) and is **`D80`'s last member**. With them, `fixtures.rs::code_only` and `source_walk.rs::CodeOnly` are both now strict subsets of the shared home — the raw-string prerequisite having been paid — so each collapse is a **deletion**, not a port; `public_fns` and `mutation_doors` stay in `topo`. the source-text guard class's shared home is `crates/test-utils/src/source.rs` (one lexer, three views: `code_only`, `code_and_literals`, `comments_only`), and the census that keeps the population honest is `crates/test-utils/tests/reader_census.rs`, whose `Unconverted` ceiling this row lowers by its own member count.

(`D61` and `D80` are landed rows cited as provenance only. In the source table this row sits after the `S94` prose paragraph, below `D265`.)

**Sequencing note (the fourth seam in `plan.md`):** `crates/test-utils/` is Track W's fence and `tcost`'s territory, and `D261` (P) and `D287` (Q) both lower the same hand-synced `UNCONVERTED_TODAY`. This lane lands its own files and its own census ENTRIES; the ceiling is **re-derived from the table at the landing**, not lowered by this row's member count — that arithmetic is right only for whichever of the two lands first.

## Was

`D61` residue.

## Finding

### S117. Every guard that reads Rust source rolls its own reader, and the population is not a list

Raised by **#788** (S92) while closing that finding's two members. Every guard below reads `.rs` source and asserts a count or a presence over raw text, with no comment awareness or with line-leading-`//` awareness only. **The direction that matters is the silent one**: a real site commented out leaves its text in the file, so the count does not move and the guard stays green over exactly the change it exists to catch.

**Twelve is a floor** — it went 7 → 9 → 11 → 12 in a single session, then moved a fourth time when lane **G-g landed a fifth reader**, a second `code_only` in `topo/src/fixtures.rs` serving `face_normal`'s re-fork guard, its own docs conceding it does not model raw strings (the `br"x\"` hole #788 had just fixed in `source_walk::CodeOnly`), re-forked because that one is `pub(crate)` and G-g could not reach it. **Two readers of different competence, one crate, one merge.**

**The `topo/src` members, sorted by what each greps for.** Served by `CodeOnly` as it shipped: `topo/src/review_d18.rs` — `body.matches("unreachable!").count() == 2` over `link_half_edges`, and `!body.contains("if let Some")`; commenting both `unreachable!`s out keeps the count at 2. Need a comments-only/literals-kept variant: `topo/src/review_d18_probes.rs` — reads the message text inside `unreachable!(…)`, and skips line-leading `//` only. (The other `topo/src` members — `face_normal.rs`, `sector_shape.rs`, `chord_join.rs` — are Track Q's `D287`.)

**This row closed on the helper shapes plus a mechanism, not on the list.** `test_utils::source` is the home: one lexer, three views (`code_only`, `code_and_literals`, `comments_only`) over a public `keeping(text, &[Region])`, plus `balanced_end`, `top_level_split`, `crate_dir`, `suite_files`.

**Three corrections (from #872's update), because they change what the collapse means:**

1. **The full-blanker population is FOUR, not three.** `topo` has **two** live blankers — `source_walk.rs`'s `CodeOnly` and `fixtures.rs`'s `code_only` — plus `pncad`'s `code_without_comments`, plus the shared one. #872 added the fourth full blanker; it did not reduce the count.
2. **The port is of the WEAKER of `topo`'s two.** `source_walk::CodeOnly` models raw strings via `raw_string_open`; `fixtures::code_only` does not, and that is what was ported. **Porting `raw_string_open` into `test_utils::source` is a prerequisite of the collapse, not an optional extra.** (The row above records this prerequisite as paid.)
3. **`topo::fixtures::code_only`'s collapse is UNOWNED** — saying it was *"G8/G9's to sequence"* was a §C3 deferral: G8 landed as #834, and G9's scope is `topo/src/boolean/{ops,reduce}.rs` plus `chord_join.rs`, which contains neither `fixtures.rs` nor `face_normal.rs`. This row is now its owner.

The port's faithfulness: 500,000 adversarial inputs across `/ * " ' \ # r`, `//!`, `///`, `'a`, `'é'`, `b"x"`, `r"x"`, `\"`, newlines and multibyte, **zero mismatches** against `fixtures::code_only`; the only body difference is `i + 1; k += 1` → `i + 2`.

**What is still open**: the six `topo/src` conversions (Track Q's `D287`, Track P's `D261`), `pncad/tests/all.rs` under Track E's **#763**, and the deletion of `topo`'s two private blankers — both now strict subsets of the shared home, so each collapse is a **deletion rather than a port**. The population was never twelve: the census at `crates/test-utils/tests/reader_census.rs` puts it at **34**.

**Verdict:** (blank — unruled.)

*Omitted (by sub-heading): the members outside `topo/src` in the twelve-member sorted list, "Why #788 did not sweep them", and the #872 ruling `I-R8` paragraph; the statement is carried in full at `D205`/`D208`.*

## Fence

Track P — `topo`'s Euler surgery, liveness and the generator. **Fence:** `crates/topo/src/{euler.rs,euler_ring.rs,euler_kill.rs,split.rs,attach.rs,movefac.rs,revert.rs,live.rs,merge_faces.rs,seqgen.rs,validate.rs}`, plus `crates/topo/src/{review_d18.rs,review_d18_probes.rs,fixtures.rs,source_walk.rs}` (added 2026-08-29, because they belonged to no track). **Block:** `D260`–`D279` / `S330`–`S349`.

## Claimed by TOPO (2026-09-04)

Moved from `work/code-quality/` to `work/topo/` when TOPO opened and
claimed **Track P whole**, per `work/code-quality/program.md` ("a row
leaves the moment a program claims it, by moving into that program's
directory keeping its id, `track:` letter and body") and the 2026-09
cut's rule that a program taking a code-quality letter takes all of it
so the schedule stays single-owner. Id, `track: P` and body unchanged.

Track P had **never had a lane** — no `smell/p-*` branch has ever
existed, while K, X and T lanes have run — and its fence
(`euler*.rs`, `validate.rs`, `live.rs`, `seqgen.rs`, `merge_faces.rs`
and the review-and-fixture readers) is exactly the `crates/topo/src`
territory that was in no program's `paths`. The three sub-lanes
`work/code-quality/plan.md` defined for P are inherited unchanged and
are restated in `work/topo/plan.md`.

## Brief (TOPO, 2026-09-05)

Dispatched as the reader sub-lane's unit, with `D264` riding
(`rides_with: D261`). Branch `topo/d261-reader-collapse`. Runs outside
the A/B experiment (single style review; no row). Survey re-derived on
main `18ea0915`:

- `crates/test-utils/src/source.rs` is the shared home: `code_only`
  (:135), `code_and_literals` (:152), `comments_only` (:167), `keeping`
  (:120), `balanced_end` (:376), `rust_sources` (:338). `topo` already
  names `test-utils` under `[dev-dependencies]`
  (`crates/topo/Cargo.toml:58`), so a `#[cfg(test)]` module in `src/`
  can reach it.
- **Four conversions, all deletions or re-points, no port:**
  1. `crates/topo/src/review_d18.rs:379-397` — raw
     `read_to_string` plus a `"\n    }\n"` body carve; becomes the
     shared `code_only` view and `balanced_end` from the fn's opening
     brace. The two assertions (two `unreachable!`, no `if let Some`)
     keep their meaning; commenting a call out must now MOVE the count,
     which is the whole point — add the row that shows it does.
  2. `crates/topo/src/review_d18_probes.rs:275-290` — line-leading `//`
     awareness only, reading the message text inside `unreachable!(…)`;
     becomes `code_and_literals` (the message is a literal and must
     survive the view). `D80`'s last member.
  3. `crates/topo/src/fixtures.rs:1012` `code_only` — DELETE; its
     `#[cfg(test)]` rows at `:1115-1137` go with it or move to the
     shared home's suite if they pin something that suite lacks (say
     which). Its one consumer outside `fixtures.rs` is
     `crates/topo/src/face_normal.rs:460` (`crate::fixtures::code_only`):
     re-point that ONE call to `test_utils::source::code_only` and
     nothing else in that file — `face_normal.rs` is code-quality Track
     Q's ground (`work/bool/D287`), announced on S-BOOL's board, and its
     raw-text reads stay D287's.
  4. `crates/topo/src/source_walk.rs:143` `CodeOnly` — DELETE the
     blanker; `public_fns`, `mutation_doors`, `body_start`, `gave_up`
     and the rest of the walk stay in `topo` and read the shared
     `code_only` view instead. The rustc-differential proof paragraph
     (`:120-140`) describes THIS blanker; when the blanker goes, the
     paragraph either moves to the shared home (if it is not already
     proven there — check `test_utils::source`'s own docs/tests before
     duplicating) or goes; it does not stay describing code that is no
     longer there.
- **The census** `crates/test-utils/tests/reader_census.rs` (S-TCOST's
  file, announced on its board): delete the four `Unconverted` entries
  (`:224-241`, plus `face_normal.rs`'s entry text at `:220` drops the
  "plus topo's private `fixtures::code_only`" clause and stays
  `Unconverted` on its raw text). `UNCONVERTED_TODAY` (`:628`) is
  **re-derived from the table as it stands at your landing** — count
  the `Unconverted` entries after your edits on the merged tree, do not
  subtract four — because `D287` lowers the same constant and only the
  first to land may do arithmetic. Say in the PR body which count you
  landed and why.
- **`D264`**: `source_walk.rs:116`, `:120`, `:391` carry
  ``[`self::tests`]`` intra-doc links that resolve only because the
  module is `#[cfg(test)]`. Reword or re-target so `scripts/doc-gate.sh`
  would pass if the module were ever documented; run the doc gate
  locally on `topo` and show it clean. `work/tcost/D113` is deciding the
  general policy for `tests/` files — this is `src/`, and the fix here
  is the sentence, not a mechanism.

Acceptance: the four census entries gone and the guard rows in
`reader_census.rs` green on the merged tree; every converted guard
still red on the change it exists to catch (one planted mutant per
guard, described in the PR body with its output); `cargo test -p topo`
touched modules and `cargo test -p test-utils` green locally; hosted CI
green on the full matrix; sweep receipt: `rg -n 'read_to_string' crates/topo/src`
listed with each hit's disposition.
