---
id: D260
kind: unit
title: live.rs's module header names schedule row D50 by name — reword it to name the guard's absence, not the row
status: closed
opened: 2026-08-21
track: P
refs: [D50, D66]
rides_with: D50
pr: 1949
closed: 2026-09-05
---

## What

`live.rs`'s module header points at a row of this schedule **by name** — *"A source-level guard can, and is placed as `SMELL-SCAN-2026-08.md`'s **D50**; until it lands the claim rests on review of this file."* Correct today, and a dangling pointer the moment `D50` lands, because a landed row is deleted from this file rather than annotated. `D66`'s class — a cross-reference is a claim site — with the target inside this register instead of another crate. The sentence wants to name the guard's absence, not the row that would supply it. **Cheap, and cheapest taken with `D50` itself.**

(Now that the schedule has moved to `work/code-quality/`, the header's pointer to `SMELL-SCAN-2026-08.md` is already stale in its path as well as, eventually, its target.)

## Was

unrowed.

## Finding

No live finding section; provenance only. The row cites no `S<n>`; it was minted directly into the table.

## Fence

Track P — `topo`'s Euler surgery, liveness and the generator. **Fence:** `crates/topo/src/{euler.rs,euler_ring.rs,euler_kill.rs,split.rs,attach.rs,movefac.rs,revert.rs,live.rs,merge_faces.rs,seqgen.rs,validate.rs}`, plus `crates/topo/src/{review_d18.rs,review_d18_probes.rs,fixtures.rs,source_walk.rs}` (added 2026-08-29, because they belonged to no track). **Block:** `D260`–`D279` / `S330`–`S349`.

## Claimed by TOPO (2026-09-04)

Moved from `work/code-quality/` to `work/topo/` when TOPO opened and
claimed **Track P whole**, per `work/code-quality/program.md` ("a row
leaves the moment a program claims it, by moving into that program's
directory keeping its id, `track:` letter and body") and the 2026-09
cut's rule that a program taking a code-quality letter takes all of it
so the schedule stays single-owner. Id, `track: P` and body unchanged.

Track P had **never had a lane** — no `smell/p-*` branch has ever
existed, while K, X and T lanes have run — and its fence
(`euler*.rs`, `validate.rs`, `live.rs`, `seqgen.rs`, `merge_faces.rs`
and the review-and-fixture readers) is exactly the `crates/topo/src`
territory that was in no program's `paths`. The three sub-lanes
`work/code-quality/plan.md` defined for P are inherited unchanged and
are restated in `work/topo/plan.md`.

## Closed

PR #1949, riding `D50`. `live.rs`'s "Guarding" section is now one
sentence naming the guard by its test name and what it checks — the
field and `Live::new` carrying no visibility, every door reaching a
lookup before it builds, the doors and construction sites being the
ones the header names, and no other file in `topo/src` building a
`Live` at all. No history, no row ids, and no pointer to a schedule
file: the sentence names the test, which is in the same module and reds
if the claim stops holding.
