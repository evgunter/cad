---
id: D263
kind: unit
title: group_regime asks "is it a plane" and the mvfs Nurbs placeholder answers "curved" by default — latent defect
status: dispatched
opened: 2026-08-21
track: P
branch: topo/d263-placeholder-regime
pr: 2548
---

## What

**The regime test asks *"is it a plane"* and a placeholder answers *"is it curved"* by default.** `group_regime` classifies with `matches!(s, Surface::Plane { .. })`, and a face carrying the `mvfs` `Nurbs` placeholder — which `ops_cube`'s faces do — is therefore classed curved and takes the recording regime. Pre-existing and outside `D38`'s rows; surfaced only because the kind-split probe added by that unit failed against it. The same *"a default answers a question nobody asked"* shape the two rows above closed, one level out: the predicate's negative arm carries two meanings and the door cannot tell them apart. **Measured, and currently unreachable**: over 3,033 door decisions the corpus shows zero `Nurbs`, zero mixed kind sets and every group kind-uniform, and no in-tree path can mint a false split because the only kernel stamper gives every surface its own `idx`. So this is a latent classification defect, not a live one — the raw `mvfs` body silently takes the *softer* regime.

Kind note: `issue`, because the row records a latent defect measured unreachable and names no fix. (`D38` is a landed row cited as provenance only.)

## Was

`D38` residue.

## Finding

No live finding section; provenance only. The row cites no `S<n>`; it is a residue of the landed `D38`.

## Fence

Track P — `topo`'s Euler surgery, liveness and the generator. **Fence:** `crates/topo/src/{euler.rs,euler_ring.rs,euler_kill.rs,split.rs,attach.rs,movefac.rs,revert.rs,live.rs,merge_faces.rs,seqgen.rs,validate.rs}`, plus `crates/topo/src/{review_d18.rs,review_d18_probes.rs,fixtures.rs,source_walk.rs}` (added 2026-08-29, because they belonged to no track). **Block:** `D260`–`D279` / `S330`–`S349`.

## Claimed by TOPO (2026-09-04)

Moved from `work/code-quality/` to `work/topo/` when TOPO opened and
claimed **Track P whole**, per `work/code-quality/program.md` ("a row
leaves the moment a program claims it, by moving into that program's
directory keeping its id, `track:` letter and body") and the 2026-09
cut's rule that a program taking a code-quality letter takes all of it
so the schedule stays single-owner. Id, `track: P` and body unchanged.

Track P had **never had a lane** — no `smell/p-*` branch has ever
existed, while K, X and T lanes have run — and its fence
(`euler*.rs`, `validate.rs`, `live.rs`, `seqgen.rs`, `merge_faces.rs`
and the review-and-fixture readers) is exactly the `crates/topo/src`
territory that was in no program's `paths`. The three sub-lanes
`work/code-quality/plan.md` defined for P are inherited unchanged and
are restated in `work/topo/plan.md`.

## Sharper witness (from D265's lane, 2026-09-06)

`ops_cube`'s six faces share the `mvfs` `nurbs_placeholder` surface
key and the structural rung is kind-agnostic
(`crates/topo/src/merge_faces.rs:1164-1166` at PR 2013's head), so
`merge_coplanar_faces` groups the WHOLE cube as one curved group and
returns `Ok` with a `PeriodClosure` skip over all six faces of a body
that has no coplanar adjacency at all. D265's control row
(`the_same_group_records_its_own_refusal_untorn`) is the executed
witness. Reported by the D265 lane; recorded here by the orchestrator.

## Brief (TOPO, 2026-09-14) — block TOPO-B3 slot 1, dual at review

**The answer to give.** `merge_coplanar_faces` never groups a face
whose surface is a placeholder (`NurbsSurface::net_state() ==
NetState::Placeholder`, the `mvfs` seed's surface) with anything —
not as planar, not as curved. A placeholder describes no locus, so it
is neither coplanar with a neighbour nor a curved chart to record a
skip over; `face_is_planar`'s two-way answer becomes three-way
(plane / curved / placeholder), `group_regime` and the structural
grouping rung read it, and the door's record says how many faces it
set aside as placeholders. Whether a group that would otherwise mix a
placeholder with described faces is a typed refusal
(`GroupKindSplit`'s shape, a third kind named) or a silent set-aside
is phase 1's call — decide from what the door's callers do with the
record and say why; a caller that today gets `Ok` with a
`PeriodClosure` skip on the placeholder cube must not silently get
`Ok` with nothing said.

**Red-first rows.** D265's control row
(`the_same_group_records_its_own_refusal_untorn`) is the executed
witness: on the merge base the whole `ops_cube` groups as one curved
group and the door returns `Ok` with a `PeriodClosure` skip over six
placeholder faces; at the head the cube forms no group and the record
names six placeholder faces. A mixed body (a cube with one face
re-described as a real plane beside its placeholder neighbours; a
`coplanar_pillow` with a placeholder cap) pins the mixed case each
way. The whole D38/D265 suite stays green; where a row asserted the
old grouping of a placeholder body, re-baseline it and say what moved.

**Receipt.** Every reader of `face_is_planar` and every `matches!(…,
Surface::Plane { .. })` in `merge_faces.rs` — which ones also need the
third arm; the `PeriodClosure` skip's callers (does any consumer read
"a skip over the cube" as information?). `S330`'s `NetState` is the
door for the kind; do not re-derive it from the net.

Branch `topo/d263-placeholder-regime`. PR title: "TOPO: the merge door
sets placeholder faces aside instead of grouping them as curved". Do
not close the item; the dual runs at review.

## Correction (2026-09-14, the fix pass on PR 2548)

`## Sharper witness` and `## Brief` above cite a control row
`the_same_group_records_its_own_refusal_untorn` as D265's executed
witness. No row by that name exists anywhere in the tree (`grep -rn`
over `crates/` at PR 2548's merge base and at its head). The executed
witness on the merge base was `the_door_records_same_face_as_a_skip`
in `crates/topo/src/merge_faces.rs`: its arena-first membrane cube
reached the recording regime only because its placeholder key read as
curved. The text above is kept as written; the red-first row PR 2548
lands is `the_placeholder_cube_forms_no_group_and_its_faces_are_named`,
with its merge-base output in the PR body.
