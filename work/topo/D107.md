---
id: D107
kind: unit
title: review_d18's kemr reaches no mutation phase in either hammer row — needs a ring-merge fixture past its plan phase
status: open
opened: 2026-08-21
track: P
---

## What

`review_d18`'s `kemr` reaches no mutation phase in either hammer row (S161), so the arms below it are attacked by nothing. Scoped work: a fixture whose ring-merge form gets it past its plan phase. **Placed on Track W until 2026-08-29 and moved here with its ground** — `review_d18` is `src/`, not `tests/`, and belonged to no track at all; see this track's fence line.

## Was

Track F.

## Finding

### S161. Two of `review_d18`'s seven hammered operators never reach the arms under attack

Found by lane F-d while replacing S76's floor, and by F4's adversarial review one step further.

`crates/topo/src/review_d18.rs` says its sweep *"drives every operator that reaches `link_half_edges` over every key"*, at the module doc and again on `hammer`. The per-operator exposure the S76 fix added measures that sentence for the first time, and it is **true of the calls and false of the arms**:

- **`kemr` enters a mutation phase in NEITHER row** — 0 of 96 calls on the spent-graft destination, 0 of ~1900 across the torn sweep. Its plan phase refuses on every input either row produces, so every `link_half_edges` site below it is attacked by nothing here.
- **`mfkrh_plug` cannot reach them at all.** `mfkrh` (`euler_kill.rs:1037-1118`) mints a face surface, adds a face and touches `face`, `loop` and `shell` records; it calls `link_half_edges` nowhere. It is in the sweep because it is an euler operator, not because it can reach a row-4 arm.

**Two different defects wearing one sentence.** `mfkrh_plug` is a *classification* error — the file counts an operator that is not in the class its prose names, and the S76 fix therefore excludes it from the floor (`LINK_OPS`) while still driving and printing it. `kemr` is a *coverage* gap: it is in the class, and nothing this file does gets it past its plan phase. Only the first is closed by #825.

**What D107 owes.** Either a fixture on which `kemr`'s plan phase succeeds — its preconditions are a ring-merge shape neither `ops_cube()` nor a torn cube presents — or the written finding that `kemr`'s row-4 arms have no input witness from this door, which is a claim of the same kind the D2 addendum's row 1 already makes elsewhere and would be worth as much. What is not admissible is the current state, where the prose asserts the coverage and the exposure line disproves it on every run.

The exposure prints `kemr: 0` before the floors, so this is derivable from any `--nocapture` run of the row rather than only by reading `euler_ring.rs`. It is **not** on the CI board: libtest captures a passing test's stdout and the release-profile job passes no `--nocapture`. That job is the `release-corruption` job, `name: corrupt input (release profile)`, and it lives in `.github/workflows/nightly.yml`; `.github/workflows/ci.yml` carries only the tombstone comment beginning *"THE `corrupt input (release profile)` JOB STOOD HERE, AND HAS MOVED TO .github/workflows/nightly.yml"*. That is a `nightly.yml` question and `ci.yml` is Track F's F8 and Track G's G-a, so it is named here rather than changed.

(No verdict line on this finding.)

## Fence

Track P — `topo`'s Euler surgery, liveness and the generator. **Fence:** `crates/topo/src/{euler.rs,euler_ring.rs,euler_kill.rs,split.rs,attach.rs,movefac.rs,revert.rs,live.rs,merge_faces.rs,seqgen.rs,validate.rs}`, plus `crates/topo/src/{review_d18.rs,review_d18_probes.rs,fixtures.rs,source_walk.rs}` (added 2026-08-29, because they belonged to no track). **Block:** `D260`–`D279` / `S330`–`S349`.

## Claimed by TOPO (2026-09-04)

Moved from `work/code-quality/` to `work/topo/` when TOPO opened and
claimed **Track P whole**, per `work/code-quality/program.md` ("a row
leaves the moment a program claims it, by moving into that program's
directory keeping its id, `track:` letter and body") and the 2026-09
cut's rule that a program taking a code-quality letter takes all of it
so the schedule stays single-owner. Id, `track: P` and body unchanged.

Track P had **never had a lane** — no `smell/p-*` branch has ever
existed, while K, X and T lanes have run — and its fence
(`euler*.rs`, `validate.rs`, `live.rs`, `seqgen.rs`, `merge_faces.rs`
and the review-and-fixture readers) is exactly the `crates/topo/src`
territory that was in no program's `paths`. The three sub-lanes
`work/code-quality/plan.md` defined for P are inherited unchanged and
are restated in `work/topo/plan.md`.

## The release-profile job's file reference re-derived against the tree, 2026-09-11

The workflow file this row's `## Finding` names was resolved against the
repository as it stood on 2026-09-11 with `grep -n` and `sed -n`: the job
had moved out of `.github/workflows/ci.yml` and into
`.github/workflows/nightly.yml`, and the body's pointer was corrected to
say so. The table below is that derivation, with the command that
reproduces each row; the commands were re-run in a second pass the same
day, after a review of the first.

The repair was authorised by Ev and carried out in place rather than
routed back; it came out of the CITE row
`d107-release-profile-job-lives-in-nightly`, which is a secondary
reference only — that row's file is deleted when CITE closes, and
nothing in this section depends on it.

**Only the pointer is touched.** The finding, its scope sentence and its
disposition are TOPO's and are unchanged; where the new pointer bears on
one of them, that is written below as a note for TOPO and not acted on.

What moved and how it was proved, at this base:

| the row said | at this base | proved by |
| --- | --- | --- |
| the release-profile job is a `ci.yml` question | the `release-corruption` job, `name: corrupt input (release profile)`, is in `.github/workflows/nightly.yml` | `grep -n "corrupt input" .github/workflows/*.yml` |
| — | `.github/workflows/ci.yml` holds the tombstone comment *"THE `corrupt input (release profile)` JOB STOOD HERE, AND HAS MOVED TO .github/workflows/nightly.yml"* | the same grep; `grep -n -A2 "corrupt input (release profile)\` JOB STOOD HERE" .github/workflows/ci.yml` (one hit, `:3370`; `ci.yml` carries three tombstones of this shape and this is the one that names this job) |
| the job passes no `--nocapture` | still true: the job's `cargo test --release -p topo --lib` step passes none, and the only `--nocapture` in that workflow is the `editor-core` latency row | `grep -n "nocapture" .github/workflows/nightly.yml` |

Both are cited by NAME above — the job by its `name:` and the tombstone
by its opening words — because the line numbers rot: the intermediate
record in `work/cite/d107-release-profile-job-lives-in-nightly` gave
`nightly.yml:492` and `ci.yml:2443` on 2026-09-04 and **neither number
resolves now**. That is this repair's own subject happening to the
repair, and it is the argument in `work/cite/S176.md` for cite-by-name.

### A note for TOPO: the routing in that sentence no longer follows

CITE does not touch it, because the routing is part of what the row
decided. But the sentence routed the question to Track F's F8 and Track
G's G-a **on the ground that it was a `ci.yml` question**, and it is not
one: the job is `nightly.yml`'s, and all `ci.yml` holds is the tombstone.
F8 and G-a are about `ci.yml`, so the premise that sent the question to
them has gone even though the disposition it supported ("named here
rather than changed") may well still be right for other reasons. Which
board — if any — this belongs on is TOPO's to say.

### A second note for TOPO: the cost argument in that sentence has weakened

CITE does not touch it, because it is part of what the row decided and
not part of what it points at. But the sentence's reason for naming
rather than changing was per-PR log volume, and the premise has moved:

- the row **runs once a night**, not on every code-tier run, so the log
  volume it would add is one log a night that nobody is blocked on and
  that is read by whoever is asking this question;
- the repository went public on 2026-09-03, so the billed-minute half of
  the CI cost arguments in `docs/CI-MINUTES-2026-08.md` is gone too.

**Whether to add `--nocapture` is not decided here and no workflow file
is touched by this repair.** `nightly.yml`'s rows are CIW's, and CIW has
already recorded that the one-line change is theirs to land on request;
whether D107's `kemr: 0` exposure is worth a board at all is TOPO's.
What this section does is stop a reader inheriting a routing that no
longer resolves and a cost argument whose premise expired.
