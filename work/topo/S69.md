---
id: S69
kind: unit
title: kfmrh's shell-fusion form is outside the fuzz catalog, and the Ledger counts solids so it cannot notice
status: closed
opened: 2026-08-21
track: P
branch: topo/s69-kfmrh-fusion-form
pr: 2014
closed: 2026-09-06
---

## What

`kfmrh`'s shell-fusion form is outside the fuzz catalog, and the `Ledger` counts solids, so it cannot notice.

Kind note: `issue`, because the row is the finding's title and shapes no fix; the finding below names three defects (the ungenerated branch, the solids-not-shells ledger, and the path-following postcondition).

## Was

unrowed.

## Finding

### S69. `kfmrh`'s shell-fusion form is outside the fuzz catalog, and the `Ledger` counts solids, so it cannot notice

S15's finding was that `split_edge` shared `mev`'s Euler vector while sitting outside the fuzz catalog; the fix added `SplitEdge`. Meanwhile `kfmrh` gained a cross-shell **shell fusion** form (`crates/topo/src/euler_ring.rs:809` — re-homes faces, kills a shell, `ArenaDelta { shells: -1 }`), and both `kfmrh_candidates` (`seqgen.rs:502`) and teardown's re-make search (`:1103`) filter on `face1.shell == face2.shell`, so the fusion branch is **never generated**.

The `Ledger` cannot notice either: its `s` is the *solids* count (`seqgen.rs:283`), not shells, so a shell-count error passes property (b) silently.

Related, same operator: `euler_ring.rs:846-862` hands `assert_euler_postcondition` an `ArenaDelta` computed as `if killed_shell.is_some() { … shells: -1 … } else { … }`, where `killed_shell` is produced by the same `cross_shell` branch that did the mutation. The postcondition therefore follows the code down whichever path it took and can no longer detect the fusion branch running when it should not have. Every other operator hands it a constant, and `euler.rs:869` describes `ArenaDelta` as *"One operator's signed shift"*, named at the site *"so a site reads as the op's actual shift"*.

**Verdict:** (blank — unruled.)

## Fence

Track P — `topo`'s Euler surgery, liveness and the generator. **Fence:** `crates/topo/src/{euler.rs,euler_ring.rs,euler_kill.rs,split.rs,attach.rs,movefac.rs,revert.rs,live.rs,merge_faces.rs,seqgen.rs,validate.rs}`, plus `crates/topo/src/{review_d18.rs,review_d18_probes.rs,fixtures.rs,source_walk.rs}` (added 2026-08-29, because they belonged to no track). **Block:** `D260`–`D279` / `S330`–`S349`.

## Claimed by TOPO (2026-09-04)

Moved from `work/code-quality/` to `work/topo/` when TOPO opened and
claimed **Track P whole**, per `work/code-quality/program.md` ("a row
leaves the moment a program claims it, by moving into that program's
directory keeping its id, `track:` letter and body") and the 2026-09
cut's rule that a program taking a code-quality letter takes all of it
so the schedule stays single-owner. Id, `track: P` and body unchanged.

Track P had **never had a lane** — no `smell/p-*` branch has ever
existed, while K, X and T lanes have run — and its fence
(`euler*.rs`, `validate.rs`, `live.rs`, `seqgen.rs`, `merge_faces.rs`
and the review-and-fixture readers) is exactly the `crates/topo/src`
territory that was in no program's `paths`. The three sub-lanes
`work/code-quality/plan.md` defined for P are inherited unchanged and
are restated in `work/topo/plan.md`.

## Brief (TOPO, 2026-09-06)

Dispatched as the generator sub-lane's unit. Branch
`topo/s69-kfmrh-fusion-form`. Outside the A/B experiment (single style
review; no row): the change is to the generator, the ledger and a
debug postcondition, not to an answer a caller reads. Kind stays
`issue` on this file until the PR closes it. Survey on main `368089da`:

Three defects, one operator, in order of what they cost:

1. **The fusion branch is never generated.** `kfmrh_candidates`
   (`crates/topo/src/seqgen.rs:502-512`) filters
   `face1.shell == face2.shell`; teardown's re-make search
   (`:1095-1112`) does the same. `kfmrh`'s cross-shell form
   (`crates/topo/src/euler_ring.rs:~809-835`: f2's surviving faces
   re-home into f1's shell, f2's shell dies, `ArenaDelta { shells: -1 }`)
   therefore has no witness in the fuzz corpus. Add the form: pairs in
   the SAME solid and DIFFERENT shells with `face2.rings.is_empty()`
   (the plan phase's own precondition — read it and cite it; `CrossSolid`
   is the refusal for the other case). Multi-shell solids exist in the
   corpus wherever `mfkrh` has run; measure how often the new row is
   askable and drawn over the standard effort, and put the number in
   the PR body with the seed. Teardown's search takes the same widening
   or says why not.
2. **The `Ledger` counts solids where the identity wants shells.**
   `seqgen.rs:~283`'s `s` is `body.solids().count()`; the Euler–Poincaré
   identity's `S` is shells, so a shell-count error passes property (b)
   silently — which is exactly what defect 1 would produce if the fusion
   branch got its count wrong. The ledger carries shells (and solids
   beside it if a property reads them); property (b) is restated on the
   right term. Then show the row is live: plant a mutant in the fusion
   branch that leaves `f2_shell` alive (skip the `self.shells.remove`)
   and show (b) reds; revert.
3. **The postcondition follows the code down its own branch.**
   `euler_ring.rs:~846-862` hands `assert_euler_postcondition` a delta
   chosen by `killed_shell.is_some()`, computed by the mutation it is
   checking, so a fusion that ran when it should not have is invisible
   to it. Derive the expected delta from the PLAN phase's decision
   (`cross_shell`, decided before any mutation) and hand the
   postcondition that constant; `euler.rs:869`'s "one operator's signed
   shift" then holds. Mutant: make the mutation take the fusion branch
   when the plan said not to (or vice versa) and show the postcondition
   reds; revert.

Fence: `crates/topo/src/seqgen.rs`, `crates/topo/src/seqgen/*`,
`crates/topo/src/euler_ring.rs` (the `kfmrh` postcondition site only;
the plan phase is read, not changed), `crates/topo/src/euler.rs` only
if `ArenaDelta`'s docs need a sentence. `S15`'s `SplitEdge` addition is
the precedent for adding a catalog row — read how it was added and what
it measured. No trailer, no empty commits, merge-only; merge
`origin/main` before the PR and whenever it moves; confirm the run's
jobs are running and poll to green in the foreground; state-sync last
(this file `status: closed`, a `## Closed` section).

Acceptance: the fusion row drawn and green over the standard effort
(number and seed in the PR body); the two mutants red then reverted;
`cargo test -p topo` touched modules green locally; hosted CI green on
the full matrix; sweep receipt: every other operator whose
postcondition delta is computed from its own mutation rather than
handed a constant (grep `assert_euler_postcondition` call sites and
read each delta argument) — list with disposition.

## Closed (2026-09-06, PR 2014)

`kind` moved `issue` → `unit`: the brief above is this file's spec and
PR 2014 is the work, which is what a unit is.

**What landed.**

- `crates/topo/src/seqgen.rs` gains two catalog rows.
  `OpChoice::KfmrhFuse` is `kfmrh`'s shell-fusion form, enumerated on
  the operator's own plan phase (distinct faces, one solid, `f2`
  ring-free) with the vector `f −1, r +1, s −1` and `h 0` — not the
  same-shell form's `h +1, s 0`, which eq. 9.2 forces once `s` is the
  shell count. `OpChoice::Movefac` is the shell partition, with the
  vector `h +1, s +1`.
- `Movefac` is here because defect 1 could not be closed without it.
  The brief's premise — multi-shell solids wherever `mfkrh` has run —
  does not hold: `mfkrh` splits a shell's SURFACE into components and
  leaves the single shell ENTITY in place
  (`crates/topo/src/euler_kill.rs:977-983`), and the only two doors
  that mint a shell are `mvfs` (one per solid) and `movefac`. Without
  a `movefac` row the fusion row would have been asked on every step
  and drawn on none.
- The `Ledger`'s `s` is now `body.shells().count()` — eq. 9.2's own
  `S` — with the solid arena length carried beside it on its own term
  so `mvfs`/`kvfs` are still held to it.
- `kfmrh`'s debug postcondition takes a delta fixed in the PLAN phase
  from `cross_shell`, not one read back out of the mutation's
  `killed_shell`. `crates/topo/src/euler_ring.rs`'s Euler-vector doc,
  which stated `h +1` for both forms, states each form separately.
- `teardown`'s re-make search falls back to a same-solid cross-shell
  sibling, without which a `movefac`-partitioned solid cannot reach
  `kvfs`. `first_empty_outer_extra_face` still tries same-shell first.
- The seqgen fuzz row's case count rides `CAD_FUZZ_EFFORT`; it was a
  literal 48 with no dial at all. `selection_is_pinned_over_a_fixed_stream_set`
  is re-pinned for the two new rows.

**Evidence.** Fusion row at effort 25 (seed `0xc0688fa5c718a639`):
askable on 965 of 29002 steps, drawn 106 times, with 8 roundtrips
executed and 16 skipped on the documented shapes. The permanent
witness is `review_m1_pr4.rs`'s op-kind coverage row, which now
requires `kfmrh_fuse` and `movefac` and completes in 356 steps. Two
mutants reported in the PR body: a solids-counting ledger stayed green
over 480 cases with the fusion row carrying the same-shell vector,
and a mutation taking the wrong branch now reds on the arena delta
rather than only on the validator. Hosted CI green on the full matrix,
run 34017742782.

**Residue, each with its own file** (not scheduled by this section —
these are the files):

- `work/topo/fused-two-shell-body-doc-predates-movefac.md` — the
  fixture doc that says its shape is not publicly constructible.
- `work/topo/seqgen-proptest-row-logs-no-seed.md` — the proptest row's
  green runs record nothing.
- `work/topo/movefac-row-skips-three-component-shells.md` — the
  deliberate candidate filter and what closing it would cost.
