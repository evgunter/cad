---
id: D289
kind: unit
title: Inline ssi/enclose.rs's private ring alias onto RingInterval::from_certified and pin the crossing
status: closed
opened: 2026-09-02
track: Q
refs: [1533]
closed: 2026-09-04
branch: claude/code-quality-tracks-review-k0c0zf
---

## What

**`S89`'s second alias, filed by CERT-M1**: `crates/geom-brep/src/ssi/enclose.rs:192-197`'s private `fn ring<T: CertifiedEnclosure>` is `RingInterval::from_certified` under another name — inline it, delete the restatement (it shares a verbatim sentence with the `spline/hull.rs` copy, which is the finding's point). This crossing is pinned by NO row the decoration-seam header names (CERT-M1's fix pass says so); add the pin with the inlining.

## Was

CERT-M1 (PR 1533), filed.

## Finding

### S89. The one-home fix for the ring crossing minted three local aliases

`RingInterval::from_certified` is the declared one home, and two private one-line wrappers sit on top of it, each carrying its own multi-paragraph restatement of the same rule — and the two share a verbatim sentence. **Citations re-derived 2026-09-01**, because neither was where the finding said: `bracket` is `crates/geom-core/src/spline/hull.rs:116` (Track N's ground, not `ring_interval.rs`) and `ring` is `crates/geom-brep/src/ssi/enclose.rs:195` (Track Q's).

A unit that unifies duplicates minting named copies is the fix reproducing what it closed.

Also open: `trv()`/`healthy()` and the whole `the_fixture_is_a_finite_bracket_that_cannot_certify` row are restated verbatim in three test files, two of which share a filename in sibling directories and run in the same `geom` binary (`crates/geom/tests/{curves,surfaces}/decoration_ring_coords.rs`, and `topo/src/props.rs`'s `bracket_seam_tests`).

**Verdict:** (blank — unruled.)

## Fence

Track Q — `topo`'s boolean, census and charts, and the predicate ledger. **Fence:** `crates/topo/src/{boolean/,splitting/,census.rs,chord_join.rs,chart_region.rs,face_normal.rs}`, `crates/geom-brep/src/{ssi*,pcurve_cache.rs,nurbs_iso.rs,edge_nurbs.rs}`, `docs/predicate-dimension-audit.md`. **Block:** `D280`–`D299` / `S350`–`S369`.

## Closed

The alias is gone: 42 call sites now spell
`RingInterval::from_certified` and the twelve-line restatement is
deleted, along with the orphan seam paragraph above it (whose content is
the module docs' *"The M6-2 seam: generic scalars in, ring out"*, which
states it already). Four sites that crossed the SAME operand twice in
one expression bind it once instead (`implicit_enclosure`'s sphere and
cylinder residuals, `implicit_gradient_enclosure`'s two) — the crossing
is named at the door, and the `sqr` comment's `never r * r` reads
straight again.

**The pin the row asks for already exists**, found on reading rather
than written here: `enclose.rs`'s own `tests::decoration_seam` module
carries three rows over these crossings, green under `--features
interval`. What is stale is the SENTENCE in
`crates/geom-core/tests/decoration_seam.rs`'s header that says no row
pins this crossing — a Track W path, so it is filed rather than fixed:
`decoration-seam-header-names-no-pin-for-enclose`.
