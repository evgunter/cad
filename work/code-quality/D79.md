---
id: D79
kind: unit
title: Fix lily.rs: the header
status: parked
opened: 2026-08-21
track: X
blocked_on: [L2]
refs: [796, 787]
---

## What

`lily.rs`, read end to end for the first time (**`S130`**, whose members
are this row's members) — a 118-line header at 40% comment

## Was

Track G

## Finding

### S130. `demos/tour/src/lily.rs` — the first end-to-end read (roll-up, raised by #787)

**Row: Track X's `D79`**, which is this finding under a row number —
same file, same members, same description. Neither is a second finding.

**Raised by #787's style review**, which took the file as free ground:
the second scan's coverage record named `lily.rs` as its highest-yield
uncovered file — sampled, never read end to end — and nothing had read
it since. As the file stands today it is 4,154 lines, 1,682 comment /
2,353 code (40.5%; a comment line is one whose first non-blank
characters are `//`, code is every other non-blank line, and 119 lines
are blank), with a 118-line `//!` header at `lily.rs:1-118` before the
first item — the tour's most geometrically involved scene and the one
written most like a user modelling a real object, which is what makes
it worth reading.

- (f) **40% comment, a 118-line header, accumulated one titled essay at
  a time.** S116(e)'s class, one crate over. It is `L2`'s (`S38`'s), not
  a track's: `plan.md` puts the cross-cutting comment sweeps after every
  track empties.

**Verdict:** none recorded (no verdict line in the source).

## Parked, and on what

The one surviving member is waiting on something outside Track X, which
is why this row is parked rather than open: **(f)** is `L2`'s, which
`plan.md` puts after every track empties. `D79`'s takeable members, (d)
and (e), landed and are deleted above; **(b)**, the shadow tuple
algebra beside `Vec3`, is deleted here — PROPS's
`lily-authoring-needs-shadow-vector-algebra` answered the library
question it was parked on (no `Vec3` door is missing; the layer rule is
compose at `f64` in the kernel's vector types, lift at the door) and
rewrote `lily.rs`'s authoring half through it (#1954).

## Fence

Track X — `demos/`, and the design doc's companion table. Fence:
`demos/` (Rust and Markdown; its Python is not X's — see the Python seam
in §D), `docs/DESIGN.md`'s companion table. Block: `D400`–`D419` /
`S470`–`S489`. Two items, and it is small because its ground is small;
it shares no file with anything above, so it can be taken alongside
another track by one orchestrator without breaking the partition.
