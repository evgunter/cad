---
id: D207
kind: unit
title: tess-meter's rows.rs asserts only that cell counts are positive, so a δ_s retune at columns() is invisible
status: closed
opened: 2026-08-21
track: K
closed: 2026-09-03
---

## What

**`tools/tess-meter/tests/rows.rs` asserts only `grid_cells > 0 && span_opt_cells > 0`**, so a `δ_s` retune at `columns()`' call site into `span_opt_cells` / `best_split_cells` is invisible to it. That is `D105`'s composition defect one call site further out, and it takes the same fix: assert the composition, not the parts — `D105` landed `best_split_scan` and `the_shipped_optimizer_is_the_shipped_scan` as the pattern to copy. Three retunes of that shape moved the reported cell count by up to +100% while the whole gated job stayed green.

(`D105` is a landed row cited as provenance only.)

## Was

`D105` residue.

## Finding

No live finding section; provenance only. The row cites no `S<n>`; it is a residue of the landed `D105`.

## Fence

Track K — the instruments, and what they cannot see. **Fence:** `scripts/gates/` less `gate-roster.sh` and `probe-suite-census.sh`, `tools/`, `docs/K-REPORT.md`. **Block:** `D200`–`D219` / `S270`–`S289`.
