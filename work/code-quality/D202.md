---
id: D202
kind: unit
title: tess_meter::face_rows turns a missing measurement into "this face is not on the sized lane"
status: closed
opened: 2026-08-21
track: K
refs: [C15]
closed: 2026-09-03
---

## What

`tess_meter::face_rows`'s `nurbs: by_face.get(&patch.face).map(columns)` turns a MISSING measurement into *"this face is not on the sized lane"* — a silent miss reading as a lane fact, one level upstream of the join `C15` just fixed and the same shape.

## Was

`C15` residue.

## Finding

No live finding section; provenance only. The row is a residue of `C15`'s fix (see `C15` for `S73`, the finding that fix closed the mis-join half of); it cites no `S<n>` of its own.

## Fence

Track K — the instruments, and what they cannot see. **Fence:** `scripts/gates/` less `gate-roster.sh` and `probe-suite-census.sh`, `tools/`, `docs/K-REPORT.md`. **Block:** `D200`–`D219` / `S270`–`S289`.
