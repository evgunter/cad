---
id: D244
kind: unit
title: Inline spline/hull.rs's private bracket<E> alias of RingInterval::from_certified and delete its restatement
status: open
opened: 2026-09-02
track: N
refs: [D289, D384, 1533]
---

## What

**`S89`'s first alias, filed by CERT-M1**: `crates/geom-core/src/spline/hull.rs:113-118`'s private `fn bracket<E: CertifiedEnclosure>` is `RingInterval::from_certified` under another name, with an eight-line restatement of the door's rule above it. Inline it at its call sites and delete the restatement; the rule's one home is `from_certified`'s own doc, which (since CERT-M1) carries no caller census either. `geom-core/tests/decoration_seam.rs`'s header names this crossing; keep that sentence true when the alias goes.

## Was

CERT-M1 (PR 1533), filed.

## Finding

### S89. The one-home fix for the ring crossing minted three local aliases

`RingInterval::from_certified` is the declared one home, and two private one-line wrappers sit on top of it, each carrying its own multi-paragraph restatement of the same rule — and the two share a verbatim sentence. **Citations re-derived 2026-09-01**, because neither was where the finding said: `bracket` is `crates/geom-core/src/spline/hull.rs:116` (Track N's ground, not `ring_interval.rs`) and `ring` is `crates/geom-brep/src/ssi/enclose.rs:195` (Track Q's — row `D289`).

A unit that unifies duplicates minting named copies is the fix reproducing what it closed.

Also open: `trv()`/`healthy()` and the whole `the_fixture_is_a_finite_bracket_that_cannot_certify` row are restated verbatim in three test files, two of which share a filename in sibling directories and run in the same `geom` binary (`crates/geom/tests/{curves,surfaces}/decoration_ring_coords.rs`, and `topo/src/props.rs`'s `bracket_seam_tests`) — that is Track W's `D384`.

**Verdict:** (blank — unruled.)

## Fence

Track N — `geom`, and the spline and linalg substrate. **Fence:** `crates/geom/src/`, `crates/geom-core/src/{spline/,linalg/}`. **Block:** `D240`–`D259` / `S310`–`S329`.
