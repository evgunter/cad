---
id: corner-config-tag-all-concave-trihedron
kind: ruling
title: Design conversation - an honest CornerConfig tag for the all-concave trihedron (currently MixedConvexity convex 0)
status: open
opened: 2026-08-31
github: 1355
refs: [644, 919, 1306, 1347, 1360]
needs_ev: true
---

## From GitHub issue 1355

Opened 2026-08-31; 1 comment.

**For Ev — a vocabulary extension per the SeamVertex path (ratify, then mint).** Raised at BLEND-3's review adjudication (PR #1347, issue #919).

## The question

When a **fillet** is requested at an all-concave trihedron, the refusal today is `CornerConfig::MixedConvexity { convex: 0 }` — a "mixed-convexity vertex (0 of 3 edges convex)" — which is false of a uniform corner: nothing about it is mixed. This is the misleading-name class (BLEND-5's `RimSupport::Plane`-on-a-cone, #1306's `CapEnd`), pre-existing, surfaced by BLEND-3's widening (the chamfer now *carves* that corner, so only the fillet still emits the label).

BLEND-3's first draft minted `CornerConfig::ThreeConcaveEdges` for it as execution. Its review found the cited precedent inverted: `SeamVertex` — the one prior `CornerConfig` mint — went through a design conversation ratified by you (`docs/ARMS3-DESIGN.md`, RATIFIED 2026-08-25, #992) the day before its implementation landed, under OQ6's explicit reservation that corner/run-out taxonomy is yours to own. `docs/CURVED-DESIGN.md`'s scope box likewise enumerates the ratified corner-configuration vocabulary. So the tag was pulled from the unit (the chamfer widening needs no new vocabulary — the fillet's refusal stays byte-identical), and the label question comes here instead.

## The options

1. **Mint `ThreeConcaveEdges`** (recommended): honest tag, `RunOutStopAtVertex` policy, refusal prose saying the flat corner patch carves it for a chamfer and the concave ball (#644) would for a fillet. BLEND-3's review left the Display-sentence pitfalls mapped (a first-draft sentence self-contradicted; the fix is known).
2. **Keep `MixedConvexity{convex:0}` but fix only its sentence** — no enum change; the prose stops saying "mixed" when convex==0. Smaller, but the tag's NAME still lies to a matching caller.
3. **Leave it** — the label stays false of uniform corners until #644 (the convexity-parametric fillet) forces the vocabulary question anyway.

A 👍 on option 1 (or a comment) is enough; the mint is then a small execution PR (the reviewed shape exists). Option 1's natural time is either now or folded into #644's design conversation, since that unit re-opens the fillet's corner taxonomy regardless.

## Comments

**2026-08-31** — comment:

Landscape update from BLEND-4 (PR #1360, issue #644): the convexity-parametric fillet corner is implemented, so **both verbs now carve the all-concave trihedron** and `MixedConvexity { convex: 0 }` is no longer minted anywhere — the corner classifier admits both uniform trihedra and only the literal counts `1|2` remain as refusal payloads.

What that does to the options here (still your call, nothing decided in the PR):

- **Option 3's premise is resolved by the carving**: the label is no longer "false until #644" — it is gone from the refusal surface entirely.
- **Option 2** (fix the sentence only) has nothing left to fix: no site renders the `convex == 0` sentence.
- **Option 1** (mint `ThreeConcaveEdges`) would now name a configuration that *carves*, i.e. it would be the in-scope sibling of `ThreeConvexEdges` rather than a refusal tag. Whether the carved configuration deserves its own tag (e.g. for a future emit-time or persisted-name vocabulary) remains the OQ6 corner-taxonomy question this issue reserves for you; the enum's docs now point here for exactly that.

No action needed unless you want the tag for its own sake.

---
_Generated by [Claude Code](https://claude.ai/code)_

## Home

Code quality: a misleading-name finding whose answer only Ev gives, which is exactly the `kind: ruling` file that program's register provides for; S-BLEND, whose ground it names, is closed (`docs/S-BLEND-EXIT-WALK.md`) and its tracker directory is gone.
