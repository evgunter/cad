---
id: D30
kind: unit
title: quad.rs holds a second span search and a second index clamp because KnotVector cannot represent what it needs
status: open
opened: 2026-08-21
track: R
refs: [C3]
---

## What

`quad.rs` holds a second span search and a second index clamp, because `KnotVector` cannot represent what the module needs. **Same file as `C3` and sequenced with it.**

## Was

Track C.

## Finding

No live finding section; provenance only.

## Fence

Track R — the measuring consumers: `geom-brep` and `mesh/`. **Fence:** `crates/geom-brep/src/` less `ssi*`, `pcurve_cache.rs`, `nurbs_iso.rs` and `edge_nurbs.rs` (which are Q's), plus `crates/mesh/` — widened 2026-08-28 from `geom-brep/src/props/` only, so `patch_bound.rs`, `offset_meters.rs`, `nurbs_hull.rs` and the rest of the crate's `src/` are R's. **Block:** `D300`–`D319` / `S370`–`S389`.
