---
id: D212
kind: unit
title: The gate-side half of ArcCarrierScalar's retirement — GAP 3's text, the arc_fillet.rs filter, and the roster entry
status: open
opened: 2026-09-03
track: K
rides_with: G4
refs: [D68]
---

## What

**The gate-side half of Track V's `G4`, and it RIDES WITH that row**
(`work/README.md`: *"A rides-along is its own file with `rides_with:`
naming its carrier. Closing the carrier does not close the passenger;
lint refuses a live passenger on a closed carrier."*). It is Track K's
fence and Track V's diff, so it needs a file of its own and no schedule
of its own: it lands in `G4`'s change, and `G4` may not close without
it.

When `G4` retires `profile`'s `ArcCarrierScalar`, three things in
`scripts/gates/bounds-allowlist.sh` stop being true at once:

- **KNOWN GAP 3's text**, which names this trait as its worked example,
  cites `D68`'s answer against a live alias, and names
  `plant_alias_uses_invisible` — the must-PASS fixture that plants the
  declaration and its use shapes. The fixture goes with the text.
- the `BOUNDS_ALIAS_ROSTER` entry `D68` added, whose census reds the
  moment the declaration goes — by design, so the retirement cannot be
  silent. The retirement deletes the entry in the same change.
- the file filter `gate_grep -vE '^crates/profile/src/path/arc_fillet\.rs$'`,
  which exists for the DECLARATION and has nothing left to exempt once
  the declaration is gone.

**There are no `family.rs` or `program.rs` filters, and there never
were.** The only `profile` entry in `gate()` is `arc_fillet.rs`; the
uses in those two files are invisible to the scan, which is `D68`'s
whole finding. The sentence that says otherwise is `G4`'s and came from
UV-R8's ledger row (`work/code-quality/logs/SMELL-UV-LOG.md:80`, out of
this track's fence and still carrying it); it is corrected in `G4.md` by
the same change that files this. It matters because it made two entries
`D68` deliberately REFUSED to mint read as pre-existing rows merely to
be deleted, at the one place whose job is telling `G4`'s taker what to
edit.

## Was

`D68` residue, filed at that row's close.

## Finding

No live finding section; provenance only. `S124` is `D68`'s and closed
with it. The standing statement this row depends on — that the uses are
invisible to any matcher whose output would not be exactly those two
files, and why that output is the trap rather than the fix — is `D68`'s
`## Answer`, which survives in that row's closed file.

## Fence

Track K — the instruments. **Fence:** `scripts/gates/` less
`gate-roster.sh` and `probe-suite-census.sh`, `tools/`,
`docs/K-REPORT.md`. The reaching half of `G4` (the `profile` and `pncad`
edits that retire the trait) is Track V's and is not this row's.
