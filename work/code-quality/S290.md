---
id: S290
kind: unit
title: Two prose citations in dual.rs and real.rs name geom::projection, a module renamed to projection_policy
status: open
opened: 2026-09-02
track: M
refs: [874]
---

## What

**Two prose citations name a `geom` module that no longer exists (filed by CERT-N2).** `geom-core/src/dual.rs:792` and `real.rs:616,656` cite `geom::projection`'s `mid` (issue 874's class). `geom/src/` had three modules named `projection`; the private one holds the shared §6.1 policy rather than an implementation, and closing `S116(b)` renamed it `projection_policy`. `mid` itself did not move — only the module path — so this is one word in three sentences.

## Was

CERT-N2, filed.

## Finding

No live finding section; provenance only. `S290` is itself the row's number (from this track's `S290`–`S309` block), not a finding heading; `S116(b)`, the member whose closing renamed the module, has already left the `S116` roll-up (its surviving members are (a), (c), (e), (f), (h)–(q), (u); none is (b)).

## Fence

Track M — the scalar and certification traits. **Fence:** `crates/geom-core/src/{real,ring_interval,dual,interval,k_stats}.rs`, `interval-transcendentals/`, `crates/bvh/`, `crates/topo/src/props.rs`. **Block:** `D220`–`D239` / `S290`–`S309`. (`crates/topo/src/props.rs` was drawn onto this track 2026-09-02; `crates/topo/src/validate.rs` stays Track P's.)
