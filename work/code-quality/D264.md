---
id: D264
kind: unit
title: source_walk.rs carries three [`self::tests`] intra-doc links that only survive because the module is #[cfg(test)]
status: open
opened: 2026-08-21
track: P
refs: [D261]
---

## What

`topo/src/source_walk.rs:116`, `:120`, `:391` carry three surviving `[`self::tests`]` intra-doc links. They resolve today only because the module is `#[cfg(test)]`, so rustdoc never documents it and the doc gate never sees them — and they become a `-D warnings` failure the moment such a module moves anywhere documented, which is exactly what `D61` did to its own copy in `test-utils`. `clippy` is blind to the lint; only `doc-gate.sh` catches it. **Cheapest taken with `D261`**, which already collapses this file.

(`D61` is a landed row cited as provenance only.)

## Was

`D61` residue.

## Finding

No live finding section; provenance only. The row cites no `S<n>`; it is a residue of the landed `D61`.

## Fence

Track P — `topo`'s Euler surgery, liveness and the generator. **Fence:** `crates/topo/src/{euler.rs,euler_ring.rs,euler_kill.rs,split.rs,attach.rs,movefac.rs,revert.rs,live.rs,merge_faces.rs,seqgen.rs,validate.rs}`, plus `crates/topo/src/{review_d18.rs,review_d18_probes.rs,fixtures.rs,source_walk.rs}` (added 2026-08-29, because they belonged to no track). **Block:** `D260`–`D279` / `S330`–`S349`.
