---
id: D320
kind: unit
title: Replace skin.rs's hand-built lift_surface with the infallible NurbsSurface::map_scalar lift
status: open
opened: 2026-08-21
track: T
refs: [D321]
---

## What

`sweep/src/skin.rs:760`'s `lift_surface` — the one production copy of the scalar lift outside `geom/` (citation re-derived 2026-09-02; the `:774` it was filed at is now a section comment). It rebuilds the control net by hand and goes back through `NurbsSurface::new`, so it returns a `Result` whose error is unreachable for a surface that already validated, and `sweep/src/loft.rs:348` maps that error into `LoftError::Skin(SkinError::Structure)`. `NurbsSurface::map_scalar(T::from_f64)` is the infallible, exact lift the payload now carries: the function, its `Result`, the `loft.rs` mapping and the four `sweep/tests/` callers' `.expect("lifts")` go with it.

## Was

Track N, filed.

## Finding

No live finding section; provenance only.

## Fence

Track T — `sweep/`. **Fence:** `crates/sweep/` (both `src/` and, by exception to W's fence, the `sweep/tests/` files its own rows name). **Block:** `D320`–`D339` / `S390`–`S409`.
