---
id: D321
kind: unit
title: Replace loft.rs's hand-spelled lift_affine with Affine3::map(T::from_f64)
status: open
opened: 2026-09-02
track: T
refs: [D320, 1536]
---

## What

**`sweep/src/loft.rs:215`'s `lift_affine` is `Affine3::map(T::from_f64)` (filed by CERT-N1, beside `D320`)** — `geom_core::linalg` now carries the leaf maps one type over from `Vec3::map` (`Mat3::map`, `Affine3::map`); the eight-line hand spelling goes.

## Was

CERT-N1 (PR 1536), filed.

## Finding

No live finding section; provenance only.

## Fence

Track T — `sweep/`. **Fence:** `crates/sweep/` (both `src/` and, by exception to W's fence, the `sweep/tests/` files its own rows name). **Block:** `D320`–`D339` / `S390`–`S409`.
