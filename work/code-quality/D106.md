---
id: D106
kind: unit
title: Split the ratification ledger out of bounds-allowlist.sh's 204-line header
status: closed
opened: 2026-08-21
track: K
refs: [D209]
closed: 2026-09-03
---

## What

`bounds-allowlist.sh` is a 204-line header in front of a 20-line function, grown three times for three honest reasons — split the ratification ledger out of the script.

## Was

Track F.

## Finding

No live finding section; provenance only. The row cites no `S<n>`; the closest surviving statement is `S116(m)` (carried in full at `D209`): the header says the argument *"live[s] in ONE home: geom-core/src/real.rs … Not restated here; keep this a pointer"* and is immediately followed by ~100 lines restating rulings that `real.rs:365-556` also carries, with the copies already drifted (`:69-73` is a self-correction). `D209` (CERT-M1's residue) is the later, narrower statement of the same trim and should be sequenced with this row.

## Fence

Track K — the instruments, and what they cannot see. **Fence:** `scripts/gates/` less `gate-roster.sh` and `probe-suite-census.sh`, `tools/`, `docs/K-REPORT.md`. **Block:** `D200`–`D219` / `S270`–`S289`.
