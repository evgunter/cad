---
id: D288
kind: unit
title: boolean/ops.rs:210-211 restates MergeCoplanarOutcome::skipped's doc and both halves are now false
status: open
opened: 2026-08-21
track: Q
refs: [C6]
---

## What

`boolean/ops.rs:210-211` restates `MergeCoplanarOutcome::skipped`'s doc — *"Declared-licensed merge groups the output stage SKIPPED … faces + the actual refusing diagnostics"* — and **both halves are now false**: curved runs skip with no declaration at all, and the payload is a typed `MergeCoplanarError`, not rendered diagnostics. `D38` corrected the sentence at its origin and could not cross the fence to this copy. A second spelling of one sentence, drifting exactly as `S4` says such a pair does.

## Was

`D38` residue.

## Finding

The row cites `S4`, the repo-wide duplication finding (~80 lines, a table of five concepts with a row-by-row steelman); its statement, verdict and the parts that bear on this row are carried. Its own row is Track V's `C6`.

### S4. One vocabulary, N hand-synced copies — the dominant repo-wide shape (excerpt)

- **Confidence**: sure
- **Found independently by six scans.**
- **Row:** Track V's **`C6`**, which carries the three members that are genuinely blocked; the rest of the table is unrowed.

The table's five concepts: profile `Step` verbs (**6** copies — `profile::Step` / `ProgramStep` / `WireStep` / `StepArg` / content-key tag table / `pncad-py`'s PATHS lattice + its `.pyi`); `RoleSeg` → `SegTag` (kernel enum → editor-core mirror → `pncad` re-export → a second 40-variant py mirror → 40-arm `to_kernel` → 40-arm inverse tripwire → 1316-line `.pyi`, anchor `pncad-py/src/py/select.rs:82`); node kinds (~10 parallel match tables, `node.rs:423`, `eval/mod.rs:1325`); "which payloads carry a `StableName`" (4 lists — `edit.rs:1096`, `node.rs:949`, `refactor.rs:801`, `resolve/mod.rs:911`); "node has no usable value" (5 typed + 1 stringly — `resolve/mod.rs:266`, `resolve/hit.rs:23`, `resolve/vdiff.rs:69`, `appearance.rs:172`, `names/geompred.rs:488`).

**Steelman (2026-08-18), row by row:** profile `Step` verbs SURVIVES; `RoleSeg` → `SegTag` SURVIVES IN PART (the `.pyi`'s 40 members are unpinned; the py mirror is forced by the orphan rule — only generatable); node kinds DOES NOT SURVIVE as stated (re-scoped to *wildcard* arms it survived, measured `node.rs` 9, `eval/mod.rs` 5, `resolve/mod.rs` 4, `edit.rs` 3, `refactor.rs` 3, `persist/check.rs` 2 — a count whose counting rule was never stated; whoever takes that row states the rule first); `StableName` lists SURVIVES; "no usable value" SURVIVES IN PART.

*One confirmation the report did not cite: the hand-synced tag table has already produced a live measured bug.* `MODEL-AB-LOG.md:782` — *"MAJOR-1 = `Step::AtToward`'s memo content-key tag 28 COLLIDED with `ArcContinue`'s existing 28 — latent memo collision, a hit would serve wrong geometry"*. Caught by a reviewer, not by a type. S4's failure mode, realised.

**Verdict:** ACCEPTED (Ev, 2026-08-18). *"Oh boy, good findings — these look like they'll be a lot of work to fix but definitely worth it."*

Omitted: *The two instruments a taker of the wildcard half needs*; *`evaluate`'s bound, restated in five places*; *Two mirrors are forced and therefore unfixable*; the cheapest-to-hardest ranking.

## Fence

Track Q — `topo`'s boolean, census and charts, and the predicate ledger. **Fence:** `crates/topo/src/{boolean/,splitting/,census.rs,chord_join.rs,chart_region.rs,face_normal.rs}`, `crates/geom-brep/src/{ssi*,pcurve_cache.rs,nurbs_iso.rs,edge_nurbs.rs}`, `docs/predicate-dimension-audit.md`. **Block:** `D280`–`D299` / `S350`–`S369`.
