---
id: D20
kind: unit
title: D5's +46% on the seqgen lane is real and unattributed after #722 — closes on an attribution off hosted CI
status: open
opened: 2026-08-21
track: P
refs: [722]
---

## What

D5's +46% on the `seqgen` lane is real and, after #722 excluded the candidate it was charged to, unattributed. **Closes on an attribution off hosted CI — a number, or a written finding that it is inherent.**

(`D5` here is `docs/DESIGN.md`'s ratified decision D5, not a schedule row.)

## Was

Track E.

## Finding

No live finding section; provenance only. The row cites no `S<n>`, and no surviving finding in the document carries the measurement; the sentence above is the whole record.

## Fence

Track P — `topo`'s Euler surgery, liveness and the generator. **Fence:** `crates/topo/src/{euler.rs,euler_ring.rs,euler_kill.rs,split.rs,attach.rs,movefac.rs,revert.rs,live.rs,merge_faces.rs,seqgen.rs,validate.rs}`, plus `crates/topo/src/{review_d18.rs,review_d18_probes.rs,fixtures.rs,source_walk.rs}` (added 2026-08-29, because they belonged to no track). **Block:** `D260`–`D279` / `S330`–`S349`.
