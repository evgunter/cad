---
id: reroute
kind: program
title: S-REROUTE — route the rest of a homed class onto its door
status: ready
opened: 2026-09-26
area: infra
prefix: reroute/
tag: (S-REROUTE orchestrator)
paths: []
keep_out: [this program claims NO paths and inherits SUITE's reason - crates/*/tests/* and crates/test-utils/* are S-TCOST's and S-TINT's in every crate and crates/sweep/src/test_support.rs and crates/topo/src/test_support_impl.rs are BLEND's and TOPO's so the shared homes land there by announced seam, S-TINT owns the ground and the test-integrity charter and this program owns only the duplication question - a row that cannot go red is S-TINT's and one thing spelled n times is this program's and a row that is both goes to S-TINT first, demos/tour/* and demos/wild/* are never converted - they are evidence about the public API from an outside consumer's seat and a demo that reaches into a dev-gated test_support stops being evidence about the library (SUITE's ruling 2026-09-15 restating the tree's settled practice), docs/GUIDE.md and rustdoc examples fall under the same rule for the same reason - they are written from a seat that cannot reach a doc(hidden) door, a census is not a population until its SCOPE as well as its pattern is re-derived - git grep over every tracked file with no path argument is the only form that makes no path claim]
priority: P4
---

**Route the rest of a class onto the door it already has.** Cut from
S-DUP on 2026-09-26 (Ev: "you can do such splits without asking"). S-DUP's
census-first method homes a class and then files the class's remainder as
a row; those remainders are the whole of this program. Each row here names
a door or a settled rule that already exists on main, and the work is to
route the remaining members onto it — no door to design, no decision to
take. A row that turns out to need a new door goes back to S-DUP by
`git mv`.

Why a separate track: S-DUP measured 40.5 against its 30-point budget after
two drain batches, because each drain filed its class's remainder as fast
as it closed its head. A priority-seam cut was degenerate (all but two of
S-DUP's rows are P4), so the cut is along the one line that separates the
work by kind: a row that needs a door (S-DUP) against a row that needs
only routing (here). Method and discipline: S-DUP's `work/dup/plan.md`
method items apply unchanged, with this program's plan adding only what
differs.
