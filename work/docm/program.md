---
id: docm
kind: program
title: DOCM — the document model
status: open
opened: 2026-09-03
area: api
prefix: docm/
tag: (DOCM orchestrator)
ab_band: 1800-1899
paths: [crates/editor-core/src/persist/*, crates/editor-core/src/program.rs, crates/editor-core/src/doc.rs, crates/editor-core/src/edit.rs, crates/editor-core/src/node.rs, crates/editor-core/src/names/role.rs, crates/editor-core/src/eval/parts.rs, crates/editor-core/src/eval/memo.rs, crates/editor-core/src/resolve/hit.rs, crates/editor-core/src/resolve/pick.rs, crates/editor-core/src/resolve/mod.rs, crates/editor-core/src/mate.rs, crates/editor-core/src/mate/*, crates/editor-core/src/assembly.rs, docs/RECIPE-DOORS-DESIGN.md]
keep_out: [code-quality Track V cedes the paths above to this program (Ev 2026-09-03) and keeps the rest of editor-core and crates/profile, M10's analysis lane (analysis.rs distribution.rs drive.rs measure.rs and the product.rs Dual arms) is untouched — the check-registry subject edits product.rs by announced seam, SEAT keeps verbs/* and names/geompred.rs and names/flush.rs — the DS1 migration of flush.rs is SEAT's hand-off, S-BOOL keeps resolve/vdiff.rs and crates/profile/*, LIB keeps the pncad facade and bindings — the frame-from-face door and the memo/resolver question are taken with LIB's recorded hand-off and LIB answers the .pyi side, mate.rs and assembly.rs came from S-MATE at its exit (MATE-EXIT ratified, DOC-LEDGER sweep 6), the viewer builds of every answer here are CHROME's or VIEW's, no ruling is resolved by implementing it — each question is an [ev] PR first]
---

The editor-core document layer — the persisted recipe vocabulary, the
`DocEdit` set, document identity, and the frames and selectors the
viewer and the mate tool consume — under one owner for the first time.
Three programs' keep_outs called this ground contended or "a
conversation before a unit"; this program is the conversation. Class D
throughout: it opens with one `[ev]` PR per question and hands each
build to FIX, CHROME or VIEW when ruled. Takes the Track V rows that
live on this ground. Charter, the eleven questions and their order:
`work/docm/plan.md`; narrative in `work/docm/log.md`.
