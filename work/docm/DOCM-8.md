---
id: DOCM-8
kind: unit
title: A merged face's name is a flat constituent set; a member-space declaration resolves through the fold's merges
status: open
opened: 2026-09-06
---


## Spec

`docs/DOCM-8-SPEC.md` (binding; pre-draw S/M / STRUCTURAL logged
there). From Ev's 2026-09-06 chat ruling on
`member-space-declarations-are-order-shaped-across-a-chain` (which
closes at merge, pointing here): whatever mints a `Merged` mints it
flat; nothing flattens downstream. The look-through at the union's
routing step is the orchestrator's reading of DM4, stated in DM4 at
this spec's commit. Block DOCM-B3 slot 1 (FABLE) is its taker.

## Closed (2026-09-07)

Merged as PR 2073 (one stop at the pass-through shape, the spec
amended and the lane resumed as the same arm; the v6 dual's union fix
pass: one `covers` home for reading a flat set, the prose bounded to
merges, a mint-site refusal, the declared-pair report traced to the
kernel). Record: the DOCM-8 row in `docs/MODEL-AB-LOG.md` (ordinal
1807, sample #154) and the MERGED entry in `work/docm/log.md`. The
spec is deleted per the ledger. The finding it answered,
`member-space-declarations-are-order-shaped-across-a-chain`, is
closed. Residue filed by the fix pass:
`member-space-look-through-stops-at-splits-containment-and-fragmented-merges`,
`two-emitter-refusals-a-legal-declared-union-reaches`,
`area-overlap-contact-admitted-but-unmerged-refuses-at-the-next-step`
(kernel), `join-desync-on-the-star-fixture` (`topo`).
