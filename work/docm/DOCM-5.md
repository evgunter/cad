---
id: DOCM-5
kind: unit
title: The check registry's subject: one gather per landing, residents handed a subject, assemble over a gathered product
status: closed
opened: 2026-09-04
closed: 2026-09-04
pr: 1871
branch: docm/5-check-subject
---


## Spec

`docs/DOCM-5-SPEC.md` (binding; pre-draw M / STRUCTURAL), from the
plan's ruling on `check-registry-gathers-product-twice` (the finding
this unit answers; it closes at merge, pointing here): `run_checks`
computes the product once and hands residents a subject; `assemble`
takes a pre-gathered product; `DocSession::land` gathers once for its
three consumers. The `product.rs` Dual arms are edited only by
announced seam to M10.

## Closed (2026-09-04)

Merged as PR 1871 (no stop; the v6 dual's union fix pass under three
rulings: the lazy gather, `Subject::Unavailable` carried into the door,
document identity at the door). Record: the DOCM-5 row in
`docs/MODEL-AB-LOG.md` (ordinal 1804, sample #130) and the MERGED entry
in `work/docm/log.md`. The spec is deleted per the ledger. The finding
it answered, `check-registry-gathers-product-twice`, is closed. Residue:
the Python doors still gather twice
(`work/lib/python-check-and-assembly-doors-gather-twice.md`); the scene
layer's extra gather (`work/view/scene-gathers-the-landed-product-twice-more.md`);
debug-only counters have no gate (`work/issues/`).
