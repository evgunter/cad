---
id: DOCM-9
kind: unit
title: The certified locally-valid range: a query that proves instead of samples
status: closed
opened: 2026-09-13
closed: 2026-09-14
pr: 2534
branch: docm/9-certified-range
---


## Spec

`docs/DOCM-9-SPEC.md` (binding; pre-draw M / STRUCTURAL logged
there). From the plan's open question 3,
`certify-locally-valid-range-instead-of-sampling` (which closes at
merge, pointing here); Ev's 2026-09-13 chat ruling on pacing: the
sampling probe stays the interactive answer, the certified range is
an on-demand query whose result replaces the probe's. The last unit
on DOCM's slate; block DOCM-B3 slot 2 (OPUS) is its taker.

## Closed (2026-09-14)

Merged as PR 2534 (no stop; the v6 dual's union fix pass under one
bilateral MAJOR — the `DecisionFlip` arm's prose about `within` —
and the rulings that the walk stays, the corpus behaviour is stated,
and the real probe runs beside the query). Record: the DOCM-9 row in
`docs/MODEL-AB-LOG.md` (ordinal 1808, sample #187) and the MERGED
entry in `work/docm/log.md`. The spec is deleted per the ledger. The
finding it answered, `certify-locally-valid-range-instead-of-sampling`,
is closed. Residue filed: the viewer affordance (CHROME), the Python
door and the export guard's cfg asymmetry (LIB), the parametric
polygon loop and the driver's budget-priced failing leaf (PROPS).
The program's last unit; the exit sweep follows.
