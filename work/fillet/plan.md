# FILLET — blend completion, second pass (plan)

**STATUS: OPEN (2026-09-03).** Opened 2026-09-03 from `docs/WORK-TRACKS-2026-09.md` (FILLET section), which is this
program's charter until this plan supersedes it. Live state is
`work/fillet/log.md`'s tail and the item files beside this plan, never
this file.

Branch prefix (the #396 convention): **`fillet/`** — unit branches
`fillet/<unit>-<slug>`, orchestrator branch `fillet/orchestrator`.
Away-channel tag `(FILLET orchestrator)`. A/B ordinal band
**FILLET = 2000–2099**, claimed in `docs/MODEL-AB-LOG.md`'s banding
entry in the opening commit, per that entry's rule.

## Charter

Finish what S-BLEND's exit walk left: the material-adding closed-rim
band, the repaired pole rim's several arcs on one host face, the
ruled-spine terminations, and the refusal vocabulary at both blend
doors. The substrate (`crates/sweep/README.md` BLEND-VOCAB V1–V4,
ARMS3 A3-1…3; `crates/profile/README.md` "Enclosing tangency") is
ratified and cited, never re-litigated.

## Review posture

Full v6 dual with Fable specs for the H units; the E openers take a
single style review.

## Unit order

E openers, first:

1. `fillet-nonpositive-radius-false-fact-refusal` — chamfer's
   `NonpositiveSize` door check applied to `fillet_edges`; flip the
   characterisation probe to pin the typed refusal.
2. `recourse-sentences-owe-followability-pin` — a composed pin that
   EXECUTES every `FILLET*_RECOURSE` sentence and asserts it builds, or
   records the gap at the site; it also inventories which recourses
   lie, which is input to the H units.
3. `bare-f64-margin-payload-family` — named payload shapes
   (`Option`/`Indeterminate` carried whole) in the fillet battery; the
   `edge_nurbs.rs` twin is TRIM's ground and filed there.

H:

4. `concave-closed-rim-has-no-band` — relax `resolve_rim`'s convexity
   gate, make the excise/merge walk orientation-agnostic, a concave
   fixture with a closed form; L.
5. `repaired-pole-rim-serves-no-closed-door` — the annulus closed-rim
   arm learns that one planar host face may carry several rim arcs of
   one circle in one loop; the p4 probe is the acceptance row.
6. `extrude-cap-rim-smooth-arm-noop` — construct an input that reaches
   the no-op `Smooth` arm, or state the true unreachability argument;
   S but H in kind.
7. `fillet-ruled-spine-arms-no-surgery` — curved-support trimline
   descriptions and chain terminations at caps and seams; blocked on
   the OQ6 run-out taxonomy, so last.

D, each an `[ev]` PR:

- `corner-config-tag-all-concave-trihedron` (code-quality ruling,
  already on Ev) — whether `ThreeConcaveEdges` is minted at all.
- `nocornersidecandidate-has-no-producer` — delete the variant or keep
  it with a stated reason; a mined witness or an argument none exists.
- `fillet-refusal-describes-unbracketed-crossing` — an attribution rule
  for the arc-carrier refusal channel, or name the crossing in the
  payload.
- `no-public-rim-arc-selector` — the consumer door: key shape (carrier
  circle versus named entity), co-surface seam exclusion built in,
  "not one rim" refused typed.

Track T's rows `D320`–`D325` land as riders on the units that touch
their files; the park on #1360 is stale and is cleared at the first
dispatch.

## Exit shape

The seven land, the four rulings are answered, Track T is empty; the
walk convention applies.
