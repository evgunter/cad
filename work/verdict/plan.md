# VERDICT — the plan

the gates props answers through: the size and coincidence bands, the epsilon vocabularies, the door identity checks

Opened 2026-09-20 by PROPS's priority-seam cut
(`work/README.md`, Track size). Nothing dispatched.

## The slate

**32 budget points** of dispatchable work against a ceiling of 30.

| pri | item | cost | title |
|---|---|---|---|
| P1 | `blend-payloads-outside-the-margin-family` | D | blend payloads outside the seven-field margin family: two dropped margins and the sweep's third blind spot |
| P1 | `blend-size-gate-unmetered-under-epsilon` | D | blend size gate: a positive size under the band's zero still reaches a false-fact refusal at both doors |
| P1 | `coincidence-zone-priced-budget-at-the-floor` | H | The coincidence zone of a magnitude slot refuses DegenerateExtrusion on every sub-box yet is bisected to the depth floor and priced Budget |
| P1 | `d2-addendum-has-no-row-for-an-admitted-input-answered-wrongly` | D | design: the D2 addendum has no row for an input that is ADMITTED and answered wrongly, which is what issue 1598 was for three months |
| P1 | `m6-sense-gate-recorded-residuals` | D | M6-6 sense-gate residuals — the recorded classes outside the gate, with their flip conditions |
| P1 | `pair-doors-outside-the-three-do-not-check-document-identity` | D | Beyond product/assemble/placement, (document, evaluation) doors do not check the pairing, and the three that do spell the predicate three ways |
| P1 | `props-curved-carries-two-readings-of-d9-unreachable-vs-poison` | D | props/curved.rs answers a kernel-bug-only state two ways — unreachable! at the wedge helper, a poison value at unreachable_zero "rather than panicking (D9)" |
| P1 | `props-two-eps-vocabularies-five-sites` | D | props/ — S25's two-epsilon-vocabularies class is open at three signatures and two call sites |
| P1 | `the-scalar-lift-convention-mints-doors-faster-than-consumers` | D | Measured: of the eight map_scalar rungs scalar_lift.rs's convention names, one has a production consumer outside its crate; both top rungs have no non-test consumer anywhere |
| P2 | `mc-lanes-draws-are-not-reproducible-from-outside-the-crate` | E | the MC lane's draws are not reproducible from outside editor-core |
| P2 | `part-per-node-logs-dropped-with-nested-evaluation` | D | A part's per-node verdict and escalation logs are dropped with its nested Evaluation |
| P4 | `k-lint-roster-wants-a-kernel-side-vocabulary` | D | k-lint's roster pin parses kernel source because the fence forbade the two cheaper shapes |
| P4 | `should-classify-replays-error-enum-arms-be-deleted` | E | Should classify_replay's two error-enum arms be deleted? The precondition is discharged; the measurement is not |

## Order

`d2-addendum-has-no-row-for-an-admitted-input-answered-wrongly` first,
because it is the rule the rest of this slate is measured against and
because its absence is what let issue 1598 sit for three months. It is
a design PR, not a code unit.

Then the two blend gates together (`blend-size-gate-unmetered-under-epsilon`,
`blend-payloads-outside-the-margin-family`) — one family, two doors —
and `props-two-eps-vocabularies-five-sites`, which is the collapse
several other rows are waiting on.

## Review posture

OPEN, for this program's first dispatch. PROPS inherits protocol v7
(`docs/MODEL-AB-LOG.md`, Ev 2026-09-19): the dual on triaged-in units
only, opus/opus outside it. Nobody has re-asked the triage question for
this slate, so the first orchestrator answers it here rather than
inheriting an answer.
