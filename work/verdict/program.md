---
id: verdict
kind: program
title: VERDICT — the gates props answers through: the size and coincidence bands, the epsilon vocabularies, the door identity checks
status: open
opened: 2026-09-20
area: kernel
prefix: verdict/
tag: (VERDICT orchestrator)
ab_band: 9500-9599
paths: [crates/geom-core/src/k_stats.rs]
keep_out: [opened by PROPS's 2026-09-20 priority-seam cut (Ev, in chat) per work/README.md Track size - PROPS measured 108.5 budget points and was cut into tracks meant to run in PARALLEL (Ev, in chat: for these high priority tracks it is ideal to have several components that can be worked on in parallel), the siblings are PROPS NURBS LINALG VERDICT and they share their parent's territory by design - shared ground is legitimate by the README's 2026-09-20 rule and what is owed is awareness while a lane is LIVE, so run scripts/work.py territory on your branch and announce the seam in the PR rather than drawing a fence, ENCL and STACK hold the certified-enclosure half, QUAD owns props/quad.rs, SSI and PCERT own the geom-brep certification lanes, PORT took the four refusal rows whose class is the crate boundary]
priority: P1
---
The gates and vocabularies a property answer passes through on its way
out, and the places two of them disagree. A positive size under the
band's zero still reaches a false-fact refusal at BOTH blend doors; the
coincidence zone of a magnitude slot refuses `DegenerateExtrusion` on
every sub-box yet is bisected to the depth floor and priced `Budget`;
`props/` carries two epsilon vocabularies across three signatures and
two call sites; and `props/curved.rs` answers one kernel-bug-only state
two ways — `unreachable!` at the wedge helper, a poison value at
`unreachable_zero` "rather than panicking (D9)".

The design row is the one to read first: **the D2 addendum has no row
for an input that is ADMITTED and answered wrongly**, which is the
shape issue 1598 had for three months and the shape most of this
program's siblings keep finding.

Charter and order: `work/verdict/plan.md`; narrative in `work/verdict/log.md`.
