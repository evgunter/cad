---
id: D98
kind: unit
title: unit_segment clamps a degree it could refuse, and the claim licensing the clamp is the wrong claim
status: closed
opened: 2026-08-21
track: N
parent: CERT-N3
closed: 2026-09-04
---

## What

`unit_segment` clamps a degree it could refuse, and the claim licensing the clamp is the wrong claim.

## Was

Track E.

## Finding

No live finding section; provenance only. The row cites no `S<n>` and no surviving finding in the document mentions `unit_segment`; the sentence above is the whole record.

## Fence

Track N — `geom`, and the spline and linalg substrate. **Fence:** `crates/geom/src/`, `crates/geom-core/src/{spline/,linalg/}`. **Block:** `D240`–`D259` / `S310`–`S329`.

## Closed

Landed by CERT-N3 as D2-addendum row 0: `KnotVector::unit_segment(degree: NonZeroUsize)` — degree 0 is not a spellable argument, so nothing is clamped and nothing is refused. A `compile_fail,E0308` doctest on the constructor pins the boundary (red against the clamping signature, green after), and `geom-core/tests/unit_segment.rs` pins the Bezier vector against `clamped()` at every degree. Every caller names its degree by type; the two placeholder payloads write `NonZeroUsize::MIN`.
