---
id: D31
kind: unit
title: sweep::skin::make_compatible and geom::curves::fit::deviation_from are one routine — home it in spline/algebra.rs
status: closed
opened: 2026-08-21
track: N
parent: CERT-N3
closed: 2026-09-04
---

## What

`sweep::skin::make_compatible` and `geom::curves::fit`'s `deviation_from` are ONE routine in two crates, and the proposed home is `geom-core/src/spline/algebra.rs`. **The `sweep/src/skin.rs` call site is this row's.**

## Was

Track E.

## Finding

The row cites no `S<n>` by number; its finding is the corresponding row of the `S18` roll-up (*Certified numeric derivations duplicated across crates*, confidence sure for each row):

> **The union-and-refine routine built on the interior-knot query** — 2 crates. `sweep::skin::make_compatible` and `geom::curves::fit::deviation_from` are **one routine written twice**: each unions two knot vectors' interior runs and refines both to the union. Giving the *query* one home (`spline/knots.rs`'s `runs_in`, behind `interior_knots()` / `knot_runs()`) did not close the routine built on it — the two only became legible as one once both opened with the same expression (**D31**). Anchor: `sweep/src/skin.rs`, `geom/src/curves/fit.rs`; proposed home `geom-core/src/spline/algebra.rs`.

**S18 Verdict:** ACCEPTED (Ev, 2026-08-18) — see S16. "They should certainly be unified." S18's postmortem is row by row and has no entry for this row (it was added after the postmortem); the postmortem's standing lesson for the class is that *"don't lift the unmerged sibling's machinery" is correct lane hygiene and becomes permanent duplication unless the block that creates it also books a unify-after-both-merge item.*

## Fence

Track N — `geom`, and the spline and linalg substrate. **Fence:** `crates/geom/src/`, `crates/geom-core/src/{spline/,linalg/}`. **Block:** `D240`–`D259` / `S310`–`S329`.

## Closed

Landed by CERT-N3: one routine, two callers. `geom_core::spline::algebra::union_refinements` (built on the typed `interior_knot_runs`) is the structure half; `NurbsCurve{2,3}::refine_to_union` applies it; `sweep::skin::make_compatible` and `fit::deviation_from` both call it, each keeping its own error mapping. Measured bit-identical to both retired spellings on a corpus with rational sections and mismatched multiplicities (32/32 pairs, 5/5 sets, 3,211 float components; the public fit and skin doors byte-identical end to end).
