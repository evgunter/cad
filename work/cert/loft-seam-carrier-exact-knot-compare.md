---
id: loft-seam-carrier-exact-knot-compare
kind: issue
title: loft_body refuses at degree ≥ 2 on generic parameterizations: pcurve_cache compares the seam carrier's knots/weights by exact equality
status: open
opened: 2026-08-30
github: 1316
refs: [1314]
---

## From GitHub issue 1316

Opened 2026-08-30; 1 comment.

(S-CERT orchestrator) Found by CERT-5 (PR 1314) while building its lily-leaf flip fixture; not that unit's ground to fix (`pcurve_cache.rs` is the PCURVE-adjacent seam CERT-8's layering review is told to treat with P-2's resume state in view).

`loft_body` refuses with `Pcurve(Certify { IsoUnsupported: "the seam carrier is not the chart's own boundary row (its knot/weight structure differs)" })` at `HalfEdgeKey(11v1)` on generic parameterizations at degree ≥ 2. Reproducer: identical `arc_section` sections stacked at *n* uniform heights — builds at n = 3, 5, 6, 9, **refuses at n = 4, 7, 8**; non-uniform heights refuse at every n ≥ 3. Mechanism: `pcurve_cache.rs:3444` compares knots/degree/weights by **exact** equality, and a degree-≥2 skin fit *solves* for its weights, returning them equal only to rounding — so whether the door opens depends on whether the fit's arithmetic happens to reproduce bits.

`sweep_body` (rotating frames) is unaffected — which is why CERT-5's lily blade builds. The refusal is D2-addendum row-2-shaped (valid input the lane could serve, refused on a bit accident); whoever takes it decides between honest structural comparison at a stated tolerance with an argued soundness story, or making the skin fit reproduce the chart's own boundary row exactly. Classification owed by the taking unit.

## Comments

**2026-08-31** — comment:

(S-CERT orchestrator) Measured pocket examples from CERT-5's review and fix pass (PR 1314) — the buildability pocket is substantially narrower than this issue's original examples suggested. Arc-bearing profile, pure-translation `loft_body`, measured on PR 1314's final head:

- **Degree 1**: builds at n = 3..9, both spacings.
- **Degree 2, uniform heights**: builds ONLY at n = 3, 5, 6, 9; refuses at 4, 7, 8.
- **Degree 2, non-uniform**: builds ONLY at n = 4.
- **Degree 3**: refuses at every n ≥ 4.

The refusal is `pcurve_cache.rs:3444`'s exact-equality knot/weight compare, as filed. `sweep_body` (rotating frames) is unaffected — the lily blade builds at n = 9, degree 3. Practical consequence recorded during CERT-5: off-grid interior u knots are effectively unreachable through the native loft door outside this pocket, so exercising that axis needs the import door or patch-level fixtures.

---
_Generated by [Claude Code](https://claude.ai/code)_

## Home

`work/cert/` — PCURVE is closed and the issue routes its residue to CERT-8's layering review, which is told to treat this seam with P-2's resume state in view.
