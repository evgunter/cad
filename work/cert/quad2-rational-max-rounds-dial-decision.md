---
id: quad2-rational-max-rounds-dial-decision
kind: issue
title: QUAD2_RATIONAL_MAX_ROUNDS 7→8 would flip dm1 first-class and two shipped refusals — a dial decision owing a row-by-row refusal argument
status: open
opened: 2026-08-30
github: 1315
refs: [1314]
---

## From GitHub issue 1315

opened 2026-08-30, 1 comment.

(S-CERT orchestrator) Filed from CERT-5 (PR 1314), which measured the lane to this decision and correctly declined to take it unilaterally.

After the knot-aligned cells, dm1's residual is **1.5435e-6 against the 1.024e-6 target — a factor of 1.51**, with a per-round trace converging at ratio 3.99 (textbook quartering, no floor). One more schedule round (`QUAD2_RATIONAL_MAX_ROUNDS` 7→8) would land ~3.9e-7 and flip `WILD_IMPORTS` 9→10. It would also start certifying two shipped refusals: quarter-torus (1.146e-6) and the two-span half cylinder (1.3035e-6 — an ordinary metre-scale part the docs call out as missing by 27%).

Cost: 4× worst case for composite-arm patches, but only ~2× for `w`-uniform-in-v patches (v cells are O(1) after CERT-5), and bodies that used to grind all 7 rounds now often certify early.

What the decision owes before anyone turns the dial: a row-by-row argument for the refusals that would narrow (per the D2 addendum's posture — a widened admission set is a refusal-set change even when every new admission is correct), and re-derived floors/doc tables. S-CERT's fence; natural homes are CERT-5's fix pass (if its dual review wants it) or a small follow-up unit.

## Comments

**2026-08-31** — S-CERT orchestrator:

(S-CERT orchestrator) Correction to this issue's figures, from CERT-5's dual review and fix pass (PR 1314). The 1.5435e-6 residual this issue was filed with was measured mid-development (after the hull blocks were knot-aligned, before the shared area rule was — the meter's *denominator* moved; flux width proved bit-identical across that change). On the PR's final head the authoritative numbers are:

- dm1 residual **1.8849954892592344e-6** against the 1.024e-6 target = **1.84×** (both blinded reviewers independently measured 2.0797e-6 on the review head; the fix pass's area-hull intersection then tightened the denominator again to 1.885e-6).
- Per-round trace still quarters cleanly (2.0624e-2 → … → 1.885e-6, ratio → 3.99): no floor.
- One more schedule round (`QUAD2_RATIONAL_MAX_ROUNDS` 7→8) ⇒ ~4.7e-7, clearing the target with ~2.2× margin — slightly better than this issue originally stated.

The decision this issue frames is unchanged: the dial still owes the row-by-row refusal argument before anyone turns it.

---
_Generated by [Claude Code](https://claude.ai/code)_

## Home

`work/cert/` — the rational-patch quadrature dial is in S-CERT's `crates/geom-brep/src/props/*` ground, the issue names S-CERT's fence, and it was filed from CERT-5.
