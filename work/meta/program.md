---
id: meta
kind: program
title: META — the tracker and the process instruments
status: open
opened: 2026-09-04
area: infra
prefix: meta/
tag: (META orchestrator)
ab_band: 2800-2899
paths: [scripts/work.py, scripts/ci-pin.py, work/README.md, docs/prompts/*, docs/MODEL-AB-LOG.md, docs/DOC-LEDGER.md]
keep_out: [CIW amends docs/prompts/implementer-discipline.md SS2 (what a run gates, which trailer means what) whenever its own runs change and does not wait on this program - CIW's keep_out already cedes scripts/work.py to the tracker in the other direction, so the fence is written on both sides, every program writes its OWN rows and ordinal claims into docs/MODEL-AB-LOG.md by its band and this program never edits another program's row - what it owns there is the file's rules and rosters (the band allocation entry, the protocol sections, the stopping rule), docs/DOC-LEDGER.md sweep entries are written by the program being swept at its own exit - this program owns the ledger's format and its accuracy, not the entries, no program's plan.md or log.md or item files are this program's to edit - a stale citation in another program's slate is routed to its owner and never fixed across the fence (that is the one-file-one-item rule and stale-track-t-citations-in-fillet-and-cert is the standing instance), scripts/gates/* is code-quality Track K's and scripts/ci-filter.py is S-TCOST's, the registers this program custodies (m6-carried-items-register, decide-flagged-dimensional-debt-inventory) have rows that are OTHER programs' work - custody means keeping them accurate and routing rows, never executing one]
---

The tracker's own instruments, which the 2026-09-03 cut left unowned
while cutting eleven tracks with them: `scripts/work.py` and the
`territory` check, `work/README.md`'s contract, the implementer and
reviewer briefs in `docs/prompts/`, the A/B ledger's rules, and
`docs/DOC-LEDGER.md`'s format. Plus custody of the two cross-program
registers no fence owns. Class E at its core with two `[ev]` rulings,
one of them overdue by about nine dispatches. Charter and unit order:
`work/meta/plan.md`; narrative in `work/meta/log.md`.
