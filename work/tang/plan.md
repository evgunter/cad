# TANG — declared tangency, germ and pierce, the pinch machinery (plan)

**STATUS: OPEN, awaiting its first sitting.** Opened 2026-09-20 by
CURVED's cut (Ev, in-chat). Live state is `work/tang/log.md`'s tail
and the item files beside this plan, never this file.

Branch prefix: **`tang/`** — unit branches `tang/<unit>-<slug>`,
orchestrator branch `tang/orchestrator`. Away-channel tag
`(TANG orchestrator)`. A/B ordinal band **TANG = 6100–6199**, recorded
in `docs/MODEL-AB-LOG.md`'s banding entry in the opening commit.

## Charter

A declared kiss or cusp between two curved operands is decided by the
residual-sign story the DEV-1 set names, not by a witness the lane
happens to find; a pierce ring joins on every carrier it can be minted
on; the equal-radius intersecting family (valence 4) has a design.
Every unit here begins as a design conversation with Ev — `[ev]`
spec PRs — because each one either revises ratified ground (DEV-1,
#131's cusp ruling) or opens a shape the kernel has no precedent for.

## Order (as inherited from CURVED's plan, 2026-09-20)

1. `m9-3-semantic-residues` items 4–5 (contfp's pre-pass order; move
   `tangent_locus` out of `rest.rs`) — E–M, the lane's opener.
2. `dev1-cylinder-sphere-circle-locus-arm` (D: revise the DEV-1 set;
   the residual-sign story is the gate).
3. `declared-cusps-second-order-wedge-arm` items 3–4 (#131's ruling).
4. `torus-declared-rest-lane-banked` (the 0/2π kissing arm; lily wall
   1's retirement path — REACH's `torus-operand-gate-admission` is the
   other half, coordinate at dispatch).
5. Germ and pierce: `arc-aware-point-in-loop` (with #1077) →
   `pierce-ring-has-no-join-arm` → `boolean-refuses-on-arc-carrier-not-arc`
   (door 2: a curve×curved-surface root finder and a curved-face ring).
6. `pinch-carrying-machinery-valence-4` (D→H: spec first; parked on
   SEAT-6, the parameter-identity channel).
7. The spiric's C9 ring `sqrt` question (CURVED's exit residue: the
   two-oval regime's ring configuration, `docs/CURVED-SPIRIC-DESIGN.md`
   §0a "(A) then (B)") lands here as an `[ev]` conversation if CURVED
   closes before it is asked.

## Review posture

Protocol v7 (`docs/MODEL-AB-LOG.md`, Ev 2026-09-19). Every unit here
that changes a decision is a protocol unit by v7's second criterion
(broad or hard-to-reverse); the E-class riders run opus/opus outside
it. Blocks draw from 6100 on.

## Exit shape

The DEV-1 set is revised or reaffirmed by ruling, the kiss/cusp arms
land under it, the pierce ring joins, and the pinch design is ratified
or parked by ruling; the walk convention applies.
