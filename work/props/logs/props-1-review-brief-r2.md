# PROPS-1 review brief — lane r2 (stored before dispatch; identical to the other lane's modulo this name)

You are one of two independent blinded reviewers of PR #1918 (unit PROPS-1 of the PROPS program) in evgunter/cad, a B-rep CAD kernel in Rust. Your worktree is /home/user/lanes/review-p1-r2, detached at the frozen head acc0719ad2ebffee3f8e2792fa9b911c6ec2fd7b; the diff under review is `git diff origin/main...HEAD` there. Work ONLY in that worktree; never edit /home/user/cad or any other /home/user/lanes/* directory. Do not edit the reviewed files; probes you write go in NEW files under crates/geom-core/tests/ (or the consumer crate's tests/) in your worktree and are reported, not pushed. You have no access to the other reviewer's report and must not look for it; do not post anything on GitHub. Never state which model you are and do not speculate about the implementer's.

Read first, in full, by path: /home/user/cad/docs/prompts/reviewer-style-lane.md (the questions and the confidence vocabulary; name the questions you exercised), /home/user/cad/docs/PROPS-1-SPEC.md (the binding spec), the PR body via mcp__github__pull_request_read (get, owner evgunter, repo cad, pullNumber 1918), /home/user/cad/docs/DUAL-DESIGN.md §DL6, /home/user/cad/memories/output-stability-as-justification.md, and the reviewed files at HEAD: crates/geom-core/src/linalg/{frame.rs,vec.rs,point.rs}, crates/geom-core/tests/props1_evidence.rs, crates/geom-core/tests/bounds_census.rs, work/props/PROPS-1.md.

Environment (part of the brief): CARGO_TARGET_DIR=/home/user/review-p1-r2-target and CARGO_INCREMENTAL=0 on every cargo command; debug profile only; build one crate at a time (`-p geom-core --features interval` first; consumer crates only for the claims that need them) and run `df -h /` before each build — if under 3 GB free, stop building and report what you could not execute rather than filling the disk. Two other lanes share this 4-core box; expect slow builds; foreground, one command at a time; a job that could outlive 600 s runs setsid-detached to a log and is polled in the foreground; never end your turn with background work running.

Claims to falsify (execute where a probe can decide them):
- C1 SOUNDNESS. The new mirror translation n̂·(2·(n̂·q)) and the new rejection (onto × self) × onto / |onto|² ENCLOSE the true reflection / rejection for every point of a wide input box at Interval, in both lanes, on inputs the unit did not draw (random normals incl. near-axis and near-degenerate, anchors at mm/m/100 m scale, wide and exact). The pins in props1_evidence.rs are the unit's evidence; find inputs they do not cover.
- C2 THE TABLE. Reproduce the evidence table's headline numbers ([0,0,4e-9] for the exact-axis mirror at half-width 1e-9; [2e-9,2e-9,0] for the axis rejection; the 1.0–1.12× wide-normal reading) from the committed instrument; say whether the corpus literals and the printed digits agree with the PR body.
- C3 DRIFT. "No committed expectation moved" — falsify by executing the consumers: pncad-py's test_placed_union.py mirror rows (python suite), sweep offd_r1_probes, topo m3_pr2_reduce, mesh planar chart rows, editor-core mate/coset rows, and the hosted render lanes' verdict on the PR; and check whether any reject_from or mirror output is STORED as description data anywhere (a plane/cylinder u_ref, a placement, a stored radial in replace_face.rs:695 / offset.rs:192) such that a content key or STEP golden could move. The unit answers "mesh::planar::ChartFrame is a private f64 tessellation chart bearing no key" — is that the whole answer?
- C4 THE WEAKENED CLAIM. project + reject = self was exact and is now ≤4 ulp of the largest component. Is 4 the right number (measure adversarially: large |onto|, tiny |self|, near-parallel), and does any consumer re-sum the pair expecting self (editor-core/src/mate/coset.rs, topo/src/replace_face.rs, geom-brep/src/offset.rs)?
- C5 THE SWEEP. The reading sweep's hit list over crates/geom-core/src/linalg/ (8 files): the new hit at vec.rs:449 (orthonormal_basis's s − s·(y²·r) mentions s twice) — correct, and correctly filed as a rider rather than fixed? The "not the shape" dispositions (normalize irreducible; Mat3::inverse reuses the determinant for bit identity; project_onto's three mentions of onto) — falsify any. Is svd.rs / lsq.rs genuinely concrete f64?
- C6 DOCS. The rewritten doc comments state the present evaluation order (D9) and the invariant only — no history, no retired-spelling archaeology (discipline §4) — at mirror_across_plane, reject_from, and both lerp paragraphs; the lerp paragraph's Interval cost (2·width(self) at t = 1, exact at t = 0) is true.
- C7 OUT OF FENCE. The PR ports a bounds_census roster line for profile's anchor_span as HandedOff to Track V, worded from that door's doc. Verify the premise (is bounds_census's every_sole_bracket_bound_door_is_in_the_roster red on main at the interval lane without it?), and judge whether a roster line ported by another program is a sound ratification under the census file's own contract.
- C8 D2-ADDENDUM and ε. "No variant retired, no refusal added, no ε posture change" — true?

Report back in ≤150 lines: verdict on the four-term ladder (APPROVE / APPROVE-WITH-FIXES / NOT-MERGEABLE-AS-IS / REJECT); findings as MAJOR / MINOR / NOTE, each one line of prose with a file:line; the CODE QUALITY REPORT rubric (counts MAJOR/MINOR/NOTE; spec deviations reported vs silent; ratings 1–5 with one line of evidence each for idiom/structure, test quality, doc/comment honesty); which style-lane questions you exercised; every probe file you wrote (path, result) so the useful rows can be adopted as ordinary tests.
