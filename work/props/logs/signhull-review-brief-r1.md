# PROPS sign-hull review brief — lane r1 (stored before dispatch; identical to the other lane's modulo this name)

You are one of two independent blinded reviewers of PR #2468 (the PROPS program's sign-hull unit `interval-orthonormal-basis-sign-hull`: `Vec3::orthonormal_basis` no longer transfers a sign through `copysign` — it crosses the normal with a world axis chosen by a comparison on the normal's own components and normalises, through a new `Real` door `select_le_zero(d, when_le, when_gt)` implemented on `f64`, `Interval`, `Dual`, `Sym` and the `k_stats` probe; every stored `u_ref` in the tree re-blessed with a locus receipt; M10-5's planar re-chart in `editor-core::clearance` retired) in evgunter/cad, a B-rep CAD kernel in Rust. Your worktree is /home/user/lanes/review-sh-r1, detached at the PR's frozen head c6f5d7aea220a5bf0578f10c60ab4c28bd5a2772; the diff under review is `git diff 0312083aa3891955ebd322787eea407bfc7df71c...HEAD` there (47 files, +2174 −1484; `0312083aa3891955ebd322787eea407bfc7df71c` is the merge base with main). Work ONLY in that worktree; never edit /home/user/cad or any other /home/user/lanes/* directory. Do not edit the reviewed files; probes you write go in NEW files under the relevant crate's tests/ in your worktree and are reported, not pushed. Your private scratch directory is /home/user/review-sh-r1-tmp; do not use the session scratchpad under /tmp. You have no access to the other reviewer's report and must not look for it; do not post anything on GitHub (mcp__github__pull_request_read is the only GitHub tool you use). Never state which model you are and do not speculate about the implementer's.

Read first, in full, by path: /home/user/cad/docs/prompts/reviewer-style-lane.md (answer its questions with judgement; name the ones you exercised; use its confidence vocabulary); the binding spec /home/user/lanes/review-sh-r1/docs/PROPS-SIGN-HULL-SPEC.md — its ruling (option 1: a decided world axis, no sign transfer — Ev's, on #1944) is settled; **the spec's particular comparison (smallest-magnitude component, ties to the highest index) is NOT what shipped, and that deviation is the first thing you judge (C1)**; the PR body via mcp__github__pull_request_read (get, owner evgunter, repo cad, pullNumber 2468) — read §Deviations first; /home/user/lanes/review-sh-r1/work/props/interval-orthonormal-basis-sign-hull.md in full (the four measurements of #1939 and both ruling sections); the reviewed code at HEAD: `crates/geom-core/src/linalg/vec.rs` (`orthonormal_basis` and its rows), `crates/geom-core/src/real.rs` (`select_le_zero`'s contract), the impls in `interval.rs`, `dual.rs`, `sym.rs` (+ `sym/{algebra,report}.rs`, the three-child node), `k_stats.rs`; `crates/editor-core/src/clearance.rs` (the retirement; the `refines` door that stays); `crates/geom-brep/tests/onb_wall_frame_interval.rs` (the red-first row); the census instruments `crates/{editor-core,step-export}/tests/onb_wall_normal_census.rs`, `crates/step-import/tests/onb_wild_normal_census.rs`; `crates/editor-core/src/node.rs` (`Datum::FaceFrame::spin`'s doc line); the four filed rows the PR names under `work/{shell,blend,bool,fix}/`.

Environment (part of the brief): CARGO_TARGET_DIR=/home/user/review-sh-r1-target, CARGO_INCREMENTAL=0, TMPDIR=/home/user/review-sh-r1-tmp on every cargo command; debug profile; `-j2`; ONE crate at a time (geom-core first, both feature lanes — default and `--features interval`; then geom-brep, editor-core, step-export by the touched suites' names, never whole workspaces); `df -h /` before EVERY build — this 4-core box runs four review lanes at once: under 3 GB free, stop building and report what you could not run. Foreground, one command at a time; a job that could outlive 600 s runs setsid-detached to a log under your scratch directory and is polled with a timed foreground loop (`sleep` may be blocked; use a timed `until` loop); never end your turn with background work running; never park on a waiter.

Claims to falsify (execute where a probe can decide them):
- C1 THE COMPARISON (the deviation). Shipped: `b1 = normalize(e_z × n)` when `|n.z| ≤ max(|n.x|, |n.y|)`, else `normalize(e_y × n)`; seam on the 45° cone. The spec's rule (smallest-magnitude component, ties to the highest index) put the seam through every axis direction and, at `Interval` over an ε-box, hulled two prism walls by a quarter turn, blew M10-5's pair budget and refused DOCM-1's cap frame — the PR's census says 778 of 817 corpus faces sit on that rule's tie set against 20 on the shipped seam. (i) Reproduce both numbers on the three corpora. (ii) The 20 are `die_chamfer`'s sixteen 45° chamfer faces and four wild faces: on `main` (Duff's construction) those faces had `n.z` definitely nonzero and were EXACT at `Interval`; on this head they sit ON the seam and hull. Measure what a 45° chamfer face does now at `Interval` through the consumers that matter — M10's clearance engine with the re-chart retired (`clearance::window_of`, the `refines` door), DOCM's derived frames — on `die_chamfer` or a chamfered cube: a face that was served on main and refuses or hulls on this head is a MAJOR REGRESSION and the headline. (iii) Then judge the rule itself: is a comparison `|n.z| ≤ ρ·max(|n.x|, |n.y|)` at some ρ ≠ 1 (a seam at an elevation CAD does not use — say, atan(1/2)), or any other decided order, strictly better on this corpus and on the axis directions? Measure the corpus at a few ρ. The orchestrator adjudicates the rule with Ev in view; your measurement decides it.
- C2 THE DOOR, PER IMPL. `f64`: total, bit-deterministic, both signed zeros one tie. `Interval`: the ENCLOSURE PROPERTY — for every `f64` normal inside an `Interval` normal, the `f64` frame lies inside the `Interval` frame, at decided comparisons, at the point tie `[0,0]` (which DECIDES — argue why that is sound when the `f64` replay saw `−0.0`), and at a straddle (hull of both candidates at `Def`, never `Trv`/empty). Attack with random boxes straddling the cone. `Dual`: the tangent follows the read arm; at a straddle the tangents hull (`min_deriv`'s convention) — is the closed form `b1' = v'/‖v‖ − v(v·v')/‖v‖³` pinned on both arms? `Sym`: the first three-child node — do node ids and the atom-key hash distinguish a three-child `Select` from every two-child shape (a collision is a MAJOR)? `Probe`: delegates bit-for-bit.
- C3 DL6 AND POISON. "Poison flows from the decision always, from a candidate only through the arm read": verify at `Interval` and `Dual` that the unread arm's `normalize(0)` never leaks (the cap `±e_z` on the `e_y` arm; a straddled cone with a near-zero candidate); "unbounded only where the box contains the zero vector" — measured in `examples/r2_onb_dl6.rs`; try to refute.
- C4 THE RE-BLESS RECEIPTS. 18 STEP fixtures: 51 `DIRECTION` records moved across 11 files and every plane's `(origin, normal)` bit-identical — reproduce record by record; the 15 evaluation digests' locus receipt (`exact_mass_pins_hold`, `corpus_evaluations_bit_equal_realized_vs_idealized` green untouched); `m4_pr6_golden` unchanged and WHY (`u_ref` derived, never stored); the two deleted files' justifications.
- C5 THE (c) FINDINGS. `min_clearance`'s `hi` reads `[0, 0.0258]` on a notched pair whose true clearance is 0.1 — the PR calls it pre-existing (a non-convex boundary's AABB window covers off-face points; which superset carries the spurious pair is coincidence). Is that right, or did THIS frame create the wrong `hi` where the old frame's rectangle was right by construction? Reproduce on main with a rotated frame if you can; if the new frame is what makes `hi` wrong on a common body, that is a MAJOR of this unit, not shell's. The non-unit posture change (orthogonal, not orthonormal) — is anything in the tree relying on the old "garbage" posture?
- C6 M10-5'S RETIREMENT. `in_plane_axis`, `chart_frame`, `unit_axis`, `Window::chart_axis`, `GeometryWitness::{a_chart_axis,b_chart_axis}` deleted; every `m10_5_*` row green on the stored frame; the `refines` door stays and no fixture reaches it — confirm; the public-field deletion's consumers (grep every crate, demos and tools).
- C7 RED-FIRST. The quoted red (`u_ref.z = [−1, 1]` on wall 1 at the merge base) reproduces in a scratch checkout with its own target dir; the green's claim "as tight as their own normal (within `4·width(n) + 4·ε`)" on all twelve, and the ε-box straddle column (0 undecided) — falsifiable?
- C8 THE SWEEP. Nine `copysign` sites: the four filed (revolve's `max` fold, blend's nappe selector, the fillet turn side, the fillet bulge apothem) really this shape? the four "keep" (cbrt, cone nappe, curved's membership distance, tangent_point's signed radius) really sign transfers? the svd one f64-only? The pattern's blind spot (`min`/`max`/`abs` weights) — probe one file the sweep could not see.
- C9 DOCS, POSTURE, LANDING. `vec.rs`'s constructor doc present tense (discipline §4); citations by name (§7); the `FaceFrame` doc line; no tolerance read moves; no trailer, no empty commit, no `Co-Authored-By`; `work.py lint`; run 34719540390 on the head (verify via get_check_runs); the four filed rows well-formed and on the right slates (`work.py territory`); the out-of-workspace roots compiled (a required trait method); the `TRIM3_MUTANT` incidental.
- C10 SHAPE (the style lane's own questions): a two-way `select_le_zero` as THE branching primitive beside `copysign` — the right door, or one door too many on `Real`? the three-child `SymNode` for one caller; the deleted witness fields; is the construction readable as the two-arm comparison it is?

Report back in ≤150 lines: verdict on the four-term ladder (APPROVE / APPROVE-WITH-FIXES / NOT-MERGEABLE-AS-IS / REJECT); findings as MAJOR / MINOR / NOTE, one line of prose each with a symbol-name citation (a line number may ride beside it); the CODE QUALITY REPORT rubric (counts; spec deviations reported vs silent; ratings 1–5 with one line of evidence each for idiom/structure, test quality, doc/comment honesty); the style-lane questions exercised; every probe file you wrote (path, result) so useful rows can be adopted as ordinary tests.
