# PROPS mignitude-floor review brief — lane r1 (stored before dispatch; identical to the other lane's modulo this name)

You are one of two independent blinded reviewers of PR #2469 (the PROPS program's mignitude-floor unit `offset-fit-mignitude-floor-on-norm-e`: `Composite::cell_bound` in `crates/geom-brep/src/offset_fit.rs` bounds `‖E‖` from below through the sign witness `D = Ẽ·M̃` — `‖E‖ ≥ |D|/(w̃·‖M̃‖)` — as the max of three lower bounds, so every offset certificate tightens or holds; 24 of a 70-request corpus moved, one door-level bound GREW by 1.8% through the schedule) in evgunter/cad, a B-rep CAD kernel in Rust. Your worktree is /home/user/lanes/review-mig-r1, detached at the PR's frozen head c8fd6403303b4ef929cce5e7d54dd8a649ab0203; the diff under review is `git diff 0312083aa3891955ebd322787eea407bfc7df71c...HEAD` there (6 files, +533 −130; `0312083aa` is the merge base with main). Work ONLY in that worktree; never edit /home/user/cad or any other /home/user/lanes/* directory. Do not edit the reviewed files; probes you write go in NEW files under the relevant crate's tests/ in your worktree (or a `#[cfg(test)]` module you add in a scratch copy of the crate under your scratch directory when a probe needs private items) and are reported, not pushed. Your private scratch directory is /home/user/review-mig-r1-tmp; do not use the session scratchpad under /tmp. You have no access to the other reviewer's report and must not look for it; do not post anything on GitHub (mcp__github__pull_request_read is the only GitHub tool you use). Never state which model you are and do not speculate about the implementer's.

Read first, in full, by path: /home/user/cad/docs/prompts/reviewer-style-lane.md (answer its questions with judgement; name the ones you exercised; use its confidence vocabulary); the binding spec /home/user/lanes/review-mig-r1/docs/PROPS-MIGNITUDE-FLOOR-SPEC.md (its ruling — the witness bound as the construction — is the orchestrator's and settled; you falsify the soundness, the rounding directions, the measurements and the receipts, not the choice of bound); the PR body via mcp__github__pull_request_read (get, owner evgunter, repo cad, pullNumber 2469); /home/user/lanes/review-mig-r1/work/props/offset-fit-mignitude-floor-on-norm-e.md and the two items the unit filed (`work/props/offset-fit-door-bound-is-not-monotone-in-the-cell-bound.md`, `work/props/offset-fit-stall-face-has-no-fixture.md`); the reviewed file `crates/geom-brep/src/offset_fit.rs` — the module doc's certificate decomposition (~70–200), `Composite`, `build`, `cell_bound`, the new in-module row `the_sign_witness_floors_norm_e_where_the_components_straddle_zero`, the loop's `measure`/marking (`cut = hull_sup * 0.5`) — and `crates/geom-brep/src/offset_meters.rs` (`mig`, `sqrt_down`, `sqrt_up`, `cell_normal`'s assemblies A/B/C), `geom_core::ring_interval::RingInterval` (division and multiplication rounding), `crates/geom-brep/tests/offset_fit.rs` and `tests/budget_faces.rs`, `crates/geom-brep/README.md` O3.

Environment (part of the brief): CARGO_TARGET_DIR=/home/user/review-mig-r1-target, CARGO_INCREMENTAL=0, TMPDIR=/home/user/review-mig-r1-tmp on every cargo command; debug profile; `-j2`; ONE crate at a time — geom-brep only unless a claim needs topo or sweep; `df -h /` before EVERY build — this 4-core box is shared with another lane and the disk margin is small: under 3 GB free, stop building and report what you could not run. Foreground, one command at a time; a job that could outlive 600 s runs setsid-detached to a log under your scratch directory and is polled with a timed foreground loop (`sleep` may be blocked; use a timed `until` loop); never end your turn with background work running; never park on a waiter.

Claims to falsify (execute where a probe can decide them):
- C1 SOUNDNESS AND ROUNDING. `‖E‖ ≥ |D|/(w̃·‖M̃‖)` is exact in ℝ; the certified reading needs `mig(D)` on the LOWER side, `w̃` and `sup‖M̃‖` on the UPPER side, the quotient's `.lo()`, every step outward in the ring, `.hi()` of the cell bound read once at the end. Read every rounding direction in the new lines and in `sqrt_up`'s use; then ATTACK by execution: on the micron row's grids (and a rational base, and a base far from the origin), sample `E = S_fit − S` densely inside each cell and compare the sampled minimum of `‖E‖` against the cell's `e_lo` — any cell where `e_lo` exceeds the sampled minimum by more than sampling resolution is a MAJOR (the bound would be unsound, and every tightened certificate with it). Also probe the witness on a cell where `D`'s hull is barely definite: does `mig(D)` near zero make the term harmlessly small, never negative or NaN?
- C2 THE MAX OF THREE FEEDS BOTH PLACES. `e_lo` (the divisor of `dist`) and `e_floor` (the `τ²/‖E‖` divisor) each take the max of the sound bounds available at that point; verify the order of evaluation (does `dist` use the new `e_lo`, and `e_floor` the max with `|d| − dist`?) and that nothing reads a stale componentwise value.
- C3 THE MEASUREMENTS REPRODUCE. The red (`e_lo = 1.5798e-8`, `hull_sup = 3.2219e-4` at round 4, the `1e-9` arm's `3.791e-7`) at the merge base (a scratch checkout under your scratch directory, its own target dir — a shared target reuses the base lib for the head, a measured hazard) and the green (`1.707e-5`, `e_lo/|d| = 0.5606`, factor 18.872x at round 4, 1.0098x at round 5) at the head; the term-by-term tables.
- C4 THE RE-BASELINE AND THE BOUND THAT GREW. Re-run the 70-request corpus (`budget_faces`' bases × 7 `d` × 5 tolerances) at base and head: the 24 moved / 46 unchanged split, the 807.7x, the seven face changes. The one that grew (bumpy `d = 1e-5`, `8.189454e-10` → `8.337390e-10`): the PR argues fixed-grid monotonicity (no cell rises) plus schedule reordering through `cut = hull_sup * 0.5`. Verify the cell-by-cell no-rise claim on the two grids by execution; then judge the filed item — is a marking rule that makes the door-level bound monotone in the cell bound available cheaply (an absolute cut, the union of markings), and should this unit have taken it? A second grown bound the PR did not list is a MAJOR.
- C5 THE FIT'S BITS. Reproduce the three FNV digests at base and head (knots + control + weights); confirm `interpolate_offset_grid` reads no certificate; run `mesh::r2_bytes` if any offset certificate feeds a digest line (say whether one does).
- C6 THE FACE ROWS. `RefinementStalled` now has NO fixture at any door — the budget-faces fix pass's verdict-before-budget ordering (its M1) lost its witness. Is the replacement row `a_single_non_improving_round_is_the_budgets_face_not_the_stalls` asserting what its name says (mutate the ordering in a scratch copy: does it red?), and can a stall fixture be found on this head with a modest search (bases, `d`, tolerances)? The `d ∈ {1e-8, 1e-9}` boundary row and the new bumpy nanometre row: falsifiable (a wrong face or a looser bound reds them)?
- C7 THE SWEEP. `offset_meters::cell_normal`'s "keep" (assemblies B and C are together-readings, no `D`-shaped witness of `m`): read it; is B's midpoint projection actually a sound lower bound on `‖m‖` as written? The scalar sites (`zero_free_lower_bound`, `exhaust.rs`, `enclose.rs`, `interval.rs`, `signed.rs`) — is any of them a norm assembly in disguise?
- C8 DOCS. The module doc's "what it did not buy" paragraph rewritten in the present tense (discipline §4, no history); the `BoundNotFinite` doc's removed "518-request search" receipts — was the removal right (the search was under the old bound), and does the variant's reachability statement still hold at the head (measure: does any request still reach `BoundNotFinite`? the PR says `d ∈ {1e-8, 1e-9}` do); the README O3 sentence naming `D`'s double duty (a re-wording because code moved, not a design change — agree?); the D2 sentence at the enum; discipline §7 (cite by name).
- C9 POSTURE. No tolerance read moves; no trailer, no empty commit, no `Co-Authored-By`; `work.py lint`; run 34719680314 on the head (verify via get_check_runs); the two filed items — well-formed, each a real finding, each on the right slate (`work.py territory`).
- C10 SHAPE (the style lane's own questions): the `ELow::{Componentwise, Witness}` switch — is it a test-only selector living in production code, and is there a cleaner shape (the in-module row computing the componentwise term itself)? `cell_bound` after the change: readable as the three-bound max it is, or a sequence of reassignments? The `M̃` channels kept on `Composite`: the right home, or should `build` produce `sup‖M̃‖` per cell once?

Report back in ≤150 lines: verdict on the four-term ladder (APPROVE / APPROVE-WITH-FIXES / NOT-MERGEABLE-AS-IS / REJECT); findings as MAJOR / MINOR / NOTE, one line of prose each with a symbol-name citation (a line number may ride beside it); the CODE QUALITY REPORT rubric (counts; spec deviations reported vs silent; ratings 1–5 with one line of evidence each for idiom/structure, test quality, doc/comment honesty); the style-lane questions exercised; every probe file you wrote (path, result) so useful rows can be adopted as ordinary tests.
