# PROPS k-stats review brief — lane r1 (stored before dispatch; identical to the other lane's modulo this name)

You are one of two independent blinded reviewers of PR #1969 (the PROPS program's k-stats unit `k-stats-escalation-channel-and-redo`: the verdict log redone as a bracket with a stack, and the escalation channel beside it) in evgunter/cad, a B-rep CAD kernel in Rust. Your worktree is /home/user/lanes/review-kstats-r1, detached at the frozen head e832a1fc284a249db178f15c3fcdc7b51fba54a7; the diff under review is `git diff origin/main...HEAD` there (34 files, +741 −219). Work ONLY in that worktree; never edit /home/user/cad or any other /home/user/lanes/* directory. Do not edit the reviewed files; probes you write go in NEW files under the relevant crate's tests/ in your worktree and are reported, not pushed. Your private scratch directory is /home/user/review-kstats-r1-tmp (create it); do not use the session scratchpad under /tmp — it is shared with another lane. You have no access to the other reviewer's report and must not look for it; do not post anything on GitHub. Never state which model you are and do not speculate about the implementer's.

Read first, in full, by path: /home/user/cad/docs/prompts/reviewer-style-lane.md (answer its questions with judgement; name the ones you exercised; use its confidence vocabulary), /home/user/cad/docs/PROPS-KSTATS-SPEC.md (the binding spec; the ruling — bracket with a stack, returned value measured and declined — is the orchestrator's and settled; you falsify the measurement and the construction, you do not re-open the ruling), the PR body via mcp__github__pull_request_read (get, owner evgunter, repo cad, pullNumber 1969), /home/user/lanes/review-kstats-r1/work/props/k-stats-escalation-channel-and-redo.md, and the reviewed files at HEAD: crates/geom-core/src/k_stats.rs, crates/editor-core/src/eval/mod.rs (the bracket at run_op and NodeValue/NodeError), crates/editor-core/src/eval/parts.rs (PartCache::get — the one hunk outside the spec's fence), crates/editor-core/src/drive.rs (classify_replay), crates/editor-core/tests/m10_3_driver_interval.rs, crates/editor-core/tests/asm2a_instantiate.rs, crates/editor-core/tests/m10_7_r2_probes_interval.rs, crates/geom-core/tests/k_stats_doors.rs.

Environment (part of the brief): CARGO_TARGET_DIR=/home/user/review-kstats-r1-target and CARGO_INCREMENTAL=0 on every cargo command; debug profile; one crate at a time (geom-core first, then editor-core — its `--features interval` test binary is the heavy one — then the converted tests in sweep/topo/profile/geom-brep/step-import by name, not whole suites); `df -h /` before each build — another lane shares this 4-core box: under 3 GB free, stop building and report what you could not execute. Foreground, one command at a time; a job that could outlive 600 s runs setsid-detached to a log and is polled in the foreground with a timed loop; never end your turn with background work running; never park on a waiter.

Claims to falsify (execute where a probe can decide them):
- C1 THE NESTING FIX UNDER MEMO REUSE AND CANCELLATION (the spec's first reviewer target). The unit says an instantiate node's log is the instantiate op's OWN decisions, identical on a part-cache hit and a miss (466 vs 466 on its fixture), and that the miss path's nested evaluation is shielded by its own bracket whose frame is DISCARDED. Construct the cases: a memo hit that never runs the op (what frame receives what, and is the log the memo's or empty?); a cancelled prefix (an evaluation cancelled inside a nested part: does the bracket pop, and does the instantiator's frame stay clean?); two instances racing under the parallel schedule (is the log schedule-independent — run the row several times and under both schedules); a part inside a part (two nested miss paths). Any decision recorded on the wrong node, or a log that differs between schedules, is a MAJOR.
- C2 THE ESCALATION CHANNEL'S COMPLETENESS (the spec's second target). Find an `Indeterminate` produced by a predicate that never reaches `classify` — a `sign_within` call outside the `Decide` funnel, an `Indeterminate` minted directly by an op (`Err(Indeterminate{..})` constructions in sweep/topo/profile/geom-brep), an escalation on a path that runs BEFORE the node's bracket opens (the profile pre-pass and the mate solve the unit names as finding 1). For each, say whether `drive::classify_replay` would still price it right (the two error-enum arms kept behind the channel) or mis-price it as `Bisect`/`Budget`; execute at least one.
- C3 THE RED-FIRST ROWS. Reproduce both reds against main's mechanism exactly as the body says (swap main's k_stats.rs in; run the nesting row; then the planted-flip row with classify unchanged) and the greens at this head. Check the sliver-mass arithmetic on `slab(20ε, 40ε)` independently: is 22.5 % the band's share at K = 10, and is the remaining 2.51 % `Budget` really the coincidence zone (finding 3) — measure it rather than accept it.
- C4 BIT IDENTITY OF THE VERDICT LOGS. The witness-vector keys and every vdiff golden are claimed byte-identical; the accounting goldens (`m10_6_accounting_{1e-6,1e-9,1e-12}` and `golden_r2/base_m10_6_accounting_*`) moved. Execute: the key goldens at all three ε rows; then judge whether the accounting move is entirely the acceptance's (the strips priced SliverTerminal) or partly the nesting fix's / the parts.rs shield's — the body claims the former. A moved key anywhere, or a golden re-blessed for a reason the body does not state, is a finding. Also: is re-cutting M10-7's `golden_r2/` "merge base's bytes" files (deviation 5) legitimate, or did the unit erase a differential another program relies on?
- C5 THE BRACKET AS A TYPE. `!Send` pinned by compile_fail (confirm the code the snippet actually emits at 1.97.0; stable rustdoc does not compare codes); `Drop` pops; the `ManuallyDrop` in `finish`; the mis-nesting rule (debug_assert; release discards above, never steals below) — construct the out-of-order case in a probe and check the release-mode claim; the rayon work-stealing argument (LIFO on the call stack ⇒ LIFO on the frame stack) — is it true for `join` AND for `scope`/`par_iter` as the eval actually uses them? Read how the eval spawns work.
- C6 THE MEASUREMENT THAT DECLINES THE RETURNED VALUE. 530 call sites / 261 enclosing functions / 104 pub signatures. Re-run the grep as stated; check the counting rule (doc lines excluded, nearest preceding `fn`); is the number honest and does it support "unaffordable", or is there a cheaper returned-value spelling the body did not consider (e.g. a sink threaded only through the ten `run_op` doors with the ops' inner calls unchanged)? Your judgement, not a re-ruling: the orchestrator's ruling stands either way, but a dishonest measurement is a finding.
- C7 DEVIATIONS AND FENCE. Five argued deviations (NodeError carries escalations; the parts.rs hunk; Ok-nodes-with-escalations bisect; goldens re-blessed; the M10-7 row's bless arm). Each: needed, argued at the door, and disclosed? Anything outside the fence NOT disclosed? `work.py territory --base origin/main` — 23 paths claimed, all announced; verify. The M10-8 keep-out: `git merge-tree` against origin/m10/m10-8-arc-family, and read whether any hunk touches the probe-gated half.
- C8 DOCS AND D2. The module doc's OPEN OBLIGATION paragraph: is it now discharged in present tense with the argument in the doc, not history (discipline §4)? The D2-addendum classification (row 0; dropped mid-op; panic; out of order; thread crossing) — every row pinned or stated as claimed? `vdiff`'s persisted-shape sentence and both `escalations` fields say "not persisted".
- C9 THE SWEEP. The thread-local hit table (CURRENT, SINK, FRAMES, SESSION, OPAQUE_SEQ) — complete for geom-core? Run the grep; check the blind-spot list (statics outside thread_local!, other crates) is honest; is `SINK`'s "not converted" the right call given the same install/take shape the verdict log had?
- C10 ε POSTURE AND CI. None claimed: confirm no tolerance read moved. CI is the full matrix (no CI-Config trailer); the python suite: run `cargo check -p pncad-py` in your target if the closure tier skipped it and report.

Report back in ≤150 lines: verdict on the four-term ladder (APPROVE / APPROVE-WITH-FIXES / NOT-MERGEABLE-AS-IS / REJECT); findings as MAJOR / MINOR / NOTE, one line of prose each with a file:line; the CODE QUALITY REPORT rubric (counts; spec deviations reported vs silent; ratings 1–5 with one line of evidence each for idiom/structure, test quality, doc/comment honesty); the style-lane questions exercised; every probe file you wrote (path, result) so useful rows can be adopted as ordinary tests.
