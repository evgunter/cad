# MESH-12 review brief — lane r1 (stored before dispatch; identical to the other lane's modulo this name; stored on the PROPS block branch because PROPS runs this S-MESH unit's dual at Ev's direction)

You are one of two independent blinded reviewers of PR #1617 (unit MESH-12 of the S-MESH program, finished under the PROPS program at Ev's direction: a sphere meridian span past the per-edge winding bound refuses typed at the parse under one name on every consumer and the half-turn clamp that folded such spans short is deleted; the rim-continuation coherence condition is measured dead through the import door and recorded so, with its one live Euler-door witness pinned) in evgunter/cad, a B-rep CAD kernel in Rust. Your worktree is /home/user/lanes/review-mesh12-r1, detached at the PR's frozen head 3daab7d807773f551d49e9cc6fa1079b4ebb0047; the diff under review is `git diff f729fbaf4c65ff239c2aba7d6c597c0f5f3fc401...HEAD` there (12 files, +780 −202; `f729fbaf4` is the merge base with main). Work ONLY in that worktree; never edit /home/user/cad or any other /home/user/lanes/* directory. Do not edit the reviewed files; probes you write go in NEW files under the relevant crate's tests/ in your worktree and are reported, not pushed. Your private scratch directory is /home/user/review-mesh12-r1-tmp; do not use the session scratchpad under /tmp — it is shared with another lane. You have no access to the other reviewer's report and must not look for it; do not post anything on GitHub (mcp__github__pull_request_read is the only GitHub tool you use). Never state which model you are and do not speculate about the implementer's.

Read first, in full, by path: /home/user/cad/docs/prompts/reviewer-style-lane.md (answer its questions with judgement; name the ones you exercised; use its confidence vocabulary); the binding spec /home/user/lanes/review-mesh12-r1/docs/MESH-12-SPEC.md (its rulings — refuse at the parse rather than fold, the OR-branch on the rim-continuation witness — are the spec's and settled; you falsify the construction and its claims, not the rulings); the PR body via mcp__github__pull_request_read (get, owner evgunter, repo cad, pullNumber 1617) — its gate record still names the older head 0e053a727; the head you review is the merge-forward of that head with main (three commits: the merge, a one-line fix at crates/geom-brep/tests/cert1_sphere_polar.rs:177, an empty trailer commit); /home/user/lanes/review-mesh12-r1/work/mesh/MESH-12.md, and the two issue files `grep -rl "1601\|1588" work/` finds; the reviewed files at HEAD: crates/geom-brep/src/props/curved.rs (`require_meridian_span_within_period` ~1634, `sphere_meridian_pole_margins` ~1705, `sphere_boundary` ~1804, `require_one_chart_branch` ~513, the torus arm ~1937-1985 and `du_of_rims`), crates/topo/src/coherence.rs (whole), crates/geom-brep/tests/{mesh12_saturated_span,cert1_sphere_polar,mesh11r1_probes,mesh11r2_probes,mesh11r2_base_probes}.rs, crates/topo/tests/mesh12_rim_row_reach.rs, crates/mesh/tests/mesh8_corpus_coherence.rs, docs/predicate-dimension-audit.md (the new row and the replaced paragraph), and certification's own decide in crates/geom-brep/src/ssi/certify.rs (`interval_span_winding`, `WindingExceeded`) plus MESH-10's torus precedent `props_meridian_pieces_winding`.

Environment (part of the brief): CARGO_TARGET_DIR=/home/user/review-mesh12-r1-target and CARGO_INCREMENTAL=0 and TMPDIR=/home/user/review-mesh12-r1-tmp on every cargo command; debug profile; `-j2`; one crate at a time (geom-brep first, both feature lanes — default and `--features interval`; then topo, mesh, step-import by the touched suites' names, not whole workspaces); `df -h /` before each build — three other lanes share this 4-core box: under 3 GB free, stop building and report. Foreground, one command at a time; a job that could outlive 600 s runs setsid-detached to a log under your scratch directory and is polled with a timed foreground loop (`sleep` may be blocked; use a timed `until` loop); never end your turn with background work running; never park on a waiter.

Claims to falsify (execute where a probe can decide them):
- C1 THE DECIDE IS CERTIFICATION'S, RE-DECIDED. `require_meridian_span_within_period` classifies `Margin::levered(τ − (t1 − t0), R)` against the run's band and is claimed to give the SAME dispositions as `certify.rs`'s `interval_span_winding` (the ladder: τ + 0.99·zero/R admits, τ + 1.01…9.9·zero/R escalates, τ + 10.1·zero/R refuses). Verify against certification's own arithmetic on the same spans (sign of the margin, the lever, the band edges, `Zero` vs `Positive` at the boundary) on both scalar lanes; a span certification admits that the parse refuses, or the reverse, is a MAJOR.
- C2 THE CLAMP'S DELETION IS SOUND ON EVERY ADMITTED SPAN. The helper's doc argues: an admitted span exceeds τ by at most `zero/R`; on it `cos(dt/2)` is within `(zero/2R)²/2` of −1; that can reclassify only a pole within `zero/2R` of the span's endpoint, whose chord is inside the band, so the fold folds and the door admits whatever the sign. Check the algebra, then probe it: admitted spans at τ + {{0.5, 0.99}}·zero/R with a pole placed at, just inside, and just outside `zero/2R` of the endpoint, comparing the fold's area/flux against the OLD clamped helper (rebuild it in a probe, or check out the merge base in a scratch copy) — any admitted span whose ANSWER changed is a MAJOR; an admitted span whose classification changed but whose answer did not is a finding to state.
- C3 EVERY CONSUMER FLIPS TOGETHER — completeness. The body names four consumers plus `require_one_chart_branch` (which does not run the parse and got the decide repeated). Enumerate every caller of `sphere_meridian_pole_margins`, `sphere_boundary` and `curved_face` across `crates/*/src` (mesh's `curved.rs`, topo's `validate.rs` and `props.rs`, the chart doors) and show each either runs the decide or provably cannot receive a span past τ; a fifth door that reads a raw span is a MAJOR. Is "one name" really one name — same `what` string at every site, and is a `NotIsoRectangle` the honest variant for a span defect at the branch door?
- C4 THE CERT-1 ROW RE-AIM. `a_multi_wrap_span_covers_both_poles` → `a_multi_wrap_span_is_refused_at_the_parse`: reproduce the claimed defect at the merge base (a `2π + 2δ` hand-built span folding short by `(1 − cos δ)/2`, 36 of 400 at δ up to 0.375) and confirm the 3π row passed on a `+0.0` residual; then judge the change's statement in the PR against discipline §3. The merge-forward's fix at `cert1_sphere_polar.rs:177` (`band()` for `Band::linear(Tol::witness())`) — same band?
- C5 THE RIM-CONTINUATION CONDITION IS DEAD THROUGH THE IMPORT DOOR — the claim that carries deliverable 3. The body's measurement (`pcurve_loop_continuity` refuses `R·Δv ≥ ε` before any props decide; the examination reports `R·Δv ≥ ε`; the intersection is one point the arithmetic misses by 3e-20 m) is recorded as PROSE ONLY — no committed row pins it, the STEP generator was a scratch script. Reproduce it at one ε row with your own generator (a sphere face whose rim row is two on-sphere circle arcs a band apart, junctions at the mean latitude), and try to BREAK it: do the re-mint's continuity margin and the coherence band's margin use the same lever and the same rounding, or is there a sliver `R·Δv` where import admits AND the examination reports? A constructible fixture refutes the record and is a MAJOR; a record that is right but unpinned by any row is a MINOR with the row you would add.
- C6 THE EULER-DOOR WITNESS ROWS (`topo/tests/mesh12_rim_row_reach.rs`): reports at 1.5ε and 1.9ε, quiet at 0.5ε, refused by the endpoint pin at 3ε, at three ε rows — are the rows falsifiable (mutate the coherence band or the gap on a scratch copy; do they red?), and is the shape-door/branch-door-admit vs flux-lane-refuse claim pinned with the actual variant names? The header cites issue 1615 (the census panic) — is the panic reproduced at Δv = 0 as claimed?
- C7 D9. The `mesh::r2_bytes` two-build digest is claimed byte-identical between the merge base and this head at three ε rows. Re-take it yourself (a scratch checkout of `f729fbaf4` under your scratch directory, its OWN target dir — a shared target dir reuses the base lib for the head, a measured hazard); any differing digest line is a MAJOR.
- C8 THE SWEEP. Discipline §5's hit list (the torus single-edge meridian span read raw into `sin_cos`; the rim Δu-sum span; `chart.rs:193`'s `clamp`; `unwrap_near`) — is it complete for the shape *a stored span read raw past the winding bound* over `props/curved.rs` and `topo/chart*.rs`? `chart_region.rs` was grepped, not read: read its span reads. Issue 1618 is claimed filed for the torus and rim siblings — find the file.
- C9 ε POSTURE, DOCS, LANDING. The audit row (`docs/predicate-dimension-audit.md:~332`) states key/band/lever truthfully; the retracted paragraph is replaced by the invariant in the present tense (discipline §4 — no history in code docs: read the helper doc and `coherence.rs`'s header for narrative); the trailer decision argued; the three-ε battery; no `Co-Authored-By`; the empty trailer-only commit `3daab7d80` (note it — the trailer could have ridden a content commit); `work.py lint`; hosted run 34016450337 on this head (verify via get_check_runs).
- C10 SHAPE (the style lane's own questions): is a parse-time refusal the right HOME for a certification invariant re-decided (versus the helper deciding once for both callers), and does repeating the decide at the branch door make two homes of one rule?

Report back in ≤150 lines: verdict on the four-term ladder (APPROVE / APPROVE-WITH-FIXES / NOT-MERGEABLE-AS-IS / REJECT); findings as MAJOR / MINOR / NOTE, one line of prose each with a file:line; the CODE QUALITY REPORT rubric (counts; spec deviations reported vs silent; ratings 1–5 with one line of evidence each for idiom/structure, test quality, doc/comment honesty); the style-lane questions exercised; every probe file you wrote (path, result) so useful rows can be adopted as ordinary tests.
