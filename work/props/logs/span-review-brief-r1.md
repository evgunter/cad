# PROPS span review brief — lane r1 (stored before dispatch; identical to the other lane's modulo this name)

You are one of two independent blinded reviewers of PR #1952 (the PROPS program's Span sweep, unit `span-carries-its-knot-vector`, Ev's ruling A) in evgunter/cad, a B-rep CAD kernel in Rust. Your worktree is /home/user/lanes/review-span-r1, detached at the frozen head 2eccccead4c23e6e3dfaffa00b1044f941a9e3a7; the diff under review is `git diff origin/main...HEAD` there (37 files, +1344 −1453). Work ONLY in that worktree; never edit /home/user/cad or any other /home/user/lanes/* directory. Do not edit the reviewed files; probes you write go in NEW files under the relevant crate's tests/ in your worktree and are reported, not pushed. You have no access to the other reviewer's report and must not look for it; do not post anything on GitHub. Never state which model you are and do not speculate about the implementer's.

Read first, in full, by path: /home/user/cad/docs/prompts/reviewer-style-lane.md (answer its questions with judgement; name the ones you exercised; use its confidence vocabulary), /home/user/cad/docs/PROPS-SPAN-SPEC.md (the binding spec — note its curve-door and SurfaceWindow paragraphs, which the unit deviates from with an argument), the PR body via mcp__github__pull_request_read (get, owner evgunter, repo cad, pullNumber 1952), /home/user/lanes/review-span-r1/work/props/span-carries-its-knot-vector.md, /home/user/lanes/review-span-r1/work/props/coefficients-carry-their-knot-vector.md (the residue the unit filed), /home/user/lanes/review-span-r1/crates/geom-core/README.md (the new companion clause) and the reviewed files at HEAD: crates/geom-core/src/spline/{knots,locate,basis,hull}.rs, crates/geom/src/surfaces/nurbs.rs, crates/geom/src/curves/nurbs.rs.

Environment (part of the brief): CARGO_TARGET_DIR=/home/user/review-span-r1-target and CARGO_INCREMENTAL=0 on every cargo command; debug profile; one crate at a time (geom-core first, then geom, geom-brep, step-import; both feature lanes where a claim needs it); `df -h /` before each build — two other lanes share this 4-core box: under 3 GB free, stop building and report what you could not execute. Foreground, one command at a time; a job that could outlive 600 s runs setsid-detached to a log and is polled in the foreground; never end your turn with background work running; never park on a waiter.

Claims to falsify (execute where a probe can decide them):
- C1 THE MUTATION-HOLD CHECK. The unit says no site holds a span across a mutation because the spline layer has no `&mut self` method and every knot-algebra door is `&self -> Self`, and pins the constraint with an E0506 `compile_fail` row. Verify the grep claim over geom-core/src/spline, geom/src/curves/nurbs.rs, geom/src/surfaces/nurbs.rs AND the consumers (geom-brep, mesh, step-import); find any site where a Span/SpanSet/SurfaceWindow outlives a rebinding of its vector/surface in a way the borrow checker would NOT catch (e.g. a span minted from a clone, or from a vector that is then dropped and re-read from another binding).
- C2 UNREPRESENTABILITY. Construct every mismatch the old API allowed — a span from vector A fed to basis_funs/span_hull/derivative_span_hull on data of B; a SpanSet from A used on B; a window from surface A on surface B's doors; the curve doors (eval_in_span and siblings) with a span from another curve's knots — and show each is a compile error with the stated code, or, where it still typechecks, that the door reads the span's/window's own vector and says so. Judge the unit's deviation (the three surface doors moved ONTO SurfaceWindow<T>, which gains a T parameter reaching two signatures outside geom): does it deliver what the spec's shape would not, and what does the T parameter cost callers?
- C3 BIT IDENTITY. No numeric output moved. Execute the unit's identity pin and extend it: rational curves/surfaces, interior multiplicities, both feature lanes, the geom-brep patch_bound/ssi consumers, mesh/chords.rs, step-import/recognize.rs. Any ulp anywhere is a finding.
- C4 RETIRED GUARDS. `KnotVector::admits`, `NurbsSurface::admits` and the span half of `span_indices` are gone: per guard, was every former consumer of its false/None accounted for (D2 classification per retired guard), and does any public door that previously POISONED on a bad span now do something else for a still-representable misuse (the coefficient↔vector pairing — the residue)? Are the residue item's three behavioural rows real and is its description of the surviving length-only guard right?
- C5 HONEST compile_fail ROWS. Each row names the error code; compile the rows against this head and confirm the code emitted is the one named (stable rustdoc does not compare codes — CERT-M3's lesson); each has a twin differing in one identifier that resolves.
- C6 DOCS. `crates/geom-core/README.md` SPLINE-DESIGN S1 and the docs/DESIGN.md companion-table row: present tense, the invariant only, the one pairing left open named; no history (discipline §4); the DESIGN.md edit is the table row and nothing else. `Span`'s PartialEq (by address or by indices?) — is the choice argued and right?
- C7 SWEEP AND FENCE. The reading sweep's hit list and its stated blind spot (topo/sweep/profile/editor-core/pncad grepped, not read); the dual.rs edit is exactly one signature; work.py territory's crossings all argued as mechanical consequences.
- C8 THE PYTHON-SUITE SKIP. CI ran TIER=closure with RUN_PNCAD_PY=false on a change to geom-core/geom public signatures. Does pncad-py (crates/pncad-py) compile against anything this PR changed? Run `cargo check -p pncad-py` (or the cheapest equivalent) in your target and report. If it would break, that is a MAJOR the hosted run could not see.
- C9 ε POSTURE. None claimed; confirm no tolerance read moved.

Report back in ≤150 lines: verdict on the four-term ladder (APPROVE / APPROVE-WITH-FIXES / NOT-MERGEABLE-AS-IS / REJECT); findings as MAJOR / MINOR / NOTE, one line of prose each with a file:line; the CODE QUALITY REPORT rubric (counts; spec deviations reported vs silent; ratings 1–5 with one line of evidence each for idiom/structure, test quality, doc/comment honesty); the style-lane questions exercised; every probe file you wrote (path, result) so useful rows can be adopted as ordinary tests.
