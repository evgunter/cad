# PROPS sphere-pole-side review brief — lane r2 (stored before dispatch; identical to the other lane's modulo this name)

You are one of two independent blinded reviewers of PR #2741 (the PROPS sphere-pole-side unit: a rim's TRAVERSAL names which side of it the face's interior lies on, so a rim-only polar cap measures instead of refusing `DegenerateFace`, and a half-cap's L-shaped complement refuses typed instead of cancelling a closed sphere to volume `0.0`) in evgunter/cad, a B-rep CAD kernel in Rust. Your worktree is /home/evan/cad-lanes/review-sph-r2, detached at the PR's frozen head 53a9bee9eec1b86dee390eda6b02c8994bed942c; the diff under review is `git diff bfc577bbdd8d911141269a16137c8a9adacfa5ed...HEAD` there. **Every Bash call starts with `cd /home/evan/cad-lanes/review-sph-r2 && ...`** — the harness resets cwd between calls. Work ONLY in that worktree; never edit /home/evan/projects/cad, /home/evan/.mngr/worktrees/* or any other /home/evan/cad-lanes/* directory. Do not edit the reviewed files; probes go in NEW files under the relevant crate's tests in your worktree (or a `#[cfg(test)]` module in a scratch copy under your scratch directory when a probe needs private items) and are reported, not pushed. Your private scratch directory is /home/evan/cad-lanes/review-sph-r2-tmp. You have no access to the other reviewer's report and must not look for it; post nothing on GitHub — `gh pr view 2741 --repo evgunter/cad --json body --jq .body` and `gh pr checks 2741 --repo evgunter/cad` are the only calls you make, and if either returns a rate-limit error say so and move on. Never state which model you are and do not speculate about the implementer's.

Read first, in full, by path: /home/evan/cad-lanes/review-sph-r2/docs/prompts/reviewer-style-lane.md (answer its questions with judgement; name the ones you exercised; use its confidence vocabulary); the binding spec /home/evan/cad-lanes/review-sph-r2/docs/PROPS-SPHERE-POLE-SIDE-SPEC.md **including its 2026-09-14 amendment** (its construction and its rulings are the orchestrator's and settled; you falsify the construction, its measurements and its claims, not the rulings — except where the unit DEVIATED from it, which is yours to adjudicate); the PR body; and the four items under `work/props/` the unit names, plus the one it filed.

Environment (part of the brief): `CARGO_TARGET_DIR=/home/evan/cad-lanes/review-sph-r2-target CARGO_INCREMENTAL=0 TMPDIR=/home/evan/cad-lanes/review-sph-r2-tmp` on every cargo command; debug profile. **This is a 9 GB WSL2 VM with a machine-wide build mutex that is heavily contended** — read `memories/agent-lane-operations.md`'s "Build concurrency" section, wrap every heavy cargo call in `local-scripts/with-build-slot.sh`, **pass no `-j`**, prefer `-n` with retries over a blocking wait, and expect to lose the slot for long stretches (the implementer saw a `-x` battery starve and ~13 consecutive refusals). Prefer narrow runs — `cargo test -p geom-brep --test all -- <row>` and the named `topo` suites — over anything workspace-wide, and let hosted CI be the record for the matrix. Foreground only; a job that could outlive 600 s runs `setsid`-detached to a log under your scratch dir and is polled by a timed `until` loop; never end your turn with background work running; kill any detached job the moment its evidence is superseded.

Claims to falsify (execute where a probe can decide it):
- C1 SOUNDNESS OF THE SIGN. σ = `d_u_sign` × the face's sense bit is claimed to name which side of a rim the interior lies on, for every sphere face the kernel can mint. Attack it: build faces whose rim traversal and sense bit disagree with the levels, both poles, both traversals, both sense bits, rims at the equator and near a pole, multi-arc rims, and a rim whose arcs are traversed inconsistently if the doors let you mint one. A face where σ names the wrong side, or where the new refusal fires on a face that is legitimately an iso-rectangle, is a MAJOR.
- C2 THE CAP'S NUMBER. `2πR²(1 − sin v₀)` and `2πR²(1 + sin v₀)` at `rel < 1e-12`, the flux `s_f·R·Area`, and through the PUBLIC doors a ball cut by one plane certified at all three tiers against `πh²(3R − h)/3` with `volume_pad = area_pad = 0`. Re-derive the closed forms yourself and re-run the rows; check a cap whose rim is BELOW the equator, a hemisphere exactly, and a cap so shallow that `1 − sin v₀` loses precision.
- C3 THE COMPLEMENT'S REFUSAL. The L-shaped complement now refuses `props_rim_interior_side` while face A still measures exactly, and WHICH of the pair refuses is the sense bit's call. Verify both ways round, and then ask the harder question: is refusing the complement the right answer, or does the kernel now refuse a face a user can legitimately build? Find the most defensible L-shaped face you can and say what happens to it.
- C4 THE ANCHORING INVARIANCE, which the spec's amendment demanded. `props_rim_interior_side`'s recorded signs must not move under a pure re-anchoring of a loop. Re-run the unit's row your own way (rotate the edge list differently, more than one body, both ε extremes), and confirm the unit did not mint a fresh instance of the cycle-order defect it was warned about. The unit reports `props_rim_side` still moves — check that this is the pre-existing open row and not something it made worse.
- C5 THE `boundary_material_sign` DEVIATION — the unit's own, and the one you most need to adjudicate. The spec said that arm should take the predicate too; the unit argues it CANNOT, because check 6 compares the boundary's encoding against the sense bit and σ reads that bit, so the comparison would become a tautology; it instead answers `MaterialSign::Unencoded` for the rim-only case. Read check 6, read the arm, and decide whether the argument holds. If it does, say so; if the arm could take a weaker form of the predicate that is not a tautology, that is the finding.
- C6 EVERY POPULATION THAT MOVED. Two census digests (`voided_rod`'s verdicts 40→38, `bulged_extrusion`'s 19→18, identical at three ε), a verdict multiset row, the predicate-dimension audit's four hand-derived counts, MESH-12's rim-only row flipping at `Δv = 0`, and `review_m2_pr7_props::check_patch` changing its input from `sense: true` to `sigma > 0.0`. Verify each moved for the reason given and only that reason. **The `check_patch` change is a test INPUT change** — satisfy yourself it does not hide a case the row used to cover.
- C7 THE TWO ASSUMPTIONS THE SPEC GOT WRONG, reported by the unit: the die's pips were never the rim-only shape (they are revolved, so they carry a seam meridian), and the cone apex sibling is not the same three-line change (no second candidate, and `fn cone` takes no sense bit) so it was measured and filed rather than served. Check both claims; a sibling that IS cheap to serve and was filed instead is a finding.
- C8 THE RETIREMENT. `Rim`'s `d_u: T` is gone and `props_rim_dir_group` reaches no funnel site. Confirm nothing else read the scalar, and that the discrete comparison that replaced it cannot answer differently for any multi-arc rim.
- C9 DOCS, POSTURE, LANDING. The `fn sphere` docs' "established / not established" list honest about what the traversal now establishes AND what it still does not; discipline §4 (present tense) and §7 (cite by name); the D2 addendum's missing row disclosed rather than added to ratified text — is that the right call?; ε posture; no `Co-Authored-By`, no `CI-Config:` trailer, no empty commits; `work.py lint`; the three items `status: review` with their resolutions written in, the fourth left open with its measurement appended; hosted run 35068825589 green on the head.
- C10 SHAPE (the style lane's own questions): two new predicates on one file — do both earn their names? Is the rim-only arm a special case bolted beside the general one, or does it fall out of it? Does anything here mint a fresh instance of the defect it closes?

Report back in at most 150 lines: verdict on the four-term ladder (APPROVE / APPROVE-WITH-FIXES / NOT-MERGEABLE-AS-IS / REJECT); findings as MAJOR / MINOR / NOTE, one line of prose each with a symbol-name citation; the CODE QUALITY REPORT rubric (counts; spec deviations reported vs silent; ratings 1–5 with one line of evidence each for idiom/structure, test quality, doc/comment honesty); the style-lane questions exercised; every probe file you wrote (path, result) so useful rows can be adopted as ordinary tests.
