# PROPS coeffs review brief — lane r1 (stored before dispatch; identical to the other lane's modulo this name)

You are one of two independent blinded reviewers of PR #1985 (the PROPS program's coeffs-window unit `coefficients-carry-their-knot-vector`: the Span sweep's residue closed the same way — `SplineCoeffs<'a, E>` minted only from its `KnotVector`, `CoeffWindow<'a, E>` carrying the pair, every free `hull` door a method on one of the two) in evgunter/cad, a B-rep CAD kernel in Rust. **This PR is already MERGED** (merge `55d541ae5`) — the implementer lane landed it before the review was dispatched, an orchestrator brief defect that is disclosed on the record; your review is therefore of a merged head and your findings become a fix-pass PR, not a hold. Nothing about your task changes: review the diff as if it were open. Your worktree is /home/user/lanes/review-coeffs-r1, detached at the PR's head 4521bd6584b8dcfca6d1ca16e7063d64152729c4; the diff under review is `git diff ea11576b4...HEAD` there (30 files, +1487 −782; `ea11576b4` is the base main at the merge). Work ONLY in that worktree; never edit /home/user/cad or any other /home/user/lanes/* directory. Do not edit the reviewed files; probes you write go in NEW files under the relevant crate's tests/ in your worktree and are reported, not pushed. Your private scratch directory is /home/user/review-coeffs-r1-tmp; do not use the session scratchpad under /tmp — it is shared with another lane. You have no access to the other reviewer's report and must not look for it; do not post anything on GitHub (mcp__github__pull_request_read is the only GitHub tool you use). Never state which model you are and do not speculate about the implementer's.

Read first, in full, by path: /home/user/cad/docs/prompts/reviewer-style-lane.md (answer its questions with judgement; name the ones you exercised; use its confidence vocabulary); the binding spec, deleted at this head — `git show ea11576b4:docs/PROPS-COEFFS-SPEC.md` in your worktree (the ruling — option (a) in the structural form, ruling A of the Span sweep one level down — is the orchestrator's and settled; you falsify the construction and its claims, not the ruling); the PR body via mcp__github__pull_request_read (get, owner evgunter, repo cad, pullNumber 1985); /home/user/lanes/review-coeffs-r1/work/props/coefficients-carry-their-knot-vector.md (closed) and /home/user/lanes/review-coeffs-r1/work/props/span-carries-its-knot-vector.md (the precedent); /home/user/lanes/review-coeffs-r1/crates/geom-core/README.md §SPLINE-DESIGN S1; and the reviewed files at HEAD: crates/geom-core/src/spline/hull.rs (whole), knots.rs (the mints), net.rs, crates/geom-brep/src/ssi.rs, crates/geom-brep/src/ssi/certify.rs, crates/geom-brep/src/props/quad.rs, crates/geom/src/curves/nurbs.rs (the weights' derivative site), crates/mesh/src/chords.rs, and the new suites crates/geom-core/tests/coeffs_bit_identity{,_interval}.rs.

Environment (part of the brief): CARGO_TARGET_DIR=/home/user/review-coeffs-r1-target and CARGO_INCREMENTAL=0 on every cargo command; debug profile; one crate at a time (geom-core first, both feature lanes — default and `--features interval`; then geom, geom-brep, mesh by the touched suites' names, not whole workspaces); `df -h /` before each build — another lane shares this 4-core box: under 3 GB free, stop building and report. Foreground, one command at a time; a job that could outlive 600 s runs setsid-detached to a log under your scratch directory and is polled with a timed foreground loop; never end your turn with background work running; never park on a waiter.

Claims to falsify (execute where a probe can decide them):
- C1 BIT IDENTITY (the spec's first reviewer target). Two digests are claimed captured at the merge base through the RETIRED spellings and unchanged at head: `coeffs_bit_identity.rs` (960 rows, `0xdedc_bc91_037f_daab`) and `coeffs_bit_identity_interval.rs` (480 rows, `0x077f_2c93_d2ac_b9b5`), plus `geom`'s 1001/11 151-row span digests unchanged. Verify the capture is real: check out the capture commit `0d0740146` (or `ea11576b4` with the suite file copied in) in a scratch copy under your scratch directory, run the suite against the FREE functions, and confirm the digest; then run at head. Extend the corpus where the unit's corpus is thin (degrees, multiplicities, rational weights near zero, `Interval` brackets of nonzero width, the whole-domain doors, `derivative_coeffs` on degree 1). Any ulp anywhere is a MAJOR.
- C2 THE CONSUMERS (the spec's second target). For each re-spelled consumer (`ssi.rs::pcurve_windows`, `ssi/certify.rs::{box_chain, probe_tube_chart}`, `quad.rs::{bspline_range_hull, range_hull, differenced, the DerivTake closures, the collapse arm}`, `net.rs::knot_step`, `geom/curves/nurbs.rs` weights' derivative, `mesh/chords.rs::differenced`): were the coefficients and the knot vector ALWAYS from the same object before (so the mint's refusal is dead by construction as the body claims), and does the re-spelling preserve the exact poison behaviour the retired door had at each site (a mint `None` transcribed to the door's poison)? A consumer whose coefficients came from a different vector than its span is a MAJOR finding about `main` (filed, not this unit's).
- C3 THE MINTS AND THE TYPE. `KnotVector::coeffs`/`coeffs_rational` are the ONLY constructors (grep for struct literals, `pub(crate)` doors, `Default`); `CoeffWindow` is minted only by `SplineCoeffs::{span, span_at}`; the three `compile_fail` rows (E0308 / E0451 / E0061) emit exactly those codes at 1.97.0 (stable rustdoc does not compare codes — compile each snippet yourself) and each has a legal twin differing in one respect; `Debug` prints addresses, equality is address-plus-indices. Deviation 1: weight positivity stays a per-window POISON rather than a mint-time refusal, and a pair minted WITHOUT weights answers poison at the rational doors — is that the right D2 classification, and is it stated at the door? Deviation 2: weights as `Option<&[f64]>` on one type — does any door read weights it should not, or vice versa? Deviation 3: `derivative_coeffs`'s `len < 2` branch dropped as unreachable — prove or refute the unreachability (the mint's count check and `KnotVector::clamped`'s degree floor).
- C4 THE RETIRED POISON ROUTES, D2-classified. `span_indices` and `span_weights_positive` are gone: per former poison route, which consumer read it, and is the state now unrepresentable (row 0) or refused at the mint (row 1)? Is the whole-domain family (`domain_hull`, `derivative_coeffs`, `sup_norm_bound*`) really closed — no free function in `hull.rs` takes a coefficient array (`grep -n "pub fn" hull.rs`) — and did the move reach a crate the spec did not name?
- C5 THE RED-FIRST CLAIM. Three residue rows went red at the type level and were replaced (`the_mint_refuses_exactly_the_wrong_length`, `every_window_answers_what_the_domain_door_hulls_over_it`, `a_curves_channels_mint_against_its_own_vector`); the constructor row stays. Are the replacements falsifiable (would a wrong mint or a wrong window red them — try a mutation on a scratch copy), and is the (b)/(c) pair really covered by the `compile_fail` rows?
- C6 DOCS. README S1 rewritten to the closed state (present tense, `InteriorKnot` the deliberate exception, the mints bullet); `docs/DESIGN.md`'s companion row summary changed — was it false before (the body says so) and is the new one-line true? `InteriorKnot`'s doc names `SplineCoeffs` as the second precedent. Discipline §4: no history in code docs.
- C7 SWEEP AND FENCE. The body's three findings-for-the-orchestrator (`bspline_eval_ring*` evaluators keep the loose shape; `TensorNet`/`(kv_u, kv_v, net)` grids; `compose.rs::to_bezier_spans(kv, coeffs)`) — is the hit list complete for the shape *a coefficient array beside a vector or span it was not minted against* over `crates/*/src`? What can the pattern not match? `work.py territory --base ea11576b4`: the body lists the tcost paths; verify the count and owners.
- C8 THE ONE CI RED. The first push was red on `discipline (evaluation-code)` (`check-interval-cfg-additive.py`: interval cfgs gate items, not blocks), fixed in `d8c8a8d1e`. Read the fix: does the twin interval suite now carry its cfg on items as the gate demands, and is the gated suite registered in `tests/all.rs` correctly for both lanes?
- C9 ε POSTURE AND LANDING. None claimed — confirm no tolerance read moved. No `Co-Authored-By` on any commit; the ledger's `## Per-merge deletion` entry with a working recovery SHA; the item's `## Closed` records the ruling; `work.py lint` green; run 33969355105 green on the full matrix (verify via get_check_runs).

Report back in ≤150 lines: verdict on the four-term ladder (APPROVE / APPROVE-WITH-FIXES / NOT-MERGEABLE-AS-IS / REJECT — read as "what the fix-pass PR owes", since the head is merged); findings as MAJOR / MINOR / NOTE, one line of prose each with a file:line; the CODE QUALITY REPORT rubric (counts; spec deviations reported vs silent; ratings 1–5 with one line of evidence each for idiom/structure, test quality, doc/comment honesty); the style-lane questions exercised; every probe file you wrote (path, result) so useful rows can be adopted as ordinary tests.
