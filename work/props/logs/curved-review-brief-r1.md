# PROPS curved-residues review brief — lane r1 (stored before dispatch; identical to the other lane's modulo this name)

You are one of two independent blinded reviewers of PR #2924 (the PROPS curved-residues unit: the three residues the sphere-pole-side unit measured and did not serve — the cone apex cap, the shape door's sense-free rim-side residue, and a rim-level rule that manufactured its error by feeding `f64::NAN` into a decide) in evgunter/cad, a B-rep CAD kernel in Rust. Your worktree is /home/evan/cad-lanes/review-cr-r1, detached at the PR's frozen head 3f918e8080b7d73fdad82d65bf08ad59b37c02e3; the diff under review is `git diff 7fb16ac60c2bdfff0c0fe4834d4c2586a7d9b021...HEAD` there. **Every Bash call starts with `cd /home/evan/cad-lanes/review-cr-r1 && ...`** — the harness resets cwd between calls. Work ONLY in that worktree; never edit /home/evan/projects/cad, /home/evan/.mngr/worktrees/* or any other /home/evan/cad-lanes/* directory. Do not edit the reviewed files; probes go in NEW files under the relevant crate's tests in your worktree (or a `#[cfg(test)]` module in a scratch copy under your scratch directory when a probe needs private items) and are reported, not pushed. Your private scratch directory is /home/evan/cad-lanes/review-cr-r1-tmp. You have no access to the other reviewer's report and must not look for it; post nothing on GitHub — `gh pr view 2924 --repo evgunter/cad --json body --jq .body` and `gh pr checks 2924 --repo evgunter/cad` are the only calls you make, and if either returns a rate-limit error say so and move on. Never state which model you are and do not speculate about the implementer's.

Read first, in full, by path: /home/evan/cad-lanes/review-cr-r1/docs/prompts/reviewer-style-lane.md (answer its questions with judgement; name the ones you exercised; use its confidence vocabulary); the binding spec /home/evan/cad-lanes/review-cr-r1/docs/PROPS-CURVED-RESIDUES-SPEC.md (its construction and its rulings are the orchestrator's and settled; you falsify the construction, its measurements and its claims, not the rulings — except where the unit DEVIATED from it or CONTRADICTED it, which is yours to adjudicate); the PR body; and the three items under `work/props/` the unit names plus the two it filed under `work/tess/`.

Environment (part of the brief): `CARGO_TARGET_DIR=/home/evan/cad-lanes/review-cr-r1-target CARGO_INCREMENTAL=0 TMPDIR=/home/evan/cad-lanes/review-cr-r1-tmp` on every cargo command; debug profile. **This is an 8-core 9 GB WSL2 VM with a machine-wide build mutex that is heavily contended by other programs' lanes** — read `memories/agent-lane-operations.md`'s "Build concurrency" section, wrap every heavy cargo call in `local-scripts/with-build-slot.sh`, **pass no `-j`** (capping jobs measurably worsens contention), prefer `-n` with retries over a blocking wait, and expect to lose the slot for long stretches. Never run two batteries at once — that is the documented OOM shape on this box, and it has crashed the machine before. **DISK IS TIGHT: the box is at 94% with about 17 GB free, most of it other programs' build caches which are not yours to delete.** Check `df -h /` before each heavy build; if free space falls below 6 GB, stop building, say so in your report, and decide the rest by reading. Prefer narrow runs — `cargo test -p geom-brep --test all -- props_cone_apex_cap`, `cargo test -p topo --test mesh12_rim_row_reach`, the named `mesh`/`topo` suites — over anything workspace-wide, and let hosted CI (run 35550288011, green on this head, full matrix) be the record for the matrix. Foreground only; a job that could outlive 600 s runs `setsid`-detached to a log under your scratch dir and is polled by a timed `until` loop; never end your turn with background work running; kill any detached job the moment its evidence is superseded.

Claims to falsify (execute where a probe can decide it):
- C1 THE TRANSPOSED PREMISE — the first thing to attack, and the reason this unit exists. The sphere-pole-side unit shipped a MAJOR because its spec ASSERTED a premise instead of requiring a decide. This unit transposes that unit's premise onto the cone and argues the transposition needs no sense bit: "σ is `d_u_sign` under the face's one sense bit, so unanimity of σ IS unanimity of `d_u_sign`". Attack that equivalence directly for every cone face the kernel can mint — both nappes, both traversals, both sense bits, multi-arc rims, a rim whose arcs are traversed inconsistently if the doors let you mint one, and a cone whose axis is not `+Z`. If unanimity of σ and unanimity of `d_u_sign` can come apart on any mintable face, the fold is reading a premise it has not established and that is a MAJOR. Then ask the second-order question the sphere unit's MAJOR turned on: does `cone_apex_level` fold an UNBOUNDED complement and answer it with the cap's area? The unit says the closure guard is what stops that — test it, do not take it.
- C2 THE CAP'S NUMBER AND ITS FLUX. Re-derive `sin α·Δu·|v_hi² − v_lo²|/2` and its whole-turn form `π·v²·sin α` yourself rather than checking their arithmetic, and re-run the rows your own way: both traversals, both nappes, a very shallow and a very steep α, and a `v` small enough to lose precision. Then the sharp one: flux is asserted **exactly `0.0`** because the apex sits at the origin so the anchored term — the only term a cone's flux has — vanishes. **Move the apex off the origin** and check the cap's flux is then correct rather than accidentally right; a closed form verified only at the one position where its only term is zero is a row that proves nothing. Certify a cone-capped solid through the PUBLIC doors at all three tiers against a hand-derived volume if you can build one.
- C3 THE CLOSURE GUARD. `props_rim_only_closed`, the sphere's guard generalised to the kind's own azimuthal arm. The unit quotes four shapes that answer an area through the public `curved_face` with the guard removed — half a rim (ratio 0.5), a quarter rim (0.25), the same full rim stated twice (2.0), a full rim plus an extra half arc (1.5). Reproduce those four, then find a FIFTH the guard misses: arcs that sum to a full turn but wind the wrong way, a rim stated three times, arcs that overlap partially, a rim closed to within an ε of a turn. A shape that answers an area it should refuse is a MAJOR.
- C4 THE FOLD'S PLACEMENT, which the unit calls load-bearing. It lives in `cone_boundary` rather than `fn cone` "so all three doors read the same extent", and that is what lets `boundary_material_sign`'s cone arm read a definite side off the cap, which is how tier 3's check 6 catches the traversal that bounds the rest of the nappe. Verify all three doors do read the same extent; verify `+u → Encoded(Negative)` and `−u → Encoded(Positive)`; and verify `validate_geometric` raises `CurvedSenseInverted` naming **exactly** the offending face and no other, on a body built through the Euler doors. Then ask whether a face the kernel legitimately builds is now refused by check 6 that was not before.
- C5 THE ZERO-EXTENT PATCH MUST STILL REFUSE. The unit says requiring unanimity is what keeps the true zero-extent patch — rims at one level traversed opposite ways — at `DegenerateFace`, and that a generator-bearing zero-extent face is not folded either. Build both and check. Also check the claim made for the SHAPE door that a zero-extent face still passes it, since the two statements are about different doors and it would be easy to have moved one while quoting the other.
- C6 THE 40-PAIR MEASUREMENT, reproduced independently. `d9_mesh_goldens`, 40 body/δ pairs at six lane/eps points; exactly ONE body moved (`apex_crossing_bowtie`, both δ), refusal → refusal, `d64f3d06937361f5` → `9cddb7a3543db550`. Re-run it your own way. Then the question the count does not answer: is that corpus capable of exercising the new premise at all? If the 40 pairs contain few or no multi-rim curved faces, "one moved" is a fact about the corpus and the unit's STOP clause was never really at risk — say so if that is what you find.
- C7 THE RE-BASELINED RED — the orchestrator flagged this one to the lane and the lane ruled on it, so the ruling is the lane's and is yours to adjudicate. `mesh12_rim_row_reach::the_shape_door_admits_the_rim_only_cap_and_the_flux_lane_reads_the_gap` went from `Ok(())` to `Escalated { props_rim_side }`. The lane's argument: margin `−1.3163737899724026e-9` against `Band { zero: 1e-9, escalate: 1e-8 }` is the axial-sine reading of `R·Δv = 1.5e-9` shrunk by `cos v̄ = cos 0.5 = 0.8776`, the two extremes are 1.3 nm apart at a 1 nm zero band, so the face is genuinely undecidable and escalate-never-guess is the ratified posture. Check the arithmetic, check audit note N8 says what is claimed, and then check the STOP-clause reasoning: the lane says that body does not mesh today either way (issue 1615), so no mesh was lost. Verify that independently — if a body that meshes today stops meshing, the spec's STOP clause fires and the residue should not have been taken.
- C8 THE MANUFACTURED ERROR. `mixed_levels` no longer feeds `f64::NAN` into `classify`; it returns the typed refusal directly. Verify the typed refusal is the same OUTCOME CLASS callers always got — a caller that matched on the old escalation must not now fall through. Re-run the sweep your own way and probe the four blind spots the unit declares (a NaN bound to a local first, produced arithmetically, `±inf` used the same way, a NaN reaching a margin through a helper); find a fifth site if there is one. Check the siblings really are left explicitly and not half-changed: `unreachable_zero`'s poison 4-tuple and `torus_meridian_orient`'s `unreachable!`.
- C9 THE DISCLOSED COVERAGE LOSS. `work/tess/apex-crossing-branch-premise-has-no-body-level-witness.md` records that the branch door's cone arm is no longer reached through `tessellate`, and argues the loss is "a witness, not a premise" because a props-level row on a hand-built bow still pins it. Adjudicate that: is a hand-built props-level fixture an adequate substitute for a body-level witness, or has a real integration path gone dark? This is the unit's own disclosed cost and the place it is most likely to have talked itself into comfort.
- C10 EVERY POPULATION THAT MOVED. Five baselines, each with a stated reason: the `d9_mesh_goldens` digest pair, `mesh11_arc_branch`'s refusal name (`NotOneChartBranch` → `NotIsoRectangle { props_rim_side }`, row renamed), the MESH-12 row, a DELETED sphere row (`the_cone_apex_cap_still_refuses_degenerate_face`), and a renamed review probe. Verify each moved for the reason given and ONLY that reason. **The deleted row deserves particular attention** — satisfy yourself the cone's new rows cover what it used to, and that deleting rather than re-pointing it hides nothing. Check the pinned apex-cap verdict multiset is complete and that the two predicates the fold adds really are quantities the arm already read.
- C11 DOCS, POSTURE, LANDING. Four sentences the change falsified were corrected (`props/mod.rs`'s "the linear kinds' rim-only faces have no extent to name at all" and "the SHAPE DOOR asks only the first of the two", `rim_interior_side`'s sphere-only rationale, `sphere_extent_margin`'s note) — check the sweep for falsified prose was complete rather than stopping at four, and that the `predicate-dimension-audit.md` rows are still derivable from the greps they name. ε posture "nothing moved": verify no new predicate name, comparand or lever. Discipline §4 (present tense) and §7 (cite by name, not line number); no `Co-Authored-By`, no `CI-Config:` trailer, no empty commits; `python3 scripts/work.py lint`; the three items `status: review` with `pr:` set; hosted run 35550288011 green on the head.
- C12 SHAPE (the style lane's own questions): the cone arm and the sphere arm now share a fold and a guard — does the cone case fall out of the general one or sit bolted beside it? Does `props_rim_only_extent` naming two kinds over two comparands earn that, or is it one name doing two jobs? Does anything here mint a fresh instance of the defect it closes — in particular, does the unit assert a premise anywhere the way the spec it descends from did?

Report back in at most 150 lines: verdict on the four-term ladder (APPROVE / APPROVE-WITH-FIXES / NOT-MERGEABLE-AS-IS / REJECT); findings as MAJOR / MINOR / NOTE, one line of prose each with a symbol-name citation; the CODE QUALITY REPORT rubric (counts; spec deviations reported vs silent; ratings 1–5 with one line of evidence each for idiom/structure, test quality, doc/comment honesty); the style-lane questions exercised; every probe file you wrote (path, result) so useful rows can be adopted as ordinary tests.
