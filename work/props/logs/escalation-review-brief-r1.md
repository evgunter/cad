# PROPS escalation-channel review brief — lane r1 (stored before dispatch; identical to the other lane's modulo this name)

You are one of two independent blinded reviewers of PR #2928 (the PROPS escalation-channel unit: eight shipped sites asked the K-funnel, got a DEFINITE sign, then minted their own `Indeterminate`, so a caller saw an escalation the frame's log did not — the unit closes that, and then classifies the ~40-variant deletion sweep the k-stats spec named) in evgunter/cad, a B-rep CAD kernel in Rust. Your worktree is /home/evan/cad-lanes/review-esc-r1, detached at the PR's frozen head aef477d2104df0493d24583b29b574b933ef05a6; the diff under review is `git diff fef229c6633ac3f5718336837644bd265755396a...HEAD` there. **Every Bash call starts with `cd /home/evan/cad-lanes/review-esc-r1 && ...`** — the harness resets cwd between calls. Work ONLY in that worktree; never edit /home/evan/projects/cad, /home/evan/.mngr/worktrees/* or any other /home/evan/cad-lanes/* directory. Do not edit the reviewed files; probes go in NEW files under the relevant crate's tests in your worktree (or a `#[cfg(test)]` module in a scratch copy under your scratch directory when a probe needs private items) and are reported, not pushed. Your private scratch directory is /home/evan/cad-lanes/review-esc-r1-tmp. You have no access to the other reviewer's report and must not look for it; post nothing on GitHub — `gh pr view 2928 --repo evgunter/cad --json body --jq .body` and `gh pr checks 2928 --repo evgunter/cad` are the only calls you make, and if either returns a rate-limit error say so and move on. Never state which model you are and do not speculate about the implementer's.

Read first, in full, by path: /home/evan/cad-lanes/review-esc-r1/docs/prompts/reviewer-style-lane.md (answer its questions with judgement; name the ones you exercised; use its confidence vocabulary); the binding spec /home/evan/cad-lanes/review-esc-r1/docs/PROPS-ESCALATION-CHANNEL-SPEC.md (its construction and its rulings are the orchestrator's and settled; you falsify the construction, its measurements and its claims, not the rulings — except where the unit DEVIATED from it or CONTRADICTED it, which is yours to adjudicate); the PR body; and the two items the unit names under `work/props/` plus the two it filed under `work/msolve/` and `work/curved/`.

Environment (part of the brief): `CARGO_TARGET_DIR=/home/evan/cad-lanes/review-esc-r1-target CARGO_INCREMENTAL=0 TMPDIR=/home/evan/cad-lanes/review-esc-r1-tmp` on every cargo command; debug profile. **This is an 8-core 9 GB WSL2 VM with a machine-wide build mutex that is heavily contended by other programs' lanes** — read `memories/agent-lane-operations.md`'s "Build concurrency" section, wrap every heavy cargo call in `local-scripts/with-build-slot.sh`, **pass no `-j`** (capping jobs measurably worsens contention), prefer `-n` with retries over a blocking wait, and expect to lose the slot for long stretches. Never run two batteries at once — that is the documented OOM shape on this box, and it has crashed the machine before. Prefer narrow runs — `cargo test -p geom-core --test k_stats_doors -- <row>` and `cargo test -p geom-brep --test kstats_escalation_channel` — over anything workspace-wide, and let hosted CI (run 35496898199, green on this head) be the record for the matrix. Foreground only; a job that could outlive 600 s runs `setsid`-detached to a log under your scratch dir and is polled by a timed `until` loop; never end your turn with background work running; kill any detached job the moment its evidence is superseded.

Claims to falsify (execute where a probe can decide it):
- C1 IS THE HOLE ACTUALLY CLOSED STRUCTURALLY? This is the unit's central claim and the one to attack hardest. The spec asked it to choose between RECORDING the mint and making it IMPOSSIBLE; it chose impossible, on the argument that "a caller never holds a definite sign long enough to reject it in private, because the call that would have given it the sign gives it the refusal instead." Test that literally: is the old `decide` (and every other door that returns a definite sign) still reachable by a `geom-brep` author? If it is, the hole is closed by USE at eight sites, not by construction, and the unit's argument overstates what the doors achieve. Write the code a future site would write — call `decide`, match `Positive`, mint an `Indeterminate` by hand — and see what stops you. The census `shipped_geom_brep_code_builds_no_indeterminate_of_its_own` is the only thing that does; read it, and then defeat it the three ways its own doc comment admits (a helper, a type alias, an `include!`) and any fourth way you find. A census whose blind spots are wide enough to drive the original bug through is a MINOR at least; a claim of structural closure that is really a lint over one crate is the finding.
- C2 THE CENSUS'S REACH. It scans `crates/geom-brep/src/**/*.rs` only. Eight crates in the sweep's range mint `Indeterminate`, and the unit itself FILED four more sites in `topo` and one in `mate`. Is scoping the guard to one crate the right call, or does it leave the guard naming a property the kernel does not have? Check the comment/literal blanking and the `#[cfg(test)]` exclusion do what they say (feed it a source file that should trip it and one that should not).
- C3 THE GATE RECORDS BOTH CHANNELS. "A gated rejection records the classifier's definite verdict AND the gate's `Invalid` escalation beside it, in decision order", and "the verdict channel is unchanged by gating — byte-identical by construction", which is what keeps `resolve::vdiff`'s POSITIONAL comparison valid across this seam. Verify positionally, not by count: build a bracket over a mixed sequence of gated and ungated decisions and compare the verdict vector against the same sequence through `decide`. Check the no-double-record row (`a_gate_over_an_in_band_margin_records_one_escalation_with_its_margin`) is testing what it says — an in-band margin makes `classify` escalate on its own, so the gate must not push a second entry. Check `record_escalation` really is the module's single write (grep the push sites) and that `classify_in`'s `Err` arm routes through it.
- C4 THE THREE DOORS, EACH ON ITS OWN. `decide_positive` treats a definite `Zero` OR `Negative` as "the question was never validly posed" — is that right at all seven sites that take it, or is a definite `Negative` lever arm at some site a real, reportable fact rather than an indeterminacy? `decide_nonzero` returns `NonzeroSign` so the caller cannot write an arm for zero — check the conversion back is total and that no caller reconstructs a `Sign` and reintroduces the arm. `gate_measured` "decides nothing and logs no verdict" — is a door that records an escalation with no verdict beside it coherent with the bracket's contract, and does `drive::classify_replay` or any log reader assume every escalation has a verdict at its position? That last one is a correctness question with a probe.
- C5 THE SWEEP'S POPULATION. The unit says the item's own recipe finds 38 variants across five crates and is blind to the field name `diag` and the `geom_core::Indeterminate` spelling, and that the true class is **82 across nine crates**. Reproduce both numbers your own way — do not re-run their command, write your own. Then attack the blind spots they ADMIT are still open (an `Indeterminate` inside a `Box`, an `Option`, or a wrapper struct; a fourth field name) and find variant 83 if it exists. A population that is still undercounted after the unit corrected it once is a finding proportional to what it hides.
- C6 ZERO RETIRED, AND WHY. The spec said "retire only what the measurement says is a pure wrapper"; the unit retired NONE, on a structural argument: the escalation log is a per-bracket side channel, `record_escalation` drops what it is given when no frame is open, and for nine named crates and every library consumer the log is empty, so retiring a variant deletes information rather than moving it. Attack the argument at its hinge — the enumeration of `Bracket::open` call sites (four shipped, plus `detached` in two places). Verify that enumeration is complete and current. Then look for the counterexample the argument needs to survive: a variant ALL of whose constructions and consumers demonstrably sit inside an open bracket, which would be a pure wrapper by the unit's own test and should have been retired. If you find none, say so with what you searched; if you find one, that is a MAJOR against the sweep's conclusion.
- C7 THE ZERO THAT THE SPEC PREDICTED WOULD BE NON-ZERO. The spec's posture says "recorded-verdict populations WILL move"; the unit measured that none did, and calls the zero the receipt. Verify the three sub-claims: verdicts byte-identical (C3 covers the mechanism; here check the goldens and census digests really were re-run rather than assumed), escalations `+1 per gated rejection` with **no corpus document reaching one**, and driver verdicts unchanged. Then ask the question the zero raises: if no corpus document reaches a gated arm, is the gating corpus blind to this entire change, and are the four new unit rows carrying the whole of its verification? Say what a document that DID reach one would have to look like, and whether the corpus should gain it.
- C8 `classify_replay`'S TWO ARMS. The spec ruled "do not touch them until the channel carries the op-minted family". It now does, and the unit kept them anyway, with the reason MOVED: two shipped paths escalate where no bracket is open (the whole-document mate solve, and a rayon map whose units decide without a frame). Verify both paths, and verify the claim that deleting the arms would turn them into `_ => Bisect` and silently drop the terminal-sliver test. Then read `drive.rs`'s rewritten comment against what is now true (discipline §4: present tense, no history).
- C9 THE THREE THINGS FILED RATHER THAN SERVED. `work/msolve/mate-lane-escalations-reach-no-nodes-log.md` (family 3, measured NOT closed by family 1, plus `mate/coset.rs` hand-building an `Indeterminate`), `work/curved/contact-verify-mints-indeterminates-outside-the-funnel.md` (four sites, two of them MIS-TYPED — a definite `Positive` residual dressed as `MarginDiag::Invalid`), and "family 2 has no residue, measured not assumed". Check each is genuinely out of scope rather than cheap to serve, and check the mis-typing claim by reading the two sites: a definite positive residual reported as a poisoned margin is a correctness bug, and if it is one, is filing it the right disposition or should it have been fixed here? Discipline §6 is the standard.
- C10 SEAMS AND API. `geom-core`'s `k_stats` gains public surface (addition only, nothing removed, no signature changed) — verify that literally. The `test-utils` reader-census ledger line, the `predicate-dimension-audit.md` edits (seven sites moved spelling; `gate_measured` deliberately NOT in the alternation and the reported name still outside the table) — is the audit still derivable from the greps it names? The unit asserts no ratified decision changes and shows the `git log -S` it ran; re-run that check yourself rather than taking it.
- C11 DOCS, POSTURE, LANDING. The rewritten `k_stats` module paragraph honest about what the channel now carries AND what it still does not (the empty-log-outside-a-bracket property is load-bearing for C6 and should be stated where a reader will find it); ε posture "none" — verify no band, comparand or verdict moved; discipline §4 (present tense) and §7 (cite by name, not line number); no `Co-Authored-By`, no `CI-Config:` trailer, no empty commits; `python3 scripts/work.py lint` passes; both `work/props/` items `status: review` with `pr:` set and their resolutions written in; hosted run 35496898199 green on the head.
- C12 SHAPE (the style lane's own questions): three new doors on one module — do all three earn their names, or is `gate_measured` a fourth concept wearing a door's clothes? Does the gate fall out of `classify` or sit bolted beside it? Does anything here mint a fresh instance of the defect it closes — in particular, does the unit itself introduce a place where a definite answer is held and privately rejected?

Report back in at most 150 lines: verdict on the four-term ladder (APPROVE / APPROVE-WITH-FIXES / NOT-MERGEABLE-AS-IS / REJECT); findings as MAJOR / MINOR / NOTE, one line of prose each with a symbol-name citation; the CODE QUALITY REPORT rubric (counts; spec deviations reported vs silent; ratings 1–5 with one line of evidence each for idiom/structure, test quality, doc/comment honesty); the style-lane questions exercised; every probe file you wrote (path, result) so useful rows can be adopted as ordinary tests.
