---
id: props
kind: program
title: PROPS — enclosure certificates and interval honesty
status: open
opened: 2026-09-03
area: kernel
prefix: props/
tag: (PROPS orchestrator)
ab_band: 2400-2499
paths: [crates/geom-core/src/k_stats.rs, crates/editor-core/src/stackup.rs]
keep_out: [a SUCCESSOR — dispatches nothing until S-CERT's exit walk is ratified, geom-brep/src/props/* and offset_fit.rs and patch_bound.rs and geom-core/src/* and geom/src/* are S-CERT's until then and join the paths above at its exit, code-quality Tracks R and N are claimed whole at opening as S-CERT claimed M and N — C3/D30 are S-MESH's under MESH-R and D244/D31/S235/C24 S-CERT's under CERT-N3, both moved out of work/code-quality at their claim, and they arrive here at the inheritance rather than being fetched from there, the DL6 contract (docs/DUAL-DESIGN.md) is ratified — the linalg lane is an audit against it and not a design conversation, bounds-allowlist.sh is Track K's — the overshoot-payload ruling names the seam, the tess-budget re-baseline is this program's after S-CERT's exit per S-MESH's keep_out, M10's analysis lane files (analysis.rs distribution.rs drive.rs measure.rs) are inherited at M10's sweep — the verdict-channel units edit drive.rs by announced seam until then]
---

S-CERT's residue and M10's certification residue as one program: the
`offset_fit` certificates, the sphere polar extent, the rational quad
lane, the linalg interval-honesty audit against the ratified DL6
contract, and the k_stats verdict channel. Class H with a few E riders
and six rulings. A successor: holds M10's and the unowned items now and
inherits S-CERT's twenty at its exit. Charter and lanes:
`work/props/plan.md`; narrative in `work/props/log.md`.
