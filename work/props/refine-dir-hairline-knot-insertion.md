---
id: refine-dir-hairline-knot-insertion
kind: issue
title: refine_dir's exact-equality insertion guard leaves knot pairs one ulp apart - de Boor divides by the hairline
status: open
opened: 2026-08-31
github: 1358
refs: [1314, C3, D30]
---

## From GitHub issue 1358

Opened 2026-08-31; 1 comment.

(S-CERT orchestrator) Filed from CERT-5's fix pass (PR 1314), which measured its way past an initially-wrong attribution: the dual review blamed `knot_aligned_cuts`' exact dedup for sliver blow-ups, and the fix pass proved the blow-up tracks the *refinement* grid instead — a knot at 1/16+1ulp (a refine point, not a block edge) blows up; at 0.313+1ulp (neither grid) nothing happens.

**Mechanism**: `refine_dir` inserts refinement points guarded by `!kv.knots().contains(&t)` — exact equality — so a stated knot one ulp from a refinement point yields two knots an ulp apart, and de Boor divides by that difference. **Measured, pre-existing, and worse on main**: at 1/16+1ulp the affected width reads 0.4455 on main vs 0.188 post-CERT-5; at 3/8+1ulp, 0.1138 vs 0.0505 (CERT-5's per-span/intersection changes dampen but do not remove it). Reachable from import-stated knot vectors.

The shape of a fix mirrors what CERT-5 shipped for its own cut list (a yield-within-8-ulp guard), but `refine_dir` is shared ground and moving it re-derives every rational floor — a deliberate, scoped change with a re-baselining pass, not a local edit. S-CERT hands the measurements over rather than taking it unasked; digits and probes on PR 1314 (`cert5_arm_and_cells.rs` and the fix commits' messages).

Related, same family, filed together rather than separately: the `inner` knot-slice expression (`kv.knots()[1..len-1].to_vec()`) is copied **verbatim five times across three crates** — `quad.rs:659`, `quad.rs:1101`, `patch_bound.rs:316`, `mesh/chords.rs:241`, `mesh/chords.rs:327` — with no sentence admitting it. Track R's consolidation (C3/C-m/D30) is the natural owner; recorded here so the copy count survives.

## Comments

**2026-08-31** — comment:

(S-CERT orchestrator) Residual instance with digits, from CERT-5's delta re-review on the fix head (PR 1314, `ed7a7623`): with the unit's own `SLIVER_CUT_ULPS` guard in place, a knot within ~1e-12 of a `QUAD2_REFINE_SPANS` grid point still costs ~5.2e-2 width (≈51,000× a 1.024e-6 target) — essentially unchanged from pre-guard, because the hairline span is created by `refine_dir`'s insertion at the grid point one ulp away, exactly this issue's mechanism. The delta lane verified the unit's guard is not the lever (it drops *grid* cuts; the insertion happens downstream). So this issue is the sole remaining owner of that width class; nothing else in the tree records it.

---
_Generated by [Claude Code](https://claude.ai/code)_

## Home

S-CERT: `refine_dir` lives in `crates/geom-brep/src/props/quad.rs`, inside S-CERT's `crates/geom-brep/src/props/*` territory, and enclosure quality is its charter; the knot-slice copy class named at the end belongs to Track R's `quad.rs` consolidation (`C3`, `D30`) per S-CERT's keep_out.
