---
id: DECIDE-3
kind: unit
title: the canonical root at the mint site: every sqrt atom keyed on its argument's value class, with the two reads behind every value-free fold
status: dispatched
opened: 2026-09-21
priority: P1
cost: H
branch: decide/3-canonical-root
refs: [the-candidate-norm-needs-a-canonical-square-root, the-decision-door-is-opaque-to-the-tier]
---

## What

SYM-10's Phase 2, as Ev ruled it on #2970 (shape 1): a canonical form
for `sqrt` atoms — content split, a primitive polynomial under the
root, `sqrt(N/D) = sqrt(N)/sqrt(D)` under a side condition argued
once, `sqrt(R²) = |R|` — living in `mint_atom`'s `Sqrt` arm so the
walk, `trig` and the registry key through one door; with it the two
certified reads (at `Select`, and at `min`/`max`) behind every
value-free fold. Acceptance: the sign-hull rows green with their
assertions untouched, no decision LOST on the six measured documents
(the plate's eight registered decisions meet again by construction),
every digest, ceiling and split that moves re-baselined and said.
Block DECIDE-B1 slot 0 (H / NUMERIC, pre-draw); protocol v7 IN, the
full v6 dual. Spec: `docs/DECIDE-3-SPEC.md`.
