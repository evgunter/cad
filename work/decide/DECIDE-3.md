---
id: DECIDE-3
kind: unit
title: the canonical root at the mint site: every sqrt atom keyed on its argument's value class, with the two reads behind every value-free fold
status: closed
opened: 2026-09-21
priority: P1
cost: H
branch: decide/3-canonical-root
refs: [the-candidate-norm-needs-a-canonical-square-root, the-decision-door-is-opaque-to-the-tier]
closed: 2026-09-22
---

## What

SYM-10's Phase 2, as Ev ruled it on #2970 (shape 1): a canonical form
for `sqrt` atoms — content split, a primitive polynomial under the
root, `sqrt(N/D) = sqrt(N)/sqrt(D)` under a side condition argued
once, `sqrt(R²) = |R|` — living in `mint_atom`'s `Sqrt` arm so the
walk, `trig` and the registry key through one door; with it the two
certified reads (at `Select`, and at `min`/`max`) behind every
value-free fold. Acceptance: the sign-hull rows green with their
assertions untouched, no decision LOST on the six measured documents
(the plate's eight registered decisions meet again by construction),
every digest, ceiling and split that moves re-baselined and said.
Block DECIDE-B1 slot 0 (H / NUMERIC, pre-draw); protocol v7 IN, the
full v6 dual. Spec: `docs/DECIDE-3-SPEC.md`.

## Closed (2026-09-22)

Merged as #3039 into `props/sign-hull` (landing head `5d3bc7e8b`, run
35687782592). The dual on `a200f768d`: R1 OPUS MERGEABLE-AFTER-FIXES
1/5/6, R2 FABLE NOT MERGEABLE 3/6/6 (the side condition's atom-table
source unsound — removed, gated by a dead-arm row); the fix pass A–S;
the STOP at C lifted by Ev's correction on #3039 (02:03Z: the "no
decision LOST" acceptance above was the orchestrator's spec text, not
the ruling; the ruling is shape 1 plus "never skip out on a change that
would make the code better because it would require rebaselining"),
and item H's concession reversed per Ev (02:05Z) into A0's constant
fold; the delta by R1 on `9399ddaf7`: MERGEABLE, five non-blocking
findings taken as a polish pass. Spec deleted with its
`docs/DOC-LEDGER.md` entry, which names the acceptance's provenance.
The link's `carrier_on_surface_2` stays measured on
`rule-g-trades-sixteen-of-the-links-carrier-on-surface-2` (P1, SYM-9).
