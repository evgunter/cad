# EMIT — the plan

the name table a verb emits, and the names that vanish from it

Opened 2026-09-20 by WIRE's priority-seam cut
(`work/README.md`, Track size). Picked up by an orchestrator on
2026-09-23.

## The slate

**27 budget points** of dispatchable work against a ceiling of 30.

| pri | item | cost | title |
|---|---|---|---|
| P0 | `loft-anchors-every-section-with-section-zeros-map` | H | wire_loft anchors the whole emitted name table on section 0's LoopAnchor, so a section authored rotated or reversed relative to section 0 is named by section 0's permutation |
| P0 | `seam-junction-vertex-name-cannot-be-collapsed-by-the-union-fold` | H | A seam JUNCTION vertex's multi-Seam name is a shape emit_union's collapse refuses |
| P0 | `the-b-side-contact-record-rescue-arm-never-fires` | D | The seam-vertex pass's B-side contact-record rescue arm fires at no vertex in the tree |
| P1 | `name-placed-union-spells-the-narrowing-rule-itself` | D | The 1-vs-many tie decision is hand-written at eight sites outside the door that owns it |
| P1 | `order-along-qualifier-records-no-partner-so-its-pruned-pair-vanish-cannot-be-recovered` | H | Qualifier::OrderAlong records rank and of but no partner name, so a pruned-pair vanish of an OrderAlong fragment name cannot be shadow-executed and falls to the evidence-free RecipeEdit fallback |
| P1 | `side-of-vanish-by-fragment-group-collapse-stays-at-the-evidence-free-rungs` | H | A SideOf fragment name that vanishes because its fragment GROUP collapsed (the partners unmoved) is not recovered by the shadow-exec rung and falls to the evidence-free RecipeEdit fallback |
| P1 | `three-emission-bugs-do-not-speak-the-framing-written-once-for-them` | E | Three NamingError emission bugs do not speak EMISSION_FRAMING, the sentence written once for that category |
| P3 | `names-emit-keeps-an-unguarded-two-refusal-walk` | E | names/emit keeps a two-refusal half-edge to face walk that no test distinguishes |

## Order

`loft-anchors-every-section-with-section-zeros-map` first: it renames
everything downstream of a rotated or reversed section SILENTLY, which
is the widest consequence on this slate and the one a user cannot see.

Then the two vanish classes together
(`order-along-qualifier-records-no-partner-...`,
`side-of-vanish-by-fragment-group-collapse-...`) — both fall to the
evidence-free `RecipeEdit` fallback and the shadow-exec rung is the
same machinery for both, so specifying them apart means specifying it
twice. The two `E` rows are drive-bys in `names/emit.rs`.

## Review posture

No A/B arm for this program's dispatches (Ev, in chat, 2026-09-23: the
account running it has no Fable usage left). Implementer and reviewer
lanes are both Opus; every unit gets one reviewer lane carrying claims
to falsify plus `docs/prompts/reviewer-style-lane.md`. No band ordinals
are consumed from 8300-8399 while this holds.
