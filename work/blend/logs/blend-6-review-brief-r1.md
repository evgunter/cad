# BLEND-6 dual review — reviewer R1

You are one of two independent, blinded reviewers of BLEND unit 6, PR 2215, on the FROZEN head `41b45adc4159008c02ad71dc16d3a4e33bc81ae1` of branch `blend/6-ring-clearance` (merge base `28c4e9ec6c75c88cb15bdd786138965b9a13a80e`). Read, in full and in this order: `/home/user/blend-briefs/common-lane-rules.md` rules 2–4, 6 and 13 (your own worktree; a PRIVATE `CARGO_TARGET_DIR=/home/user/blend-6r1-target` seeded with `cp -a /home/user/blend-seed-target /home/user/blend-6r1-target`; a private scratch dir `/home/user/blend-6r1-scratch`; the foreground rule; no model names anywhere; touched-crate scope on a shared disk), then `docs/prompts/reviewer-style-lane.md` IN FULL (your report names the questions you exercised and carries `sure`/`likely`/`unsure` on every finding), then the binding spec `docs/BLEND-6-SPEC.md`, the two items it names, and the PR body (`mcp__github__pull_request_read`, methods `get` and `get_diff`).

**Isolation (v6 item 5).** Another reviewer is working on the same head at the same time. Do not look for, read, or reason about their worktree, target, scratch, branch, or report; do not push to the unit's branch; push your probe rows only to `origin/blend/6-ring-clearance-r1-probes` if they are green. Your report goes to your final message only.

Check out the frozen head: `git fetch origin blend/6-ring-clearance && git checkout --detach 41b45adc4159008c02ad71dc16d3a4e33bc81ae1`. If the branch has moved past it, say so and still review that head.

**The dispatch is a hypothesis.** The spec is the orchestrator's belief about the tree, and the lane reports two errors in it that you verify rather than assume: (a) the spec labelled the boss's dome rim CONVEX and named `test_support::plane_sphere_cut` as its oracle; the lane measured it CONCAVE (the flat top runs out, the dome's tangent runs up, the material fills the other 270°, the ball rests in the void and the band ADDS material, `+0.0053…`), with the trim circle at `√0.35 > 0.5`, and homed `plane_sphere_external_cut` as the oracle; (b) the spec said "nothing meters the hostless annulus's rings today"; the lane found the pass's per-host ring walk already metered them externally (`−1.4`) behind `resolve_rim`'s ring refusal. A correction to either correction is a finding in its own right.

## Claims to falsify (the correctness lane)

- **C1** Every fixture that carved at the merge base carves bit-identically at the head: re-run `bitdump::` and `shellfix1_bitdump::` at both SHAs with `BITDUMP_DIR`/`SHELLFIX_BITDUMP_DIR` armed, in SEPARATE target dirs, and diff. The PR says eight dump files, 1288 lines, diff empty; the base tree needs the head's `bitdump.rs` and `test_support.rs` to name the added `boss base rim` row — check that `test_support.rs`'s diff against the base is purely additive, as claimed.
- **C2** The four Phase 1 carves (boss dome rim, boss top outer rim, dimple dome rim, dimple top outer rim) are tier-3-valid solids at their closed forms with `volume_pad == 0.0`: re-derive BOTH closed forms independently — the plane–sphere EXTERNAL-tangency corner (kite minus the sphere's segment minus the ball's sector) for the dome rim and `wedge_fill((1,1),(−1,0),(0,−1),0.1)` for the top rim — by your own quadrature or geometry, not by reading `test_support`; decide the dome rim's material side yourself from the body (a stored sense bit or an outward normal read at a point, not the PR's argument).
- **C3** The form choice is right at every caller of `circle_margins`: `external` for a ring the excised strip must not reach, `max(external, trim_inside_other)` for a ladder rim's circular outer boundary, `other_inside_trim` for the hostless annulus's host rings. Construct a body for which a WRONG relation would admit a crossing — a boundary circle nested inside the trim circle (the carve consuming its host); a ring in the excised strip; a convex corner arc of a mixed outer cycle — and show what the head does with each.
- **C4** The lane's D5: predicate 2's sampled screen computes the SAME containment quantity, to the bit, on every coaxial pair and runs FIRST, so the exact backstop has no reachable fixture and a form-swapping mutant reds nothing on the refusing side (the two refusing rows assert `fillet3_face_clearance`). Verify the bit-identity claim (derived `−0.04160797830996155` vs screen `−0.041607978309961546`; `−0.02` vs `−0.020000000000000018`), run the form-swap mutant yourself, and judge the filed item `work/blend/containment-margin-backstop-unreachable-behind-the-screen.md`: is a non-coaxial ring (a pip off-axis) the construction that reaches the backstop, and can you build one through the boolean door in your review?
- **C5** No sentence in `crates/`, `docs/`, `demos/` still states the ring-free condition or "neither occurs today"; every `work/fillet/` pointer under `crates/sweep` is gone (thirteen, the PR says, two dropped as closed items with no destination — were they really closed, and is "issue 1278"/"PR 1753" the right citation?). Run a sweep shaped differently from the PR's three-token one.
- **C6** The trio `m5_pr12_refusals::trio_hostless_annulus_ring_containment` escalates in band typed with the definite arm's recourse, and the K-stream cost per carve is unchanged (`circle_margins` mints no sample; count `fillet3_ring_clearance` decisions per carve before and after through the `Probe` scalar or the k-stats bracket).
- **C7** `resolve_rim`'s hostless arm's remaining "Frontier arm 2" (an outer cycle wider than the request) still refuses with its row unchanged, and the `RimShape::Ladder` guard on the outer-cycle walk stays.

For each claim: what you ran, what you saw, verdict (holds / falsified / could not exercise, with why). Reviews here include end-to-end exercise; existing pinned suites ride the PR gate — run only what gives unique signal (your probes, mutants, the two-tree differential), state the verdict is conditional on the gate being green, AND check that the gate actually ran on `41b45adc4` (`mcp__github__actions_list`, `list_workflow_runs`, branch filter; confirm head SHA and conclusion; the PR cites run 34256492816 — a cited run id is a description, not a state).

## The style lane

`docs/prompts/reviewer-style-lane.md` §2, with this unit's emphasis: **Q1** — `circle_margins` and predicate 2's sampled screen now compute one quantity in two places (D5 says to the bit): two spellings of one margin, or one closed form and one screen that happen to agree on coaxial pairs? Where else does the pass restate what the screen decides? **Q3** — what CAN go red in this unit's refusing rows if the new forms are wrong, given the screen answers first? **Q6** — D3 (both rims in one call refuses at a gate this unit does not own; recourse followed instead), D4 (`effective`'s widening unexercised) and D5 (the backstop unreachable, filed): each an improvement or a deferral owing more? **Q2** — the outer-cycle walk's doc block (`surgery.rs` ~`:2109`–`:2160`) after the rewrite. **Q8** — `surgery.rs` (the largest file touched, ~3800 lines) end to end once.

## Report (≤150 lines, your final message)

1. Verdict: MERGEABLE / MERGEABLE-AFTER-FIXES / NOT MERGEABLE, conditional on the gate as stated.
2. Per claim C1–C7: ran / saw / verdict.
3. MAJOR / MINOR / NOTE findings, each with `file:line`, 2–5 sentences, confidence; every MAJOR with one line of prose on why it is MAJOR.
4. `## Style` per the style lane's §3 (class-not-instance; where else you would look).
5. The questions you exercised, by number.
6. Code-quality rubric: counts MAJOR/MINOR/NOTE; spec deviations reported vs silent (silent counted separately); ratings 1–5 with one line of evidence each for idiom/structure, test quality (do the rows pin the real contract?), doc/comment honesty.
7. Your probe rows worth adopting, by name, on `origin/blend/6-ring-clearance-r1-probes` if green. Never on the unit's branch.
Do not post on GitHub. Cancel any detached job before reporting. Never end your turn with a job running.
