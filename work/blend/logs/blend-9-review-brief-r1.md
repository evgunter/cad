# BLEND-9 dual review — reviewer R1

You are one of two independent, blinded reviewers of BLEND unit 9, PR 2491, on the FROZEN head `3e893671529136796075045fa24e483ce0bf564d` of branch `blend/9-must-carry-one-home` (merge base with main `75a2c5f517362ab3be0e55c9dd209c97ec89758e`; the PR's own differential used the branch point `d6a8656dd`). Read, in full and in this order: `/home/user/blend-briefs/common-lane-rules.md` rules 2–4, 6 and 13 (your own worktree; a PRIVATE `CARGO_TARGET_DIR=/home/user/blend-9r1-target` seeded with `cp -a /home/user/blend-seed-target /home/user/blend-9r1-target`; a private scratch dir `/home/user/blend-9r1-scratch`; the foreground rule; no model names anywhere; touched-crate scope on a shared disk — `-p geom-brep` and `-p sweep` only; a second base-tree target is deleted the moment its differential is done), then `docs/prompts/reviewer-style-lane.md` IN FULL (your report names the questions you exercised and carries `sure`/`likely`/`unsure` on every finding), then the binding spec `docs/BLEND-9-SPEC.md`, the item `work/blend/smooth-arm-siblings-disagree-on-the-in-band-case.md`, and the PR body (`mcp__github__pull_request_read`, methods `get` and `get_diff`; the body carries the differential table, the trio, the K-count table, the mutant table, the sweep's hit list and five stated deviations).

**Isolation (v6 item 5).** Another reviewer is working on the same head at the same time. Do not look for, read, or reason about their worktree, target, scratch, branch, or report; do not push to the unit's branch; push your probe rows only to `origin/blend/9-must-carry-one-home-r1-probes` if they are green. Your report goes to your final message only.

Check out the frozen head: `git fetch origin blend/9-must-carry-one-home && git checkout --detach 3e893671529136796075045fa24e483ce0bf564d`. If the branch has moved past it, say so and still review that head.

**The dispatch is a hypothesis.** The spec is the orchestrator's belief about the tree, and the lane reports five deviations that you verify rather than assume: (a) the revolve in-band row derives the BORE radius, not the arc radius, on a precision argument (`ulp(1.6e5) ≈ 3e-11` against an ε row of 1e-12) — check the argument and whether the row is in-band at all three ε rows; (b) **no out-of-lane smooth pair is constructible through either verb** (two profile segment kinds; extrude mints plane/cylinder; revolve plane/cylinder/cone/sphere/torus; cap–wall meridian rims are never smooth), so the out-of-lane row lives at the wrapper plus a verb-level lane check — try to build one; (c) `folded_lever_arm`'s doc count was NOT rewritten to two because, the lane says, the spec conflated two sibling lists (fold siblings, accurate at three; second-order-margin siblings, accurate at two on `tangent_second_order`'s doc) — verify both lists against the code; (d) `review_fillet_h6_r1_probes::revolve_folds_an_in_band_second_order_into_a_body_tier_3_refuses` was re-baselined and renamed to assert the door's typed refusal — the unit's behaviour change, and the one row that reds with the base callers restored; (e) `upgrade_rim`'s smooth arm (`extrude.rs` ~`:1225`) is a THIRD smooth-description arm deciding by an in-code argument (whose premise the lane found wrong for `Extrusion::Vector`), which the lane FILED (`work/blend/cap-rim-smooth-arm-decides-by-argument-not-by-the-rule.md`) rather than routed through the wrapper, calling it out of the spec's scope — judge that against the spec's "both callers call it and nothing else decides a smooth join's description".

## Claims to falsify (the correctness lane)

- **C1** No stored description in any existing revolve or extrude fixture moved: re-run both `bitdump` rows (`bitdump_extrude_revolve_corpus`, the new `bitdump_other_extrude_revolve_fixtures`) at the merge base and the head in SEPARATE target dirs and diff; the PR says 13 + 9 fixtures, 172 + 92 edges, byte-identical. The base tree needs the head's `bitdump.rs` (and the wrapper, which the new row's base reading kept while reverting only the two caller files — decide whether that is a fair base and say so).
- **C2** An in-band smooth join refuses typed on BOTH verbs, and a definite one on either side stores what the contract says: construct your own near-osculating join from the resolved band (the rows derive `free = √(2·r·margin)` — that derivation is the implementer's, not your oracle) at the default ε and at least one other ε row, and read the predicate name and the payload's margin off the error.
- **C3** The wrapper reads exactly the certification schedule's interior stations (`1..CERT_SAMPLES-1` through `sample_param`, first non-Positive deciding) and NOTHING outside it decides a smooth join's description: grep `tangent_second_order` callers and every `describe_at_rest` site in the two verbs; count K samples per strut through the `Probe` scalar (the PR says 1 → 7 jet-determinate, 1 → 1 under-determined, 1 → 0 out of lane; whole verb per determinate strut 8 → 14) and say whether the doubling per strut is the spec's accepted cost or more.
- **C4** The lane gate is applied before any metering on both verbs, and an out-of-lane pair emits no sample: run the delete-the-gate mutant yourself; and test deviation (b) — a wall kind or a join either verb CAN mint that `tangent_certificate_lane` refuses.
- **C5** The symmetry claims in the wrapper's doc (κ_rel constant along an extruded wall's ruling; constant along a coaxial latitude circle) are true for every pair the lane admits: find a pair in the lane where κ_rel varies along the carrier (a non-coaxial pair? a cone against a torus?), and whether the walk's first-non-Positive-decides rule then reads the right station.

For each claim: what you ran, what you saw, verdict (holds / falsified / could not exercise, with why). Reviews here include end-to-end exercise; existing pinned suites ride the PR gate — run only what gives unique signal (your probes, mutants, the two-tree differential), state the verdict is conditional on the gate being green, AND check that the gate actually ran on `3e8936715` (`mcp__github__actions_list`, `list_workflow_runs`, branch filter; confirm head SHA and conclusion; the PR cites run 34736829969 — a cited run id is a description, not a state).

## The style lane

`docs/prompts/reviewer-style-lane.md` §2, with this unit's emphasis: **Q1** — the PR says the wrapper's walk "matches the tier-3 must-carry arm in `topo::validate` station for station, including the early exit": two spellings of one walk, or one walk with two homes? Where else is `CERT_SAMPLES` walked by hand? **Q3** — what CAN go red in the trio rows if the wrapper is wrong but the band arithmetic is right; are the ε-derived fixtures pins or restatements of the band? **Q6** — each of the five deviations: an improvement or a deferral owing more? Deviation (e) in particular: the filed item names a premise that is wrong for one door — is "the conclusion survives by a different reason" a finding to file or a defect to fix on this unit? **Q2** — `upgrade_intersection`'s doc and `extrude.rs`'s module sentence after the rewrite; the wrapper's own doc (does it argue what the code does, or more?). **Q8** — `crates/sweep/src/revolve/upgrade.rs` end to end once.

## Report (≤150 lines, your final message)

1. Verdict: MERGEABLE / MERGEABLE-AFTER-FIXES / NOT MERGEABLE, conditional on the gate as stated.
2. Per claim C1–C5: ran / saw / verdict.
3. MAJOR / MINOR / NOTE findings, each with `file:line`, 2–5 sentences, confidence; every MAJOR with one line of prose on why it is MAJOR.
4. `## Style` per the style lane's §3 (class-not-instance; where else you would look).
5. The questions you exercised, by number.
6. Code-quality rubric: counts MAJOR/MINOR/NOTE; spec deviations reported vs silent (silent counted separately); ratings 1–5 with one line of evidence each for idiom/structure, test quality (do the rows pin the real contract?), doc/comment honesty.
7. Your probe rows worth adopting, by name, on `origin/blend/9-must-carry-one-home-r1-probes` if green (file named by subject, e.g. `review_must_carry_rule_r1-probes.rs`, mounted in `tests/all.rs`). Never on the unit's branch.
Do not post on GitHub. Cancel any detached job before reporting. Never end your turn with a job running. Commits carry no trailer and no model name.
