# BLEND-15 dual review — reviewer R2

You are one of two independent, blinded reviewers of BLEND unit 15, PR 2514, on the FROZEN head `d94117f95` of branch `blend/15-recourse-roster` (full SHA from `git rev-parse origin/blend/15-recourse-roster` — say so if the branch has moved past it and still review that head; merge base with main `56f18092e372e85f6116bc53762371f5ad2e1843`). Read, in full and in this order: `/home/user/blend-briefs/common-lane-rules.md` rules 2–4, 6 and 13 (your own worktree; a PRIVATE `CARGO_TARGET_DIR=/home/user/blend-15r2-target` seeded with `cp -a /home/user/blend-seed-target /home/user/blend-15r2-target`; a private scratch dir `/home/user/blend-15r2-scratch`; the foreground rule; no model names anywhere; touched-crate scope on a shared disk — `-p sweep -p profile` and, for the one type it adds, `-p geom-core`; a second base-tree target is deleted the moment its differential is done), then `docs/prompts/reviewer-style-lane.md` IN FULL (your report names the questions you exercised and carries `sure`/`likely`/`unsure` on every finding), then the binding spec `docs/BLEND-15-SPEC.md`, the item `work/blend/escalation-recourse-dispatch-has-three-homes.md`, and the PR body (`mcp__github__pull_request_read`, methods `get` and `get_diff`; the body carries the Phase 1 roster — 51 `profile` names and 34 `sweep` names, four and five indirect sites, an empty intersection — the gap sentence's home and why, the roster rows and the order row, a four-mutant table, a 170-render differential in two target dirs, the C5 sweep with its stated blind spots, and eight stated deviations).

**Isolation (v6 item 5).** Another reviewer is working on the same head at the same time. Do not look for, read, or reason about their worktree, target, scratch, branch, or report; do not push to the unit's branch; push your probe rows only to `origin/blend/15-recourse-roster-r2-probes` if they are green. Your report goes to your final message only.

Check out the frozen head: `git fetch origin blend/15-recourse-roster && git checkout --detach $(git rev-parse origin/blend/15-recourse-roster)` and confirm it starts `d94117f95`.

**The dispatch is a hypothesis.** The spec is the orchestrator's belief about the tree, and the lane's Phase 1 measured it: at the merge base `profile`'s `_ =>` arm rendered "path junction classification: {source}" for 30 names of which only two (`path_junction_turn`, `path_junction_side`) are junction classifications — 28 got a false category — while `sweep`'s catch-all already rendered the honest F6 gap sentence for its 24 unrouted names; no name is decided in both crates, so the spec's "one name, two sentences" list is empty. The lane homed the gap sentence as `geom_core::MissingRecourse` — a `Display` NEWTYPE beside `COINCIDENCE_RECOURSE` in `predicate.rs`, not the `&str` constant the spec said — because the sentence interpolates the predicate name in the middle (deviation 2); it chose `geom-core` because `sweep` depends on `profile` and `profile` on `geom-core` alone, so it is the only crate both routers can see. The roster rows measure by RENDERING the door's own error per decided name, not by reading the match; an `INDIRECT` table declares every `decide*` call whose first argument is not a literal (a parameter, a const, a struct field) with its carrier, which the row then scans. The order row's subject is "no name sits in two layers" — the lane says a pure re-ordering of the fillet block below the match changes no text because the four layers' name sets are disjoint, so the row cannot red that. The lane reports eight deviations you verify rather than assume: (1) the brief's starting tree was wrong — BLEND-14 (PR 2509) was still open, so `ALL_RECOURSES` was `;15` and `tangent_second_order` unrouted, which the roster tolerates by measuring rather than counting; (2) the newtype; (3) `geom-core` touched — one type and one re-export; (4) the roster reader duplicated per suite rather than homed in `test-utils`; (5) `UNROUTED` reasons grouped into shared constants; (6) the differential's subject is every decided name through both doors, not the suites' refusal strings; (7) two files outside the crate scope, each one list entry a red CI row demanded (`crates/test-utils/tests/reader_census.rs`; `crates/pncad-py/src/prose_census.rs`); (8) two container restarts mid-lane, no work lost.

## Claims to falsify (the correctness lane)

- **C1** Every routed sentence renders as before: re-run the rendered-text differential in SEPARATE target dirs (the lane says 170 renders; `sweep` 0 of 85 changed; `profile` 62 of 85 changed and every one was the `_ =>` fall-through at base — check that last clause per render, not by count) and extend it to the refusal texts the profile and sweep SUITES actually produce (the spec's instrument, which the lane replaced by deviation 6): dump every `Display` of every refusal both suites emit at both SHAs and diff.
- **C2** The roster is complete: find a `decide` name in either crate's `src` the roster row does not see — a name built by `concat!` or a macro, a name decided in a `#[cfg(test)]` module, a name passed through a helper the reader's `decide*` pattern does not match, a name in a file the reader does not walk. Plant one of each shape and say which the row catches (the lane says a non-literal first argument reds `every_indirect_funnel_call_is_declared`; verify that claim for each shape).
- **C3** An unknown name renders the gap sentence at BOTH arms and nowhere asserts a category or falls silent: construct one through a test-only predicate at each door and read the text; check `MissingRecourse(None)` (no name) and the two junction keys still starting "path junction classification"; and look for a THIRD mouth — any `Display` or renderer (`editor-core`, `pncad-py`, the viewer) that receives an escalation and says something else about an unknown name.
- **C4** The dispatch order is pinned: plant a name in two families (a `fillet_*` literal in the stored-form arm; a stored-form name in `fillet_recourse_for`) and confirm the order row reds; then plant the re-ordering the lane says it cannot catch (the fillet block moved below the match) and say whether ANY row or the differential catches it, and whether the spec's "a name in two families would red" is the whole of what order means here.
- **C5** No third routing table survives anywhere in `crates/*/src`: re-run the sweep for `match source.predicate`, `Some("…")` arms on predicate names, name sets with `contains`, and `iter().find()` lookups keyed on a predicate name, including `editor-core`'s and `pncad-py`'s renderers and the viewer; adjudicate each of the lane's "not this class" dispositions (`revolve/tube.rs::door`, `validate.rs`'s near-tangency addendum, `HOLLOW_PREDICATES`), and search the blind spots the lane named (a table keyed on an error variant or a site enum; a routing function found only by reading its caller).

For each claim: what you ran, what you saw, verdict (holds / falsified / could not exercise, with why). Reviews here include end-to-end exercise; existing pinned suites ride the PR gate — run only what gives unique signal (your probes, mutants, the two-tree differential), state the verdict is conditional on the gate being green, AND check that the gate actually ran on `d94117f95` (`mcp__github__actions_list`, `list_workflow_runs`, branch filter; confirm head SHA and conclusion; the PR cites run 34784659718 — a cited run id is a description, not a state).

## The style lane

`docs/prompts/reviewer-style-lane.md` §2, with this unit's emphasis: **Q1** — the two routers now share one fall-through but keep two tables in two shapes (a function in `validate.rs` plus an inline eight-name list in `path.rs`; an inline thirteen-name match in `sweep/blend/mod.rs`): is that one rule with one home, and did the unit close the class the item names or one instance of it? Where else would you look (`revolve/tube.rs`, `editor-core/src/drive.rs`, `validate.rs:822-830`)? **Q3** — what can go red in the seven rows if a name is routed to the WRONG sentence (the roster proves routed-or-listed, not paired): is that the driven rows' job, and do they cover the sweep side? **Q6** — each of the eight deviations: an improvement or a deferral owing more? (2) and (4) in particular — a newtype where a constant was specified, and a reader copied per suite in a tree whose style lane names the hand-written census as the trap. **Q2** — the gap sentence's text and the `UNROUTED` reason constants: does a reason like "no recourse; the door's refusal is the recourse" hold for every name under it? **Q8** — `profile/src/path.rs`'s `Escalated` Display end to end once, and `sweep/src/blend/mod.rs`'s recourse section (the constants, `ALL_RECOURSES`, the `Escalated` route).

## Report (≤150 lines, your final message)

1. Verdict: MERGEABLE / MERGEABLE-AFTER-FIXES / NOT MERGEABLE, conditional on the gate as stated.
2. Per claim C1–C5: ran / saw / verdict.
3. MAJOR / MINOR / NOTE findings, each with `file:line`, 2–5 sentences, confidence; every MAJOR with one line of prose on why it is MAJOR.
4. `## Style` per the style lane's §3 (class-not-instance; where else you would look).
5. The questions you exercised, by number.
6. Code-quality rubric: counts MAJOR/MINOR/NOTE; spec deviations reported vs silent (silent counted separately); ratings 1–5 with one line of evidence each for idiom/structure, test quality (do the rows pin the real contract?), doc/comment honesty.
7. Your probe rows worth adopting, by name, on `origin/blend/15-recourse-roster-r2-probes` if green (file named by subject, e.g. `review_recourse_roster_r2_probes.rs`, mounted in the crate's `tests/all.rs` and rostered where the gates demand). Never on the unit's branch.
Do not post on GitHub. Cancel any detached job before reporting. Never end your turn with a job running. Commits carry no trailer and no model name.
