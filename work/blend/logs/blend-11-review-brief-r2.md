# BLEND-11 dual review — reviewer R2

You are one of two independent, blinded reviewers of BLEND unit 11, PR 2495, on the FROZEN head `21a5f1e4597520a465e299aaa7fb22feae7f412c` of branch `blend/11-overrun-nearest-fit` (merge base with main `d73ba7d826ecd600d69b42a3f7d7f3e0a271b743`; the PR's own measurements used the branch point `406a746a8`). Read, in full and in this order: `/home/user/blend-briefs/common-lane-rules.md` rules 2–4, 6 and 13 (your own worktree; a PRIVATE `CARGO_TARGET_DIR=/home/user/blend-11r2-target` seeded with `cp -a /home/user/blend-seed-target /home/user/blend-11r2-target`; a private scratch dir `/home/user/blend-11r2-scratch`; the foreground rule; no model names anywhere; touched-crate scope on a shared disk — `-p profile` only; a second base-tree target is deleted the moment its differential is done), then `docs/prompts/reviewer-style-lane.md` IN FULL (your report names the questions you exercised and carries `sure`/`likely`/`unsure` on every finding), then the binding spec `docs/BLEND-11-SPEC.md`, the item `work/blend/overrun-attribution-picks-the-first-candidate.md`, and the PR body (`mcp__github__pull_request_read`, methods `get` and `get_diff`; the body carries the Phase 1 table, the STOP and the orchestrator's re-scope, the rule at two sites, the mutant table, both differentials, the sweep and seven stated deviations).

**Isolation (v6 item 5).** Another reviewer is working on the same head at the same time. Do not look for, read, or reason about their worktree, target, scratch, branch, or report; do not push to the unit's branch; push your probe rows only to `origin/blend/11-overrun-nearest-fit-r2-probes` if they are green. Your report goes to your final message only.

Check out the frozen head: `git fetch origin blend/11-overrun-nearest-fit && git checkout --detach 21a5f1e4597520a465e299aaa7fb22feae7f412c`. If the branch has moved past it, say so and still review that head.

**The dispatch is a hypothesis.** The spec is the orchestrator's belief about the tree; the lane stopped once at its fence and the orchestrator re-scoped it; you verify both the re-scope and the lane's deviations rather than assume them: (a) the pick could not be spelled in `sugar.rs::arc_fillet_trims<T: Decide>` (the Bounds scope rule: no bracket read; the spec forbids a new predicate), so the ruled shape is ARM-COLLECTS (every corner-side overrun kept at the scalar, in enumeration order, both legs' numbers) + DOOR-PICKS (`path/arc_fillet.rs::map_refusal<T: Bounds>` reads `−margin.lo()`, keeps the least deficit, ties to the earlier) — is that the shipped home for such a read (`bounds_census`), and does the arm still classify all four gates for every candidate with the `?` aborting before any collection?; (b) the orchestrator ADDED a rule from the lane's own measurement: the reported LEG is the candidate's WORSE leg (the one whose overrun is the deficit), not "incoming first" — that moved 230 grid-A reports and 72 line×arc reports beyond the spec's 19; judge whether the addition is the honest reading of the recourse or a widening; (c) C3 "the recourse is tight" is FALSE at the lane's fixture: reducing the radius by the reported deficit builds, but the largest building radius is smaller than `r − δ` by a margin (authoring 674: largest building radius 0.0903) — the row pins "followable, not tight"; decide whether the spec's C3 was wrong or the reported deficit is the wrong number for the recourse; (d) the census row pins the 19 changed reports as a dump-derived table, not an independent re-derivation; (e) the seam amendment naming `path/arc_fillet.rs` is on the orchestrator branch, not on the main the lane merged.

## Claims to falsify (the correctness lane)

- **C1** Every fillet that built at the merge base is bit-identical at the head, and every refusal keeps its variant: re-run both differentials yourself (the lane dumped 19,104 authorings — grid A plus a 960-authoring line×arc grid — under `k_stats::detached`: 0 built outputs differ, 0 verdict sequences differ, 320 refusals differ, all confined to `AnchorOutsideTrimmedExtent` payloads) at the merge base and the head in SEPARATE target dirs.
- **C2** The reported candidate IS the least-deficit one and the reported leg IS the one the deficit is on: instrument the arm and the door yourself over grid A and over your own corners — arc × arc with four centres, a corner where the two overruns are on different legs, a corner where the two candidates tie to the bit — and compare the report with the least deficit; find a corner where the head's report is not the rule's answer.
- **C3** The recourse is followable at every one of the 19 changed reports (reduce the radius by the reported deficit plus one ulp and build); measure how far from tight it is on arc legs and say whether the reported number is the right one for the sentence "reduce the radius" (the setback does not scale 1:1 with the radius on arc legs — the lane's own note).
- **C4** The sample sequence per corner class is unchanged: count K samples per authoring at the base and the head through the `Probe` scalar or the `k_stats` verdict logs on your own fixtures, not only the lane's three.
- **C5** The rule's sentence is true at BOTH sites and on every path: the arm's (collect exhaustively, compare nothing) including the escalation path; the door's (least deficit, worse leg, ties) including single-candidate refusals; and `crates/profile/README.md`'s envelope prose does not now contradict either.

For each claim: what you ran, what you saw, verdict (holds / falsified / could not exercise, with why). Reviews here include end-to-end exercise; existing pinned suites ride the PR gate — run only what gives unique signal (your probes, mutants, the two-tree differential), state the verdict is conditional on the gate being green, AND check that the gate actually ran on `21a5f1e45` (`mcp__github__actions_list`, `list_workflow_runs`, branch filter; confirm head SHA and conclusion; the PR cites run 34741638444 — a cited run id is a description, not a state).

## The style lane

`docs/prompts/reviewer-style-lane.md` §2, with this unit's emphasis: **Q1** — the arm now carries a two-slot `DoesNotFit { first, second: Option }` sized by "≤2 centres": is that an invariant the code holds or a comment; where else does the crate size a payload by a comment? **Q3** — what CAN go red in the six rows if the door's read is wrong: are the 19-row census and the pinned verdict logs pins or restatements of the dumps? **Q6** — each of the seven deviations: an improvement or a deferral owing more? (b) and (c) above in particular. **Q2** — the arm's comment block (the corner-attribution sentence kept, the candidate-level sentence added) and `map_refusal`'s read-inventory doc after the rewrite. **Q8** — `crates/profile/src/path/arc_fillet.rs` end to end once.

## Report (≤150 lines, your final message)

1. Verdict: MERGEABLE / MERGEABLE-AFTER-FIXES / NOT MERGEABLE, conditional on the gate as stated.
2. Per claim C1–C5: ran / saw / verdict.
3. MAJOR / MINOR / NOTE findings, each with `file:line`, 2–5 sentences, confidence; every MAJOR with one line of prose on why it is MAJOR.
4. `## Style` per the style lane's §3 (class-not-instance; where else you would look).
5. The questions you exercised, by number.
6. Code-quality rubric: counts MAJOR/MINOR/NOTE; spec deviations reported vs silent (silent counted separately); ratings 1–5 with one line of evidence each for idiom/structure, test quality (do the rows pin the real contract?), doc/comment honesty.
7. Your probe rows worth adopting, by name, on `origin/blend/11-overrun-nearest-fit-r2-probes` if green (file named by subject, e.g. `review_fillet_overrun_nearest_fit_r2-probes.rs`, mounted in `tests/all.rs`). Never on the unit's branch.
Do not post on GitHub. Cancel any detached job before reporting. Never end your turn with a job running. Commits carry no trailer and no model name.
