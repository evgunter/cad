# BLEND-7 dual review — reviewer R1

You are one of two independent, blinded reviewers of BLEND unit 7, PR 2483, on the FROZEN head `5d2782a4e604e1721b06bc092222b15eae385f54` of branch `blend/7-closed-chain-junctions` (merge base with main `f48d2c8f74ea223e87fa455992a01b11ca82fad9`; the PR's own differential used base `9628abfd7`). Read, in full and in this order: `/home/user/blend-briefs/common-lane-rules.md` rules 2–4, 6 and 13 (your own worktree; a PRIVATE `CARGO_TARGET_DIR=/home/user/blend-7r1-target` seeded with `cp -a /home/user/blend-seed-target /home/user/blend-7r1-target`; a private scratch dir `/home/user/blend-7r1-scratch`; the foreground rule; no model names anywhere; touched-crate scope on a shared disk — this box has ~12 GB free and two review lanes share it, so a second base-tree target must be deleted the moment its differential is done), then `docs/prompts/reviewer-style-lane.md` IN FULL (your report names the questions you exercised and carries `sure`/`likely`/`unsure` on every finding), then the binding spec `docs/BLEND-7-SPEC.md`, the item `work/blend/closed-chain-junctions-pair-with-a-rotated-link.md`, and the PR body (`mcp__github__pull_request_read`, methods `get` and `get_diff`; the body carries the Phase 1 pairing table, the door table, the premise table, the mutant table and the sweep's hit list).

**Isolation (v6 item 5).** Another reviewer is working on the same head at the same time. Do not look for, read, or reason about their worktree, target, scratch, branch, or report; do not push to the unit's branch; push your probe rows only to `origin/blend/7-closed-chain-junctions-r1-probes` if they are green. Your report goes to your final message only.

Check out the frozen head: `git fetch origin blend/7-closed-chain-junctions && git checkout --detach 5d2782a4e604e1721b06bc092222b15eae385f54`. If the branch has moved past it, say so and still review that head.

**The dispatch is a hypothesis.** The spec is the orchestrator's belief about the tree, and the lane reports deviations from it that you verify rather than assume: (a) the spec's "three-arc bore" concave twin is CONVEX by the lane's measurement (a 90° wedge at the cap rim of a through-bore), and the concave rims it rowed instead are a boss foot (`block ∪ cylinder`, ladder) and a pocket floor (`block − cylinder`, annulus) — check the material side of all four fixtures off the body, not off the PR's argument; (b) the spec's "open chain carves bit-identically" acceptance became a pairing-plus-verdict pin because, the lane says, no chain with a junction carves today (the corner vocabulary refuses run-outs) — is that true of every open chain the suites build, and does the die/rod differential actually cover the open-chain paths?; (c) `Junction { vertex, arriving, leaving }` is the spec's preferred shape — check that `walk_chains`' structural rule and `fillet3_chain_g1`'s margin, lever and band are byte-identical to the base, as the constraints require; (d) the lane filed `work/blend/rim-of-refuses-extruded-multi-arc-rims.md` (`topo::query::rim_of` refuses every extruded rim of ≥2 bulged arcs because per-arc carriers differ at 1e-17) — reproduce it, and say whether it is the reason the new fixtures select their rims through `test_support` rather than the public selector.

## Claims to falsify (the correctness lane)

- **C1** Every two-link rim and open chain carves bit-identically to the merge base: re-run `bitdump::` (and any other armed dump — `shellfix1_bitdump::`, `review_arms2_r1_probes::`) at both SHAs with the dump dirs armed, in SEPARATE target dirs, and diff. The PR says 13 dumps, 1444 lines, clean; the base tree needs the head's `bitdump.rs` and `test_support.rs` to name the added extruded two-arc rows — check that `test_support.rs`'s diff against the base is purely additive.
- **C2** The three- and four-arc rims carve tier-3 valid at their closed forms with `volume_pad == 0.0` on both material sides: re-derive the plane–cylinder Pappus form yourself (`ρ²(1−π/4)` section, centroid `ρ/(6(1−π/4))` from the corner, about the rim's axis at `R∓ρ`) — the row's derivation is the implementer's, not yours — and decide each fixture's material side from the body.
- **C3** After the change, no junction of any chain the suites build is paired with a link that does not touch it: instrument it yourself (the PR exposes `test_support::walked_chains` and the row `every_junction_of_every_walked_chain_touches_both_its_links` over 54 closed walks — do not trust the row's own instrument; read `ring[j.arriving]`/`ring[j.leaving]` against the vertex's incidence independently); then construct a chain shape the suites do not build — a five-arc rim, a closed chain seeded from a different link, a rim whose links are not all arcs, a revolve's seam-split rim of three or more crossings — and check the pairing on it.
- **C4** The mutant (rotated list + positional pairing, guard removed) reds EXACTLY the N ≥ 3 rows: run it yourself over `cargo test -p sweep --test all` and list every red row; the PR says four N ≥ 3 rows plus the flipped r1 row red, the open-chain row and 36 two-arc/one-edge rows green. A row that stays green under the mutant while claiming to pin the pairing is a finding.
- **C5** The premise table is complete: find an `arcs.len() == 2` (or an equivalent two-crossing assumption — slice patterns `[a, b]`, index `[1]`, prose "two arcs", "both arcs", "the other arc") in `crates/sweep/src/**` or the surgery's annulus walk the PR did not list; the PR's sweep pattern was `len\(\), 2\)|len\(\) == 2|len\(\) != 2` and it says slice patterns and prose escape it. Run a sweep shaped differently.

For each claim: what you ran, what you saw, verdict (holds / falsified / could not exercise, with why). Reviews here include end-to-end exercise; existing pinned suites ride the PR gate — run only what gives unique signal (your probes, mutants, the two-tree differential), state the verdict is conditional on the gate being green, AND check that the gate actually ran on `5d2782a4e` (`mcp__github__actions_list`, `list_workflow_runs`, branch filter; confirm head SHA and conclusion; the PR cites run 34730583374 — a cited run id is a description, not a state).

## The style lane

`docs/prompts/reviewer-style-lane.md` §2, with this unit's emphasis: **Q1** — `test_support.rs` gained seven builders (`disc_of_arcs`, `cylinder_of_arcs_at`, `bored_block_of_arcs`, `boss_of_arcs`, `pocket_of_arcs`, `circle_arcs_at_z`, `walked_chains`) in a module the slate already records as having become four modules (`work/blend/test-support-has-become-four-modules.md`): which of them restate a builder that exists, and is `walked_chains` a test seam that belongs on the battery's public surface or a `pub(crate)` leak? **Q2** — the fourteen premise-message sites: do the rewritten messages say why two, or do they say "two" again in more words? **Q3** — what CAN go red if the junction record is wrong but the walk is right: is `debug_assert!` on incidence a pin or a comment? **Q6** — each deviation (the convex "concave twin", the open-chain acceptance narrowed, the ladder doc line, the checkpoint commits carrying instrumentation): an improvement or a deferral owing more? **Q8** — `battery.rs` end to end once.

## Report (≤150 lines, your final message)

1. Verdict: MERGEABLE / MERGEABLE-AFTER-FIXES / NOT MERGEABLE, conditional on the gate as stated.
2. Per claim C1–C5: ran / saw / verdict.
3. MAJOR / MINOR / NOTE findings, each with `file:line`, 2–5 sentences, confidence; every MAJOR with one line of prose on why it is MAJOR.
4. `## Style` per the style lane's §3 (class-not-instance; where else you would look).
5. The questions you exercised, by number.
6. Code-quality rubric: counts MAJOR/MINOR/NOTE; spec deviations reported vs silent (silent counted separately); ratings 1–5 with one line of evidence each for idiom/structure, test quality (do the rows pin the real contract?), doc/comment honesty.
7. Your probe rows worth adopting, by name, on `origin/blend/7-closed-chain-junctions-r1-probes` if green (file named by subject, e.g. `review_closed_chain_junctions_r1-probes.rs`, mounted in `tests/all.rs`). Never on the unit's branch.
Do not post on GitHub. Cancel any detached job before reporting. Never end your turn with a job running. Commits carry no trailer and no model name.
