# BLEND-14 dual review — reviewer R1

You are one of two independent, blinded reviewers of BLEND unit 14, PR 2509, on the FROZEN head `c603c8fb5facac072f97bd9a0604135f7208f08c` of branch `blend/14-contact-edge-must-carry` (merge base with main `18b193a5723288a6335c558c1b1c927985b8a163`). Read, in full and in this order: `/home/user/blend-briefs/common-lane-rules.md` rules 2–4, 6 and 13 (your own worktree; a PRIVATE `CARGO_TARGET_DIR=/home/user/blend-14r1-target` seeded with `cp -a /home/user/blend-seed-target /home/user/blend-14r1-target`; a private scratch dir `/home/user/blend-14r1-scratch`; the foreground rule; no model names anywhere; touched-crate scope on a shared disk — `-p sweep` only, and this box has three other lanes building beside you: a second base-tree target is deleted the moment its differential is done), then `docs/prompts/reviewer-style-lane.md` IN FULL (your report names the questions you exercised and carries `sure`/`likely`/`unsure` on every finding), then the binding spec `docs/BLEND-14-SPEC.md`, the item `work/blend/blend-contact-edges-mint-the-intrinsic-description-without-the-rule.md`, and the PR body (`mcp__github__pull_request_read`, methods `get` and `get_diff`; the body carries the corpus table of 158 contact edges, the near-osculating family, the stop-clause answer, the verdict mapping, the rows, the mutant, the differential, the K count, the sweep and six stated deviations).

**Isolation (v6 item 5).** Another reviewer is working on the same head at the same time. Do not look for, read, or reason about their worktree, target, scratch, branch, or report; do not push to the unit's branch; push your probe rows only to `origin/blend/14-contact-edge-must-carry-r1-probes` if they are green. Your report goes to your final message only.

Check out the frozen head: `git fetch origin blend/14-contact-edge-must-carry && git checkout --detach c603c8fb5facac072f97bd9a0604135f7208f08c`. If the branch has moved past it, say so and still review that head.

**The dispatch is a hypothesis.** The spec is the orchestrator's belief about the tree, and the lane's Phase 1 says the spec's near-osculating family cannot reach the rule: **the battery screens it first** — at `R/r_band ∈ {1.1, 1.01, 1 + 10⁻⁴, 1 + 10⁻⁶}` `fillet3_face_clearance` refuses definite-negative at every ε row (a ball nearly filling the rod drives the crease's dihedral to zero, the cylinder-side setback → `R·π/2` while the flat's width collapses); the door admits the family down to `R/r ≈ 1.2` and every admitted member is `JetDeterminate`. The lane answered the stop clause differently: on this branch headroom `= 2·margin`, so the rule is in band with the headroom definite exactly for `margin ∈ (Kε/2, Kε)`, and a rod at an ADMITTED ratio scaled into that octave (`R/r = 2`, `r ≈ 3e-8 / 3e-5 / 3e-11`) reaches the in-band verdict at all three ε rows — today it builds the intrinsic description and then refuses from inside the attachment gate. Verify both halves. The lane reports six deviations you verify rather than assume: (a) the in-band rows use the admitted-and-scaled case, the screening pinned as a row; (b) NO lane-refused pair and no `UnderDetermined` fixture is constructible through the door (margin ≤ ε ⇒ headroom ≤ 2ε ⇒ `fillet3_radius_headroom` refuses first; every admitted support is analytic on a line / coaxial-circle carrier) — that arm has no row; (c) the concave difference-branch fixture (a D-bore) refuses `BodyNotIntact` at the ruled door because the walk keys a D-hole's rim on the cap's OUTER cycle where it is a RING — filed (`work/blend/ruled-band-keys-a-d-hole-rim-on-the-caps-outer-cycle.md`); (d) the differential was taken in ONE target dir sequentially (disk); (e) the verdict mapping: `UnderDetermined → Chart { surface: s1, image: None, seam: false, declared: None }` (the `he_plus` chart, as extrude argues for `k_prev`) and `InBand(source) → BlendError::Escalated { site: BlendSite::Chain, source }` with a NEW recourse `FILLET3_CONTACT_RECOURSE` ("enlarge the radius — on a support curving the band's own way, toward half its radius of curvature — or blend a larger feature"; the lane argues `FILLET3_RADIUS_RECOURSE`'s "reduce" is the wrong direction for the only reachable case); (f) the probe census roster and the followability suite's header moved.

## Claims to falsify (the correctness lane)

- **C1** No stored description in the tree moved: re-run the bit-dump differential in SEPARATE target dirs (the lane ran one sequentially — the PR says 14 files, 1702 lines, 176 `TangentIntersection` edges byte-identical); the corpus census (158 contact edges, all `JetDeterminate`, the minima the closed form) re-taken independently on at least the die and the rod.
- **C2** The near-osculating blend really reaches the in-band verdict through the public door: construct your own from the closed form (the row's radii `{2, 1.5} × {0.55, 0.95}·Kε` are the implementer's, not your oracle) at the three ε rows, read the predicate, the margin and the recourse off the error; and attack the screening claim — find a member of the spec's family (`R/r` below 1.2) that the battery ADMITS at some ε row, or a different support geometry (a concave band in a concave support; a torus support; the corner ball's own edges) that reaches the rule's in-band or under-determined verdict without the octave trick.
- **C3** The three verdicts map to the right descriptions: the `UnderDetermined → Chart` choice (is `he_plus`'s chart with `image: None` what the certificate can certify for a contact edge, and does anything downstream read a contact edge's `Chart` differently from a `TangentIntersection`?); the `InBand → Escalated { site: Chain }` choice (is `Chain` honest for a contact edge, and does the recourse's lever — ENLARGE the radius — hold at every site the predicate fires, per the A3-2 rule?); construct a lane-refused pair if you can (the lane says none exists through the door — a `Nurbs` support? a loft?).
- **C4** The rule is called once per contact edge with the right extent and window: count K samples through the `Probe` scalar (the PR says 7 per contact edge in the rule where it was 0, plus the certificate's 7; the D-rod's four trimlines 56 vs 28 under the mutant) and compare; check `edge_extent(&curve, t0, t1, chord)` is the extent the certificate uses.
- **C5** Nothing else in `sweep::blend` mints `TangentIntersection` outside the rule (the four patterns; the corner ball's arcs — the lane says they take the same `attach_contact` path — confirm), and the filed D-hole residue is real (reproduce the `BodyNotIntact` and say whether the ruled walk's outer-cycle keying is the cause).

For each claim: what you ran, what you saw, verdict (holds / falsified / could not exercise, with why). Reviews here include end-to-end exercise; existing pinned suites ride the PR gate — run only what gives unique signal (your probes, mutants, the two-tree differential), state the verdict is conditional on the gate being green, AND check that the gate actually ran on `c603c8fb5` (`mcp__github__actions_list`, `list_workflow_runs`, branch filter; confirm head SHA and conclusion; the PR cites run 34754771075 — a cited run id is a description, not a state).

## The style lane

`docs/prompts/reviewer-style-lane.md` §2, with this unit's emphasis: **Q1** — `attach_contact` now composes `must_carry_over_edge` beside the seam and transverse arms: three arms, one rule, or is the structural pair still deciding what the rule should? Where else in `surgery.rs` is a description chosen structurally? **Q3** — what CAN go red in the six rows if the mapping is wrong but the rule is right: the lane admits no lane row can red; is the `Chart` arm a comment? **Q6** — each deviation: an improvement or a deferral owing more? (b) and (c) in particular — a filed residue with a fixture that cannot be built is the shape the style lane names. **Q2** — the new recourse constant's sentence and the shrunk comment block at the site; `FILLET3_CONTACT_RECOURSE` beside `FILLET3_RADIUS_RECOURSE` — two sentences about one lever in opposite directions, or two situations? **Q8** — `crates/sweep/src/blend/mod.rs`'s recourse section end to end once (the sixteen constants and `ALL_RECOURSES`).

## Report (≤150 lines, your final message)

1. Verdict: MERGEABLE / MERGEABLE-AFTER-FIXES / NOT MERGEABLE, conditional on the gate as stated.
2. Per claim C1–C5: ran / saw / verdict.
3. MAJOR / MINOR / NOTE findings, each with `file:line`, 2–5 sentences, confidence; every MAJOR with one line of prose on why it is MAJOR.
4. `## Style` per the style lane's §3 (class-not-instance; where else you would look).
5. The questions you exercised, by number.
6. Code-quality rubric: counts MAJOR/MINOR/NOTE; spec deviations reported vs silent (silent counted separately); ratings 1–5 with one line of evidence each for idiom/structure, test quality (do the rows pin the real contract?), doc/comment honesty.
7. Your probe rows worth adopting, by name, on `origin/blend/14-contact-edge-must-carry-r1-probes` if green (file named by subject, e.g. `review_contact_edge_must_carry_r1-probes.rs`, mounted in `tests/all.rs`). Never on the unit's branch.
Do not post on GitHub. Cancel any detached job before reporting. Never end your turn with a job running. Commits carry no trailer and no model name.
