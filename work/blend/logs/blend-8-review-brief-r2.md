# BLEND-8 dual review — reviewer R2

You are one of two independent, blinded reviewers of BLEND unit 8, PR 2505, on the FROZEN head `f792d96d0cd4e7c604cf684762a012d36f5a55c3` of branch `blend/8-ladder-split-key` (merge base with main `9739568afa42593114d90c53adcec3adc081ac5b`; the PR's own differentials used `1cd1e8206`). Read, in full and in this order: `/home/user/blend-briefs/common-lane-rules.md` rules 2–4, 6 and 13 (your own worktree; a PRIVATE `CARGO_TARGET_DIR=/home/user/blend-8r2-target` seeded with `cp -a /home/user/blend-seed-target /home/user/blend-8r2-target`; a private scratch dir `/home/user/blend-8r2-scratch`; the foreground rule; no model names anywhere; touched-crate scope on a shared disk — `-p sweep` only; a second base-tree target is deleted the moment its differential is done), then `docs/prompts/reviewer-style-lane.md` IN FULL (your report names the questions you exercised and carries `sure`/`likely`/`unsure` on every finding), then the binding spec `docs/BLEND-8-SPEC.md`, the item `work/blend/ladder-rim-phase-may-retire-a-new-split-key.md`, and the PR body (`mcp__github__pull_request_read`, methods `get` and `get_diff`; the body carries the Phase 1 census per row, the executable re-take, the mutant, both differentials, the sweep's hit list and ten stated deviations).

**Isolation (v6 item 5).** Another reviewer is working on the same head at the same time. Do not look for, read, or reason about their worktree, target, scratch, branch, or report; do not push to the unit's branch; push your probe rows only to `origin/blend/8-ladder-split-key-r2-probes` if they are green. Your report goes to your final message only.

Check out the frozen head: `git fetch origin blend/8-ladder-split-key && git checkout --detach f792d96d0cd4e7c604cf684762a012d36f5a55c3`. If the branch has moved past it, say so and still review that head.

**The dispatch is a hypothesis.** The spec is the orchestrator's belief about the tree, and the lane's Phase 1 says the belief was wrong in its premise: **the fresh-key retirement was not "unmeasured", it was LIVE** — instrumenting `rim_phase`'s split site and step (6) over the whole `sweep` suite at the merge base, 14 shipped rows already pushed a fresh key into `dead.edges` (25 ladder carves, 100 annulus carves); the witness is `test_support::boss` + `merge_coplanar_faces` (a revolve-minted cap seam running pole → rim), and `fillet_h4_concave_rim::the_boss_carves_a_concave_ladder_band_and_adds_the_cap_fill` runs both orientations in one row. Re-taken as an executable claim: 15 rows red at the base, 0 at the head over 611 recorded carves. The lane reports ten deviations you verify rather than assume: (a) the birth record is deliberately NOT unchanged — 29 of 611 records differ, every difference in `dead.edges` on exactly the 14 census rows (the spec's "identical too" rested on the wrong premise); (b) NO `editor-core` document-layer row — a `sweep` test cannot name `editor_core`, the witness body is not authorable through the recipe layer (`merge_coplanar_faces` is exposed by no `Node`), and `names/emit_blend.rs`'s retired-set guard is consulted only for output entities that are not minted, so it CANNOT observe this defect (reported, not filed — EVAL's ground); (c) the annulus's two retirement sites re-spelled through the shared `retire_fragment` (record-inert); (d) a `#[cfg(debug_assertions)]` postcondition that every retirement names a source key; (e) `split_fragment` first written `Decide + Bounds`, narrowed to `Decide` when the bounds allowlist gate fired; (f) the ladder's meridian split meets an already-a-fragment meridian 0 times in 346, the ruled band's cap-rim split 20 times in 112 — the spec's clause-1 question answered by measurement.

## Claims to falsify (the correctness lane)

- **C1** Every ladder carve at the merge base is bit-identical at the head (re-run the bit-dump differential in SEPARATE target dirs; the PR says 14 corpus files byte-identical), and the birth-record differential moves ONLY in `dead.edges` and ONLY on the 14 census rows (re-take it; a moved record anywhere else, or a moved geometry anywhere, is a finding).
- **C2** The witness reaches the orientation through a public door and the head carves it with every retirement naming a source key: re-derive from `crates/topo/src/split.rs::split_edge` which piece keeps the parent key and why (the stored direction rule), and check the lane's `split_fragment` against it — do NOT trust the census; then construct a body the census did not carve (a different seam orientation, a boolean-minted pip whose seam runs rim → pole, a ladder on a ruled band) and read its retirements.
- **C3** The mutant (the old push restored) reds the witness row on totality direction (b) and nothing else (1369 green): run it; then find a shipped row it SHOULD red and does not (the 14 census rows carve fresh-key retirements at the base — why does only the witness row go red?).
- **C4** The provenance rule has ONE spelling in `sweep::blend` (`split_fragment` / `retire_fragment`): grep for a third — the annulus phase's seam splits, `open/ruled.rs`'s cap-rim split, any `dead.edges.push` or `dead.vertices.push` that hands a key without the helper; and judge the debug postcondition — a pin or a comment?
- **C5** The census is complete over the tree's ladder fixtures: find one it missed (a ladder carve in a suite the instrument did not run, an `#[ignore]`d row, a demos/ body); and check the sweep's stated blind spots (`dead.vertices` uncovered by the runtime pattern) — is a fresh VERTEX key retired anywhere?

For each claim: what you ran, what you saw, verdict (holds / falsified / could not exercise, with why). Reviews here include end-to-end exercise; existing pinned suites ride the PR gate — run only what gives unique signal (your probes, mutants, the two-tree differentials), state the verdict is conditional on the gate being green, AND check that the gate actually ran on `f792d96d0` (`mcp__github__actions_list`, `list_workflow_runs`, branch filter; confirm head SHA and conclusion; the PR cites run 34750694108 — a cited run id is a description, not a state).

## The style lane

`docs/prompts/reviewer-style-lane.md` §2, with this unit's emphasis: **Q1** — `split_fragment`/`retire_fragment` beside `topo::split::split_edge`'s own key rule: one spelling of "which piece is the source", or a second spelling of the same rule kept in step by prose? Where else does `sweep::blend` restate a `topo` invariant? **Q3** — what CAN go red if `split_fragment` picks the wrong piece: the witness row pins direction (b) — does anything pin direction (a), and would the 14 census rows red under a different wrong pick? **Q6** — each deviation: an improvement or a deferral owing more? (b) in particular — the document layer cannot observe the class; is that a residue for EVAL's slate, and who files it? **Q4** — the item's "UNMEASURED" premise and every sentence in `surgery.rs` that rested on it. **Q8** — `crates/sweep/src/blend/open/ruled.rs` end to end once.

## Report (≤150 lines, your final message)

1. Verdict: MERGEABLE / MERGEABLE-AFTER-FIXES / NOT MERGEABLE, conditional on the gate as stated.
2. Per claim C1–C5: ran / saw / verdict.
3. MAJOR / MINOR / NOTE findings, each with `file:line`, 2–5 sentences, confidence; every MAJOR with one line of prose on why it is MAJOR.
4. `## Style` per the style lane's §3 (class-not-instance; where else you would look).
5. The questions you exercised, by number.
6. Code-quality rubric: counts MAJOR/MINOR/NOTE; spec deviations reported vs silent (silent counted separately); ratings 1–5 with one line of evidence each for idiom/structure, test quality (do the rows pin the real contract?), doc/comment honesty.
7. Your probe rows worth adopting, by name, on `origin/blend/8-ladder-split-key-r2-probes` if green (file named by subject, e.g. `review_ladder_split_key_r2-probes.rs`, mounted in `tests/all.rs`). Never on the unit's branch.
Do not post on GitHub. Cancel any detached job before reporting. Never end your turn with a job running. Commits carry no trailer and no model name.
