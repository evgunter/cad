# BLEND-10 dual review — reviewer R2

You are one of two independent, blinded reviewers of BLEND unit 10, PR 2497, on the FROZEN head `46877f8c37e9e6ce6fa488aa76ea342faa6873d3` of branch `blend/10-fillet-stored-tangency` (merge base with main `d73ba7d826ecd600d69b42a3f7d7f3e0a271b743`; the PR's differential used `7bc59c31` → `8b3d7d98`). Read, in full and in this order: `/home/user/blend-briefs/common-lane-rules.md` rules 2–4, 6 and 13 (your own worktree; a PRIVATE `CARGO_TARGET_DIR=/home/user/blend-10r2-target` seeded with `cp -a /home/user/blend-seed-target /home/user/blend-10r2-target`; a private scratch dir `/home/user/blend-10r2-scratch`; the foreground rule; no model names anywhere; touched-crate scope on a shared disk — `-p profile` (and `-p pncad-py` only for its tag census); a second base-tree target is deleted the moment its differential is done), then `docs/prompts/reviewer-style-lane.md` IN FULL (your report names the questions you exercised and carries `sure`/`likely`/`unsure` on every finding), then the binding spec `docs/BLEND-10-SPEC.md`, the item `work/blend/path-fillet-door-validator-tangency-disagree.md`, and the PR body (`mcp__github__pull_request_read`, methods `get` and `get_diff`; the body carries the three Phase 1 tables at three ε rows, the crossing turn `θ* = √(8ε/r)`, the differential, the mutant table, the K count, the sweep's hit list and nine stated deviations).

**Isolation (v6 item 5).** Another reviewer is working on the same head at the same time. Do not look for, read, or reason about their worktree, target, scratch, branch, or report; do not push to the unit's branch; push your probe rows only to `origin/blend/10-fillet-stored-tangency-r2-probes` if they are green. Your report goes to your final message only.

Check out the frozen head: `git fetch origin blend/10-fillet-stored-tangency && git checkout --detach 46877f8c37e9e6ce6fa488aa76ea342faa6873d3`. If the branch has moved past it, say so and still review that head.

**The dispatch is a hypothesis.** The spec is the orchestrator's belief about the tree, and the lane reports nine deviations that you verify rather than assume — three of them correct the spec by measurement: (a) the DOOR moved, for a reason the spec did not name: the door's computed carrier is tangent to 4.4e-16 everywhere, but a fillet is STORED as chord + bulge and `segment_straightness` classifies its sagitta `r(1−cos θ/2) ≈ rθ²/8` — below ε the stored fillet is read as a LINE, the joint takes `joint_tangency`'s line/line arm where nothing is tangent, and the declaration is contradicted; crossing turn `θ* = √(8ε/r)` (2.0e-4 / 6.3e-3 / 6.3e-6 at ε = 1e-9 / 1e-6 / 1e-12); (b) the spec's recourse ("a smaller radius keeps the stored arc conditioned") is BACKWARDS — the sagitta grows with r, so the shipped sentence names a larger turn or a LARGER radius; (c) the check runs AT THE CLOSE (`Core::fillets_carry_their_tangency`, `Core::build` fallible), not before the arc is emitted, because the outgoing joint's second segment does not exist at emission; (d) the stop clause: one cell is NOT monotone (arc × arc at ε = 1e-6: agree → disagree → agree) and the lane proceeded, arguing the fix asks the validator's own question so it is insensitive to the verdict surface's shape — judge that; (e) fence crossings into `crates/profile/src/path/family.rs` (one line) and `crates/pncad-py/{src/tags.rs, src/tests.rs, tests/test_binding_census.py}` (the exhaustive FFI tag census a new `PathError` arm cannot land without); (f) the K count is +9 / +12 / +15 decisions per fillet (line×line / line×arc / arc×arc), not the spec's "two joint classifications"; (g) `seg.rs`'s return shapes changed (`JointReading`, `chord_side`'s margin, `SegIssue::predicate()`) as the cost of one spelling; (h) TWO INTERVAL-LANE ROWS RE-STATED — at `Interval`/ε = 1e-12 the door now relays `PathError::Escalated` where `guided_replay::the_hairline_lens…` and `generic_replay::no_corpus_row_escalates_at_interval` held loops the lane says `Profile::validate` refuses at that scalar anyway (same predicate, same enclosure); verify that claim with the door's read suppressed, because a re-stated census pin is the shape that hides a regression; (i) the Phase 1 harness is committed as a `#[cfg(test)]` module in `path.rs`. Also filed by the lane: `work/blend/arc-arc-shallow-corner-legs-escalate-arc-span.md` (a shallow arc × arc corner's leg circles near-cocircular by construction; `arc_span` escalates between the legs).

## Claims to falsify (the correctness lane)

- **C1** Every fillet that built AND validated at the merge base is bit-identical at the head: re-run the differential (the PR: 60 loops built at the base, 36 validating; 36 built at the head, all validating; 0 absent, 0 moved, 0 new; the 24 dropped are exactly the base's built-but-refused loops) in SEPARATE target dirs, and check `path_differential.rs`'s recorded tables independently.
- **C2** The Phase 1 table is right about WHICH side loses the tangency and WHY: re-derive the stored form's reading yourself — the sagitta of the stored chord, `segment_straightness`'s classification, the line/line arm — at the crossing turns, and decide whether the door or the validator moved for the right reason. Then falsify the mechanism: construct a fillet whose sagitta is ABOVE ε at every row but whose stored carrier still fails `joint_tangency` (a different loss than flattening), and a fillet just above θ* that builds and validates at one ε row but not another.
- **C3** The door never emits a joint `Profile::validate` refuses FOR ITS DECLARATION: sweep every fillet fixture in the profile suites AND construct your own — arc × arc at tiny turns, a fillet at the seam, a fused-incoming fillet, a fillet whose second leg is itself a fillet — and validate each door output; the PR's promise covers the joint classifications only (a door output can still be refused for `arc_span` between non-adjacent legs, the filed residue) — say what the promise does and does not cover.
- **C4** The door's check is the validator's classifier (one spelling): no second margin expression, no new predicate name; the K count per fillet matches the PR's claim (measure with a `k_stats::Bracket` yourself); and the re-stated interval-lane rows (deviation h) hold — with the door's read suppressed, `Profile::validate` refuses the same loops with the same predicate and enclosure.
- **C5** The refusal's recourse is followable at every decade it fires: follow it (the larger turn, the LARGER radius, dropping the fillet) and show the result builds and validates at all three ε rows; and show the spec's original "smaller radius" would have made it worse.

For each claim: what you ran, what you saw, verdict (holds / falsified / could not exercise, with why). Reviews here include end-to-end exercise; existing pinned suites ride the PR gate — run only what gives unique signal (your probes, mutants, the two-tree differential), state the verdict is conditional on the gate being green, AND check that the gate actually ran on `46877f8c3` (`mcp__github__actions_list`, `list_workflow_runs`, branch filter; confirm head SHA and conclusion; the PR cites run 34745149848 — a cited run id is a description, not a state).

## The style lane

`docs/prompts/reviewer-style-lane.md` §2, with this unit's emphasis: **Q1** — `Core::fillets_carry_their_tangency` re-reads every fillet arc through `seg::build_seg` + `seg::joint_tangency`: one spelling of the validator's question, or a second pass that will drift from `judge_joints`? Where else does the door restate what validation decides? **Q3** — what CAN go red in the eight rows if the door's check is wrong but the crossing arithmetic is right; are the ε-derived fixtures pins or restatements of `θ*`? **Q6** — each of the nine deviations: an improvement or a deferral owing more? (d) and (h) in particular. **Q4** — the re-stated census pins: a premise the change invalidated, or a guarantee quietly degraded? **Q2** — `PathError::FilletArcCannotCarryTangency`'s Display and recourse; the `TangencyContradicted` sentence that still names the door; `seg.rs`'s new docs. **Q8** — `crates/profile/src/seg.rs` end to end once.

## Report (≤150 lines, your final message)

1. Verdict: MERGEABLE / MERGEABLE-AFTER-FIXES / NOT MERGEABLE, conditional on the gate as stated.
2. Per claim C1–C5: ran / saw / verdict.
3. MAJOR / MINOR / NOTE findings, each with `file:line`, 2–5 sentences, confidence; every MAJOR with one line of prose on why it is MAJOR.
4. `## Style` per the style lane's §3 (class-not-instance; where else you would look).
5. The questions you exercised, by number.
6. Code-quality rubric: counts MAJOR/MINOR/NOTE; spec deviations reported vs silent (silent counted separately); ratings 1–5 with one line of evidence each for idiom/structure, test quality (do the rows pin the real contract?), doc/comment honesty.
7. Your probe rows worth adopting, by name, on `origin/blend/10-fillet-stored-tangency-r2-probes` if green (file named by subject, e.g. `review_fillet_stored_tangency_r2-probes.rs`, mounted in `tests/all.rs`). Never on the unit's branch.
Do not post on GitHub. Cancel any detached job before reporting. Never end your turn with a job running. Commits carry no trailer and no model name.
