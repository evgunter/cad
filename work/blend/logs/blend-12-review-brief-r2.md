# BLEND-12 dual review — reviewer R2

You are one of two independent, blinded reviewers of BLEND unit 12, PR 2508, on the FROZEN head `b4403f1fbdb61321f88375cad7460d4b4a4fe9d9` of branch `blend/12-fillet-recourse-arm` (merge base with main `18b193a5723288a6335c558c1b1c927985b8a163`). Read, in full and in this order: `/home/user/blend-briefs/common-lane-rules.md` rules 2–4, 6 and 13 (your own worktree; a PRIVATE `CARGO_TARGET_DIR=/home/user/blend-12r2-target` seeded with `cp -a /home/user/blend-seed-target /home/user/blend-12r2-target`; a private scratch dir `/home/user/blend-12r2-scratch`; the foreground rule; no model names anywhere; touched-crate scope on a shared disk — `-p profile` (and `-p pncad` only for its re-export census); a second base-tree target is deleted the moment its differential is done), then `docs/prompts/reviewer-style-lane.md` IN FULL (your report names the questions you exercised and carries `sure`/`likely`/`unsure` on every finding), then the binding spec `docs/BLEND-12-SPEC.md`, the item `work/blend/fillet-escalation-site-has-no-producer.md`, and the PR body (`mcp__github__pull_request_read`, methods `get` and `get_diff`; the body carries the Phase 1 table — nine predicates, which four are driveable in band through the public door and why five are pre-empted — the map, the dispatch order, the mutant table, both differentials over 897 requests at three ε rows, two sweeps with their blind spots, and six stated deviations).

**Isolation (v6 item 5).** Another reviewer is working on the same head at the same time. Do not look for, read, or reason about their worktree, target, scratch, branch, or report; do not push to the unit's branch; push your probe rows only to `origin/blend/12-fillet-recourse-arm-r2-probes` if they are green. Your report goes to your final message only.

Check out the frozen head: `git fetch origin blend/12-fillet-recourse-arm && git checkout --detach b4403f1fbdb61321f88375cad7460d4b4a4fe9d9`. If the branch has moved past it, say so and still review that head.

**The dispatch is a hypothesis.** The spec is the orchestrator's belief about the tree, and the lane reports what it measured and where it departed — verify rather than assume: (a) FOUR of the nine `fillet_*` predicates are driveable in band through the public door at every ε row (`fillet_enclosing_carrier`, `fillet_offset_line_circle`, `fillet_offset_circles_external`, `fillet_offset_circles_internal` — the last found only on the second push, via the mixed-winding arc × arc corner) and FIVE are pre-empted, each with a stated reason (`fillet_corner_turn` by `path_carrier_meet` over 601 points; `fillet_corner_arm` by the ray-order window over 601 points; `fillet_leg_fit` and `fillet_leg_reach` by a PROOF that the exact-order band admits no finite f64; `fillet_offset_lever` with no in-band witness at any ε) — a proof and a point sweep are different kinds of evidence; test both; (b) `EscalationSite::Fillet` had no producer (seven `ProfileError::Escalated` mints, all `Segment`/`SegmentPair`/`Loop`) and was retired with its arm, its hand-built test value and its `rejections.rs` note; (c) the lane crossed the fence into `crates/pncad/tests/all.rs` (one match arm in the re-export census that matches `EscalationSite` exhaustively — the retirement cannot land without it; the brief said STOP) and `crates/test-utils/tests/reader_census.rs` (a ledger line CI demanded for the census row that reads `sugar.rs`'s source); (d) `arc_fillet::fillet_offset_line_circle_trio` was RE-BASELINED — its in-band text no longer contains "lower the tolerance" and now names `FILLET_NO_CORNER_RECOURSE`; (e) the lane filed `work/blend/fillet-inband-recourse-drops-the-tolerance-lever.md`: four of the six sentences no longer name the tolerance lever at their in-band site while every other in-band arm in the same `match` does, and cannot simply gain the clause because D4 ¶1's addendum shares each sentence with a definite sibling where the clause is false; (f) the census row `every_fillet_predicate_has_its_own_sentence_and_never_the_shared_one` reads the nine names out of `sugar.rs`'s source and, the lane says, cannot catch a swap by construction (it asks the map for what it looks for).

## Claims to falsify (the correctness lane)

- **C1** Every in-band `fillet_*` verdict reachable through the public door renders its tailored sentence and never the coincidence recourse: drive each of the four yourself (the row's fixtures are the implementer's, not your oracle) and read the text off the error; then attack the five pre-emption claims — a point sweep is not a proof; find an in-band `fillet_corner_turn`, `fillet_corner_arm` or `fillet_offset_lever` verdict the sweep missed, or confirm the proof for `fillet_leg_fit`/`fillet_leg_reach`.
- **C2** Every rendered sentence is followable at the site it fired: follow each of the four (and any you reach in item C1) and show the request it endorses builds and validates at three ε rows; then judge the filed tolerance-lever residue — is "lower the tolerance" a lever an author has at these sites, and does dropping it make any sentence false or merely shorter?
- **C3** `EscalationSite::Fillet` had no producer and nothing else read the retired arm: grep every constructor and reader in `crates/*/src`, `crates/*/tests`, `demos/`, `docs/`; the lane reports two citations left standing in `work/bool/` (S-BOOL's directory) — judge whether the retirement leaves any citation false.
- **C4** Every fillet that built at the merge base builds bit-identically and every refusal keeps its variant and payload (re-run both differentials in SEPARATE target dirs; the lane's 897 requests at three ε rows read no `Display`); the K count per fillet is unchanged (no `decide` moved).
- **C5** There is ONE map (`validate::fillet_recourse_for`): find a second spelling of name → sentence anywhere in `crates/profile` (including the retired arm's remains, the tests, the README); and the dispatch order in `PathError::Escalated`'s Display cannot hand a `fillet_*` name to the stored-form or junction arms — plant a name in two families and see which sentence renders; the lane says the three key families are disjoint and the census cannot catch a swap: is there a row that CAN?

For each claim: what you ran, what you saw, verdict (holds / falsified / could not exercise, with why). Reviews here include end-to-end exercise; existing pinned suites ride the PR gate — run only what gives unique signal (your probes, mutants, the two-tree differential), state the verdict is conditional on the gate being green, AND check that the gate actually ran on `b4403f1fb` (`mcp__github__actions_list`, `list_workflow_runs`, branch filter; confirm head SHA and conclusion; the PR cites run 34754788782 — a cited run id is a description, not a state).

## The style lane

`docs/prompts/reviewer-style-lane.md` §2, with this unit's emphasis: **Q1** — `PathError::Escalated`'s Display now dispatches three families in a stated order (the fillet nine, the stored-form eight, the junction keys): three tables in one arm, or one table with three sources? Where else does the crate route by predicate name (BLEND-15, the sibling unit, will ask the same — say what you would hand it)? **Q3** — what CAN go red if the map is wrong: the census reads the names from source and asks the map for what it looks for — the lane says a swap is uncatchable by it; what would catch one? **Q6** — each of the six deviations: an improvement or a deferral owing more? (c) the fence crossings and (e) the filed tolerance-lever residue in particular. **Q4** — every doc sentence that rested on "unreachable as rendered prose" (the constants' docs, `rejections.rs`, the README's V6, `docs/KERNEL-VERBS.md` if it names the tables). **Q8** — `crates/profile/src/validate.rs`'s recourse section end to end once (the nine constants and the map).

## Report (≤150 lines, your final message)

1. Verdict: MERGEABLE / MERGEABLE-AFTER-FIXES / NOT MERGEABLE, conditional on the gate as stated.
2. Per claim C1–C5: ran / saw / verdict.
3. MAJOR / MINOR / NOTE findings, each with `file:line`, 2–5 sentences, confidence; every MAJOR with one line of prose on why it is MAJOR.
4. `## Style` per the style lane's §3 (class-not-instance; where else you would look).
5. The questions you exercised, by number.
6. Code-quality rubric: counts MAJOR/MINOR/NOTE; spec deviations reported vs silent (silent counted separately); ratings 1–5 with one line of evidence each for idiom/structure, test quality (do the rows pin the real contract?), doc/comment honesty.
7. Your probe rows worth adopting, by name, on `origin/blend/12-fillet-recourse-arm-r2-probes` if green (file named by subject, e.g. `review_fillet_recourse_arm_r2-probes.rs`, mounted in `tests/all.rs`). Never on the unit's branch.
Do not post on GitHub. Cancel any detached job before reporting. Never end your turn with a job running. Commits carry no trailer and no model name.
