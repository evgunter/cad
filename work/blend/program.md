---
id: blend
kind: program
title: BLEND — the blend kernel and the profile fillet door
status: closed
closed: 2026-09-17
opened: 2026-09-06
area: kernel
prefix: blend/
tag: (BLEND orchestrator)
ab_band: 2900-2999
paths: [crates/sweep/src/blend/*, crates/sweep/src/revolve/*, crates/sweep/src/extrude.rs, crates/sweep/src/fillet.rs, crates/sweep/src/chamfer.rs, crates/sweep/src/skin.rs, crates/sweep/src/swept.rs, crates/sweep/src/loft.rs, crates/sweep/src/test_support.rs, crates/sweep/src/lib.rs, crates/sweep/README.md]
keep_out: [RE-SCOPED 2026-09-17 - CARVE (work/carve/) was opened in this program's cut as its one successor on the same ground and claims every path above by declaration so the two are written on both sides rather than fenced, this program dispatches nothing new and only lands its three open units (13 S90-impl blocked on PROPS' H5, 14 blend/14-contact-edge-must-carry, 15 blend/15-recourse-roster) before its exit walk, crates/sweep/tests/* is S-TCOST's and S-TINT's glob — units add rows there as ordinary tests, the profile fillet door (sugar.rs's arc_fillet_trims and validate.rs's and path.rs's fillet arms and path/arc_fillet.rs and fillet_select.rs) is PATHS' crates/profile/* glob since S-BOOL's exit on 2026-09-16 — edited here by announced seam as FILLET did and the seam records that lived in work/bool/log.md are recoverable at the ledger's sweep-15 SHA, geom-brep certify.rs and dihedral.rs are in no program's paths — the must-carry wrapper and the TangentParallel margin are PROPS-adjacent and announced to PROPS, the K floor in geom-core tolerance.rs was Ev's ruling (closed 2026-09-08 with no floor) and PROPS' file, editor-core verbs/blend.rs and names/emit_*.rs are EVAL's, code-quality Track T's remainder (crates/sweep/) is claimed whole — S116p stays a ruling in work/code-quality, the S14 ruling holds the typed-error announcement sites in blend/ — they stay typed errors while it is open]
---

FILLET's residue, re-homed at its sweep (DOC-LEDGER sweep 7) to
`work/issues/` as "the next blend program's opening slate" — this is
that program. The closed-rim reach the surgery still refuses (the
ringed host, the nested trim circle), the ladder phase's fresh-key
retirement (called unmeasured; unit 8 found it live), the two must-carry arms that disagree, the recourse
table's honesty at three doors, and the profile fillet door's
disagreements with its own validator. Class H at its core with E
openers; one `[ev]` question (the K floor). Claims code-quality Track
T's remainder. Charter and unit order: `work/blend/plan.md`;
narrative in `work/blend/log.md`. **Re-scoped 2026-09-17**: the
thirty-two residue rows its reviews filed moved by `git mv` — twenty-one
to CARVE (`work/carve/`, this program's one successor on its own
ground), eight to PATHS, one each to SYM, TOPO and META; this program
keeps its three unit rows and walks when they land.
