---
id: seat
kind: program
title: SEAT — the verb-seat program
status: open
opened: 2026-08-31
area: kernel
prefix: seat/
ab_band: 1000-1099
paths: [crates/verbs/*, crates/topo/src/query.rs, crates/topo/src/flush.rs, crates/editor-core/src/verbs/*, crates/editor-core/src/names/geompred.rs, crates/editor-core/src/names/flush.rs, crates/editor-core/tests/seat*, docs/VERB-SEAT-DESIGN.md]
keep_out: [the germ lanes' geometry-side acceptance is VERBS' (SEAT owns only the parameter-identity channel), the boolean reduction is S-BOOL's, recipe doors and bindings are LIB's, demo scenes VERBS and LIB cite by line are updated not re-authored]
---

Executes `docs/VERB-SEAT-DESIGN.md` (ratified #1388): the kernel query seat
(§1 — band derivation at operation entry, the `topo` query module, the flush
detector at the body seat), one verb vocabulary (§2 — the `Verb<T>`
declaration in `crates/verbs`, per-verb correspondence in `editor-core`,
migrated verb by verb), and the lowered parameter-identity channel (§3 —
`ParamSource`). Wave 1 and the blend, boolean and DatumValue units are merged.
Detail: `plan.md`; live narrative: `log.md`'s tail.
