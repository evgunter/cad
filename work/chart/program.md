---
id: chart
kind: program
title: CHART — the chart-side residue: pcurve certification, the chart boundary and its consumers, the plane×NURBS SSI
status: open
opened: 2026-09-20
area: kernel
prefix: chart/
tag: (CHART orchestrator)
ab_band: 6200-6299
paths: [crates/topo/src/pcurves.rs, crates/topo/src/chart_bound.rs, crates/topo/src/chart_region.rs, crates/geom-brep/src/pcurve_cache.rs, crates/geom-brep/src/edge_nurbs.rs, crates/geom-brep/src/ssi.rs, crates/geom-brep/src/ssi/*]
keep_out: [opened by TRIM's 2026-09-20 cut (Ev, in-chat) as the successor for TRIM's residue on the chart side — items that wait on a producer that does not exist yet (a General image with curvature, a Fitted cache that survives 1e-12, a revolve replaying at Interval) or on another program's file, TRIM keeps nurbs_iso.rs and docs/PCURVE-P2-SPEC.md until its exit, editor-core/src/clearance.rs is SHELL's — the clearance-window residue edits it by announced seam, crates/mesh is TESS's — the chord-count arithmetic class is filed on TESS's slate, props/quad.rs is QUAD's — the composite-rounds fallback edits it by announced seam, the Track Q riders D36 S83 D305 S331 S351 land with whichever unit opens their file]
---

TRIM's residue on the chart side, now one track: the pcurve
certification lane's open findings (`pcurve_cache.rs`: the chart box,
the rational gates, the posture guard, the sup arms), the chart
boundary and its consumers (the clearance window's cone/sphere/torus
arms, the exact-region cells, the `min_separation` lane split, the
revolved bands, the three arms tables), the plane×NURBS SSI's guards,
the trimmed lane's composite rounds and its missing curved e2e
fixture, and the loft seam's knot compare. Opened 2026-09-20 with 34
items; nothing dispatched. Charter and order: `work/chart/plan.md`;
narrative in `work/chart/log.md`.
