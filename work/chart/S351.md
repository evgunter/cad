---
id: S351
kind: issue
title: nurbs_iso placement rule is now cited from two places in geom; a move must re-aim both pointers
status: open
opened: 2026-09-02
track: Q
refs: []
---

## What

**`nurbs_iso`'s placement rule is now cited from two places in `geom` (filed by CERT-N2; pointer-only, and deliberately not made here).** Closing `S103` put pointers where the next extractor would be written — `geom/src/lib.rs`'s crate doc and `geom/src/surfaces/nurbs.rs`'s header — with the rule and its whole argument left at their one home, this track's `geom-brep/src/nurbs_iso.rs`. Nothing needs changing today. What the row buys is that a move or a rewrite of those lines re-aims two citations in another crate instead of silently orphaning them.

## Was

CERT-N2, filed.

## Finding

No live finding section; provenance only. (`S103` closed and left the document; this row is the standing watch its closure created.)

## Fence

Track Q — `topo`'s boolean, census and charts, and the predicate ledger. **Fence:** `crates/topo/src/{boolean/,splitting/,census.rs,chord_join.rs,chart_region.rs,face_normal.rs}`, `crates/geom-brep/src/{ssi*,pcurve_cache.rs,nurbs_iso.rs,edge_nurbs.rs}`, `docs/predicate-dimension-audit.md`. **Block:** `D280`–`D299` / `S350`–`S369`.

## Re-homed to CITE (2026-09-11, the cut in `docs/WORK-TRACKS-2026-09.md` addendum 3)

CITE collects the rows about the project's own text and harness rather
than its kernel: citations that rot, numbers that were reissued, and the
paperwork a lane runs on. This row is one of them.

Its class at the cut was **E** — standing watch; if triggered, re-aim
two pointers. Explicitly no work today. The class is a dispatch estimate
made by reading the row against the tree on 2026-09-11, not a verdict on
the finding, and a lane that finds it wrong says so in its PR. The id,
the `track:` letter where the row carries one, and the body above are
unchanged by the move.

## Re-homed to TRIM (2026-09-12, by the CITE orchestrator)

**CITE closes, and this watch outlives it — so it goes to the program
that owns the file it watches.**

The rule this row guards lives in `crates/geom-brep/src/nurbs_iso.rs`,
which is TRIM's `paths`. CITE held it because it is a *citation* watch:
two pointers in another crate that a move must re-aim. That was the
right home while a citations program existed; with CITE closing, a watch
whose trigger is an edit to a TRIM file belongs on TRIM's slate, where
the lane that would fire it is already reading.

**Checked at the move and NOT fired** (2026-09-12):

- `crates/geom/src/lib.rs` and `crates/geom/src/surfaces/nurbs.rs` both
  still carry the pointer, and both spell it *"`nurbs_iso` module
  docs"* — by name, with no line number;
- `crates/geom-brep/src/nurbs_iso.rs` still holds the rule and its
  argument.

So nothing is owed today. What fires the watch is unchanged: a move or a
rewrite of `nurbs_iso`'s placement rule, which must re-aim both pointers
rather than orphan them.

**One note CITE leaves behind, because it is the reason this row is
cheap.** Both pointers already cite by name, which is
`docs/prompts/implementer-discipline.md` §7 — *"cite by name; line
numbers rot"* — landed 2026-09-12. That is why this row is a watch and
not a repair: a line-numbered pointer into another crate would have
rotted on its own long ago and silently. Keep them that way if the rule
moves.
