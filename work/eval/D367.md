---
id: D367
kind: unit
title: Stop declare_all and rem_apply in editor-core dropping applied.maintenance, via PR 1503's accept funnel
status: closed
opened: 2026-09-01
track: V
refs: [1503]
pr: 2165
branch: eval/4-accept-funnel
closed: 2026-09-08
---

## Closed

PR 2165. `declare`/`declare_all` return `(Applied<P>, RecipeNodeId)`; `pncad-py`'s `declare_findings` goes back through the sugar and accepts at its swap point (the second copy of the declare body gone); `split` and `inline` record every edit through one swap point (`refactor.rs`'s `Recording`, by announced seam), so `SplitOutcome` carries `remainder_maintenance` + `part_maintenance` and `InlineOutcome` carries `maintenance`. Three rows in `crates/editor-core/tests/eval4_accept_funnel.rs` pin the remainder's join, the part's join and inline's split. Caller survey, sweep hit list and the two out-of-fence findings (LIB's Python outcome wrappers; DOCM's `replay`/`load`) are in the PR body.

## What

**The D344 class's two `editor-core` members** (found by uv-j's sweep, verified by its review): `names/flush.rs::declare_all` returns `(applied.doc, id)` and `refactor.rs::rem_apply` takes `applied.doc` + `minted` — both drop `applied.maintenance`, so a caller composing either loses the maintenance record the edit performed. The fix shape is PR 1503's one-accept-funnel, one level down; check what each caller does with the record before assuming a stored mirror exists to go stale

## Was

uv-j, unrowed

## Finding

No live finding section; provenance only. `D344` (the class this row names) landed under PR 1503 and has no live row.

## Fence

Track V — `editor-core` and `profile`. Fence: `crates/editor-core/`, `crates/profile/`. Block: `D360`–`D379` / `S430`–`S449`. Claimed whole 2026-09-01; execution record `docs/SMELL-UV-LOG.md`.

## Claimed by EVAL (2026-09-06)

Moved from `work/code-quality/` to `work/eval/` in the tracker-wide cut of 2026-09-06 (Ev's direction, in-chat), which read every open `work/issues/` file and every open code-quality row against every live program's `paths` and opened four programs for the ground none covered. Id, `track:` letter and body unchanged. Track V; `names/flush.rs` is EVAL's ground and `refactor.rs` is FIX's — the accept funnel is edited by announced seam.
