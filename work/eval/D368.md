---
id: D368
kind: unit
title: Replace the hand-lifted Vec3 in editor-core/src/eval/anchor.rs:238 with Vec3::map(T::from_f64)
status: closed
opened: 2026-09-01
track: V
refs: [1536, D385]
branch: eval/1-affine-lift
pr: 2139
closed: 2026-09-08
---

## What

**`editor-core/src/eval/anchor.rs:238` lifts a `Vec3` by hand (filed by CERT-N1)** — it is `Vec3::map(T::from_f64)`, which `geom_core::linalg` carries since CERT-N1; the hand spelling goes

## Was

CERT-N1 (PR 1536), filed

## Finding

No live finding section; provenance only.

## Fence

Track V — `editor-core` and `profile`. Fence: `crates/editor-core/`, `crates/profile/`. Block: `D360`–`D379` / `S430`–`S449`. Claimed whole 2026-09-01; execution record `docs/SMELL-UV-LOG.md`.

## Claimed by EVAL (2026-09-06)

Moved from `work/code-quality/` to `work/eval/` in the tracker-wide cut of 2026-09-06 (Ev's direction, in-chat), which read every open `work/issues/` file and every open code-quality row against every live program's `paths` and opened four programs for the ground none covered. Id, `track:` letter and body unchanged. Track V; `eval/anchor.rs` is EVAL's ground, and this row is one lane with `affine-lift-has-a-second-home-in-anchor-embed-affine`.

## Closed (2026-09-08, EVAL-1)

By construction. The row cites `anchor.rs:238`; at close the
hand-lifted `Vec3` was the `v` closure inside `map_affine`
(`anchor.rs:252` at `063bf1dc8`), and its `from_f64` instance was
`embed_affine` (`:268`), which EVAL-1 deleted. Both callers of the
`f64 → T` lift (`wire.rs:1226`, `ProfileLift::Pinned`; `anchor.rs:281`,
`embed_profile`) now go through `SketchPlane::map(T::from_f64)`, which
is `Affine3::map` over `Mat3::map` and `Vec3::map` — the
`Vec3::map(T::from_f64)` this row asked for, reached through the door
above it rather than written at the site. The `v` closure that remains
is the fallible direction's (`map_affine`, one caller,
`wire::pinned_plane`), which `Vec3::map`'s infallible `f` cannot spell;
its retirement is parked on
`work/eval/map-affine-retires-into-affine3-try-map.md`. Every corpus
golden, verdict-log golden and digest constant is byte-identical across
the change (the PR body has the receipt).
