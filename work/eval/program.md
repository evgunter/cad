---
id: eval
kind: program
title: EVAL — the evaluation seat: wiring, the verb seat and the names emitters
status: open
opened: 2026-09-06
area: api
prefix: eval/
tag: (EVAL orchestrator)
ab_band: 3000-3099
paths: [crates/editor-core/src/eval/mod.rs, crates/editor-core/src/eval/wire.rs, crates/editor-core/src/eval/anchor.rs, crates/editor-core/src/eval/schedule.rs, crates/editor-core/src/eval/slots.rs, crates/editor-core/src/verbs/*, crates/editor-core/src/names/mod.rs, crates/editor-core/src/names/README.md, crates/editor-core/src/names/attribute.rs, crates/editor-core/src/names/defer.rs, crates/editor-core/src/names/discriminate.rs, crates/editor-core/src/names/emit.rs, crates/editor-core/src/names/emit_blend.rs, crates/editor-core/src/names/emit_chamfer.rs, crates/editor-core/src/names/emit_fillet.rs, crates/editor-core/src/names/emit_sweep.rs, crates/editor-core/src/names/emit_topo.rs, crates/editor-core/src/names/emit_union.rs, crates/editor-core/src/names/flush.rs, crates/editor-core/src/names/geompred.rs, crates/editor-core/src/names/interrogate.rs, crates/editor-core/src/names/select.rs, crates/editor-core/src/names/table.rs, crates/verbs/*, crates/topo/src/query.rs, crates/topo/src/flush.rs]
keep_out: [the paths list is ENUMERATED on purpose — crates/editor-core/src/* would double-claim DOCM's and M10's and FIX's and MSOLVE's ground and territory is blind to a double claim, DOCM's persist/* program.rs doc.rs edit.rs node.rs names/role.rs eval/parts.rs eval/memo.rs resolve/* mate* assembly.rs are DOCM's — the node-vocabulary half of transform-refuses-a-patterns-instances-value is an [ev] PR announced on DOCM's board, M10's analysis lane (analysis.rs distribution.rs drive.rs measure.rs and eval/measure.rs's E3 primitives and product.rs's Dual arms) is M10's, product.rs is contended ground (DOCM's check-registry item and LIB's [ev] PR 2020) and is not claimed, resolve/vdiff.rs is S-BOOL's, checks.rs and refactor.rs are FIX's — D367's accept funnel edits refactor.rs by announced seam, LIB-G17's Node::Shell reaches crates/verbs (VerbRecord::Shell) by announced seam and LIB answers the .pyi side, crates/profile is S-BOOL's glob — the profile side of the Verb name collision is announced there, the geom-core vector doors (Affine3::map and a try_map if minted) are PROPS' — announced, editor-core/src/expr.rs param_source.rs roots.rs witness.rs finding.rs report.rs and the remaining unlisted files are UNOWNED AND NOT FINISHED — a row landing on one draws the fence in the PR that mints it]
---

The ground SEAT held and Track V never staffed, in no program's
`paths` since SEAT's sweep (DOC-LEDGER sweep 8): the evaluation
wiring (`eval/mod.rs`, `wire.rs`, `anchor.rs`), the verb seat
(`crates/verbs`, `editor-core/src/verbs`), the names emitters and the
selection engine, and the kernel query seat (`topo/src/query.rs`,
`flush.rs`). Its slate is what SEAT's and PROPS' reviews filed from
outside their fences: a verdict bracket that opens after two decisions
are made, a tag-space census blind to the tags it exists to check, an
affine lift written twice, an operand fence the math does not need,
and the emitters restating the kernel's own arguments. Class E with
two `[ev]` questions. Charter and order: `work/eval/plan.md`;
narrative in `work/eval/log.md`.
