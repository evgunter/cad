---
id: D384
kind: unit
title: Give the trv()/healthy() finite-bracket fixture one home for its three verbatim copies
status: open
opened: 2026-09-01
track: W
refs: [1533]
priority: P4
cost: E
---

## What

**`S89`'s rides-along, three copies**: `trv()`/`healthy()` and the whole `the_fixture_is_a_finite_bracket_that_cannot_certify` row are restated verbatim in `crates/geom/tests/{curves,surfaces}/decoration_ring_coords.rs` (same filename in sibling directories, one `geom` binary) and a third time in `crates/topo/src/props.rs`'s `bracket_seam_tests` (**Track M's ground since 2026-09-02**; the third copy moves only with that track's agreement, and its own bracket-door row has since landed there). One fixture home, three consumers

## Was

CERT-M1 (PR 1533), filed

## Finding

### S89. The one-home fix for the ring crossing minted local aliases

`RingInterval::from_certified` is the declared one home, and a private one-line wrapper sits on top of it carrying its own multi-paragraph restatement of the same rule (`ring`, `crates/geom-brep/src/ssi/enclose.rs`; the other, `hull.rs`'s `bracket`, was inlined by D244). **Citations re-derived 2026-09-01**: `ring` is `crates/geom-brep/src/ssi/enclose.rs:195` (Track Q's).

A unit that unifies duplicates minting named copies is the fix reproducing what it closed.

Also open (the part this row carries): `trv()`/`healthy()` and the whole `the_fixture_is_a_finite_bracket_that_cannot_certify` row are restated verbatim in three test files, two of which share a filename in sibling directories and run in the same `geom` binary (`crates/geom/tests/{curves,surfaces}/decoration_ring_coords.rs`, and `topo/src/props.rs`'s `bracket_seam_tests`).

**Verdict:** (blank in the source — unruled, not disputed.)

## Fence

Track W — the test targets: guards, doctests, fixtures and stand-downs. Fence: `crates/*/tests/` in every crate, and `crates/test-utils/`. Block: `D380`–`D399` / `S450`–`S469`. This track owns test-side mechanisms, not test coverage for other tracks' changes — a src change carries its own tests in its own PR, as always.

## Notes

The row calls itself `S89`'s rides-along, but `S89` is a finding with no live row of its own in Tracks K–X (its `ring` alias half is Track Q's ground; the `bracket` half landed as D244), so there is no carrier to name in `rides_with`; the finding is carried in full above instead.

## Claimed by S-TCOST (2026-09-04)

Moved from `work/code-quality/` to `work/tcost/` in the tracker-wide
re-home sweep of 2026-09-04 (Ev's direction, in-chat), which claimed
**Track W whole** for S-TCOST. Id, `track: W` and body unchanged.

The letter and the program are the same ground: W's fence is
`crates/*/tests/` (all crates) and `crates/test-utils/`, and S-TCOST's
`paths` are `crates/*/tests/*` and `crates/test-utils/*` — an exact
match. `work/code-quality/plan.md` recorded W's claimant as "ground is
`tcost`'s" without the rows ever moving, and no `smell/w-*` lane has
ever run, so the rows were waiting on an owner rather than on a ruling
or a dependency.

**This claim was made on Ev's direction rather than by the S-TCOST
orchestrator**, which is not how `work/README.md` expects a claim to
happen. If S-TCOST would rather not hold a row, moving it back is a
`git mv` and this section is the record of why it arrived.

The two seams `work/code-quality/plan.md` states for W are unchanged
and travel with the rows: `crates/test-utils/src/source.rs` is the
shared home three tracks' rows land in, and the `UNCONVERTED_TODAY`
ceiling is **re-derived from the table at each landing, never lowered
by a row's own member count**; and a W row whose mechanism reaches into
a crate's `src/` is **filed on the owning track** rather than edited
there.

## Moved to S-TINT (2026-09-11)

Moved by `git mv` from `work/tcost/` at S-TINT's opening. Id, title and
body are unchanged; the directory is the claim (`work/README.md`).

**Why it moved.** S-TCOST's board was re-sorted on 2026-09-11 against the
repository going public on 2026-09-03 (`work/tcost/log.md`, the
2026-09-11 seam). That sort found this row is not a cost lever in either
currency — it neither shortens the gate's critical path nor saves a
billed minute, and it was never argued on one. It reached S-TCOST by the
tracker-wide re-home of 2026-09-04, which routed rows by PATH GLOB
(`crates/*/tests/*`, `crates/test-utils/*`) rather than by question.
This program is the question it was always about: whether the suite
asserts what it claims to assert.

## Re-derived (2026-09-15, lane D)

**VERDICT: PARTIAL** — the fixture duplication is live but its
membership has changed; the `S89` header half is stale-fixed.

**STALE-FIXED — the `S89` paragraph's live claim.** The Finding says, in
the present tense, *"a private one-line wrapper sits on top of it
carrying its own multi-paragraph restatement of the same rule (`ring`,
`crates/geom-brep/src/ssi/enclose.rs`)"*. There is no `fn ring` in that
file. Every one of its 36 crossings is a direct
`RingInterval::from_certified(…)` — `:88-90`, `:227-229`, `:274`,
`:503-546`, `:655-657` among them. D289 landed; the `bracket` half was
already recorded as landed by D244. **Nothing of the `S89` half
survives.**

**LIVE, with a changed roster — the part this row carries.**

`the_fixture_is_a_finite_bracket_that_cannot_certify` exists three
times, but only two are verbatim copies of each other:

- `crates/geom/tests/curves/decoration_ring_coords.rs:47` and
  `crates/geom/tests/surfaces/decoration_ring_coords.rs:53` — the four
  body lines are byte-identical (`assert_eq!((Bounds::lo(x),
  Bounds::hi(x)), (0.0, 2.0)); assert!(x.certified_bracket().is_none());
  assert_eq!(healthy().certified_bracket(), Some((1.0, 2.0)));`). Same
  filename, sibling directories, one `geom` binary — as the row says.
- `crates/topo/src/props.rs:2831` (`bracket_seam_tests`) is **NOT** a
  verbatim copy: its fixture is `trv_pos()` = `sqrt([−1,4]) + 1`, it
  asserts `(1.0, 3.0)`, and there is no `healthy()` in that module at
  all. The row's *"restated verbatim … a third time"* is wrong about
  this member today.

`trv()`/`healthy()` are verbatim in three files, and the third is one
the row does not name:

- `crates/geom/tests/curves/decoration_ring_coords.rs:30`/`:35`,
- `crates/geom/tests/surfaces/decoration_ring_coords.rs:20`/`:24`,
- **`crates/geom-core/tests/decoration_seam.rs:61`/`:67`** — same
  bodies (`Interval::from_bounds(-1.0, 4.0).sqrt()` and
  `Interval::from_bounds(1.0, 4.0).sqrt()`), different doc prose.

Two further `trv`-family fixtures, same idea under the `+1` variant:
`crates/topo/src/props.rs:2826` and
`crates/geom-brep/tests/decoration_plane_mint.rs:30`, both `trv_pos()`.
Neither is a verbatim copy; both would be consumers of a one-home
fixture if one existed.

**Fence note that survives.** The `geom-core` copy is in
`crates/geom-core/tests/`, inside Track W's fence and this program's
glob; the `topo/src/props.rs` copy is still a `src/` site and still
Track M's, so the row's "moves only with that track's agreement" holds
for it and does not apply to the new member.

**Command.**
`grep -rn 'fn trv\|fn healthy\|the_fixture_is_a_finite_bracket_that_cannot_certify' crates/`,
then each body read and compared.

**Blind spot.** Matched by NAME (`trv`, `healthy`, the row's title). A
fourth site that builds `sqrt([−1,4])` under other names would not
appear; no value-shape sweep for `from_bounds(-1.0, 4.0).sqrt()` was run
beyond these hits.

**Recommend: keep open, retire the `S89` paragraph as closed, and
restate the roster** — two verbatim `the_fixture…` rows, three verbatim
`trv`/`healthy` pairs, one of them in a file the row never listed.
