---
id: D383
kind: unit
title: Close the bound-domination class onto test_utils::tightness: unswept members, discarded ratios, soundness slack
status: open
opened: 2026-08-29
track: W
refs: [S230, D300]
priority: P3
cost: E
---

## What

**The bound-domination class, its discarded ratios, and the soundness half.** `S121`'s shape generalises past the sites closed with it: an assertion comparing a certified quantity against a sampled one with nothing on the other side. Enumerated, all on this track's fence: `geom-core/tests/review_m5_pr7b_tensor.rs:141` and `:487`; `geom-brep/tests/offb_r1_probes.rs:202`; `geom-brep/tests/pcurve_p1a_meter.rs:245`; `geom-brep/tests/offset_fit.rs:233`, `:364`, `:369`; `geom-brep/tests/m5_pr7_ssi.rs:1414` (ceiling present, anti-vacuity floor absent); `geom/tests/curves/review_m5_pr2_e2e.rs:214` and `:218`; `geom/tests/curves/m8_14_long_turn_meter.rs:109`, a **helper** over eight carriers. **A reviewer sweep puts the count near fifteen across at least six naming vocabularies, so this enumeration is a floor, not a census.** **Three helpers already compute the exact ratio the discipline asks for and discard it**: `m5_pr7_speed_meter.rs`'s `assert_real_and_sound` returns `m / lo` to ten call sites and its one reader uses it for scale covariance, not tightness; `mesh/src/nurbs_cert.rs`'s `assert_dominates` *prints* it to eight; `review_m5_pr7b_tensor.rs`'s `falsify` returns it and the battery `eprintln!`s the worst. Those three are the cheapest members. **Two further members, found while closing `S121`'s five:** (i) `m8_14_long_turn_meter.rs:258`'s `meter_corpus_table` **asserts nothing** while carrying nineteen of the twenty carriers that justify `m5_pr7_speed_meter.rs`'s live `0.1` — the evidence base for a shipped constant is unguarded, and the row already computes every ratio; (ii) the meter-soundness slack `s >= m - 1e-12` appears **seven times across three files** (`m5_pr7_speed_meter.rs:147,165,421`, `m8_14_long_turn_meter.rs:117`, `topo/tests/m5_pr7_split_meter.rs:260,374`) with no derivation and **no sentence anywhere admitting the copies** — the undisclosed-duplicate shape, which only the data finds. **The home exists**: `test_utils::tightness` — `Sup`, `Meter`, `Anchor`, `control_net_box_diagonal`. It owns no constant in either direction, `Meter::dominates` takes the slack and its justification as arguments, and a chain that never states a ceiling **does not compile**. `S230` below is the same class under another name and **the two want one lane**; `mesh/src/nurbs_cert.rs:1538` stays Track R's `D300`

## Was

`S121` residue

## Finding

### S121. A bound-domination row with no ceiling and no floor

A certified bound is compared against a sampled true value and asserted to **dominate** it, and nothing else is asserted. `sup >= max` is monotone in the safe direction — an implementation that returned `f64::MAX`, or that lost a cancellation, satisfies it forever. **A row missing the ceiling can be arbitrarily loose; a row missing the anti-vacuity floor is satisfied for free by a fixture whose sampled residual collapsed.**

- `mesh/src/nurbs_cert.rs:1538` — `hessian_hull_dominates_sampled_second_partials`, doc *"the convexity claim, measured (never the other way round)"*; the measurement has no upper side.

**The floor and the ceiling have to be measured, not copied**, and the ceiling has to sit below a **measured degraded reading** — a constant transplanted from a sibling row, or a threshold that re-pins today's output, is `memories/output-stability-as-justification.md`'s shape rather than a fix. What this owes is a ratio at more than one ε, and it is entitled to conclude that no honest ceiling exists — a written verdict, not a silent omission, and one that must survive the arithmetic: an anchor *below* the degraded reading makes the anchor bind, it does not make a ceiling impossible. **Row: Track R's `D300`.**

The discipline now has a home — `test_utils::tightness`, which owns no constant in either direction, refuses a ceiling at or above the fixture's whole-object box, and does not compile a chain that never states a ceiling. That box is a **necessary condition and not a sufficient one**: it stops a ceiling being obviously vacuous and does not substitute for the degraded reading. The wider class, its unswept members and the three helpers that already compute the ratio and discard it are Track W's `D383`.

**Verdict:** none recorded (no verdict line in the source).

### S230 (the sibling row)

Carried in full on `S230`'s own file; this row and that one want one lane.

## Fence

Track W — the test targets: guards, doctests, fixtures and stand-downs. Fence: `crates/*/tests/` in every crate, and `crates/test-utils/`. Block: `D380`–`D399` / `S450`–`S469`. This track owns test-side mechanisms, not test coverage for other tracks' changes — a src change carries its own tests in its own PR, as always.

## Notes

Minted by the KPW session (2026-08-29..31) from lane `w1`'s residues (`D65`/`S121`, landed). The KPW log adds one more member of the same class one level in: **the helper can build the defect it exists to prevent** — `Sup::new(..).truth_at_least(..);` compiles, runs, asserts only the floor, and is `S121`'s shape again — because the intermediate builder states are not `#[must_use]`.

## Claimed by S-TCOST (2026-09-04)

Moved from `work/code-quality/` to `work/tcost/` in the tracker-wide
re-home sweep of 2026-09-04 (Ev's direction, in-chat), which claimed
**Track W whole** for S-TCOST. Id, `track: W` and body unchanged.

The letter and the program are the same ground: W's fence is
`crates/*/tests/` (all crates) and `crates/test-utils/`, and S-TCOST's
`paths` are `crates/*/tests/*` and `crates/test-utils/*` — an exact
match. `work/code-quality/plan.md` recorded W's claimant as "ground is
`tcost`'s" without the rows ever moving, and no `smell/w-*` lane has
ever run, so the rows were waiting on an owner rather than on a ruling
or a dependency.

**This claim was made on Ev's direction rather than by the S-TCOST
orchestrator**, which is not how `work/README.md` expects a claim to
happen. If S-TCOST would rather not hold a row, moving it back is a
`git mv` and this section is the record of why it arrived.

The two seams `work/code-quality/plan.md` states for W are unchanged
and travel with the rows: `crates/test-utils/src/source.rs` is the
shared home three tracks' rows land in, and the `UNCONVERTED_TODAY`
ceiling is **re-derived from the table at each landing, never lowered
by a row's own member count**; and a W row whose mechanism reaches into
a crate's `src/` is **filed on the owning track** rather than edited
there.

## Moved to S-TINT (2026-09-11)

Moved by `git mv` from `work/tcost/` at S-TINT's opening. Id, title and
body are unchanged; the directory is the claim (`work/README.md`).

**Why it moved.** S-TCOST's board was re-sorted on 2026-09-11 against the
repository going public on 2026-09-03 (`work/tcost/log.md`, the
2026-09-11 seam). That sort found this row is not a cost lever in either
currency — it neither shortens the gate's critical path nor saves a
billed minute, and it was never argued on one. It reached S-TCOST by the
tracker-wide re-home of 2026-09-04, which routed rows by PATH GLOB
(`crates/*/tests/*`, `crates/test-utils/*`) rather than by question.
This program is the question it was always about: whether the suite
asserts what it claims to assert.

## Re-derived (2026-09-15, lane B)

**VERDICT: PARTIAL**

Every line number in the `## What` enumeration is stale by ~+11 lines or
more; re-derived **by name**, against today's tree, with the commands
below. No test was run.

### The home EXISTS and is in use — this half is STALE-FIXED

`crates/test-utils/src/tightness.rs` is in the tree and `pub mod
tightness;` is declared in `crates/test-utils/src/lib.rs`, whose crate
doc names it *"its companion for a certified bound: the CEILING a
`bound >= truth` row cannot state"*. `Sup`, `Meter`, `Anchor` and
`control_net_box_diagonal` are all present with the signatures the row
describes — `Meter::dominates(slack, why)` does take the slack and its
justification as arguments.

**The `## Notes` residue is fixed too.** The note says the builder
"can build the defect it exists to prevent — `Sup::new(..)
.truth_at_least(..);` compiles — because the intermediate builder
states are not `#[must_use]`." They are now: both structs carry it, e.g.

```
#[must_use = "a bound-domination chain that never reaches `within` states no ceiling, \
pub struct Sup<'a> {
```

and the same on `Meter` naming `gives_away_at_most`. That sentence is
the proof; nothing of that sub-member survives.

**Three consumers today** (`grep -rln "tightness::" crates/`):
`geom-core/tests/m5_pr7b_tensor_compose.rs` (four `Sup` chains),
`geom-brep/tests/r1_pxn_probes.rs`, `geom/tests/curves/
m5_pr7_speed_meter.rs` (one `Meter` chain, `gently curved cubic`).

### Members STILL LIVE (re-derived by name)

| site (by name) | state |
| --- | --- |
| `review_m5_pr7b_tensor.rs::falsify` | live — `assert!(sup >= max, "{name}: FALSIFIED …")`, ratio computed and returned, no ceiling |
| `review_m5_pr7b_tensor.rs::falsification_battery_no_finite_bound_undercuts_truth` | live — still only `eprintln!("[review] battery worst finite bound/truth ratio: …")`; asserts nothing on `worst` |
| `review_m5_pr7b_tensor.rs`, the degree-budget row (`sup.is_finite() && sup >= max`, "the 54-exact case must serve soundly") | live, no ceiling |
| `offb_r1_probes.rs::r1_a_slid_fit_s_tau_limb_never_under_reports` | live — `cert.hull_sup >= true_residual`, no ceiling |
| `offb_r1_probes.rs::r1_extreme_d_near_the_collapse_bound_certifies_and_contains` | live — `worst <= cert.hull_sup`; the neighbouring `worst <= tol` bounds the SAMPLE, not the certificate |
| `offb_r1_probes.rs::r1_an_extreme_weight_rational_base_refuses_or_contains` | live — `worst <= cert.hull_sup`, no ceiling |
| `pcurve_p1a_meter.rs::the_collapsed_meter_never_understates_the_distance_to_the_surface` | live — `cert >= d * (1.0 - 1e-9)`, no ceiling |
| `offset_fit.rs::the_regularity_floor_is_positive_on_a_regular_patch_and_conservative` | live — `reg.sup >= inf`, no ceiling (its sibling `reg.floor <= inf` is the other direction) |
| `review_m5_pr2_e2e.rs` (`max_clean <= bound`, `max_dirty <= dirty_bound`) | live, both, no ceiling on either bound |
| `m5_pr7_ssi.rs` (`sup >= max` / `sup <= 2.0 * max` / eps-gated `sup <= 1e-8`) | live, **as described**: ceiling present, anti-vacuity floor absent |
| `m8_14_long_turn_meter.rs::meter_corpus_table` | live — still `println!` only, **zero assertions** |

### Members now FIXED

- `offb_r1_probes.rs::r1_certify_offset_on_a_rational_fit_never_under_reports`
  gained a measured ceiling: `assert!(cert.hull_sup <= true_residual * 5.0, …
  "(measured 2.49x when written)")`, with a comment arguing the ~2x headroom.
  Hand-rolled, not on `tightness`.
- `offset_fit.rs::the_collapse_meter_brackets_the_sphere_s_known_curvature`
  is now two-sided: `coll.reach <= r && coll.reach > 0.3 * r`, with the
  ratio printed and a comment saying the slack is *"a MEASURED slack, not
  a designed one"*. Hand-rolled.

### Counts that MOVED

- **`assert_real_and_sound` returns `m / lo` to SIX call sites, not ten**
  (`grep -c "assert_real_and_sound(" crates/geom/tests/curves/m5_pr7_speed_meter.rs`
  → 7 lines, one of which is the `fn` header). Its one reader is still
  `big`/`small` used for scale covariance (`(big - small).abs() < 1e-9`),
  exactly as the row says.
- **`nurbs_cert.rs::assert_dominates` prints the ratio at SIX call sites,
  not eight** (`grep -n "assert_dominates(" crates/mesh/src/nurbs_cert.rs`
  → 1 header + 6 calls; one of the six is inside a `for p_deg` loop). The
  `println!("{name}: truth/bound uu={…}")` is intact and nothing asserts it.
- **`meter_corpus_table` now carries TWENTY carriers, all unasserted** —
  the row said "nineteen of the twenty". Counted from the `rows` vec:
  3 named cubics + 10 `arc_curl` + 6 `helix` + `closed_circle`.
  `m5_pr7_speed_meter.rs`'s `0.1` is still live, now as
  `.gives_away_at_most(0.1, 1.0, "the per-span hull is no longer tracking
  the true speed")`.
- **`m8_14_long_turn_meter.rs`'s helper is named `assert_positive_and_sound`**
  (line 109 is still its `fn`, by luck), over 9 call sites — 8 named
  carriers plus one inside the old-vs-new corpus loop.

### The slack census — UNCHANGED, seven copies, three files, still undisclosed

`grep -rn ">= m - 1e-12" crates/ --include=*.rs`, plus the `deriv(t).norm()`
spelling:

| file | sites |
| --- | --- |
| `geom/tests/curves/m5_pr7_speed_meter.rs` | 3 (row said `:147,165,421`; today `:158,176,432`) |
| `geom/tests/curves/m8_14_long_turn_meter.rs` | 1 (`:117`, unmoved) |
| `topo/tests/m5_pr7_split_meter.rs` | 2 (`:260,374`, unmoved) |

That is 6, not 7 — the seventh is the one that CONVERTED: `m5_pr7_speed_meter.rs`
now passes `1e-12` as `Meter::dominates`'s slack argument with a written
derivation (*"a rounding budget of about 4e3 ulps"*). **So the conversion
produced the first justified copy and left six unjustified ones, and still
no sentence anywhere admits the duplication.** The undisclosed-duplicate
shape survives.

### Re-derived floor for "near fifteen across six vocabularies"

Two patterns over `crates/ --include=*.rs`:

- `^\s*[a-z_.0-9]*(sup|bound|cert|hull|_m\b)[a-z_.0-9]*\s*>=\s*[a-z_.0-9()]+\s*,?\s*$`
  — 17 hits in 9 files.
- `[a-z_.0-9]*(worst|max_|truth|sampled|true_)[a-z_.0-9]*\s*<=\s*[a-z_.0-9]*(sup|bound|cert|hull|pad)[a-z_.0-9]*\s*,?\s*$`
  — 16 hits in 8 files.

Union of files: `test-utils/src/tightness.rs` (its own doctests),
`editor-core/tests/m10_8_pins_interval.rs`, `geom-core/tests/review_m5_pr7b_tensor.rs`,
`geom-brep/tests/{cert7_r1_probes,cert7_r2_probes,approx_surface,offb_r1_probes,offb_r2_probes,offset_fit,m5_pr7_ssi}.rs`,
`geom/tests/curves/review_m5_pr2_e2e.rs`, `sweep/tests/offb_r1_loft_probes.rs`.
**Eleven live-code files** — four of them (`cert7_r1_probes`, `cert7_r2_probes`,
`approx_surface`, `offb_r1_loft_probes`) are NOT in this row's enumeration
and were not opened by this lane. The row's "floor, not a census" framing
holds and the floor is still at least fifteen.

**What these patterns cannot match**, and why this stays a floor:
the comparison written on the same line as `assert!(` (the spelling most of
the enumerated members actually use — the first pattern misses them all
and they were found by reading); the bracket spelling
`lo <= x && x <= hi` that S230's `*_pad` members use; any domination
routed through a helper (`falsify`, `assert_dominates`,
`assert_real_and_sound`, `assert_positive_and_sound` — four of them here);
any vocabulary not in the alternation. It was run over `crates/` only —
`demos/`, `tools/`, `benches/` and `interval-transcendentals/` are unswept.

### Recommendation

Do not close. The home is built, four of its verbs are exercised, and two
members were repaired by hand — but eleven named members are live and the
seven-copy slack is six copies and still undisclosed. The row's *scope*
is intact; only its counts moved.
