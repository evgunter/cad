---
id: helper
kind: program
title: HELPER — one test helper, oracle or walk with several private homes
status: ready
opened: 2026-09-20
area: infra
prefix: helper/
tag: (HELPER orchestrator)
ab_band: 9000-9099
paths: []
keep_out: [opened by TINT's 2026-09-20 priority-seam cut (Ev, in chat) per work/README.md Track size - TINT measured 207.5 (97.5 once priced) budget points and was cut into tracks meant to run in PARALLEL (Ev, in chat: for these high priority tracks it is ideal to have several components that can be worked on in parallel), the siblings are TINT VACUITY FIXTURE HELPER and they share their parent's territory by design - shared ground is legitimate by the README's 2026-09-20 rule and what is owed is awareness while a lane is LIVE, so run scripts/work.py territory on your branch and announce the seam in the PR rather than drawing a fence, the fixtures these rows share live under crates/*/tests and crates/test-utils which S-TCOST globs - a unit here adds or moves rows there as ordinary tests and says so in the PR, DUP owns the one-thing-spelled-n-times class on PRODUCTION code and these four own it on the test side]
priority: P4
---
The same class as FIXTURE one level up: not a SHAPE spelled n times but
a HELPER — a walk, an oracle, a harness — living privately in each
suite that wants it.

The half-edge-to-face walk is spelled once per test file across 91
sites; the face-to-shell-to-solid walk likewise; `run(doc, prior, ...)`
has seven private copies across editor-core's suites; the `current_exe`
re-exec harness is copied per suite **and every copy greens when it
does not run**, which is the one row here that is a guard defect rather
than a duplication; comparing a `MassProperties` by bits is written
~31 ways **and they disagree**, which is why that row is P1 rather than
P4.

Claims no paths, for the same reason FIXTURE does not.

Charter and order: `work/helper/plan.md`; narrative in `work/helper/log.md`.
