---
id: bind
kind: program
title: BIND — the bindings' own housekeeping: the stubs, the tag pins, the censuses and the prose beside them
status: ready
opened: 2026-09-20
area: api
prefix: bind/
tag: (BIND orchestrator)
ab_band: 9600-9699
paths: [crates/pncad-py/src/py/*]
keep_out: [opened by LIB's 2026-09-20 priority-seam cut (Ev, in chat) per work/README.md Track size - LIB measured 67.5 budget points and was cut into tracks meant to run in PARALLEL (Ev, in chat: for these high priority tracks it is ideal to have several components that can be worked on in parallel), the siblings are LIB BIND and they share their parent's territory by design - shared ground is legitimate by the README's 2026-09-20 rule and what is owed is awareness while a lane is LIVE, so run scripts/work.py territory on your branch and announce the seam in the PR rather than drawing a fence, PORT owns the crate-boundary refusal doors, CENSUS owns the vocabularies spelled by hand, EDIT owns the DocEdit vocabulary the facade-type rows wait on]
priority: P4
---
The bindings' housekeeping, split from the DOORS that LIB keeps — Ev's
call, in chat: the north-star half is medium priority and the rest is
low, and holding both in one slate meant the low half set the pace.

What is here: stubs that pin names but not signatures, so the options
census's stub-to-door link is unchecked; `pncad.pyi` declaring three
attributes on a class that ships seven; tags minted at the boundary
that are outside `TAG_INVENTORY`'s reach, so nothing pins their values
or sees them collide with a kernel word; prose counts of populations
the code holds; and four doc sentences describing a tree that moved.

Two rows are P1 and are the reason to read this slate rather than
skim it: `BlendVerb` and `ShellVerb` coincide field for field (the
generic correspondence waiting for its third instance), and
`persist_err` is a **319-line function projecting 14 arms through a
15-slot positional tuple**, where a transposed pair is invisible.

Almost everything else is class `E` and is drive-by material for
whoever is next in the file (`work/README.md`, "The tracker is not
comprehensive").

Charter and order: `work/bind/plan.md`; narrative in `work/bind/log.md`.
