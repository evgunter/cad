---
id: trim
kind: program
title: TRIM — the NURBS trim frontier
status: open
opened: 2026-09-03
area: kernel
prefix: trim/
tag: (TRIM orchestrator)
ab_band: 2500-2599
paths: [crates/topo/src/pcurves.rs, crates/geom-brep/src/pcurve_cache.rs, crates/geom-brep/src/nurbs_iso.rs, crates/geom-brep/src/edge_nurbs.rs, docs/PCURVE-P2-SPEC.md]
keep_out: [OPEN FOR DISPATCH since 2026-09-04 — the rim arms its two largest units wait on are nurbs_iso_derive's own in topo/src/pcurves.rs (P-2 fixed the cap-rim arm and reverted the wall-seam one) so the gate is this program's unit 1 not CURVED's, code-quality Track Q's four geom-brep files are this program's and the rows on them (D36 S394 S83 D305) moved into this directory when they were claimed — they land as riders on this program's units, crates/mesh/trimmed.rs and chords.rs are S-MESH's — the trimmed-region tessellation half is edited by announced seam, topo/src/props.rs is Track M's fence and no program's — the quadrature half is announced to PROPS, EXCH consumes the ExtrudedPoint rung of nurbs_iso_derive — whichever program dispatches first builds it and the other files a row, editor-core/src/clearance.rs is SHELL's — the window-tightening unit edits it by announced seam]
---

PCURVE's P-2 residue: the de Boor collapse extractor for interior
iso-curves, the trimmed-region quadrature and tessellation lanes, the
loft seam's knot compare, and the chart-boundary description M10-5's
clearance window needs. Class H. Open for dispatch since 2026-09-04;
the extractor is the opener and unblocks the props/tess lane itself.
`docs/PCURVE-P2-SPEC.md` is its charter input. Charter and order:
`work/trim/plan.md`; narrative in `work/trim/log.md`. Same
orchestrator as CURVED.
