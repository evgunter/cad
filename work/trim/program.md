---
id: trim
kind: program
title: TRIM — the NURBS trim frontier
status: active
opened: 2026-09-03
area: kernel
prefix: trim/
tag: (TRIM orchestrator)
ab_band: 2500-2599
paths: [crates/geom-brep/src/nurbs_iso.rs, docs/PCURVE-P2-SPEC.md]
keep_out: [CUT 2026-09-20 (Ev, in-chat) to what closes this session — CHART took the chart-side residue (31 items: pcurves.rs, pcurve_cache.rs, chart_bound.rs, chart_region.rs, ssi.rs), the chord-count arithmetic class went to TESS's slate as its item names, this program closes on TRIM-B2 slot 2 (boundary_iso_u/_v refuse instead of panic) and the deletion of docs/PCURVE-P2-SPEC.md per the ledger (superseded by TRIM-1/2/3's delivered specs)]
priority: P4
---

PCURVE's P-2 residue, delivered: the de Boor collapse extractor
(TRIM-1, #2095), the trimmed-region quadrature and tessellation
(TRIM-2, #2564 and #2863), and the chart-boundary description read by
the clearance window (TRIM-3, #1911 and #2554) — all three specs
ledgered. What remains is one E unit (`boundary_iso_u/_v` refuse
instead of panic; block TRIM-B2 slot 2) and the walk, which deletes
`docs/PCURVE-P2-SPEC.md` per the ledger. Cut down to this on
2026-09-20 (Ev, in-chat); the chart-side residue is CHART
(`work/chart/`). Charter and order: `work/trim/plan.md`; narrative in
`work/trim/log.md`. Same orchestrator as CURVED.
