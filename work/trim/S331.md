---
id: S331
kind: issue
title: validate_pcurves answers a clean bill on a body whose pcurve mint just failed — a vacuous green through a public door
status: open
opened: 2026-09-02
track: P
---

## What

**`validate_pcurves` answers a clean bill on a body whose mint just failed (filed by CERT-N2; found by the dual review, executed).** `topo/src/pcurves.rs:1602` returns an empty error list for a body where `mint_pcurves` refused a face and `clear_face_caches` removed its caches — absence of a cache is not a claim (`validate_pcurves`' own rule), so zero errors is the correct *letter* and a vacuous green through a **public** door: a caller that validates after a failed mint is told the pcurves are fine. The question is whether at-rest validation should distinguish *no cache because this class is uncovered* from *no cache because minting refused*. **Fence note**: `topo/src/pcurves.rs` is named by neither this track's file list nor Track Q's, so this row draws the fence rather than assuming one — it is placed here, beside `validate.rs`, because the question is what at-rest validation may claim; the pcurve certification machinery it consults is Q's `pcurve_cache.rs`.

Kind note: `issue`, because the row poses the question (should at-rest validation distinguish the two absences) rather than naming the fix.

## Was

CERT-N2, filed.

## Finding

No live finding section; provenance only. `S331` is the row's own number from this track's block, not a finding heading.

## Fence

Track P — `topo`'s Euler surgery, liveness and the generator. **Fence:** `crates/topo/src/{euler.rs,euler_ring.rs,euler_kill.rs,split.rs,attach.rs,movefac.rs,revert.rs,live.rs,merge_faces.rs,seqgen.rs,validate.rs}`, plus `crates/topo/src/{review_d18.rs,review_d18_probes.rs,fixtures.rs,source_walk.rs}` (added 2026-08-29, because they belonged to no track). **Block:** `D260`–`D279` / `S330`–`S349`.

## Claimed by TOPO (2026-09-04)

Moved from `work/code-quality/` to `work/topo/` when TOPO opened and
claimed **Track P whole**, per `work/code-quality/program.md` ("a row
leaves the moment a program claims it, by moving into that program's
directory keeping its id, `track:` letter and body") and the 2026-09
cut's rule that a program taking a code-quality letter takes all of it
so the schedule stays single-owner. Id, `track: P` and body unchanged.

Track P had **never had a lane** — no `smell/p-*` branch has ever
existed, while K, X and T lanes have run — and its fence
(`euler*.rs`, `validate.rs`, `live.rs`, `seqgen.rs`, `merge_faces.rs`
and the review-and-fixture readers) is exactly the `crates/topo/src`
territory that was in no program's `paths`. The three sub-lanes
`work/code-quality/plan.md` defined for P are inherited unchanged and
are restated in `work/topo/plan.md`.

## Re-homed to TRIM (2026-09-05)

Moved from `work/topo/` to `work/trim/` by TOPO, with Ev's in-chat
concurrence (2026-09-05): the mechanism the row asks about is
`crates/topo/src/pcurves.rs:1229-1300` — `mint_pcurves` swallowing
`UnsupportedCarrier` and clearing the face's caches — which is TRIM's
file and sits beside TRIM's `D36` (the payload-free `UnsupportedCarrier`
that means three things). Id, `track: P` and body unchanged; the
directory is the claim (`work/README.md`).

**Direction, not a ruling** (TOPO's read, Ev: "your call sounds good"):
the answer to the row's question is yes — at-rest validation must be
able to tell a refused mint from an uncovered class, which means the
mint stops silently clearing a refused face and leaves a typed trace,
or refuses. TOPO's tier-3 check 8 call site
(`crates/topo/src/validate.rs`, `validate_pcurves`' consumer) is
TOPO's and edits by announced seam if the shape needs it.
