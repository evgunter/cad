---
id: D36
kind: unit
title: PcurveCertifyError::UnsupportedCarrier is payload-free and means three things across 22 construction sites
status: open
opened: 2026-08-21
track: Q
refs: []
---

## What

`PcurveCertifyError::UnsupportedCarrier` is payload-free and means three different things across 22 construction sites, beside a sibling that names its class at every site.

## Was

Track E.

## Finding

No live finding section; provenance only. (The shape is the one `S19` described — one name over several unrelated states, the payload a caller needs not carried; `S19`'s postmortem was pruned when `D38` landed, and the rule it left is in the document's *How to read a finding*: name the type you want, or expect the cheapest thing that carries the information.)

## Fence

Track Q — `topo`'s boolean, census and charts, and the predicate ledger. **Fence:** `crates/topo/src/{boolean/,splitting/,census.rs,chord_join.rs,chart_region.rs,face_normal.rs}`, `crates/geom-brep/src/{ssi*,pcurve_cache.rs,nurbs_iso.rs,edge_nurbs.rs}`, `docs/predicate-dimension-audit.md`. **Block:** `D280`–`D299` / `S350`–`S369`.
