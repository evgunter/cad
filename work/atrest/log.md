# ATREST — the log

## 2026-09-20 — opened

Cut out of TOPO, which was carrying 132 budget points in one directory
— about four and a half sittings — when Ev ratified the priority and
track-size conventions in chat the same day. The cut divided TOPO on
its PRIORITY seam rather than on another territory seam, per
`work/README.md` "Track size": five successors plus the Euler-operator
remainder TOPO keeps.

7 rows arrived, each by `git mv` with its id, body and history
unchanged. Band 6400-6499 claimed in this commit
(`docs/MODEL-AB-LOG.md`). Nothing dispatched.

## 2026-09-20 — first sitting: the slate corrected, the posture answered

The plan arrived from the cut carrying **seven** of the nine rows that
`git mv`'d into the directory — `sense-inversion-is-invisible-to-tier-3-on-arc-capped-lofts`
and `tier3-prime-still-couples-plus-v-to-the-reporting-target`, both
P0, were in neither the slate table nor the order. The stated 23.5
points were the seven; the nine read **31/30**, over the ceiling the
cut was made to respect.

They are now on the slate, and the count is honest for a second
reason: `validity-refuses-an-interior-chart-singularity` is **parked**
on `work/exch/import-normalizes-the-rim-only-cap`, which states the
order outright (*"this before TOPO's validity rule, or import starts
refusing files it accepts today"*). It was `open`, which read as
dispatchable and it is not. Slate: **26/30**.

**Review posture answered** (it was recorded OPEN at the cut, for this
program's first orchestrator). Protocol v7: two rows triaged IN —
`tier3-prime-still-couples-plus-v-to-the-reporting-target` (moves a
public return type, restructures a hook with four callers, declined
once before for that reason) and `check-9-nesting-is-line-bounded-only`
(decides which walk may express a loop's region, across another
program's privacy boundary). Everything else runs opus/opus outside
the protocol with a **full** review rather than a style one: every row
here changes what the one door every program trusts admits or refuses.
Nothing here is mechanical.

**Order set.** The per-solid pair
(`an-inside-out-part-...-total-volume-is-pinned` and
`tier-3-does-not-check-shell-roles-per-solid`) runs as ONE unit: both
are tier 3 never asking which solid anything belongs to, both want
`props::classify_shells` — which `validate.rs` calls from nowhere —
and split across two lanes they would mint the same per-solid walk
twice in one file and conflict over it.

`check-9-nesting-is-line-bounded-only` is gated in two of its three
thirds, not as a row: `ArcParity` and `NoWalk` wait on
`work/tang/arc-aware-point-in-loop` (#1076); the `Disc` class — the
annular rim this row is named for — waits only on `disc_side` becoming
visible out of `boolean::contain`, which is **CONTACT's** ground since
REACH's cut and is announced there by the unit that lands it.

**Drive-by**, per `work/README.md`'s fix-it-where-you-stand rule: five
live pointers still read `work/topo/<row>` for rows this cut moved —
`crates/topo/src/validate.rs`, `crates/topo/src/shell.rs`,
`crates/sweep/tests/topo_ring_nesting.rs`, `docs/KERNEL-VERBS.md`, and
the two tracker rows that cite ours (`work/exch/import-normalizes-the-rim-only-cap`,
`work/topo/S330`). Repointed. `docs/DOC-LEDGER.md` also names one, and
is left alone: it is a dated sweep record, and the convention in that
same list is to annotate a re-home, not to rewrite the path.
