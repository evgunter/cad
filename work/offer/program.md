---
id: offer
kind: program
title: OFFER — what the viewer offers a person to do about what it shows, and what it asks of the platform beneath it
status: ready
opened: 2026-09-22
area: gui
prefix: offer/
tag: (OFFER orchestrator)
ab_band: 10100-10199
paths: [crates/viewer/src/session.rs, crates/viewer/src/session/delete.rs, crates/viewer/src/bounds.rs, crates/viewer/src/display.rs, crates/viewer/src/docio.rs, crates/viewer/src/platform.rs, crates/viewer/src/gpu.rs]
keep_out: [opened by CHROME's 2026-09-22 priority-seam cut per work/README.md Track size - CHROME measured 88 budget points against 30 and its P0 and P1 rows alone were 55 - the siblings are CHROME and FORMS and all three share crates/viewer by design - shared ground is legitimate by the README's 2026-09-20 rule and what is owed is awareness while a lane is LIVE so run scripts/work.py territory on your branch and announce the seam in the PR rather than drawing a fence, CHROME keeps how a refusal's TEXT is laid out and how long it is (error-and-check-text-overflows-its-region and its residue) - a-refusal-offers-no-action-in-the-viewer is the same failed union's other half and its unit announces itself to CHROME, VSEAM owns what the viewer holds for the document and claims session.rs and docio.rs too, crates/editor-core is EDIT's and MSOLVE's - a typed recourse the kernel's refusals do not carry is a kernel-side row filed there and never a diff from here]
priority: P1
---

**What the viewer offers a person, where it currently only says.**
A refusal ends in a recourse paragraph and offers no control for it; a
cascade delete shows how many dependents it strands but not which
names; the bounds panel prints a range and has no door to certify it;
free-move admission misses the mates a pattern placed. Each row is a
place where the viewer knows enough to hand the person an action, or
a truthful count, and hands them prose or nothing.

Two rows are the same question one layer down, at the platform: the
document seam cannot see a store edit until reopen, the file-chooser
probe reads "no session bus" off one environment variable where D-Bus
can autolaunch, and the viewer expresses no GPU adapter preference, so
a faulting ICD cannot be refused typed.

Charter and order: `work/offer/plan.md`; narrative in
`work/offer/log.md`.
