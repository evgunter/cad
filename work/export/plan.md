# EXPORT — the plan

the writing direction, and the refusal plumbing both exchange crates share

Opened 2026-09-20 by EXCH's priority-seam cut
(`work/README.md`, Track size). Nothing dispatched.

## The slate

**20 budget points** of dispatchable work against a ceiling of 30.

| pri | item | cost | title |
|---|---|---|---|
| P0 | `step-import-source-debug-in-prose-panics-the-binding` | E | step-import renders {source:?} on TransformError and BooleanError, both of which have a Display — a live panic at py::typed_err |
| P1 | `step-import-discards-the-entity-ids-that-are-its-identity-channel` | D | import_step keys its maps by the file's entity ids and discards them at the door, so an imported body has no provenance and cannot be told from a hand-built one |
| P1 | `step-lanes-spell-the-half-edge-to-face-walk` | E | step-import/src and step-export's example spell the half-edge to face walk |
| P1 | `three-crates-fold-signed-zero-with-one-body-and-three-unlinked-arguments` | D | One signed-zero fold body in three crates, each with its own paragraph of argument, none naming the others |
| P2 | `rational-patch-flux-quadrature-budget` | D | dm1's last refusal is the banked rational-patch-flux lane — QuadratureBudget on a rational cylinder wall |
| P3 | `approx-face-has-no-step-printer` | H | STEP export refuses an Approx-faced body - the writer has no OFFSET_SURFACE printer for the kind |
| P3 | `export-error-arms-delegate-no-recourse` | E | two ExportError arms state a condition and stop — Class B of the census recourse sweep, in step-export |
| P3 | `step-export-refusals-cannot-name-entities` | D | step-export refusals cannot name the entity they refuse on — no Part 21 id exists before emission |
| P3 | `step-import-joins-rendered-refusals-on-a-mark-they-may-contain` | E | two StepImportError arms join rendered refusals flat on a "; " the elements may carry |
| P4 | `signed-zero-module-hand-counts-the-bitwise-readers-it-exists-for` | E | step-import — signed_zero.rs says the importer reads bit patterns twice; it is a hand-maintained count and there are more |

## Order

`step-import-source-debug-in-prose-panics-the-binding` first — it is a
one-line fix and it PANICS, which no other row here does.

Then `approx-face-has-no-step-printer`, the substantive row: a body
the kernel can build and mesh cannot leave the tree, which makes it an
interop blocker rather than a refusal-quality one. The signed-zero fold
and the half-edge walk are conversions onto homes that already exist.

## Review posture

OPEN, for this program's first dispatch. EXCH inherits protocol v7
(`docs/MODEL-AB-LOG.md`, Ev 2026-09-19): the dual on triaged-in units
only, opus/opus outside it. Nobody has re-asked the triage question for
this slate, so the first orchestrator answers it here rather than
inheriting an answer.
