# GATHER — the plan

the product gather and member space: what the assembly road can resolve, and what it refuses

Opened 2026-09-20 by WIRE's priority-seam cut
(`work/README.md`, Track size). Nothing dispatched.

## The slate

**31 budget points** of dispatchable work against a ceiling of 30.

| pri | item | cost | title |
|---|---|---|---|
| P0 | `member-space-look-through-stops-at-splits-containment-and-fragmented-merges` | H | A member-space declaration resolves through merges only: a face consumed by a split, by containment, or inside a fragmented merged row is still order-shaped |
| P0 | `product-refuses-naming-when-one-instance-is-placed-under-two-roots` | H | product refuses Naming when one instance's names appear under two transform roots, so a document the solve accepts cannot gather |
| P1 | `node-error-kind-renders-the-slot-id-through-debug` | E | NodeErrorKind's two slot renderings dump SlotId where SlotId::label is the prose spelling |
| P1 | `parallel-node-map-loses-the-funnel-and-the-symbolic-session` | H | the evaluator's parallel node map loses the K-funnel's recordings and the symbolic session |
| P1 | `product-gate-says-verbatim-then-states-the-difference` | D | the per-part at-rest gate's trigger is one decided policy with a home in neither of the two crates that implement it (product.rs called its copy verbatim) |
| P1 | `res-target-slot-roles-are-unguarded-and-duplicate-spec-slots` | E | res_target's StepArg roles are a second spelling of spec_slots' role assignment and nothing tests them - the Target2 twins can be dropped with 1324 tests green |
| P1 | `sentinel-region-extractor-has-three-homes` | E | Three hand-rolled sentinel-region extractors; test_utils::source::sentinel_region is now the home and topo's copy has not moved to it |
| P1 | `the-third-tag-vocabulary-macro-owes-a-unification-trigger` | D | target_forms! and arc_modes! are near-copies one meta-level up, and the PR's own not-yet argument names a trigger with no schedule |
| P3 | `a-source-census-scan-that-matches-nothing-should-refuse-at-the-scan` | E | A source census whose scan matches nothing in a file that must contain its markers should refuse at the scan, not return an honest zero for an assertion to maybe catch |
| P3 | `loft-path-loses-nine-predicate-families-from-the-probe-stream` | D | Retiring the T-validation drops nine profile-validation predicate families from the loft path's Probe sample stream, and the k-lint gate cannot observe a shrunken population |
| P3 | `select-refusal-coverage-is-not-compiler-enforced-from-the-test-crate` | D | SelectRefusal is #[non_exhaustive], so its variant coverage cannot be enforced from editor-core/tests |
| P4 | `frame-linear-generic-door-has-no-consumers` | E | A CLASS - public generic doors with zero production call sites, kept alive by their own tests: Frame::linear<T> is DELETED; the two profile map_scalar rungs (PR 2409) are the half still open |
| P4 | `wire-rs-accumulation-residue-comment-ratio-and-wire-sweep` | E | wire.rs's two accumulation findings survive the header fix: 41% comment ratio, and wire_sweep exists to fail |

## Order

`member-space-look-through-stops-at-splits-containment-and-fragmented-merges`
first — a face consumed by a split is still order-shaped, which is the
positional guessing `docs/DESIGN.md`'s naming pillar banks against by
name. `product-refuses-naming-when-one-instance-is-placed-under-two-roots`
is independent and can run at once.

`parallel-node-map-loses-the-funnel-and-the-symbolic-session` is the
row to read before assuming anything about parallel evaluation here.
The six `E`/`P3`/`P4` rows batch into one unit rather than six PRs.

## Review posture

OPEN, for this program's first dispatch. WIRE inherits protocol v7
(`docs/MODEL-AB-LOG.md`, Ev 2026-09-19): the dual on triaged-in units
only, opus/opus outside it. Nobody has re-asked the triage question for
this slate, so the first orchestrator answers it here rather than
inheriting an answer.
