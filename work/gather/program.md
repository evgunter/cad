---
id: gather
kind: program
title: GATHER — the product gather and member space: what the assembly road can resolve, and what it refuses
status: ready
opened: 2026-09-20
area: api
prefix: gather/
tag: (GATHER orchestrator)
ab_band: 8400-8499
paths: [crates/editor-core/src/product.rs]
keep_out: [opened by WIRE's 2026-09-20 priority-seam cut (Ev, in chat) per work/README.md Track size - WIRE measured 85.5 budget points and was cut into tracks meant to run in PARALLEL (Ev, in chat: for these high priority tracks it is ideal to have several components that can be worked on in parallel), the siblings are WIRE EMIT GATHER and they share their parent's territory by design - shared ground is legitimate by the README's 2026-09-20 rule and what is owed is awareness while a lane is LIVE, so run scripts/work.py territory on your branch and announce the seam in the PR rather than drawing a fence, TOPO and its cut siblings own topo/src, EDIT owns the DocEdit vocabulary, CARVE and BAND own crates/sweep]
priority: P0
---
The product gather and the member-space road, plus the residue that
travels with them.

Two rows are refusals a person meets: `product` refuses `Naming` when
ONE instance's names appear under two transform roots, so a document
the solve accepts cannot gather; and a member-space declaration
resolves through merges ONLY, so a face consumed by a split, by
containment, or inside a fragmented merged row is still order-shaped —
which is the positional guessing the naming pillar banks against.

The third P0 row is the one that loses information rather than
refusing: the evaluator's parallel node map drops the K-funnel's
recordings and the symbolic session, so a parallel evaluation and a
serial one do not answer the same thing about what happened.

Charter and order: `work/gather/plan.md`; narrative in `work/gather/log.md`.
