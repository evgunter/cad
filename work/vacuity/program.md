---
id: vacuity
kind: program
title: VACUITY — rows that cannot fail: assertions on their own codomain, probes that print, floors that count what they discard
status: ready
opened: 2026-09-20
area: infra
prefix: vacuity/
tag: (VACUITY orchestrator)
ab_band: 8800-8899
paths: []
keep_out: [opened by TINT's 2026-09-20 priority-seam cut (Ev, in chat) per work/README.md Track size - TINT measured 207.5 (97.5 once priced) budget points and was cut into tracks meant to run in PARALLEL (Ev, in chat: for these high priority tracks it is ideal to have several components that can be worked on in parallel), the siblings are TINT VACUITY FIXTURE HELPER and they share their parent's territory by design - shared ground is legitimate by the README's 2026-09-20 rule and what is owed is awareness while a lane is LIVE, so run scripts/work.py territory on your branch and announce the seam in the PR rather than drawing a fence, the fixtures these rows share live under crates/*/tests and crates/test-utils which S-TCOST globs - a unit here adds or moves rows there as ordinary tests and says so in the PR, DUP owns the one-thing-spelled-n-times class on PRODUCTION code and these four own it on the test side]
priority: P3
---
**A row that cannot go red is not a test.** The sharpest half of
S-TINT's charter, as its own track: eleven tests in three mate/fix
suites that print probe answers and assert nothing; a phantom-turn row
that asserts nothing in three of four arms; sign and positivity asserts
riding a cancellation that is structurally zero, so they hold on their
own codomain; a fuzz row whose `continue` shrinks its own sample while
its coverage floor counts the discarded trials; an anti-vacuity floor
that survives deleting either reference it was built to protect.

Three rows are the other shape — a row that goes red for a reason that
is not a defect: a strict-gap floor gating hosted CI on a varying
seed, a suite that reds under a malformed ambient epsilon, and a
random-ray oracle that tripped once at eps 1e-6. The last of those is
**a test that actually failed** and is the one row here that may be
reporting a real pick defect rather than its own weakness; it is P1 and
should be read first.

This program claims no paths: the rows are spread across every crate's
suites, and each unit announces its fence in its own PR.

Charter and order: `work/vacuity/plan.md`; narrative in `work/vacuity/log.md`.
