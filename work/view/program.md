---
id: view
kind: program
title: VIEW — viewer architecture
status: open
opened: 2026-09-03
area: gui
prefix: view/
tag: (VIEW orchestrator)
ab_band: 1900-1999
paths: [crates/viewer/src/*, crates/viewer/README.md]
keep_out: [CHROME edits the same files first — this program's refactor waits for CHROME's slate or carries its remainder, the rulings this program's builds consume (layer-3 identity and free-move commit) are DOCM's, the authored-step to canonical-segment map door lives beside the lowering in crates/profile which is S-BOOL's glob — announce, the GUI-3 §5 seam is ratified design — revising it for the off-thread index is an [ev] PR, PERF cedes per-frame rendering and hover-picking here]
---

The viewer's structural questions, gated by one conversation: the
split of `session.rs` and `app.rs` into modules, ratified into
`crates/viewer/README.md`, then the picking vocabulary, status-line
ownership, per-segment focus and the off-thread pick index. Class D
with one D→H build. Opens after CHROME's slate lands. Charter and
order: `work/view/plan.md`; narrative in `work/view/log.md`.
