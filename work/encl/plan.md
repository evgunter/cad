# ENCL — plan

Live state is `work/encl/log.md`'s tail and the item files beside
this plan, never this file. Branch prefix `encl/`; away-channel tag
`(ENCL orchestrator)`; A/B band **5800-5899**, claimed in
`docs/MODEL-AB-LOG.md`'s banding entry at the cut.

## Lanes

- **The two origins** (`patch-bound-offset-fit-recentring-origins`,
  D→H): the head of the program, because it is a measurement that
  decides a design. The item says so in as many words: either the
  composite lane affords a per-cell centre, or the cell lane gives up
  its tighter one, and the micron row is the instrument for both.
- **The offset fit's residues**, all three from PROPS' two units and
  all carrying executed numbers:
  `offset-fit-door-bound-is-not-monotone-in-the-cell-bound` (the
  schedule reorders under a relative marking cut),
  `offset-fit-stall-face-has-no-fixture` (a face with no witness at any
  door), and `offset-fit-at-tight-eps-refuses-every-curved-nurbs-chart`.
- **The patch bound**:
  `rational-cells-hull-the-f64-refined-net-so-the-described-patch-escapes`
  — the described patch's true second derivative escapes its own hull.
- **Sealing the certificates** (`certificate-types-have-public-fields-and-are-forgeable`,
  D→H): five types, five owners, one shape — private fields and one
  minting door each. Announce every seam; the row names the owners.
- **What a certificate is worth**:
  `certified-hull-padding-is-the-leaf-width-not-the-lane`,
  `contribution-bounds-via-dual-interval`,
  `tangent-parallel-certifier-passes-a-transverse-arc` (a certifier
  admitting a 90-degree crossing described as a tangency — read this
  one early, it is the only row here that admits a wrong answer rather
  than a wide one), and `H11`'s normative home for the NaN-end
  postcondition.
- **The audit**: `certified-lane-non-real-contract-audit` — member by
  member against the ratified DL6 contract, which is not re-litigated.
  `approx-surface-tolerance-is-now-always-the-runs-eps` is its rider.

## Review posture

Not yet set. The parent program ran full v6 duals on kernel units
with E riders single-reviewed; whether that is right here is the
orchestrator's call at its first dispatch, and Ev took S-TCOST off
the protocol entirely on 2026-09-12, so the question is live.

## Exit shape

Every item above landed or ruled; the walk convention applies.
