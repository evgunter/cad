# ENCL — the plan

certified enclosures: what a certificate claims, and what it is worth

Re-scoped 2026-09-20 by ENCL's priority-seam cut
(`work/README.md`, Track size). Nothing dispatched.

## The slate

**28 budget points** of dispatchable work against a ceiling of 30.

| pri | item | cost | title |
|---|---|---|---|
| P0 | `offset-fit-at-tight-eps-refuses-every-curved-nurbs-chart` | H | at eps 1e-12 the offset fit refuses every curved NURBS chart, and the mint's cost at the default eps is unmeasured on a body |
| P0 | `rational-cells-hull-the-f64-refined-net-so-the-described-patch-escapes` | H | patch_bound's rational arm hulls the f64-refined net: the described patch's true second partial exceeds the certified sup by ULPs, measured exactly |
| P0 | `tangent-parallel-certifier-passes-a-transverse-arc` | H | certify: TangentParallel admits a 90-degree crossing described as a tangent intersection |
| P1 | `approx-surface-tolerance-is-now-always-the-runs-eps` | D | SurfaceSpec.tolerance is now always the run's eps on every production path; map_approx reads it as if it were the surface's own |
| P1 | `offset-fit-door-bound-is-not-monotone-in-the-cell-bound` | H | offset_fit: tightening a cell bound can raise the door's hull_sup, because the refinement marking reads the sup |
| P1 | `patch-bound-offset-fit-recentring-origins` | D | patch_bound and offset_fit recentre the same homogeneous nets against different origins — unifying the two centres needs a measurement |
| P3 | `H11` | E | Point the re-derived NaN-end postcondition sites at CertifiedEnclosure's one normative home |
| P3 | `budget-refusal-drops-the-enclosure-the-caller-needs` | E | a budget refusal from refine_to_target drops the enclosure, so a consumer pattern-matches two crates down to get it back |
| P3 | `offset-fit-stall-face-has-no-fixture` | E | offset_fit's RefinementStalled face has no fixture at any door, so the verdict-before-budget ordering is unpinned |

## Order

`tangent-parallel-certifier-passes-a-transverse-arc` first: a
certifier that ADMITS a 90-degree crossing described as a tangent
intersection is wrong in the accepting direction, which is the
expensive direction for a certificate.

Then the two `offset_fit` rows that a user meets —
`offset-fit-at-tight-eps-refuses-every-curved-nurbs-chart` (at eps
1e-12 it refuses ALL of them) and
`rational-cells-hull-the-f64-refined-net-so-the-described-patch-escapes`.
**Read PROPS' mignitude-floor unit's `## Closed` section before
specifying `offset-fit-door-bound-is-not-monotone-in-the-cell-bound`**:
it is the worked precedent that established the non-monotonicity and
left this row behind.

## Review posture

OPEN, for this program's first dispatch. ENCL inherits protocol v7
(`docs/MODEL-AB-LOG.md`, Ev 2026-09-19): the dual on triaged-in units
only, opus/opus outside it. Nobody has re-asked the triage question for
this slate, so the first orchestrator answers it here rather than
inheriting an answer.
