---
id: edit
kind: program
title: EDIT — the document model's residue: the persisted recipe, the edit vocabulary and the resolver doors
status: open
opened: 2026-09-13
area: api
prefix: edit/
tag: (EDIT orchestrator)
ab_band: 4800-4899
paths: [crates/editor-core/src/persist/*, crates/editor-core/src/program.rs, crates/editor-core/src/doc.rs, crates/editor-core/src/edit.rs, crates/editor-core/src/node.rs, crates/editor-core/src/names/role.rs, crates/editor-core/src/names/emit_blend.rs, crates/editor-core/src/eval/parts.rs, crates/editor-core/src/eval/memo.rs, crates/editor-core/src/resolve/hit.rs, crates/editor-core/src/resolve/pick.rs, crates/editor-core/src/resolve/mod.rs, crates/editor-core/src/assembly.rs, crates/editor-core/src/range.rs, crates/editor-core/REFERENCES.md, crates/editor-core/IDENTITY.md]
keep_out: [OPENED 2026-09-13 at DOCM's exit sweep (docs/DOC-LEDGER.md sweep 14) as its successor on the same ground with the same fence — every path above was DOCM's and the inherited rows say so in their bodies, mate.rs and mate/* went to MSOLVE (its paths) and are not here, docs/RECIPE-DOORS-DESIGN.md is LIB's and is not here — DOCM listed it and the overlap was one-sided, crates/editor-core/src/eval/mod.rs and eval/wire.rs and product.rs and names/emit.rs and emit_topo.rs are WIRE's by its explicit path list and the seven DOCM rows on them went there — emit_blend.rs is claimed here since 2026-09-16 because the ruled cut-off-arc row lands on it and no program listed it; the other emit_*.rs and attribute.rs defer.rs merged.rs mod.rs under names/ are claimed by nobody, resolve/vdiff.rs is S-BOOL's, the analysis lane (analysis.rs distribution.rs drive.rs measure.rs mc.rs) is PROPS's since M10's sweep and range.rs consumes it read-only by DOCM-9's spec, the viewer builds of every answer here are CHROME's or VIEW's, the pncad facade and bindings are LIB's and PORT announces on assembly.rs and node.rs and persist/wire.rs (the load door's structured refusals are PORT's row, its defect site wire.rs's Error::custom) as its keep_out says, DOOR carries four one-PR rows on these files by announcement, no ruling is resolved by implementing it — each design question here is an [ev] PR first, and the two README pages above are ratified design (DM1–DM8, DI1–DI5): a change to what a clause decides waits for Ev]
priority: P0
---

DOCM's successor: the twelve rows DOCM left open on the document-model
ground when it closed — the edit vocabulary's missing doors (no splice,
no unit edit for a document parameter, a delete that strands a payload
name), the persisted recipe's honesty (replay without maintenance, the
program's bare arguments), the
resolver's doors (identity at the pair doors beyond the three that
check, the grazing pick), and the two Track V mirrors DOCM inherited
(`C6`, `D366`). Class D where a question is open, E where the fix is
written. Charter and order: `work/edit/plan.md`; narrative in
`work/edit/log.md`.
