---
id: D70
kind: unit
title: Decide whether the 13 silent whole-row stand-downs in three test files should be epsilon-conditional at all
status: closed
opened: 2026-08-21
track: W
refs: [loud-stand-down-announcements-are-discarded-by-the-gate, plane-nurbs-certificate-bound-does-not-refine-with-eps, ssi-fit-sample-budget-is-a-constant-compared-against-a-varying-eps]
closed: 2026-09-15
---

## What

The silent whole-row stand-down: a population of 13 — a FLOOR, not an enumeration — in three files (S126)

## Was

Track F

## Finding

### S126. The silent whole-row stand-down has a population, and it is 13 in three files

Found by lane F-d while sweeping F4's class. The shape is `let … else { return; }` in the first statements of a `#[test]`: the fixture the row needs is unavailable, the row returns, and the battery log records nothing at all. The whole test is the skip, so there is no partial coverage to floor.

The population, from `crates/*/tests/**/*.rs` (excluding `all.rs`): **13 sites in 3 files** — `sweep/tests/m8_4_intersection_iso.rs` (11), `sweep/tests/review_probes_m8_4.rs` (1), `topo/tests/m5_pr7_split_meter.rs` (1). None is announced.

**Why this is smaller than it looks, and the sentence is the finding.** All thirteen stand down on an ε-conditional fixture, and the helper that returns `None` *asserts its own classification* first — `m8_4_intersection_iso.rs`'s `seam_at_eps` asserts `eps >= 1e-9` on the `Ok` arm and `eps < 1e-9` plus the refusal's own measured sup on the `Err` arm, so a mis-classified ε reds inside the helper rather than skipping. What is missing is only that the *caller* announces nothing, so the battery log cannot distinguish "this ε ran the row" from "this ε did not". That is the D45 shape — coverage nobody can read off the run — not the S84 shape.

**Not proposed here: floors.** C21 rules this class un-rolled-up because a floor concedes the skip and *whether the row should be ε-conditional at all* comes first. This finding contributes the hit list that question needs, and nothing else. Scheduled as **D70**.

**What the sweep could not match.** It keys on `let`-else with a literal `return;` in the following four lines, so it misses: a stand-down written as `if x.is_none() { return; }` or `match … { None => return, … }`; one spelled as an early `return` inside a helper the test calls; any whole-binary skip behind `#[cfg(feature = …)]` (a different, already loud idiom); and the 116 `let`-else sites whose body is `continue`, which this sweep deliberately excludes. Those are a **selector** over a heterogeneous collection — *"only the certified circle edges"* — rather than a stand-down, in every one of the ~20 read; the distinction is not one a regex can draw, and a sample was read, not all 116.

**So 13 is a FLOOR, not an enumeration** — the same qualifier S117 owed. Every miss above adds to it and none subtracts.

**A differently-shaped sweep finds a different class, and it is not this one.** F4's style review swept for *a counter that is incremented, printed, and never floored* and found **23 sites in 13 files**, including `geom-core/tests/spline_hull.rs` — in the crate S78 cited as where *"the discipline exists elsewhere in the tree"*. That is the S78 shape rather than the S84 one, its members are live rows rather than skipped ones, and it is the reviewer's to place; recorded only so the next reader does not mistake this row's 13 for the whole of *"guards that count and never floor"*.

**Verdict:** none recorded (no verdict line in the source).

## Fence

Track W — the test targets: guards, doctests, fixtures and stand-downs. Fence: `crates/*/tests/` in every crate, and `crates/test-utils/`. Block: `D380`–`D399` / `S450`–`S469`. This track owns test-side mechanisms, not test coverage for other tracks' changes — a src change carries its own tests in its own PR, as always.

## Notes

**Standing note carried from Track W's table (the source stated it once, after the table, and nothing else carries it; it is placed here because a lane combing this track's `#[ignore]`s meets it first).** There are exactly **two** `#[ignore]`d collection runs in this tree — `editor-core::m4_pr8_k_probe::dump_corpus_k_samples` and `sweep::k_report`. They produce data rather than assert anything, they are `#[ignore]`d because they run one process per ε, and **they stay, because they are `k-lint`'s only input**: `k-lint`'s CLI is `k-lint <k-probe-csv>…` and those CSVs come from `k_probe_sweep.sh`'s `run_dump`. Retiring them retires the K-telemetry gate — `K = 10` is the ratified default and `docs/k-report-data/`'s committed baseline is honest only while fresh rows are linted against it. **That is a decision about the gate, not about test hygiene**, and no test-hygiene row licenses it. Recorded because a lane combing this track's `#[ignore]`s will meet them first. **The term *"dump harnesses"* is retired**: it named a population of thirteen that does not exist, and these two are not it.

## Claimed by S-TCOST (2026-09-04)

Moved from `work/code-quality/` to `work/tcost/` in the tracker-wide
re-home sweep of 2026-09-04 (Ev's direction, in-chat), which claimed
**Track W whole** for S-TCOST. Id, `track: W` and body unchanged.

The letter and the program are the same ground: W's fence is
`crates/*/tests/` (all crates) and `crates/test-utils/`, and S-TCOST's
`paths` are `crates/*/tests/*` and `crates/test-utils/*` — an exact
match. `work/code-quality/plan.md` recorded W's claimant as "ground is
`tcost`'s" without the rows ever moving, and no `smell/w-*` lane has
ever run, so the rows were waiting on an owner rather than on a ruling
or a dependency.

**This claim was made on Ev's direction rather than by the S-TCOST
orchestrator**, which is not how `work/README.md` expects a claim to
happen. If S-TCOST would rather not hold a row, moving it back is a
`git mv` and this section is the record of why it arrived.

The two seams `work/code-quality/plan.md` states for W are unchanged
and travel with the rows: `crates/test-utils/src/source.rs` is the
shared home three tracks' rows land in, and the `UNCONVERTED_TODAY`
ceiling is **re-derived from the table at each landing, never lowered
by a row's own member count**; and a W row whose mechanism reaches into
a crate's `src/` is **filed on the owning track** rather than edited
there.

## Moved to S-TINT (2026-09-11)

Moved by `git mv` from `work/tcost/` at S-TINT's opening. Id, title and
body are unchanged; the directory is the claim (`work/README.md`).

**Why it moved.** S-TCOST's board was re-sorted on 2026-09-11 against the
repository going public on 2026-09-03 (`work/tcost/log.md`, the
2026-09-11 seam). That sort found this row is not a cost lever in either
currency — it neither shortens the gate's critical path nor saves a
billed minute, and it was never argued on one. It reached S-TCOST by the
tracker-wide re-home of 2026-09-04, which routed rows by PATH GLOB
(`crates/*/tests/*`, `crates/test-utils/*`) rather than by question.
This program is the question it was always about: whether the suite
asserts what it claims to assert.

## Context gathered before dispatch (S-TINT orchestrator, 2026-09-15)

Read for Ev; nothing here decides the row.

**The thirteen are real stand-downs and the helper is rigorous.** In
`crates/sweep/tests/m8_4_intersection_iso.rs`, `seam_at_eps` asserts on
BOTH arms — `eps >= 1e-9` on `Ok`, and on `Err` that the refusal is the
typed `Certification`/`PlaneNurbsCertificate` escalation, that
`eps < 1e-9`, that the cause carries a `MarginDiag::Value`, and that the
measured `sup > eps`. So a mis-classified ε reds inside the helper. The
eleven callers then spell `let Some(…) = seam_at_eps(false) else {
return; };` and assert nothing of their own.

**What that makes the ε-fine row.** At ε = 1e-12 the eleven rows in that
file report green having exercised one shared fact eleven times, and
none of their own subjects. The claim is genuine and worth pinning; it
simply does not need eleven carriers. `crates/topo/tests/m5_pr7_split_meter.rs`
already writes the better version at one of its two sites —
`return; // typed FitSampleBudget refusal at this ε — its own row pins it`
— naming the row that owns the claim, which is the disposition the other
twelve lack.

**So the fork is not the one the plan's summary implies.** "Make the row
assert the refusal in its `None` arm" is the wrong repair: the helper
already asserts it, and moving it to the callers mints eleven copies of
one claim — shape 3 of this program's own charter, on a row filed under
shape 1. The live options are:

1. **One owner plus a named stand-down.** One row asserts the ε-fine
   refusal (it is one claim); the other twelve return with a pointer to
   it, as `m5_pr7_split_meter.rs` already does. Cheap, honest, and
   concedes that the subjects do not run at 1e-12.
2. **Re-mine the fixture** so the seam's certified sup fits inside
   1e-12 and the eleven subjects run at every ε. This is the only option
   that ends the ε-conditionality rather than labelling it, and it may
   not be reachable: the sup is a property of the intersection's
   conditioning, so a fixture that certifies at 1e-12 is a different
   seam, and then the rows pin a different thing. Sibling shape: `D72`.
3. Delete. Wrong — the rows test real subjects at two of the three ε rows.

**And the announcement channel is broken, which rules out the obvious
fourth option.** Routing the thirteen through
`test_utils::vacuity::stood_down` would add nothing observable: that
door is a `println!`, nextest discards a passing test's stdout by
default, and the twelve gating `test (…)` jobs pass no
`--success-output`. Measured under the pinned nextest. Filed as
`work/tint/loud-stand-down-announcements-are-discarded-by-the-gate` with
its population (22 existing sites in 10 files), and **that row is a
prerequisite for any answer here that is spelled as an announcement.**

### Correction, same day: option 2 is not speculative — the lever is in the file

The paragraph above calls re-mining the fixture *"the only option that
ends the ε-conditionality"* and guesses it "may not be reachable". **The
second half is wrong and is withdrawn.** The lever already exists in
`m8_4_intersection_iso.rs`, is already documented there, and is already
applied to one row of the same file:

```rust
const INTERIOR_COLUMN_SCALE: f64 = 1.0 / 1024.0;
```

Its own doc states the whole argument: *"The fixture's ATTACHMENT is
ε-conditional at scale 1: the seam's certified between-samples sup is a
LENGTH — 6.217e-12 m — so at ε_in = 1e-12 the declare-and-check rung
refuses and no body is built at all, which is how the row this replaces
came to assert nothing at one of the three ε the matrix draws (#1167).
The sup scales with the model and ε does not, so the fixture is built
small enough that every cell of the ε table exercises the same thing."*
And: *"A power of two, so every coordinate stays exact and the
construction is `offset_square_prism`'s, only smaller."*

So the defect this row is about **was diagnosed and repaired once
already, for exactly one row**, by the lane that took #1167.
`intrinsic_seam_at(swap, scale)` is the parameterised fixture;
`intrinsic_seam(swap)` is `intrinsic_seam_at(swap, 1.0)`; the eleven
stand-downs are the callers that kept the 1.0 and the
interior-column row at `m8_4_intersection_iso.rs`'s `INTERIOR_COLUMN_SCALE`
call site is the one that did not. At 1/1024 the sup is ~6.07e-15 m,
inside ε = 1e-12 by ~165×, so the scaled fixture certifies at every cell
of the matrix.

**The population therefore splits three ways, by MECHANISM, and only the
first is cheap:**

- **11 of 13 — `sweep/tests/m8_4_intersection_iso.rs`.** The lever is
  in-file, measured and precedent-backed. The work is moving the callers
  onto it and scaling whatever each row asserts in absolute metres
  (e.g. `an_imported_domain_chart_mints_the_boundary_intersection`'s
  `let wide = 3.0 * 3.0_f64.sqrt();`) — mechanical, row by row, and not
  a geometry question. The stand-down then has nothing left to announce.
- **1 of 13 — `sweep/tests/review_probes_m8_4.rs`.** Same fixture, same
  ~6.22e-12 sup, same mechanism — but `offset_square_prism()` and
  `flat_bowed_seam(&body)` there take NO scale argument. The lever has
  to be ported before it can be pulled. A reviewer should ask whether
  that port is the same two functions duplicated across two files, which
  would make it a shape-3 row as well.
- **1 of 13 — `topo/tests/m5_pr7_split_meter.rs`.** A DIFFERENT
  mechanism: `ssi_branch_or_budget` refuses on
  `SsiError::FitSampleBudget` from a cylinder∩sphere SSI fit, i.e. a
  sample-count budget rather than a certificate sup. Dilation plausibly
  helps here too — the fit's sample demand is governed by the same
  deviation-over-ε ratio, which is linear in scale — but the fixture
  carries absolute numbers of its own (`SsiDomain { half_extent: 1.5,
  extent: 2.0, floor_scale: 1.0 }`) and the refusal is raised inside the
  SSI fitter, so this one is a MEASUREMENT and not an assertion. It is
  also the one site of the thirteen that already names the row owning
  its claim, so it is the least urgent.

**What is still genuinely Ev's.** Not "is option 2 reachable" — it is,
for 11 and probably 12 of the 13. The question that survives is whether
shrinking a fixture to 1/1024 to keep it inside a fixed absolute ε is
the right answer or a tell. It buys an ε-table that exercises one thing
everywhere, and it pays by testing the kernel on a millimetre-scale
prism because the metre-scale one cannot be certified at 1e-12 — which
is a true fact about the kernel that the stand-down is currently the
only record of. #1167 already ruled that trade once, for one row; this
row asks whether that ruling generalises to the other twelve or whether
the ε-fine refusal is a claim worth keeping a carrier for.

**Unverified here, and the lane owes it:** that the interior-column row
is in fact green at ε = 1e-12 is the doc comment's claim, not a
measurement taken by this session. Run it before building on it.

### The ε-conditionality is a kernel artifact, not a fixture property (Ev, 2026-09-15)

Ev's reading, and it holds: *a valid intensional description should be
valid under any decrease in ε, unless it is ulp-scale*. It is not
ulp-scale here — at metre coordinates ε = 1e-12 is ~4500 ulps.

Traced. Limb 2 of the plane × NURBS certificate bounds
`sup_t |S(P(t)) − C(t)|`, where `P(t)` is **the certificate's own**
degree-1 interpolant through a **fixed** schedule
(`PXN_FIT_SAMPLES = 33`, `CERT_SAMPLES = 9`, `PXN_IMAGE_DEGREE = 1`, all
`const`). Both error sources its docs name — the image's `O(h²·κ_uv)`
deviation and the ring's per-span widening — are functions of the
schedule step and the model scale, and neither is a function of ε. And
this fixture's carrier is EXACT: both walls come from one `loft_body`
call, the seam edge is the loft of a profile vertex and simultaneously a
boundary iso-curve of the bowed patch, and every control point of that
corner has `y = −scale` exactly, so it lies in the restated plane
exactly. The true deviation is zero and the whole 6.217e-12 m is
certifier discretization.

**So option 2 above is not "re-mine the fixture" either.** Shrinking the
prism works because it shrinks a bound that should not have been
compared against ε in the first place. #1167 pulled that lever once for
one row; generalising it to the other twelve would propagate a
workaround for a kernel defect into eleven more fixtures.

Filed on the owning program as
`work/trim/plane-nurbs-certificate-bound-does-not-refine-with-eps`
(`crates/geom-brep/src/edge_nurbs.rs` is TRIM's territory), with the
measurement that would settle whether the bound is refinable.

**What this does to D70.** The row's own question — should these
thirteen be ε-conditional at all — now has a candidate answer that is
neither of the two options above: **eleven of them should not be
ε-conditional, because the thing making them ε-conditional is a defect
elsewhere.** D70 does not get to fix that, and should not wait on it
either. The disposition to take is the one `m5_pr7_split_meter.rs`
already writes — each stand-down names the row that owns its claim, and
here that pointer is the TRIM row — with the eleven re-joined to the
matrix if and when the certificate refines. The 13th
(`topo/tests/m5_pr7_split_meter.rs`, `FitSampleBudget`) is a different
mechanism and is unaffected by any of this.

## Closed — ruled: no, and the defect is filed elsewhere (Ev, in-chat, 2026-09-15)

**The question was "should these thirteen rows be ε-conditional at all".
The answer is no.** Ev: *"we should fix the defect (or, like, you would
file the issue for fixing the defect and move it off your slate)."*

Nothing on this row is test-suite work. The thirteen stand-downs are
two kernel defects wearing one test-suite costume, and both are now
filed on the ground that owns them:

- **Twelve** — the eleven in `sweep/tests/m8_4_intersection_iso.rs` and
  the one in `sweep/tests/review_probes_m8_4.rs` — stand down on the
  plane × NURBS certificate, whose limb-2 bound is a function of a fixed
  sample schedule and does not refine with ε, on a carrier that is exact
  by construction. Filed as
  `work/trim/plane-nurbs-certificate-bound-does-not-refine-with-eps`.
- **One** — `topo/tests/m5_pr7_split_meter.rs` — stands down on
  `SSI_MAX_FIT_SAMPLES`, a `const` cap on a sample count the marcher
  grows as ε shrinks. Filed as
  `work/issues/ssi-fit-sample-budget-is-a-constant-compared-against-a-varying-eps`,
  in `issues/` because no open program's `paths` claim
  `crates/geom-brep/src/ssi.rs`.

**When those land the stand-downs disappear on their own** — the rows
stop taking the `None` arm because there is no refusal to take — so
there is no test-side change to schedule here, and none is scheduled.
The three options this row's earlier sections weighed (announce them,
re-mine the fixture, delete) are all withdrawn: each was a way of living
with the ε-conditionality, and the ε-conditionality is not this
program's to live with.

**What this row leaves behind, deliberately, and where it went.** The
finding that the tree's loud stand-down door is shut on the gate is
S-TINT's own and stays on this slate as
`work/tint/loud-stand-down-announcements-are-discarded-by-the-gate` —
it was found here but it is not about these thirteen, and it survives
their disappearance. Nothing else from this row is carried: the sweep's
"13 is a FLOOR" qualifier, the 116-`continue`-site exclusion and the
`#[ignore]`d-collection-run standing note were inherited framing for a
question that is now answered, and they die with this row rather than
being re-filed as work nobody asked for.
