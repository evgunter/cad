---
id: C18
kind: unit
title: Close the three residues of H12's own enumeration left open by #734, all coverage or prose
status: open
opened: 2026-08-21
track: W
refs: [734]
priority: P3
cost: D
---

## What

Three residues of H12's own enumeration, left open by #734 — a tests-only unit, so all three are coverage or prose

## Was

Track C

## Finding

No live finding section; provenance only. The three residues are enumerated in #734's body, not in this document.

## Fence

Track W — the test targets: guards, doctests, fixtures and stand-downs. Fence: `crates/*/tests/` in every crate, and `crates/test-utils/`. Block: `D380`–`D399` / `S450`–`S469`. This track owns test-side mechanisms, not test coverage for other tracks' changes — a src change carries its own tests in its own PR, as always.

## Claimed by S-TCOST (2026-09-04)

Moved from `work/code-quality/` to `work/tcost/` in the tracker-wide
re-home sweep of 2026-09-04 (Ev's direction, in-chat), which claimed
**Track W whole** for S-TCOST. Id, `track: W` and body unchanged.

The letter and the program are the same ground: W's fence is
`crates/*/tests/` (all crates) and `crates/test-utils/`, and S-TCOST's
`paths` are `crates/*/tests/*` and `crates/test-utils/*` — an exact
match. `work/code-quality/plan.md` recorded W's claimant as "ground is
`tcost`'s" without the rows ever moving, and no `smell/w-*` lane has
ever run, so the rows were waiting on an owner rather than on a ruling
or a dependency.

**This claim was made on Ev's direction rather than by the S-TCOST
orchestrator**, which is not how `work/README.md` expects a claim to
happen. If S-TCOST would rather not hold a row, moving it back is a
`git mv` and this section is the record of why it arrived.

The two seams `work/code-quality/plan.md` states for W are unchanged
and travel with the rows: `crates/test-utils/src/source.rs` is the
shared home three tracks' rows land in, and the `UNCONVERTED_TODAY`
ceiling is **re-derived from the table at each landing, never lowered
by a row's own member count**; and a W row whose mechanism reaches into
a crate's `src/` is **filed on the owning track** rather than edited
there.

## Moved to S-TINT (2026-09-11)

Moved by `git mv` from `work/tcost/` at S-TINT's opening. Id, title and
body are unchanged; the directory is the claim (`work/README.md`).

**Why it moved.** S-TCOST's board was re-sorted on 2026-09-11 against the
repository going public on 2026-09-03 (`work/tcost/log.md`, the
2026-09-11 seam). That sort found this row is not a cost lever in either
currency — it neither shortens the gate's critical path nor saves a
billed minute, and it was never argued on one. It reached S-TCOST by the
tracker-wide re-home of 2026-09-04, which routed rows by PATH GLOB
(`crates/*/tests/*`, `crates/test-utils/*`) rather than by question.
This program is the question it was always about: whether the suite
asserts what it claims to assert.

## Re-derived (2026-09-15, lane A)

**VERDICT: REPRODUCES** — all three residues are live. But this row is
**cross-wired to the wrong `H12`**, and that has to be fixed before
anyone cuts a lane on it.

### The cross-wire, which is the important finding

`#734` is *"test(ssi): the sweeps' other never-silence doors get
acceptance rows (H12)"* — Track **C** lane C-d, merged 2026-08-20. Its
`H12` is a **Track C finding id** (a clause in `docs/SMELL-SCAN-2026-08.md`
§D's C1 row: *"the SSI sweeps' other never-silence doors have no
acceptance row"*). It has **nothing to do** with `work/tint/H12.md`, which
is the Track **H** unit about eleven never-collected `compile_fail`
doctests (`S214`).

So three tracker statements are wrong:

- this row's `refs: [H12, 734]` points at the wrong `H12`;
- `work/tint/H12.md`'s Notes line *"`C18` carries three residues of this
  row's own enumeration"* is false — `H12.md`'s enumeration is the
  doc-fence census, and `C18` carries nothing from it;
- `work/tint/plan.md`'s pairing *"`H12`, `S216` and `C18` are one
  question from three sides (what a doctest in `tests/` is, and whether a
  `compile_fail` row verifies the reason it names)"* is wrong on `C18`.
  `H12`/`S216` are one question from two sides; `C18` is a third,
  unrelated question about SSI never-silence door coverage in
  `crates/geom-brep/`.

**Not edited here** — the plan and `H12.md` are outside this lane's
fence. Flagged for the orchestrator.

### What the three residues actually are

From #734's *Recording* section: *"**§D gains row C18**, carrying the
three residues of this unit's own enumeration: the four Account-duty
cells (with what was tried and why it did not reach them, so the next
taker does not repeat it), the macro blind spot on the population sweep,
and the file header's interval-scalar claim."*

(The PR body is the only record — `gh` is not installed here and the
clone is shallow to 2026-09-04, so this was read through the GitHub API.)

### Residue 1 — the four Account-duty cells: **LIVE, verbatim**

`crates/geom-brep/tests/m5_pr7_ssi.rs` still carries the thirteen-cell
grid in the comment block headed *"The grid these rows sit in, and the
cells that still have none"*, and it still says:

> **Five cells still have no row**, and this block does not close them:
> — the cell budget and the poison arm under the **Account** duty, in
> both lanes (four cells).

with the same negative result (`floor_scale` at 0, 1e-12 and 1e-9 both
terminate `Ok`; *"a new fixture, not a new assertion"*). The duty axis is
live in the source: `crates/geom-brep/src/ssi/exhaust.rs` still has
`enum SweepDuty`, `SweepDuty::accounts`, and the `Seed`/`Account(tubes)`
pairs at both `sweep_r3` and `sweep_chart_plane` entry points. The six
rows #734 added are all present by name in `m5_pr7_ssi.rs`
(`the_floor_clamped_chart_run_refuses_typed_with_a_banked_tube_set`,
`an_unaffordable_seed_floor_refuses_the_cell_budget_typed`,
`an_unaffordable_chart_seed_floor_refuses_the_cell_budget_typed`,
`a_degenerate_r3_operand_refuses_the_enclosure_typed`,
`a_poisoning_control_net_refuses_the_enclosure_typed`,
`an_infinite_chart_speed_refuses_rather_than_receipting`), and no
Account-duty row has joined them.

### Residue 2 — the macro blind spot: **LIVE, and the sweep re-derives the same**

#734's population argument reduced to *"who calls `plane_nurbs_ssi`"* and
swept the whole tree for the literal name. Re-run today over `*.rs`,
`*.py` and `*.md`: the definition (`geom-brep/src/ssi.rs`), one re-export
(`geom-brep/src/lib.rs` — the row's `:86` is now `:132`, line rot), four
prose mentions (`ssi.rs`'s table, `intersect.rs`'s message,
`ssi/march.rs`, `topo/tests/review_ssiflat_r1_probes.rs`), two doc
mentions and the README, and **calls in exactly two files, both
`crates/geom-brep/tests/`** — `m5_pr7_ssi.rs` and
`review_m5_pr7b_ssi.rs`. Same answer as #734's sweep. **The blind spot is
unchanged**: a caller reaching the door through a macro-generated path
would not match, and nothing in the tree closes that.

### Residue 3 — the interval-scalar claim: **LIVE, verbatim**

The claim is in `m5_pr7_ssi.rs` at `definitely_positive`'s doc:

> *"The battery runs this suite at ε ∈ {1e-6, 1e-9, 1e-12} **and at the
> interval scalar**, so every probe value and every planted corruption
> has to be placed relative to the band the run resolved …"*

Against the tree: `crates/geom-brep/tests/all.rs` mounts
`#[path = "m5_pr7_ssi.rs"] mod m5_pr7_ssi;` **unconditionally** — no
`#[cfg(feature = "interval")]` — and every fixture in the file is
`f64`-typed (`NurbsSurface<f64>`, `Point3<f64>`, `cutting_plane()`).
`crates/geom-brep/Cargo.toml`'s own comment says the `interval` feature
is *"exercised by the **feature-gated test modules** in CI's interval
lane"*, and this is not one of them. So the suite is **compiled** in the
interval lane but does not run **at** the interval scalar, which is what
the sentence claims. The residue is exactly as filed.

### Recommendation

Do not close. Re-point `refs:` at the correct subject (this is a
`crates/geom-brep/tests/` row, not a doctest row), and repair `H12.md`'s
Notes line and `plan.md`'s three-sides pairing.

### New defect found while re-deriving (not fixed, not filed — reported)

The grid block's **fifth** unrowed cell — *"the chart-speed guard's ZERO
arm … a wall whose chart speed is exactly zero has no fixture"* — has
had a fixture since some point after #734:
`a_collapsed_net_refuses_on_the_zero_speed_arm_not_the_budget`, ~850
lines below the grid in the same file, drives `spread` down to 1e-315 and
asserts the refusal carries *"chart speed is zero"* by name. The grid was
not updated. The file's own census contradicts the file.

## A second defect in the same prose block (S-TINT orchestrator, 2026-09-15)

Found by lane A while re-deriving this row; recorded here rather than
filed separately because **this row's taker cannot avoid it** — residue 1
is the four Account-duty cells, and closing them means editing the very
grid block that carries this.

`crates/geom-brep/tests/m5_pr7_ssi.rs`'s header block says *"Five cells
still have no row"* and names the fifth as the chart-speed guard's ZERO
arm — *"a wall whose chart speed is exactly zero has no fixture."*
About eight hundred lines below, in the same file,
`a_collapsed_net_refuses_on_the_zero_speed_arm_not_the_budget` drives
`spread` to 1e-315 and asserts the refusal names *"chart speed is zero"*.

**The cell has a row and the census that counts it never learned.** So
the block is wrong in the direction that matters least on its own — it
under-claims coverage — but it is the same shape this program exists
for: a hand-kept enumeration inside a test file, with nothing mechanical
that goes red when the tree moves under it. Five is four.

`#734` recorded door 13 as *"still none"* and listed it separately from
this row's four, so the fifth cell is NOT one of this row's residues.
What is this row's is the block itself: whoever edits it for the four
fixes the fifth line in the same pass, or the next reader inherits a
census that was wrong twice.
