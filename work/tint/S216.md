---
id: S216
kind: unit
title: Make the repo's ~39 compile_fail rows verify the reason they claim, with machinery rather than a sweep
status: open
opened: 2026-08-21
track: W
refs: [H12, 886]
priority: P3
cost: D
---

## What

The repo has ~39 `compile_fail` rows and not one verifies what it claims — 28 of the 36 collected ones carry an error code that is never compared to anything. **The generalisation of `H12`, and the two want one lane**

## Was

unrowed

## Finding

### S216. The repo has ~39 `compile_fail` rows and not one verifies what it claims

(Long finding; carried: the statement, the two-half table, the measurement, the population, the floor, what closure needs, the sample and the hit list. Omitted by lead-in: *"Number allocated by the Track H orchestrator"* (a note on why the number was not drawn from the lane's block), the paragraph *"The other half, `S214`"* (a pointer to S214, carried on `H12`), and *"What is NOT claimed"* — that the remaining 28 rows are passing for the wrong reason; they were not audited one by one, #886's three were each confirmed to fail for their stated obligation and to go green under `Dual64`→`f64`; the finding is that nothing would notice if one started passing for the wrong reason.)

**One finding, two halves, found six hours apart by two lanes, and neither half is visible from the other.**

| where | rows | what is wrong |
|---|---|---|
| `crates/*/src/` (library targets) | **36**, of which **28 carry an error code** | collected and run, but **the error code is never compared to anything** |
| `crates/*/tests/` (integration targets) | **11** | **never collected at all** — `S214`, which is where that half is recorded |

**Read the two together or neither is sized right.** `S214`'s rows are inert evidence that reads as executable; this half's rows execute but verify less than they say. The union is that **no `compile_fail` row in this repo currently checks its own stated reason.**

**Raised by H-g on review of #886**, and found in this lane's own new rows: the lane wrote *"a mistyped path reds rather than passes"* in a PR body, and that sentence was false about every `compile_fail` row in the repo.

**The measurement.** Two rows planted in `crates/geom/src/projection_policy.rs` on toolchain 1.97.0: `compile_fail,E0277` actually emits `E0308` (type mismatch); `compile_fail,E0308` actually emits `E0425` (undefined symbol). `cargo test --doc -p geom`: **`4 passed; 0 failed`**, no warning, no diagnostic. **The code after the comma is not compared to anything.** Reproduced independently by the reviewer and by the lane.

**The population of this half.** 36 `compile_fail` rows across the library targets, of which **28 carry an error code** — 10 `E0599`, 9 `E0277`, 7 `E0308`, one `E0451`, one `E0382` — spread over 9 files. Every one of the 28 asserts *"this fails, and here is why"* and delivers only *"this fails"*. The 8 uncoded rows claim less and therefore misstate nothing, but they verify no more.

**Why that gap is not academic.** `compile_fail` is satisfied by a **typo**. A row that stops compiling because an item was renamed, a path went stale, or a feature gate moved is indistinguishable from a row that stops compiling because the guarantee it pins is still holding — and the green is the same either way. These rows are load-bearing: they are the only in-tree evidence for the sealed-trait matrix (`profile/src/path.rs`), the key-type discipline (`topo/src/entity.rs`, `body.rs`), and now the `CertifiedBounds` evictions (#886).

**THE FLOOR, and it is the part that changes what a fixer should do.** Correcting every annotation does not close this, because for an **inherent method no annotation exists that would distinguish the proof from a typo.** `c.project(p)` at an evicted scalar emits `E0599` (*"the method exists but its trait bounds were not satisfied"*), and `c.projekt(p)` — a misspelling — emits `E0599` too. Only the **free function** case can be pinned by code: `E0277` carrying *"required by a bound in …"*, which is what `topo`'s `chart_region_overlap` row emits and is why that one row is genuinely sound. So the 28 rows split again, into *"could be pinned if the annotation were checked"* and *"could not be pinned by any annotation."*

**Whoever takes this decides what the rows are FOR before deciding how to spell them.** Annotating the inherent-method rows correctly still documents the obligation and is worth doing — but **it buys no verification**, and a sweep that stops there and reports the class closed leaves a worse state than today, because the rows would then look checked.

**What a real closure needs** is machinery, not a sweep: `trybuild` (which compares stderr, not a code) or a gate that extracts each row, compiles it, and matches the emitted code against the annotation. Neither is in the tree — the repo has no `trybuild` dependency. **Not takeable as a doc edit.**

**A sample has now been compiled, and one row is already defective.** Eleven of the ~39 rows were extracted and built one at a time. Ten failed for the reason they name. **One did not**: a missing `use` in the block makes it fail with an extra `E0433` (unresolved path) alongside the obligation it claims, so it would keep passing after the guarantee it pins was deleted. The other 28 coded rows are still uncompiled and may carry the same defect.

**The hit list**: `profile/src/path.rs` (16), `quantity/src/units.rs` (8), `topo/src/entity.rs` (3), `geom/src/projection_policy.rs` (2), `topo/src/review_m0_pr7.rs` (2), and one each in `editor-core/src/eval/memo.rs`, `geom-core/src/dual.rs`, `profile/src/lib.rs`, `topo/src/body.rs` and `topo/src/chart_region.rs`.

**Verdict:** none recorded (no verdict line in the source).

## Fence

Track W — the test targets: guards, doctests, fixtures and stand-downs. Fence: `crates/*/tests/` in every crate, and `crates/test-utils/`. Block: `D380`–`D399` / `S450`–`S469`. This track owns test-side mechanisms, not test coverage for other tracks' changes — a src change carries its own tests in its own PR, as always.

## Notes

A row numbered `S<N>` colliding with the surviving finding of the same number, per the document's own convention. Note the hit list is mostly `crates/*/src/` (library targets): the machinery (a `trybuild`-style gate) is this track's; the rows it would check sit on other tracks' fences.

## Claimed by S-TCOST (2026-09-04)

Moved from `work/code-quality/` to `work/tcost/` in the tracker-wide
re-home sweep of 2026-09-04 (Ev's direction, in-chat), which claimed
**Track W whole** for S-TCOST. Id, `track: W` and body unchanged.

The letter and the program are the same ground: W's fence is
`crates/*/tests/` (all crates) and `crates/test-utils/`, and S-TCOST's
`paths` are `crates/*/tests/*` and `crates/test-utils/*` — an exact
match. `work/code-quality/plan.md` recorded W's claimant as "ground is
`tcost`'s" without the rows ever moving, and no `smell/w-*` lane has
ever run, so the rows were waiting on an owner rather than on a ruling
or a dependency.

**This claim was made on Ev's direction rather than by the S-TCOST
orchestrator**, which is not how `work/README.md` expects a claim to
happen. If S-TCOST would rather not hold a row, moving it back is a
`git mv` and this section is the record of why it arrived.

The two seams `work/code-quality/plan.md` states for W are unchanged
and travel with the rows: `crates/test-utils/src/source.rs` is the
shared home three tracks' rows land in, and the `UNCONVERTED_TODAY`
ceiling is **re-derived from the table at each landing, never lowered
by a row's own member count**; and a W row whose mechanism reaches into
a crate's `src/` is **filed on the owning track** rather than edited
there.

## Moved to S-TINT (2026-09-11)

Moved by `git mv` from `work/tcost/` at S-TINT's opening. Id, title and
body are unchanged; the directory is the claim (`work/README.md`).

**Why it moved.** S-TCOST's board was re-sorted on 2026-09-11 against the
repository going public on 2026-09-03 (`work/tcost/log.md`, the
2026-09-11 seam). That sort found this row is not a cost lever in either
currency — it neither shortens the gate's critical path nor saves a
billed minute, and it was never argued on one. It reached S-TCOST by the
tracker-wide re-home of 2026-09-04, which routed rows by PATH GLOB
(`crates/*/tests/*`, `crates/test-utils/*`) rather than by question.
This program is the question it was always about: whether the suite
asserts what it claims to assert.

## Confirmed at this seat: rustdoc does not enforce the error code (2026-09-15)

(S-TINT orchestrator.) This row's premise — a `compile_fail` row whose
`EXXXX` is compared to nothing — reproduces on demand, independently of
the measurement it was filed from. A one-item crate:

```rust
/// ```compile_fail,E0369
/// let x: u32 = "not a number";
/// ```
```

The body emits **E0308** (mismatched types), not the E0369 it claims.
`cargo test --doc` reports `compile fail ... ok` and the suite passes.
So the annotation is inert: rustdoc checks THAT the block fails and
never WHY, and every `compile_fail,EXXXX` in the tree is a guard on
compilation with a decorative reason attached.

**The consequence for `H12`'s disposition**, which is the reason this
was measured: moving `H12`'s eleven blocks into `geom-core/src` where
rustdoc runs them buys a REAL guard on the negative capability — the day
`Real` grows `PartialOrd`, the block compiles and the doctest goes red —
and buys **nothing at all** on the stated reason. The two halves are
separable and only the first comes free with the move. This row owns
the second.

## Re-derived (2026-09-15, lane A)

**VERDICT: REPRODUCES, and the population has grown 39 → 61 with the
"uncoded" half gone entirely.** The premise is untouched; every number
in the finding is stale, in the direction that makes the row bigger.

**Instrument** (both halves, one pattern, no builds):
`find crates -path '*/src/*' -name '*.rs'` / `-path '*/tests/*'`, then
`grep -n '```compile_fail'`, splitting `,EXXXX` from bare.

| | row says | tree today |
|---|---|---|
| `crates/*/src/` rows | 36 | **50** |
| …of which carry an error code | 28 | **50** |
| …carrying NO code | 8 | **0** |
| files holding them | 9 | **16** |
| `crates/*/tests/` rows (`S214`, on `H12`) | 11 | **11** |
| union | ~39 | **61** |

**The 8 uncoded rows are gone as a category.** Every one of the 50
library-target rows now carries an `EXXXX`. The row's sentence *"The 8
uncoded rows claim less and therefore misstate nothing"* no longer
describes anything in the tree: **every** `compile_fail` in `crates/*/src`
now asserts *"this fails, and here is why"* and delivers only *"this
fails"*. That makes the finding stronger, not weaker.

**The hit list, re-derived by file name.** Nine of the row's ten entries
survive at the same counts — `profile/src/path.rs` (16),
`quantity/src/units.rs` (8), `topo/src/entity.rs` (3),
`topo/src/review_m0_pr7.rs` (2), `geom/src/projection_policy.rs` (2), and
one each in `geom-core/src/dual.rs`, `profile/src/lib.rs`,
`topo/src/body.rs`, `topo/src/chart_region.rs`.

**One listed member is GONE**: `editor-core/src/eval/memo.rs` exists and
contains no `compile_fail` at all (`grep -rn 'compile_fail'` over
`crates/editor-core/src/` returns nothing). Whoever takes this should not
go looking for it.

**Seven files are NEW to the hit list**, 15 rows between them:
`geom-core/src/spline/hull.rs` (5), `geom-core/src/spline/knots.rs` (4),
`geom/src/curves/nurbs.rs` (2), `topo/src/validate.rs` (1),
`topo/src/euler.rs` (1), `geom-core/src/k_stats.rs` (1),
`geom-brep/src/edge_nurbs.rs` (1). 36 − 1 + 15 = 50, which reconciles.

**The code histogram is no longer the row's.** Row: 10 E0599, 9 E0277,
7 E0308, one E0451, one E0382 (28). Tree: 14 E0599, 11 E0277, 9 E0308,
4 E0616, 4 E0451, 2 E0597, 2 E0423, 2 E0061, 1 E0506, 1 E0382 (50).
**Four codes are new to the population** — E0616, E0597, E0423, E0061 —
and none of them was in the row's split of *"could be pinned"* against
*"could not be pinned by any annotation."* That split has to be redone
over 50 rows before a fixer decides what the rows are for.

**The load-bearing sites the row names are all still load-bearing**, and
the one row it calls genuinely sound still is: `chart_region_overlap`'s
fence in `topo/src/chart_region.rs` is `compile_fail,E0277` over
`chart_region_overlap(b, f, b, f, band)` at `Body<Dual64>`, the free
function whose `E0277` carries *"required by a bound in …"*.

**`trybuild` is still absent.** `grep -rn 'trybuild'` over every
`Cargo.toml` and over `Cargo.lock` (495 packages) returns nothing, so
*"neither is in the tree"* and *"not takeable as a doc edit"* both hold.

**I agree with the orchestrator's `## Confirmed at this seat` section**
and have nothing to subtract from it; my re-derivation is the population,
not the mechanism, and the two do not overlap.

**What my sweep could not match, stated because a floor without one is a
claim:** a fence opened from a `#[doc = "…"]` attribute rather than a
`///`/`//!` line; a `compile_fail` sited in a crate root outside
`crates/` (checked separately and empty — `demos/`, `tools/`, `review/`,
`review-probes/`, `local-scripts/`, `benches/`,
`interval-transcendentals/` carry zero between them); and, the one that
matters, **the row's own "what is NOT claimed"** — whether any of the 50
now passes for the wrong reason. Eleven were compiled once and one was
already defective; the other 39 have still never been compiled one at a
time, and settling that is a run, not a read.
