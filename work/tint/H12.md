---
id: H12
kind: unit
title: Move, convert or delete the eleven never-collected compile_fail doctests in geom-core/tests/
status: open
opened: 2026-08-21
track: W
refs: [S216, D113, 886]
priority: P3
cost: D
---

## What

Eleven `compile_fail` doctests in `geom-core/tests/` have never been collected (S214) — each asserts the compiler rejects a specific program, so each is a negative proof no tier has ever run

## Was

Track H

## Finding

### S214. Eleven `compile_fail` doctests in `geom-core/tests/` have never been collected, and each one asserts the compiler rejects something

**Raised by the Track H orchestrator, 2026-08-21**, from a fact lane H-g established while implementing `S90`'s ruling: **a doctest in a `tests/` target is never collected.** H-g verified it empirically — it planted a deliberately-failing `compile_fail` block in `crates/geom/tests/dual_foot_tangent.rs` and `cargo test --doc -p geom` reported **zero** rows from the file. That is why #886 sites its compile-fail rows in `geom/src/projection_policy.rs`, where rustdoc runs them.

**This is `D113`'s root cause with a second and worse consequence, which `D113` does not draw.** `D113` already records that *"`cargo doc` builds no test targets, so every [intra-doc link in a `tests/` file] is inert — never rendered, never resolved, never checked, on any tier."* The same fact applies to **fenced code blocks**, and there the failure is not a dead link but a **dead proof**.

**Census, over `crates/*/tests/**.rs`.** 28 opening fences inside `///` or `//!` comments across 17 files. **15 are `text` and 2 are `sh` — those are correctly annotated, never meant to execute, and are not this finding.** The remaining **11 are `compile_fail,EXXXX`**, all in Track H's own ground:

| file | count | codes |
|---|---|---|
| `geom-core/tests/review_m0_pr2.rs` | **8** | E0369 ×2, E0277 ×2, E0599 ×3, E0605 |
| `geom-core/tests/review_m0_pr3.rs` | **3** | E0369, E0599, E0277 |

**Why this is the sharp end of the cannot-go-red class — `C21`'s class, whose two hand-run-artefact members were `S110`, closed by #1329 — rather than another member of it.** A vacuous assertion passes for a bad reason; a `compile_fail` block in a `tests/` target **is not run at all**, and what it claims is that *the compiler rejects a specific program*. So each of the eleven is a negative proof about the type system that **no tier has ever evaluated** — and negative proofs rot in the one direction nothing else catches: the day a bound is loosened or a method added, the program starts compiling and the block that would have gone red is not there to. These are M0-era probes (`Real` has no `PartialOrd`, no equality, no bad cast) pinning exactly the kind of property later work erodes.

**What is owed.** Move them where rustdoc runs them — `geom-core/src`, the pattern #886 uses — or convert them to a form a tier executes, or delete them and say the claims are unguarded. **What they must not do is stay somewhere that reads as executable and is not.** Whoever takes this runs each block *first*: a block that has never been collected may not even fail correctly any more, and **an eleven-of-eleven pass would be the surprising outcome, not the reassuring one.**

**Not swept beyond `crates/*/tests/`.** Doc-comment fences under `demos/`, `tools/`, `review/`, `review-probes/` and `local-scripts/` are not censused here; the same fact applies wherever a fence sits in a non-`lib` target, and `crates/bvh`-style unowned ground is where it will be missed.

**Verdict:** none recorded (no verdict line in the source).

## Fence

Track W — the test targets: guards, doctests, fixtures and stand-downs. Fence: `crates/*/tests/` in every crate, and `crates/test-utils/`. Block: `D380`–`D399` / `S450`–`S469`. This track owns test-side mechanisms, not test coverage for other tracks' changes — a src change carries its own tests in its own PR, as always.

## Notes

`S216` is the generalisation of this row and the two want one lane. **`C18` does NOT carry residues of this row** — that sentence was a cross-wire and is corrected below under *Re-derived*: #734's `H12` is a Track C finding about the SSI sweeps' never-silence doors, a different finding that happens to share this row's id.

## Claimed by S-TCOST (2026-09-04)

Moved from `work/code-quality/` to `work/tcost/` in the tracker-wide
re-home sweep of 2026-09-04 (Ev's direction, in-chat), which claimed
**Track W whole** for S-TCOST. Id, `track: W` and body unchanged.

The letter and the program are the same ground: W's fence is
`crates/*/tests/` (all crates) and `crates/test-utils/`, and S-TCOST's
`paths` are `crates/*/tests/*` and `crates/test-utils/*` — an exact
match. `work/code-quality/plan.md` recorded W's claimant as "ground is
`tcost`'s" without the rows ever moving, and no `smell/w-*` lane has
ever run, so the rows were waiting on an owner rather than on a ruling
or a dependency.

**This claim was made on Ev's direction rather than by the S-TCOST
orchestrator**, which is not how `work/README.md` expects a claim to
happen. If S-TCOST would rather not hold a row, moving it back is a
`git mv` and this section is the record of why it arrived.

The two seams `work/code-quality/plan.md` states for W are unchanged
and travel with the rows: `crates/test-utils/src/source.rs` is the
shared home three tracks' rows land in, and the `UNCONVERTED_TODAY`
ceiling is **re-derived from the table at each landing, never lowered
by a row's own member count**; and a W row whose mechanism reaches into
a crate's `src/` is **filed on the owning track** rather than edited
there.

## Moved to S-TINT (2026-09-11)

Moved by `git mv` from `work/tcost/` at S-TINT's opening. Id, title and
body are unchanged; the directory is the claim (`work/README.md`).

**Why it moved.** S-TCOST's board was re-sorted on 2026-09-11 against the
repository going public on 2026-09-03 (`work/tcost/log.md`, the
2026-09-11 seam). That sort found this row is not a cost lever in either
currency — it neither shortens the gate's critical path nor saves a
billed minute, and it was never argued on one. It reached S-TCOST by the
tracker-wide re-home of 2026-09-04, which routed rows by PATH GLOB
(`crates/*/tests/*`, `crates/test-utils/*`) rather than by question.
This program is the question it was always about: whether the suite
asserts what it claims to assert.

## Re-derived before dispatch (S-TINT orchestrator, 2026-09-15)

**One clause of this finding does not survive contact with the files.**
The row says *"what they must not do is stay somewhere that reads as
executable and is not."* Both files disclose, in their own headers,
before the first block:

- `review_m0_pr2.rs`: *"They are kept in `compile_fail` doctest form to
  match the repo idiom (see `crates/topo/src/entity.rs`), but note
  rustdoc does not run integration-test doctests, so these are
  documentation of the review's findings, not executed gates."*
- `review_m0_pr3.rs`: *"Kept in `compile_fail` doctest form to match the
  repo idiom …; note rustdoc does not run integration-test doctests, so
  these document the review's findings rather than executing as gates."*

So they do **not** read as executable — they say the opposite, at the
top, unprompted. The row's harm statement has to be narrowed
accordingly: this is not a disclosure failure and nobody is being misled
by the file. What remains, and remains real, is that **eleven claims
about the type system have no guard**, and the disclosure is what makes
that a choice rather than an accident.

**Which sharpens the question to the one worth asking:** is the claim
worth guarding? For most of the eleven, yes — *"`Real` exposes no
ordering"*, *"no equality"*, *"no `Ord`-family sorting"* are permanent
API contracts, not one review's findings, and they break silently the
day someone adds a derive. A contract belongs on the item it constrains
(`geom_core::Real`), as a `compile_fail` doctest rustdoc actually runs —
which is exactly what `crates/topo/src/entity.rs`, the idiom both
headers cite, already does. The review-artifact framing is the thing to
drop: a permanent contract archived as a souvenir of M0 PR 2 is filed
under the wrong subject.

**Eleven blocks are not eleven claims.** `review_m0_pr2.rs` and
`review_m0_pr3.rs` both carry an `E0369` "generic `Real` code cannot
compare" block; whoever takes this dedupes before moving, and the count
that lands is a count of CLAIMS.

**One of the eleven is a different animal.** `review_m0_pr3.rs`'s
`E0277` block is an *ergonomics probe*: it documents that `BandError`
deliberately does not convert into `Indeterminate`, because the ratified
calling convention has the operation's error enum absorb `BandError` at
entry. That is a statement about `Band::linear`'s contract and the block
already points at its docs; it belongs there, not on `Real`.

**And the move does not buy what the annotation promises** — measured
separately and recorded on `S216`: rustdoc checks THAT a `compile_fail`
block fails and never WHY. A block annotated `E0369` whose body emits
`E0308` passes. So relocating buys a real guard on the negative
capability and nothing on the named reason; the reason is `S216`'s.

## Re-derived (2026-09-15, lane A)

**VERDICT: REPRODUCES** — the eleven blocks are present, in the same two
files, with the same eleven codes; only the surrounding census is stale.

**The eleven, re-derived by name, not by line.**
`grep -rn '```compile_fail' over crates/*/tests/**.rs` returns exactly
eleven hits, all in `crates/geom-core/tests/review_m0_pr2.rs` (8) and
`review_m0_pr3.rs` (3). The code histogram over those eleven is E0599 ×4,
E0369 ×3, E0277 ×3, E0605 ×1 — the same multiset the row's table gives
(pr2: E0369 ×2, E0277 ×2, E0599 ×3, E0605; pr3: E0369, E0599, E0277).
Nothing has moved, been converted or been deleted.

**The census AROUND them is ~2.6x stale, and the row should say so.**
Instrument: `grep -nE '^\s*(///|//!)\s*```' over
`find crates -path '*/tests/*' -name '*.rs'`, splitting tagged opening
fences from bare ones.

| | row says | tree today |
|---|---|---|
| opening fences in `crates/*/tests` doc comments | 28 | **72** |
| files carrying one | 17 | **52** |
| `text` | 15 | **49** |
| `sh` | 2 | **11** |
| `ignore` | (not counted) | **1** |
| `compile_fail,EXXXX` | **11** | **11** |

The 72 bare fence lines are exactly the 72 closings, so there is no
untagged (plain-Rust) doctest hiding in a `tests/` target — a fourth
shape that would also never be collected, checked for and not found.

**The two headers the orchestrator quotes are verbatim in the tree**, at
`review_m0_pr2.rs`'s and `review_m0_pr3.rs`'s module docs, above the
first fence. I agree with the orchestrator's narrowing: neither file
reads as executable. I also confirm the two sub-claims that section adds:

- **The duplicate is real.** `review_m0_pr2.rs`'s first block is
  `pub fn cmp<T: Real>(a: T, b: T) -> bool { a < b }` and
  `review_m0_pr3.rs`'s LEAK CHECK 1 is
  `fn f<T: Real>(a: T, b: T) -> bool { a < b }` — one claim, two blocks,
  both `E0369`. Eleven blocks, ten claims.
- **The `E0277` ergonomics probe is a different animal**, as described:
  its body is `Band::linear(Tol::witness())?` inside a
  `Result<Sign, Indeterminate>`, and its own prose already points at
  `Band::linear`'s docs.

**The idiom both headers cite is live.** `crates/topo/src/entity.rs`
carries three `compile_fail` fences in `src/`, where rustdoc runs them,
so the relocation target named by the row and by both file headers still
exists and still works the way they say.

**The row's stated blind spot, re-derived.** `demos/`, `tools/`,
`review/`, `review-probes/`, `local-scripts/`, `benches/` and
`interval-transcendentals/` all still exist and carry **zero**
`compile_fail` fences between them, so the unswept ground named in
*"Not swept beyond `crates/*/tests/`"* is today empty of this defect.
`crates/bvh` — the row's example of unowned ground — has one doc fence
(`tests/aggregator_headers.rs`) and it is ```` ```text ````.

**What my sweep could not match:** a fence opened with a language tag
this repo does not use elsewhere, a fence inside a `#[doc = "…"]`
attribute rather than a `///`/`//!` line, and a block that has drifted
into compiling — settling THAT needs a run, which is `S216`'s half and
is not what this row claims.
