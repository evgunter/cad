# STRUT — the plan

one sweep rule with several homes: the strut rule, the chain model and the walks beside them

Opened 2026-09-20 by CARVE's priority-seam cut
(`work/README.md`, Track size). Nothing dispatched.

## The slate

**18.5 budget points** of dispatchable work against a ceiling of 30.

| pri | item | cost | title |
|---|---|---|---|
| P1 | `battery-holds-the-chain-data-model-beside-the-predicates` | D | battery.rs holds the chain data model, the walk and a recourse audit beside the predicates its header promises |
| P1 | `blend-spells-the-half-edge-to-face-walk-five-times` | E | sweep/src spells the half-edge to face walk five times where topo now exports one door |
| P1 | `contact-edge-arm-is-picked-from-the-carrier-kind-not-the-dihedral` | D | sweep: attach_contact picks seam/transverse/smooth from the carrier KIND where the sweep verbs ask classify_dihedral first |
| P1 | `cylinder-cylinder-cylinder-arm-has-a-body-after-all` | D | The CylinderCylinderCylinder arm's 'no body yet' is falsified by the extrude door: the arm is reachable and has no row |
| P1 | `extrude-strut-rule-has-three-homes-and-fixed-context-argument-lists` | D | extrude's strut rule is spelled in three places and three functions carry the same fixed context as loose arguments |
| P1 | `interpolate-columns-averaged-knots-could-be-mirror-symmetric` | D | interpolate_columns' averaged knots could be minted mirror-symmetric when the parameters are |
| P1 | `seed-finder-home-reads-only-the-y-station` | E | the homed seed finder reads center.y, so every z-poled fixture rolls its own scan |
| P1 | `segment-curve-takes-a-section-index-only-to-fill-an-error-payload` | E | segment_curve's section index exists only to fill an error payload and is the first line a sweep caller writes |
| P3 | `perp-unit-takes-an-unchecked-unit-axis` | E | blend/arms.rs perp_unit takes a unit axis it does not check — the Meridian's stored axis, so no caller holds the witness yet |
| P4 | `blend-mod-recourse-docs-accumulate` | E | blend: mod.rs's recourse constants and BlendError carry 200+ lines of justification prose |
| P4 | `test-support-has-become-four-modules` | E | sweep's test_support is four modules in one file, with six f64/_at twin pairs |

## Order

Widest population first, because the home each row wants is the same
shape: `blend-spells-the-half-edge-to-face-walk-five-times` (five
copies, and `topo` already exports the door), then
`extrude-strut-rule-has-three-homes-and-fixed-context-argument-lists`,
then `battery-holds-the-chain-data-model-beside-the-predicates`.

`contact-edge-arm-is-picked-from-the-carrier-kind-not-the-dihedral` is
the one row here that changes an ANSWER rather than a spelling — read
it first if you would rather start with something that can be tested.
The four `E` rows are drive-bys for whoever is next in their file.

## Review posture

OPEN, for this program's first dispatch. CARVE inherits protocol v7
(`docs/MODEL-AB-LOG.md`, Ev 2026-09-19): the dual on triaged-in units
only, opus/opus outside it. Nobody has re-asked the triage question for
this slate, so the first orchestrator answers it here rather than
inheriting an answer.
