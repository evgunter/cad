---
id: paths
kind: program
title: PATHS — the profile lattice: the authoring algebra's carriers, closers, joints and its two-plane frame
status: ready
opened: 2026-09-16
area: kernel
prefix: paths/
tag: (PATHS orchestrator)
ab_band: 5000-5099
paths: [crates/profile/*, docs/PATHS-DESIGN.md]
keep_out: [crates/profile/src/path/arc_fillet.rs and crates/profile/src/fillet_select.rs and the FILLET_*_RECOURSE consts in validate.rs are BLEND's (the profile fillet door — FILLET's claim carried into BLEND; PATHS edits them by announced seam only), crates/profile is SMELL track V fence ground (seam noted, as S-BOOL's was), the Python façade over the lattice (pncad-py's path bindings and .pyi) is LIB's — a vocabulary change announces there before landing, docs/PATHS-DESIGN.md is a RATIFIED design page — a change to what a clause decides is an [ev] PR and a re-word forced by an approved change lands with it, the persist schema is contended ground — announce on the away channel before landing]
priority: P1
---

S-BOOL's successor for the PATHS lattice (`crates/profile`): the
authoring algebra's carriers, closers and joints (`docs/PATHS-DESIGN.md`
is the ratified design; the Q1 ruling chain's sixth round — "every
zero-turn joint is a declared tangent joint; the lattice never asks
whether carriers are the same" — is its live doctrine), the lift onto a
sketch plane and the frame witness that dies at the read-back boundary,
and the arc-length and sweep legs' angle domain. Opened by S-BOOL's
orchestrator at S-BOOL's exit (2026-09-16) with the eight lattice items
S-BOOL left open as its slate — a coherent track on one territory, per
`work/README.md`'s closing rule. Charter, slate and process:
`work/paths/plan.md`; narrative in `work/paths/log.md`.
