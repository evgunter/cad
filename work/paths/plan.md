# PATHS — the profile lattice (plan)

**STATUS: OPEN, awaiting its first sitting.** Opened 2026-09-16 at
S-BOOL's exit (`docs/S-BOOL-EXIT-WALK.md`) by S-BOOL's orchestrator, as
the successor program for the `crates/profile` lattice S-BOOL held
under its charter's Q1 ruling chain (BOOL-8/11/12/9/10). Live state is
`work/paths/log.md`'s tail and the item files beside this plan, never
this file.

Branch prefix (the #396 convention): **`paths/`** — unit branches
`paths/<unit>-<slug>`, orchestrator branch `paths/orchestrator`.
Away-channel tag `(PATHS orchestrator)`. A/B ordinal band
**PATHS = 5000–5099**, recorded in `docs/MODEL-AB-LOG.md`'s banding
entry in the commit that opens this program, per that entry's rule.

## Charter

The lattice as `docs/PATHS-DESIGN.md` ratifies it and as S-BOOL's five
lattice units left it: the vertex table is a cache and authoring goes
through the lattice only (BOOL-9); the straight continuation and the
declared point-target continuation are the sixth round's structural
joints (BOOL-8/11); the closing leg declares its arrival at the seam
(BOOL-12); `arc_continue` is retired (BOOL-10). What this program owns
is the residue that work surfaced and did not fund: three spellings of
the arc carrier where the module says one; the closers whose arrival
is constructed rather than declared (`arc_closer`, `Via`, `Center`);
the lift comparator's misclassification of a declared-joint
difference; `carriers_are_identical` reading carrier identity at the
lattice against the sixth round; the sketch plane's lost frame witness
and its homeless lift rule; the tangent legs' folded over-full angle.

## Ratified ground (cited, not re-litigated)

- `docs/PATHS-DESIGN.md` (ratified #124) and its §3/§4 as the Q1 chain
  re-worded them (BOOL-8, BOOL-11, BOOL-12, BOOL-10's spellings).
- The sixth-round ruling (Ev, in-chat, 2026-09-13, on BOOL-10's PR):
  every zero-turn joint is a declared tangent joint; the lattice never
  asks whether two carriers are the same.
- D2's typed refusals; D9's byte identity for the lily and the tour
  (the demos author through the public lattice since BOOL-9).

## The slate (inherited from S-BOOL, 2026-09-16)

- `arc-carrier-has-three-spellings-under-a-comment-saying-one` — one
  home for the closed form (seg.rs vs path.rs vs lift.rs). S.
- `carriers-are-identical-reads-carrier-identity-at-the-lattice` —
  `family.rs`'s `carriers_are_identical` (`path_carrier_identity`) is
  a chain-side reader the sixth round retires or re-words. S/M.
- `lift-comparator-misclasses-a-declared-joint-difference` — the lift
  census comparator's `tangent_joints` blind spot. S.
- `arc-closer-constructed-from-arrival-tangent` (issue 1578) and
  `via-and-center-arc-closers-declared-arrival` (issue 1579) — the
  closers' declared arrival, BOOL-12's pattern applied to the arc
  closers. M each; one unit if the seam vocabulary is shared.
- `sketch-plane-holds-the-affine-and-the-witness-dies-at-the-read-boundary`
  and `sketch-plane-lift-choice-has-no-home` — the frame witness
  (`OrthoFrame`, FRAME-WITNESS #2675) carried through `SketchPlane`,
  and one home for the lift rule. M together.
- `sweep-arclen-legs-fold-an-over-full-angle` — θ ≥ 2π refuses typed
  instead of folding (found at BOOL-10's review). S.

## Process

v6 duals as S-BOOL ran them (`docs/MODEL-AB-LOG.md` normative): one
implementer plus the cross-model dual review and the union fix pass;
implementer blocks `PATHS-B<n>` drawn branch-side; ordinals claimed on
main at review dispatch from 5000–5099; record-at-merge with per-phase
tokens and wall-clock; blinding discipline verbatim; hosted CI the only
gate; the implementer and reviewer prompts by path.

## Exit shape (proposed)

The lattice has one spelling of each carrier form, every closer
declares its arrival, the comparator and `carriers_are_identical`
agree with the sixth round, the sketch plane carries its witness to
the read-back boundary with one lift rule, and the tangent legs refuse
an over-full angle typed. Every unit merged on its own green hosted
head; the walk convention applies at exit.
