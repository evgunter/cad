---
id: tier
kind: program
title: TIER — the symbolic tier's frontier, its cost and its prose: what it bounds, what it costs, and what its header claims
status: open
opened: 2026-09-20
area: kernel
prefix: tier/
tag: (TIER orchestrator)
ab_band: 8700-8799
paths: []
keep_out: [opened by SYM's 2026-09-20 priority-seam cut (Ev, in chat) per work/README.md Track size - SYM measured 78.5 budget points and was cut into tracks meant to run in PARALLEL (Ev, in chat: for these high priority tracks it is ideal to have several components that can be worked on in parallel), the siblings are SYM DECIDE TIER and they share their parent's territory by design - shared ground is legitimate by the README's 2026-09-20 rule and what is owed is awareness while a lane is LIVE, so run scripts/work.py territory on your branch and announce the seam in the PR rather than drawing a fence, FRAME owns the who-answers-give-me-a-frame family and the decision-door row is the fold it is owed, CLEAR holds the row where this tier and the clearance engine do not compose, PROPS owns the analysis lane]
priority: P2
---
The tier seen from outside: **the ceiling that bounds each document
today, what the tier costs to run, and what its own header says about
it.**

The frontier rows are P2 — `docs/DESIGN.md`'s reach goal, error
propagation, is what they serve: certification over a parameter box for
IMPLICIT quantities, dependency widening in the numeric channel of
margins, and the plate ceiling now being the scaffold pushforward.

Beside them the measured cost — **the E12 tier is 95% of the M10-3
interval drive, 20.8x measured** — and the structural rows that make it
hard to work on: `geom-core`'s `sym.rs` is 3898 lines behind a 347-line
header, and `SymRegistration` flattens witness and registry into six
arms.

Four header rows close the set, and they are the cheapest work on this
slate: three sentences the code has outgrown, a DAG paragraph that
disagrees with the fold it describes, a `# Freezing` section claiming
every freeze is counted when only the plain walk counts, and item docs
carrying unit archaeology. All four are drive-bys for whoever opens the
file (`work/README.md`, "The tracker is not comprehensive").

Charter and order: `work/tier/plan.md`; narrative in `work/tier/log.md`.
