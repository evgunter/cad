---
id: guard
kind: program
title: GUARD — the gates and the guards they lack
status: open
opened: 2026-09-11
area: infra
prefix: guard/
tag: (GUARD orchestrator)
ab_band: 3600-3699
paths: [scripts/gates/*]
keep_out: [crates/geom-core/tests/bounds_census.rs and crates/test-utils/src/source.rs are S-TINT's and S-TCOST's ground - the bound-list and reader-guard rows edit the shell side here and announce the Rust side on the seam, tools/* and docs/K-REPORT.md and docs/TESS-BUDGET.md are INSTR's - S115's instrument members route there and are not landed from here, .github/workflows/* and local-scripts/* and scripts/check-*.py are CIW's - the parity-fence row WRITES A CLAUSE about that ground and edits no file in it, the certified doors the bounds gate would have to see (crates/geom-brep/src/props/*, crates/profile/src, crates/mesh/src, crates/bvh/src) belong to PROPS and others - S210's reach into them is a row filed on the owner, never a diff from here, crates/profile/src/path/arc_fillet.rs and the ~49 profile sites G4 collapses are S-BOOL's crates/profile/* glob - G4 edits them by announced seam as its own body already says]
---

The half of code-quality Track K that came back when GATES closed
(`docs/DOC-LEDGER.md` sweep 7), plus every finding since about what the
gates cannot see. `scripts/gates/*` whole, and nothing else: the readers
the gates share, the selftests that cannot observe what a gate names, the
bounds allowlist and the two censuses that have no guard of their own.
Class M at its core with four H rows and one E; no ruling is open on it.
Charter and unit order: `work/guard/plan.md`; narrative in
`work/guard/log.md`.
