---
id: D292
kind: issue
title: edge_axial_span's conic arm subdivides along an axis where the directional exact extremum exists in closed form
status: open
opened: 2026-09-04
track: Q
refs: [1879]
---

## What

`boolean::boxes::edge_axial_span`'s conic arm projects an arc onto an arbitrary axis by `ARC_SAMPLES` samples plus the 1-D sagitta charge, where the exact extremum exists in closed form: the per-coordinate body of `geom::curves::boxes` — `c ± √((a·û·n)² + (b·v̂·n)²)`, admitted when `atan2` of the pair lies in the span — generalized from a coordinate axis to a unit direction `n`. The boolean lane's `axial_window` has the ordering to read it; the census mirror (`face_reach`) does not, so the same two-arithmetics rule as the edge box applies (`EdgeBoxRule`'s conic bullet). `geom` would grow the directional door first — Track N's ground; file that half there when this is taken.

## Was

Filed by CERT-N3 at S235's landing, from the S16-class sweep of `topo/src/boolean` (a hand-derived bound standing beside a shipped exact one).

## Fence

Track Q — `topo`'s boolean, census and charts, and the predicate ledger. **Fence:** `crates/topo/src/{boolean/,splitting/,census.rs,chord_join.rs,chart_region.rs,face_normal.rs}`, `crates/geom-brep/src/{ssi*,pcurve_cache.rs,nurbs_iso.rs,edge_nurbs.rs}`, `docs/predicate-dimension-audit.md`. **Block:** `D280`–`D299` / `S350`–`S369`.

## Re-homed to PRED (2026-09-11, the cut in `docs/WORK-TRACKS-2026-09.md` addendum 3)

PRED collects the rows where one numeric fact is decided in several
places, each with its own margin. This row is one of them.

Its class at the cut was **H** — closed-form directional extremum:
numeric work, new geom door, census mirror, two tracks. The class is a
dispatch estimate made by reading the row against the tree on
2026-09-11, not a verdict on the finding, and a lane that finds it wrong
says so in its PR. The id, the `track:` letter where the row carries
one, and the body above are unchanged by the move.
