---
id: frame
kind: program
title: FRAME — who answers "give me a frame", and by what rule
status: open
opened: 2026-09-20
area: kernel
prefix: frame/
tag: (FRAME orchestrator)
ab_band: 5900-5999
paths: [crates/geom-core/src/linalg/frame.rs]
keep_out: [CUT FROM PROPS 2026-09-20 with Ev's agreement in chat — the family's head is PROPS' sign-hull unit (PR #2468, Ev's option-1 ruling on #1944), which PROPS lands and this program inherits the rest of the family from, exactly as PROPS inherited from S-CERT at its exit walk; until that lands, crates/geom-core/src/linalg/vec.rs carries an unmerged rewrite of orthonormal_basis and NOTHING here touches that region; crates/geom-core/src/* and crates/geom/src/* stay PROPS' paths and this program reaches them by announced seam, which is the honest record of a family whose code is spread across two crates' door sets; SHARED GROUND IS EXPECTED and is not a conflict (Ev, in chat, 2026-09-20) — what is owed is awareness when a lane is live on the same file]
priority: P1
---

One question asked eight ways: who answers "give me a frame", and by
what rule. The kernel has at least four doors that turn a direction
into a basis — `Vec3::orthonormal_basis`, `path_start_frame`,
`frame_from_unit_aim`, the exact-frame mints — and no rule saying
which a caller should reach for, what each guarantees, or what any of
them does when the direction is not decided.

Cut from PROPS on 2026-09-20. The family's head is PROPS' sign-hull
unit: Ev ruled (option 1, on #1944) that the frame constructor crosses
the normal with a decided world axis and transfers no sign, and that
unit is built and holding on a fold SYM owes it (see
`work/sym/the-decision-door-is-opaque-to-the-tier.md`). PROPS lands it;
this program takes the rest of the family from that point. **Read the
sign-hull unit's PR and that SYM row before specifying anything**: they
establish that a frame chosen by a DECISION is opaque to the symbolic
tier, which is a constraint on every door this program might mint.
