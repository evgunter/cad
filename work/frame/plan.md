# FRAME — plan

Live state is `work/frame/log.md`'s tail and the item files beside
this plan, never this file. Branch prefix `frame/`; away-channel tag
`(FRAME orchestrator)`; A/B band **5900-5999**, claimed in
`docs/MODEL-AB-LOG.md`'s banding entry at the cut.

## Lanes

- **The doors, and the missing rule** — the program's spine, and the
  one place it should produce a ruling rather than a unit first:
  `two-doors-answer-an-axis-off-a-tangent-with-no-rule-for-which`,
  `path-start-frame-is-the-only-frame-from-a-tangent-and-is-named-for-the-start`,
  `frame-from-unit-aim-takes-perp-and-its-norm-as-an-unverifiable-pair`
  (a witness the callee cannot check), and
  `exact-frame-mints-cover-three-of-the-world-frames`. Four rows, one
  question; an `[ev]` ruling on the rule probably precedes the units.
- **What the constructors do to an undecided direction**:
  `orthonormal-basis-poisons-vertical-planes` (the residue the
  sign-hull unit's ruling does not itself close),
  `normalize-overflow-yields-zero-axis` (a norm-squared overflow
  silently becoming the ZERO vector), and
  `rotation-about-re-divides-a-decided-axis` (a caller holding the
  unit witness pays for the normalize again).
- **A frame built where the normal is not settled**:
  `a-widened-derived-placement-normalises-a-straddling-newell-sum`.

Sibling on PROPS' ground, to consult rather than claim:
`torus-meridian-orient-builds-a-frame-on-an-undecided-normalize` is
this program's shape in `props/curved.rs`'s torus arm, which PROPS is
working; read it, and announce a seam if a rule minted here should
reach it.

## Review posture

Not yet set. The parent program ran full v6 duals on kernel units
with E riders single-reviewed; whether that is right here is the
orchestrator's call at its first dispatch, and Ev took S-TCOST off
the protocol entirely on 2026-09-12, so the question is live.

## Exit shape

Every item above landed or ruled; the walk convention applies.
