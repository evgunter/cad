---
id: m6-carried-items-register
kind: issue
title: M6 carried-items register — every carried/re-banked item from the ratified exit walk, with owners and sequencing
status: open
opened: 2026-08-08
github: 250
refs: [161, 178, 214, 222, 226, 239, 243]
---

## From GitHub issue 250

opened 2026-08-08, 3 comments.

Consolidated register per Ev's ask at M6 closure (PR #243 comment 5224869607). One row per carried item from docs/M6-EXIT-WALK.md (the ratified done-state); each names its owner and its flip/re-open condition. The M8-bound family folds into the M8 opening plan when C7 opens it — this register is the checklist that folding consumes.

## The C7/M8 family (the concentration — implement with contacts+clearance)
- **Curved REST contact implementation** (design ratified, CONTACT-DESIGN #178): C7 join lane = M8's ruled opener. Flip: the join lane merges.
- **Lily walls 1, 2, 7** (G1 tube unions; flower∪stem; tangent-subtract tepal carving): declared conformal/tangent contact classes. Flip: C7's conformal class covers each. The **lily rebuild** is the demo moment (ruled: rides M8).
- **Lily wall 14** (near-tangency distance query): M8 signed clearance (min_clearance is C7's co-designed twin).
- **Internal-tangency fixture row** (#161 §2c, never-reached lull item): rides the C7 implementation, whose subject it is.

## Sense-gate residuals (#226 — already pinned in-tree with flip conditions; listed for one-place completeness)
- Conic-trimmed cylinder walls slip both gates → flip: ellipse-rim material-side encoding.
- Rimless-band half-flip invisible → flip: shell-level orientation check / second encoding channel (sized with the next props unit).
- NURBS faces bit-free → flip: NURBS material-side encoding (stage-1 recognition era).
- Arc-bounded planar caps check-6-exempt → flip: check 6's arc-bounded planar arm.

## Hygiene / opportunistic
- **Canary-gated latency refresh** (never-reached lull item): banked hygiene, opportunistic at the next lull; canary must be healthy (the 20-45× CPU pin history).
- **k-lint gate flip with interpretation-discipline message** (Ev's design, #243 comment 5224869607): S-sized CI-infra unit, dispatching next — will close its own row here.
- **ε=1e-6 blind window** ([4e-5, 1e-3), measured + probe-pinned at #239): ε-policy question — whether 1e-6 remains a supported row for sub-millimetre corpora; revisit if a production corpus at that scale appears.

## Re-banked M5 frontiers (grounds + triggers in DESIGN.md; unchanged by M6 closure, listed so this register is total)
- Canal-surface blend: consumer-gated (re-opens with Band-3 fillet breadth).
- Cyl×sphere germ chords: the SSI lift removed the storage half; the join-lane analog remains (chase when a consumer lands).
- NURBS extent test: the certified-extent argument has never been derived (C12.1, per-arm).
- Joined-path sweep lane + the ≥0.5-turn frontier (#222); rational flux/area lane (arc-bearing profile walls).
- Fillet family named refusals incl. the tangent-seam case (lily wall 6; verified still refusing at walk assembly).

## Triage (recorded, no milestone obligation)
- Lily walls 4 (spheroid surface kind — D3 closed-enum extension), 5 (mirror instancing — Band-3 patterns/mirror), 9 (variable-radius/taper), 13 (per-feature tessellation δ — display-mesh lane era).
- F10/F12-class attribution + arm-policy questions: tracked at #214 (dimensional-debt inventory), not duplicated here.

Closing discipline: a row closes by naming the merging PR; this issue closes when every non-triage row is closed or has moved into a milestone plan.

## Comments

**2026-08-08** — orchestrator:

Register row closed: **k-lint gate flip — DONE, merged as #253** (2026-08-08). The row is now "k-lint (gate)": findings fail the run at exit 2 with the interpretation-discipline message (re-derive per the K-REPORT runbook or demote with recorded justification; geometry changes to silence the lint are the forbidden move); harness breakage keeps its distinct exit-1 voice; the committed baseline gates green at all three ε rows.

**2026-08-08** — orchestrator:

Sidecar-row amendment (from #256's tail): the declared-analytic refusal lane BANS keep-as-spline (Ev) — exact analytic geometry gets exactly one native representation, restoring universal byte-identical round trip when the sidecar lands. Recorded consequence to design deliberately: parametric families crossing exact analyticity refuse in a band around the crossing (no sweep-stable spline repair exists); recourse = structural restructure or box-avoidance (M8-certifiable).

**2026-08-08** — orchestrator:

New banked row (from #260, Ev's ask): **gate-minted validity currency** — generalize the tier-3′ wrapper precedent so un-validated bodies are unrepresentable at API boundaries (only the shared at-rest gate mints the currency type; 'forgot to validate' becomes a compile error). Kernel-wide signature design; record = #260. The near-term rung (import routes through the shared gate) ships as M7's tier-at-import unit.

## Home

`work/issues/`: a cross-milestone register whose rows are owned by S-MATE, VERBS, S-CERT and the code-quality tracks at once, so no single open program's territory or charter takes it.
