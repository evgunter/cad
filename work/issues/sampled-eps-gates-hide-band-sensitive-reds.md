---
id: sampled-eps-gates-hide-band-sensitive-reds
kind: issue
title: Sampled-eps gates let eps-band-sensitive rows merge latently red at unsampled matrix points - the next branch inherits the red
status: open
opened: 2026-08-31
github: 1356
refs: [1277, 1303, 1314]
---

## From GitHub issue 1356

opened 2026-08-31, 1 comment.

(S-CERT orchestrator) Class finding with a live instance, filed so it can warn other programs.

**The mechanism.** The hosted gate samples one {lane} × {ε} point per SHA (`lane=both` widens the compile modes but the ε row stays drawn). A unit whose new or affected rows have ε-band-dependent premises can merge green while being red at an unsampled ε row — and the red then belongs to *main*, surfacing on whatever unrelated branch first draws that point.

**The instance.** CERT-3 (PR 1277) merged with gates at ε=1e-6, CERT-4 (PR 1303) with gates at ε=default. Their enclosure improvements changed behavior at the tight band: on the first branch to draw (interval, ε=1e-12) — CERT-5's PR 1314, whose diff touches neither file — two tests fail: `profile::generic_replay::the_anchor_coincident_corner_reduces_to_input_width_at_interval` (CERT-4's own row) and `sweep::m5_s13_pips_interval::certified::interval_finding_union_is_bracketed` (an M5-era pin expecting an escalation that the narrowed mapped-source enclosures now serve — a refusal retired by improvement, with the pin never re-derived). The repair is being ported into PR 1314 as band-honest re-pins, per the CERT-1 per-band precedent.

**What the class wants** (whoever owns CI conventions decides; S-CERT is a reporter with one repair in flight):
- A unit whose rows' premises are band-dependent should pin the ε row by trailer at its gate (the trailer exists — M10-3 merged "interval, 1e-12, trailer-pinned"), or run the local three-ε sweep on those rows before merge (`memories/local-battery-scope.md`'s "beats the gate to a failure" case is exactly this — the gate structurally cannot cover the other rows on one SHA).
- A review brief's ε-honesty item should distinguish "consults no tolerance" from "premise varies by band" — the second needs per-band pins, not a no-op statement.

## Comments

**2026-08-31** — orchestrator:

(S-CERT orchestrator) Instance count correction: the latent red at (interval, ε=1e-12) was **four tests, not two**, all verified failing identically on `origin/main` at f660408a before the repairs were ported into PR 1314: the two originally named, plus `review_arceval_r1_probes` and `m5_s12_curved_ops_interval` (sharing `RECUT_MAPPED_ENCLOSURE_HI`, whose own doc says re-measure-and-re-state — done, 1.1414768974413613e-12 → 1.1362773333939659e-12). All four repairs are per-band-honest re-pins or doc-sanctioned re-statements, none a re-baseline to preserve an old outcome; details in that PR's ported-repairs commit message.

---
_Generated by [Claude Code](https://claude.ai/code)_

## Home

A CI-convention class finding the issue itself hands to "whoever owns CI conventions"; S-QA (gates that lie) is closed and no open program's territory covers `.github/workflows/*`, so it lands in `work/issues/`.
