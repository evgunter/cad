---
id: LIB-B-DISTRIBUTIONS
kind: unit
title: binding census family B-DISTRIBUTIONS
status: open
opened: 2026-09-06
---

Queued mechanical census family (the B-RESOLVE shape): sweep the
family's bindings against the census contract, construct the
previously unconstructible pins where the surface now allows, and
re-cut the census rows honestly. Families share the census/tags/test
files, so at most two run concurrently, staggered.

## Derived scope

`crates/pncad-py/tests/test_binding_census.py` charters
`B-DISTRIBUTIONS` in `FAMILIES` (ERROR-DESIGN E1/E2): bind
`Distribution`'s four forms (`crates/editor-core/src/distribution.rs`)
onto a `DocParam` constructor and the analysis doors that read them
back (the analyzed box, tail mass and leaf mass), with
`DistributionFault` and the band's typed measure refusal as arms a
Python caller can dispatch on. The census names the sharp edge this
family closes: `set_doc_param` is create-or-replace, so a `DocParam`
rebuilt from a dimension and a number DELETES an annotation
silently, and `set_doc_param_value` is the value-only door a Python
caller must use until then. One `NOT_BOUND` entry cites the family
(`Distribution`); the sweep decides what else it wants bound or
listed.

## Home

LIB's (the Python surface is outside M10's fence, the same routing
DOCM used for B-FACE-FRAME and B-PART). Filed 2026-09-06 at the
program's reactivation; `LIB-B-FACE-FRAME` and `LIB-B-PART` named it
as "unscheduled alongside".
