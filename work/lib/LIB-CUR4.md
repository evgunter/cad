---
id: LIB-CUR4
kind: unit
title: curation 4 — the three payload twins CUR3 banked
status: closed
branch: lib/curation-4
opened: 2026-09-03
closed: 2026-09-03
refs: [LIB-TUBE]
pr: 1633
---

Mechanical under the 08-29 ruling (no A/B row; brief-as-spec, the PERR
precedent). CUR3 (#1262) banked three uncurated-payload twins of the
DanglingRef carriage: `BandField` ← `BandError::InvalidValue` (the semantic
twin — a two-arm pure discriminant of a prelude-carried refusal); the
`FilletError` submodule trio `FilletSite`/`CornerConfig`/`RunOutPolicy` (the
structural twin of the ReadbackError lift); the `ValidationError` key-bearing
trio `CensusContact`/`RingContact`/`StaleDeclaration`. Each gets the CUR3
treatment where measurement supports it: carry beside its carrier, pin
matchability, census rows on the RootFault precedent — or an argued
non-carriage where the corpus rule says no consumer needs it. Also closes
CUR3's named blind spot one rung: a struct-payload sweep (the enum-indexed
scan cannot see struct payloads) over the carried refusal set, findings
tabled.
