---
id: LIB-CUR5
kind: unit
title: curation 5 — the five payload rows under the carrier rule
status: closed
branch: lib/cur5
opened: 2026-09-08
refs: [payload-rung-re-sweep-finds-five-more-uncurated-discriminants, mesh-index-numbers-cross-as-prose-under-a-projecting-door, mesh-pick-error-is-unmatchable-under-node-pick-error, resolution-failure-arms-are-unmatchable-under-resolution, escalation-payload-is-uncarried-under-thirteen-refusals, loop-key-is-uncurated-and-invisible-to-payload-scans, next-payload-rung-under-the-cur3-cur4-carriages]
pr: 2169
closed: 2026-09-08
---

Mechanical under the 08-29 ruling (the CUR/CUR2/CUR3/CUR4 precedent).
Five banked payload rows, settled by one rule (LB17) — **a payload's
category follows what its CARRIER does at the crossing**, the rule the
CUR3/CUR4 pair wrote at the census's `BlendError` entry.

**Delivered.** `MeshPickError` beside `NodePickError` in
`crate::select` (`MeshPick` stays interior, so CUR3's construction
argument is untouched), with `mesh_pick_error_tag` and the payload's
discriminant at `NodePickError.index_variant`. `ResolveError`,
`ResolutionFailure` and `ResolveIndeterminate` beside `Resolution`,
with `resolve_error_tag`, `resolve_indeterminate_tag` and
`Resolution.variant` — `Diagnosis`, `Tombstone`, `TieWitness`,
`RecipeEditRef` and `Resolved` stay interior. `Indeterminate` on the
prelude. `LoopKey` beside its three sibling keys. `EntityId`, `GeomRef`
and `ContactFinding` — the rung CUR3 and CUR4 both stopped at.

**Measured, not assumed.** `MarginDiag` is INTERIOR: no curated
refusal's Python projection exposes the escalation's discriminant and
this unit adds none, so the `BandField` reading applies one payload
over, with its falsifier written beside the carriage. The LB13 stop
clause did not fire: the guard names `editor-core`'s
`EntityRef`/`EntityKey`/`Entry` — the document layer's (body index,
key) pair and its name-table entry — and `EntityId`/`GeomRef` are
`topo::entity`'s own type-erased sums, in the module the prelude
already carries four keys out of.

**Not covered.** The three numbers `MeshPickError` carries still cross
as prose (`mesh-index-numbers-cross-as-prose-under-a-projecting-door`).
The class re-sweep at this merge base found the banked set empty and
five new uncurated discriminants
(`payload-rung-re-sweep-finds-five-more-uncurated-discriminants`).
The remaining key kinds under `EntityId`/`GeomRef` are an argued stop
rather than a banked one: a key has no discriminant to owe, and the
sums' discriminants are matchable now.
