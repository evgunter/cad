---
id: LIB-BENCH-PATTERN
kind: unit
title: the flat-pack posts as a pattern, measured through the mates and the gate
status: closed
branch: lib/bench-pattern
opened: 2026-09-08
refs: [bench-flat-pack-star-is-now-a-pattern-job]
pr: 2199
closed: 2026-09-08
---

Author the flat-pack layout's posts as `Node.pattern` beside the
`placed_union` spelling in `crates/pncad-py/tests/bench_scene.py`, and
MEASURE whether the plural value answers the same product, names,
placements and A5 gate — then ship the spelling the measurement
chooses and correct the audit to what was measured.

The layout declares nothing, so the cluster, the solve and the minted
declarations are `TestBenchStand`'s expectations; the layout's are the
material, the placement outline, the per-placement cap frames, the
instance-qualified names and a gate that passes OUTRIGHT. Whether a
mate head can stand on a patterned copy is measured separately,
outside the scene.
