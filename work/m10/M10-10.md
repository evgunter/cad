---
id: M10-10
kind: unit
title: the form-level mechanism: the arc's trig atoms made exact (rule D) with rules A/B per node made affordable, so the plate's four identity residuals go at once and its real study certifies
status: dispatched
opened: 2026-09-07
refs: [M10-9, plate-ceiling-is-now-the-scaffold-pushforward, first-refusal-at-twice-the-ceiling-is-an-order-artefact]
branch: m10/m10-10-form-level
---

Opened from M10-9's measured result (PR #2048): read as the over-band
set at ceiling + δ, every measured document is bounded by
`carrier_matches_mapped_source`, and the plate is FOUR identity
residuals from a macroscopic real margin — the first three worth 2×
together, the fourth worth 1.68e5×, landing the plate at 0.263 of its
real study against `assert_bound`. A per-identity door cannot reach
that cliff; the family has to go at once, and the mechanism is
form-level. Ev, in chat: "1 sounds good!" — the form-level unit
before the exit walk is re-cut — with the standing ruling that
sequencing decisions with a recommendation do not wait on him.
Block M10-B5 slot 1.

**The mechanism.** The two spellings of an arc — the sketch
pushforward anchored on `a` through `sin(s·θ)` and `−2·sin²(s·θ/2)`
with `θ = 4·atan(bulge)`, and the carrier through the sagitta closed
forms and its own frame — meet only where the trig collapses. Rule D
makes the trig atoms of `q · atan(X)` exact: `sin(atan X) =
X/sqrt(1 + X²)`, `cos(atan X) = 1/sqrt(1 + X²)`, multiples by the
Chebyshev recurrences, halves by the positive branch (a theorem of
`atan`'s range, no value read); rules A and B close the ring. It runs
per node in the early walk alongside the plain form, memoized and
bounded so the walk M10-8 measured at 138 s per replay is affordable;
the ring width the four residuals need is measured, not assumed.

**Acceptance** is the program's exit condition: the two-hole plate's
real study returns certified leaves bounded by genuine flips, not by
ε — the whole-certifying box at 0.263 of the study at three ε rows,
the first refusal beyond it a real margin — with the five documents
re-measured and the 2×-ceiling instrument retired. The binding spec
is `docs/M10-10-SPEC.md`.

