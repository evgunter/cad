---
id: M10-9
kind: unit
title: the registered-identity door: discharge by provenance (E12's reserve), so the two-hole plate's real study certifies
status: dispatched
opened: 2026-09-06
refs: [M10-8, plate-rim-residual-needs-the-wide-coefficient-ring, declared-tangency-needs-the-registered-identity-door]
branch: m10/m10-9-registered-identity
pr: 2048
---

Opened from M10-8's measured miss (PR #1828): with the constant fold,
rule C and the early walk all on, the two-hole plate's real study is
still bounded at `7.81e2 · ε` by `carrier_endpoint_start` — the arc
rim's `‖q − c‖ = r`, whose expansion needs ~640 coefficient bits the
shipped ring (256) cannot afford at a cost the bracket could bear
(229 s per leaf at 4096 bits). Ev, in chat: "proceeding with 1, as
you recommended" — option 1 being this unit on block M10-B4 slot 3
rather than re-cutting the exit walk with the plate as the frontier.

**The mechanism** is E12's reserve, taken: a session door through
which a CONSTRUCTOR registers an identity it guarantees — the arc's
`‖q − c‖ = r` is a theorem of the sagitta closed forms (equal
squares, both sides non-negative by construction), and a fillet's
`|h| = r` is a theorem of `fillet_arc_carrier` — verified at the
witness, refused typed when it lies, consulted by the normal form so
the residual is the zero form, and COUNTED apart from the tier's two
theorem kinds (`SymCounts::registered`, its own K token). The numeric
channel's bits are untouched (the `(q − c)/r` spelling is rejected
for exactly that). The ring-width alternative is measured in one
table and expected not to ship.

**Acceptance** (E12's own): the plate's real study certifies at
three ε rows with refusals only among flips, slivers and the tail,
its ceiling no longer scaling with ε; the pad's `carrier_line_circle`
discharged or its obstacle filed with both rendered forms; the
M10-8 pins re-cut as positive; the tour's stop 1 the certified study.
The binding spec is `docs/M10-9-SPEC.md`.

