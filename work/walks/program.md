---
id: walks
kind: program
title: WALKS — one topological relation, implemented n times across topo/src
status: ready
opened: 2026-09-20
area: kernel
prefix: walks/
tag: (WALKS orchestrator)
ab_band: 6600-6699
keep_out: [opened by TOPO's 2026-09-20 priority-seam cut (Ev, in chat) per work/README.md Track size - TOPO keeps the Euler operators proper and its band 2700-2799, the sibling cuts are ORIGIN ATREST TQUERY WALKS PROBE, a row here that has to edit another of the six announces the seam in the PR that lands it rather than drawing a second fence]
priority: P1
---
The entrenching class on topology's own ground: **one relation, spelled
n times, each copy re-deciding what it means.** The face-loop walk
`once(face.outer).chain(face.rings)` written out at 33 sites, each
re-deciding what a non-`Cycle` boundary is; three answers to "is this
loop inside that one" with three boundary postures; the per-shell glue
relation implemented three times, where the test-support copy is the
one that drifts; the Euler characteristic carried by three types.
Every one of these gets harder to collapse with each new caller, which
is what puts the track at P1.

**This program claims no paths.** Every row crosses files five other
programs own, so each unit announces its fence in the PR that lands it
— the DOOR convention (`work/door/program.md`), for the same reason.

Charter and order: `work/walks/plan.md`; narrative in `work/walks/log.md`.
