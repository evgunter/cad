---
id: clippy-panic-gate-blind-in-macros
kind: issue
title: clippy's panic-family gate is blind inside every macro_rules! body
status: review
opened: 2026-08-13
github: 454
refs: [447]
track: K
branch: gates/panic-free-macro-bodies
---

## From GitHub issue 454

Opened 2026-08-13; 0 comments.

The workspace's panic-family lint gate — the enforcement mechanism for D9's "the kernel never panics on any input" — does not apply to code written inside a `macro_rules!` body. **clippy does not lint inside `macro_rules!` expansions.**

`[workspace.lints.clippy]` sets `unwrap_used`, `expect_used`, `panic`, `unreachable`, `todo`, `unimplemented` to `warn`, and CI runs `-D warnings`, so these are hard errors — everywhere except inside a macro body, where they are silently invisible.

## Repro

Verified independently on this toolchain (clippy 1.97.0):

```rust
// [lints.clippy] expect_used = "warn"
macro_rules! make { ($n:ident) => { pub fn $n(v: Option<u32>) -> u32 { v.expect("in macro") } }; }
make!(from_macro);
pub fn written_directly(v: Option<u32>) -> u32 { v.expect("direct") }
```

`cargo clippy` warns **once**, on `written_directly`. The macro-generated `expect` produces no diagnostic.

## How it surfaced

Found during review of #447. The curve API is generated by `nurbs_curve!`, and an intermediate revision of that branch carried three

```rust
.expect("locate_spans returns a nonempty first span")
```

calls inside the macro body. CI was fully green with them present. They have since been removed by that PR, so there is no known live instance today — but nothing caught them, and nothing would catch the next one.

## Scope

`crates/*/src/` currently contains 10 `macro_rules!` bodies across 9 files:

```
crates/editor-core/src/persist/strict.rs
crates/editor-core/src/program.rs
crates/geom-curves/src/nurbs.rs
crates/geom-curves/src/projection.rs
crates/geom-curves/src/fit.rs
crates/pncad-py/src/py/quantity.rs
crates/pncad-py/src/py/path.rs
crates/quantity/src/lib.rs
crates/topo/src/review_m1_pr5_internal.rs
```

Two of those files contain panic-family calls at all: `topo/src/review_m1_pr5_internal.rs` (16) and `geom-curves/src/fit.rs` (1). **These are file-level counts, not confirmed to be inside macro bodies** — #447's review reports the remaining in-macro instances are in `#[test]` functions, where the lints are sanctioned anyway. Separating the two requires a proper audit; that audit is part of this issue, not a premise of it.

## Why it matters

D9 makes "no panics on any input" a ratified property, and this lint is how it is enforced rather than asserted. A gate that is trusted and has a hole is worse than a gate known to be partial: every reviewer who sees green clippy on a macro-generated API reads it as "no panic constructs here," and that reading is currently unfounded.

The exposure is not hypothetical — the curve evaluation API, one of the kernel's hottest and most-used surfaces, is entirely macro-generated.

## Possible directions

Not a recommendation; this needs a decision.

1. **One-time audit.** Hand-check the 10 macro bodies, record the result. Fixes today, not tomorrow.
2. **Token-grep CI check.** Scan `macro_rules!` bodies in `crates/*/src/` for panic-family tokens. Crude, no type information, will need an allow mechanism for `#[test]` bodies — but it catches the whole class and is cheap.
3. **Lint the expansion.** Run the panic-family lints over `cargo expand` output in CI. Most faithful, heaviest, and the mapping from a diagnostic back to source is poor.
4. **Keep macro bodies thin.** Have each macro delegate to a normal generic `fn` that clippy *does* see, so the macro only forwards. Structural rather than checked — in keeping with how #447 resolved its own indexing question — but it is a convention, and conventions decay unless checked (cf. `check-test-aggregation.sh`'s own rationale).

(2) and (4) compose: the grep becomes cheap to keep green once bodies are thin.

---

Filed from the misc lane. No action taken beyond verifying the repro and counting the surface.

## Home

`work/issues/`: the fix is a workspace-lint / CI-gate change — S-QA's ground, and S-QA is closed.

## Re-homed (2026-09-04)

Moved from `work/issues/` to `work/code-quality/` in the tracker-wide
re-home sweep of 2026-09-04 (Ev's direction, in-chat), and given
`track: K` — `docs/WORK-TRACKS-2026-09.md` already surveyed it as "Track
K's ground" and did not cut it into a track; this move is that survey
line executed. Id, body and header are otherwise
unchanged; any `## Home` section above naming `work/issues/` is
superseded by this line and is kept as the record of why the file was
parked there.

## Claimed by GATES (2026-09-06)

Moved from `work/code-quality/` to `work/gates/` in the tracker-wide cut of 2026-09-06 (Ev's direction, in-chat), which read every open `work/issues/` file and every open code-quality row against every live program's `paths` and opened four programs for the ground none covered. Id, `track:` letter and body unchanged. Track K; the gate is GATES' to build, the ten macro bodies it audits are their owners' and are read only.

## Direction taken (2026-09-06)

Option (2), the token-grep gate, composed with option (1)'s audit:
`scripts/gates/panic-free-macro-bodies.sh` reads `crates/*/src` through
`lib.sh`'s code-only view, tracks each `macro_rules!` body by balanced
`{}`/`()`/`[]` nesting, and reports `.unwrap`, `.expect`, `panic!`,
`todo!` and `unimplemented!` inside one. Option (3) (`cargo expand`) and
option (4) (thin macro bodies as a convention) are not taken.

`unreachable!` is NOT matched. It was in the family this row named on
2026-08-13, and D9's D2 addendum removed it from
`[workspace.lints.clippy]` on 2026-08-19: matching it here would make a
macro body stricter than the `fn` beside it and would red `nurbs_fit!`
today. The gate is the stanza, reaching where clippy cannot.

The allow is `#[cfg(test)]` — the attribute on the item or an enclosing
module, and a file whose `mod` line is `#[cfg(test)] mod x;`. No path
allowlist: nothing in the live tree needs grandfathering.

## The audit

Re-derived 2026-09-06: the surface has moved from this row's ten bodies
in nine files to **24 bodies in 15 files** (`geom-curves` is now
`geom/src/curves`; `editor-core/src/expr.rs`, `names/role.rs`,
`node.rs`, `profile/src/path/program.rs`, `test-utils/src/lib.rs` and
`topo/src/validate.rs` carry bodies this row did not list). Two bodies
carry a panic-family token; the other 22 carry none:

- `crates/geom/src/curves/fit.rs:376` `nurbs_fit!` (spans 376-804) —
  one `unreachable!` at 714, on a `let-else` that observes a kernel-bug
  state. Production, sanctioned, and out of the gate's family.
- `crates/topo/src/review_m1_pr5_internal.rs:113` `leak_probe!` — one
  `.unwrap()` at 116. **Test-only**: the file is reached only through
  `#[cfg(test)] pub(crate) mod review_m1_pr5_internal;`
  (`crates/topo/src/lib.rs:206`) and the macro sits inside a `#[test]
  fn`. This row's file-level count of 16 for that file resolves to 1
  inside the macro body and 15 in `#[test]` functions clippy already
  sees.

**No production instance.** The gate is green on the live tree at the
merge base, so it lands without an allowlist and without a
grandfathered population.

## Residue

The `#[cfg(test)] mod x;` resolution the gate needs is the textual,
declaring-directory one the two gates that already do this use; it is
not rustc's for a declaration outside a crate root, and the repair
belongs to `gate-mod-path-resolved-textually`, whose population this
gate joins.
