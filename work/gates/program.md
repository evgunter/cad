---
id: gates
kind: program
title: GATES — the CI gate scripts
status: open
opened: 2026-09-06
area: infra
prefix: gates/
tag: (GATES orchestrator)
ab_band: 3100-3199
paths: [scripts/gates/*]
keep_out: [code-quality Track K is claimed whole with METER — this program takes the scripts/gates/* half (gate-roster.sh and probe-suite-census.sh included — retired Track J's two files that no fence ever drew) and METER takes tools/* and the K-REPORT and TESS-BUDGET documents, .github/workflows/* and local-scripts/* are CIW's — a new gate's wiring row is one announced line there, crates/*/tests/* is S-TCOST's — a gate's own selftest fixtures live beside the gate and are this program's, crates/geom-core/src/real.rs (the Bounds scope rule and its ledger) and geom-core/tests/flagged_census.rs are PROPS' and S-TCOST's — the paragraph bounds-tripwire owes and the S13 ledger half are edited by announced seam, the macro bodies clippy-panic audits and the LoopBoundary discard sites S49 registers are their owners' files and are read only — the audits land as riders on the owners' units, root Cargo.toml's [workspace.lints] is retired Track J's ground and unowned — a row landing there draws the fence in the PR that mints it, D212 lands with G4 (Track V on crates/profile — S-BOOL's glob) and not before]
---

Code-quality Track K's `scripts/gates/*` half, unstaffed since the
09-03 `smell/k-*` lanes stalled without a PR: the compound-`Bounds`
allowlist's matcher redesign and its four smaller defects, the
bit-identity debug-only gate's parser and its second subject, the
interval-square allowlist's module resolution, the readings nothing
re-derives, a gate for the panic family inside macros, a derived
census for the `LoopBoundary` discards, and the grep gates' known
defects. Every row is a shell script with a selftest; none waits on a
ruling. Class E throughout. Charter and order: `work/gates/plan.md`;
narrative in `work/gates/log.md`.
