---
id: D102
kind: unit
title: The compound-Bounds gate anchors on `+`, and misses where-clause and rustfmt multi-line compound bounds
status: open
opened: 2026-08-21
track: K
refs: [D103, D68]
---

## What

The compound-`Bounds` gate anchors on `+`, which is not how Rust expresses a compound bound — `where T: A, T: B` is silent, and so is the multi-line form `rustfmt` converges on (S158).

## Was

Track F.

## Finding

### S158. The compound-`Bounds` gate anchors on `+`, and `+` is not how Rust expresses a compound bound

**[verified, with compiled counterexamples]** Found by the F-e verification pass, and it **subsumes S59 rather than sitting beside it**. S59 was *"the matcher is blind to an alias"*; this is *"the matcher is blind to a spelling"*, and the alias case is one member.

`scripts/gates/bounds-allowlist.sh` matches a `…Bounds` identifier adjacent to a `+`. Rust has several ways to write the same obligation, and the others are silent. **Plain compound bounds, no alias involved:**

```rust
pub fn f<T>(_t: T) where T: Decide, T: Bounds {}   // gate exits 0
pub fn g<T: Decide>(_t: T) where T: Bounds {}      // gate exits 0
```

**And the alias form, which is `bounds-allowlist.sh`'s KNOWN GAP 4:**

```rust
pub trait Bracket: CertifiedEnclosure           // gate exits 0
where
    Self: Bounds,
{
}
pub fn use_it<T: Decide + Bracket>(t: T) { t.lo(); t.sgn(); }
```

**The fact that makes this a hole rather than a curiosity: that last form is what `rustfmt --edition 2021` converges on** from the single-line spelling the gate *does* catch. A gap a formatter produces out of the caught form is the resting state, not a corner case. Two further members, independent of each other: the same declaration in an **allowlisted** file is silent (so a file ratified for one bound silently ratifies this one — that half is **S159**, row `D103`), and a chain through a non-`…Bounds`-named intermediate is silent.

**Not a violation today.** Every `where`-clause `…Bounds` line in `crates/*/src` was checked: no live instance in an unratified file. This is a hole in the instrument, not a breach of the rule.

**Deliberately not closed by #791**, on the orchestrator's ruling: it is a redesign of what the gate matches, no line-based matcher can reach the formatter-stable spelling, and widening the declaration matcher far enough to try false-positives on a trait generic over a **sole** bracket bound (`trait ArrivalSpec<T: CertifiedBounds>`), which is outside the class. **That last clause no longer describes the instrument** (PR #2029, `trait-generic-sole-bracket`): the declaration matcher is now a reader that skips a balanced `<…>` after the trait name, so a bound on the trait's own type parameter is excluded one step before any widening is weighed, and that construct is not what such a widening would cost. The rest stands — whoever takes this will red a population nobody has counted, so **F-R6's grandfathering caveat becomes live on a real residue** rather than the empty one #791 met.

**Verdict:** (blank — unruled.)

## Fence

Track K — the instruments, and what they cannot see. **Fence:** `scripts/gates/` less `gate-roster.sh` and `probe-suite-census.sh`, `tools/`, `docs/K-REPORT.md`. **Block:** `D200`–`D219` / `S270`–`S289`.

## Claimed by GATES (2026-09-06)

Moved from `work/code-quality/` to `work/gates/` in the tracker-wide cut of 2026-09-06 (Ev's direction, in-chat), which read every open `work/issues/` file and every open code-quality row against every live program's `paths` and opened four programs for the ground none covered. Id, `track:` letter and body unchanged. Track K whole goes to GATES (the `scripts/gates/*` half) and METER (the `tools/*` half); this row is the `bounds-allowlist.sh` matcher redesign.

## Landed

`bounds-allowlist.sh` reads the shared code-only view in its STATEMENT
record shape and matches a compound bound by BOUND TARGET: a type
parameter, `Self`, or an applied type such as `Sym<T>` that carries a
`…Bounds`/`…Enclosure`-named trait beside at least one further bound
term, wherever in the record the two are written. `T: Decide + Bounds`,
`where T: Decide, T: Bounds` and `<T: Decide>(…) where T: Bounds` are
one hit; a SOLE bracket bound is not, in a generic list or in a `where`
clause. The trait-declaration test stays a second, separate reason to
fire — a bracket door handed to a NAME, sole or not — because it is not
a case of the grouping rule and folding it in would lose
`trait Bracket: CertifiedBounds`.

**The hit set did not move.** Every one of the 163 line-records the old
matcher caught falls inside exactly one of the 163 statement records the
new one catches, and no statement record is caught that held no old
record: 26 files, all ratified, and the same 183 pinned occurrences.
F-R6's grandfathering caveat therefore never fired — there was no
population to grandfather, and no pin moved.

**What did move is the alias census.** `EvalScalar`
(`crates/editor-core/src/eval/mod.rs:1551`) is a second named compound
bound — `Decide + … + geom_core::Bounds` gathered under one name — and
its declaration is written as a multi-line supertrait list, whose first
line (`pub trait EvalScalar:`) names no bracket door, so the old line
reader could not see it as a declaration at all. It is now rostered against
the 2026-07-29 (M5 PR 8) driver amendment its own doc cites. The
population the roster called closed was not.

KNOWN GAP 1 narrows to the record boundary (a bound assembled across two
statements — an `impl` block's generics and a method's own `where`);
KNOWN GAP 4 narrows to a declaration whose name is pasted by a macro,
where there is no `trait <name>` token to read.
