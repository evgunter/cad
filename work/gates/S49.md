---
id: S49
kind: issue
title: A `continue` with a prose justification is indistinguishable, to grep and to CI, from one without
status: closed
opened: 2026-08-18
refs: [620, 737]
branch: gates/loop-boundary-discards
pr: 2044
closed: 2026-09-06
---

## Finding

- **Where**: `crates/topo/src/census.rs`, `crates/topo/src/splitting/rules.rs`,
  and the 26 sites the pattern below returns
- **Raised by**: the W1a fix lane (#620) and its reviewer, 2026-08-18. **No
  §D row** — the wave ID this was scheduled under is retired and nothing
  replaced it.

**The defect has now been found four times, each time by someone reading the
code**: the census's arm-1 planar × planar skip, the two residues #637
recorded, and #737's third instance in arm 1. The bar those closures set is a
paragraph — *a deferral names an arm that asks the SAME question about the
SAME pair* — and a paragraph is not an instrument. Nothing in `scripts/` or in
CI can tell a justified `continue` from an unjustified one.

**The class is measurably larger than its instances.** A sweep for
`LoopBoundary::Cycle { first } = … else { continue }` returned **29 sites in
18 files** when the style lane ran it; re-run at `eef045ac` with
`LoopBoundary::(Cycle|Empty).*else \{ … continue \}` over `crates/` it
returns **26 sites in 17 files**, of which three inside
`census.rs`/`rules.rs` have been audited and **none of the rest has**.

**What that pattern cannot match**, and what is therefore in neither number:
the same handling written as a `match` arm rather than a let-else — **15
further sites**, two of them (`topo/src/seqgen.rs:1080`,
`editor-core/src/names/emit.rs:422`) `LoopBoundary::Empty { .. } => {}`, which
is the residue's exact reading in match form, uninspected; `continue`s that
drop a loop for a different reason (an arena miss); and the same class over any
other enum.

**The nearest existing precedent for an instrument** is not an allowlist.
`scripts/gates/` holds three allowlist-shaped gates and one **derived-census**
gate (`probe-suite-census.sh`) — one that derives its population rather than
listing it, and refuses an empty answer or a drop below a floor. A deferral
register would take that shape.

**Verdict:** ACCEPTED (Ev, 2026-08-18) — *"should be scheduled but i have no
opinion on when"*.

## Was

unrowed

## Claimed by GATES (2026-09-06)

Moved from `work/code-quality/` to `work/gates/` in the tracker-wide cut of 2026-09-06 (Ev's direction, in-chat), which read every open `work/issues/` file and every open code-quality row against every live program's `paths` and opened four programs for the ground none covered. Id, `track:` letter and body unchanged. Unlettered; the derived-census gate is GATES' to build in `scripts/gates/`, and the audit of the 26+15 discard sites lands as riders on their owners' units.

## The instrument

`scripts/gates/loop-boundary-discards.sh` is the register: it derives
every `LoopBoundary` discard under `crates/*/src` on each run and reds
on a live discard no entry names, on an entry that is not well formed,
and on an entry standing for a different number of discards than it
pins — zero (its site is gone) or more (a new discard that would
otherwise have inherited an existing entry's disposition). Re-derived
at this branch's merge base: **80 sites in 40 files** (73 let-else,
7 match-arm; 31 `continue`, 47 `return`, 2 empty arms) under **75
entries**, of which **2 are marked audited** and 73 are not. The two
audited are `census.rs`'s `face_points` and `line_bounded` deferrals
inside `sweep_cross_solid_backstop`; `splitting/rules.rs` holds none,
because both of its `Empty` arms bind the lone vertex and use it — one
refuses by name with `CorruptOperand`, the other measures the vertex —
so neither is a discard any more.

The AUDIT of the 73 unaudited entries is not this row's and not GATES':
each site belongs to the program that owns its file (CURVED, S-BOOL,
TOPO, and EVAL for `editor-core/src/names/emit.rs`), and the register
is what those audits close against. The register itself — the
`REGISTER` array in the gate — is the live list, file by file and `fn`
by `fn`, with each entry's disposition beside it.
