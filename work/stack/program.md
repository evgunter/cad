---
id: stack
kind: program
title: STACK — what a certified enclosure CLAIMS: the stackup's hull, the non-real contract and the forgeable certificate family
status: open
opened: 2026-09-20
area: kernel
prefix: stack/
tag: (STACK orchestrator)
ab_band: 9100-9199
paths: [crates/editor-core/src/stackup.rs]
keep_out: [opened by ENCL's 2026-09-20 priority-seam cut (Ev, in chat) per work/README.md Track size - ENCL measured 40.5 budget points and was cut into tracks meant to run in PARALLEL (Ev, in chat: for these high priority tracks it is ideal to have several components that can be worked on in parallel), the siblings are ENCL STACK and they share their parent's territory by design - shared ground is legitimate by the README's 2026-09-20 rule and what is owed is awareness while a lane is LIVE, so run scripts/work.py territory on your branch and announce the seam in the PR rather than drawing a fence, PROPS owns the analysis lane and the flux arms, QUAD owns props/quad.rs, SSI and PCERT own the geom-brep certification lanes]
priority: P2
---
The contract half of certified enclosure, split from the arithmetic
half ENCL keeps. P2 because these rows serve `docs/DESIGN.md`'s reach
goal — error propagation, distributions over model parameters — rather
than anything shipping today.

A stackup's certified worst-case hull pads by the LEAF width where the
lane's own width is what it owes; E5's contribution bounds from
`Dual<Interval>` derivative enclosures are unbuilt; and nothing states
what a certified lane owes when a value goes non-real, which is a
contract audit rather than a defect.

The fourth row is the latent one: `MassProperties` and the certificate
family — `PcurveCertificate` and four siblings — have **public fields**,
so a downstream crate can construct a certificate that was never
earned. P3 on its own terms (nothing forges one today) and here because
it is the same question: what a certificate's existence is evidence
of.

Charter and order: `work/stack/plan.md`; narrative in `work/stack/log.md`.
