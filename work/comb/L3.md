---
id: L3
kind: unit
title: The remaining S35 roll-up rows
status: open
opened: 2026-08-21
refs: [S35]
---

## What

The remaining `S35` roll-up rows — the Tier-2 table of smaller findings
across `editor-core`, `topo`, `geom-brep`, `step-import`, `profile`,
`geom`, `mesh` and `bvh` that no track row names individually. `S35`
carries the table.

## Why last

Lowest value density; several will be resolved incidentally by the
tracks. The table spans every track's territory, so it is partitioned
into rows on the owning tracks as each is taken, and what is left when
the tracks empty is this unit.

## Re-homed to COMB (2026-09-11, the cut in `docs/WORK-TRACKS-2026-09.md` addendum 3)

COMB is code-quality's *Last, deliberately* group given a program of its
own, plus the roll-ups that no single track can hold. This row is here
because it is workspace-wide by construction and collides with every
fence.

Its class at the cut was **H** — residue of a 60+ row table spanning
every track's territory; scope unknown until tracks empty. The class is
a dispatch estimate made by reading the row against the tree on
2026-09-11, not a verdict on the finding, and a lane that finds it wrong
says so in its PR. The id, the `track:` letter where the row carries
one, and the body above are unchanged by the move.
