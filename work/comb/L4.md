---
id: L4
kind: unit
title: Re-check every disposition that reasoned about a gate's direction from one side
status: open
opened: 2026-08-21
refs: [1643]
---

## What

**Re-check every disposition in this register that reasoned about a
gate's direction from ONE side.** `S120(a)`'s second clause, and the
only half of it that never found a home: a fallback inside a
*comparison* has two sides, and a sweep that dispositioned one of them
— *"a broken reading shrinks the denominator, so the gate is MORE likely
to fire"* — answered half the question while reading as if it had
answered all of it. That reasoning appears throughout the findings.
`D105` closed the two `tess-meter` instances; the population is the
rest. (`S120`'s other members are Track K's `D64`.)

## Why last

Register-wide, and it audits this program's own dispositions rather than
any track's files — so it collides with every track exactly as `L1`–`L3`
do, and it cannot be scoped to a fence.

## Re-homed to COMB (2026-09-11, the cut in `docs/WORK-TRACKS-2026-09.md` addendum 3)

COMB is code-quality's *Last, deliberately* group given a program of its
own, plus the roll-ups that no single track can hold. This row is here
because it is workspace-wide by construction and collides with every
fence.

Its class at the cut was **H** — register-wide re-audit of one-sided
gate reasoning; collides with every track. The class is a dispatch
estimate made by reading the row against the tree on 2026-09-11, not a
verdict on the finding, and a lane that finds it wrong says so in its
PR. The id, the `track:` letter where the row carries one, and the body
above are unchanged by the move.

## Refs at code-quality's sweep (2026-09-11)

`work/code-quality/` left the tracker (`docs/DOC-LEDGER.md`, sweep 11)
and its closed rows went with it. `D64` is now cited by its closing PR
1643.
