# COMB log

Newest entries at the bottom; the tail is the program's live status.
Plan: `work/comb/plan.md`.

## Opened (2026-09-11)

Opened in the tracker cut of 2026-09-11 (Ev's direction, in-chat;
`docs/WORK-TRACKS-2026-09.md` addendum 3). Thirteen rows moved in by
`git mv`, all from `work/code-quality/`, each with a `## Re-homed`
record.

This program is the cut's hardest track by class — eleven `H` of
thirteen, no `E` at all — and the least urgent by design. Giving the
*Last, deliberately* group a directory does not make it takeable; what
it changes is that the group now has an owner who can PARTITION the four
roll-ups, which is work that can start today and which nobody has been
doing because a roll-up in a holding directory has no lane.
