---
id: L1
kind: unit
title: Comb and rename the milestone-named test suites, per suite, never as a rename pass
status: open
opened: 2026-08-21
refs: [S36]
---

## What

`S36` — comb-and-rename, **per suite**, never a rename pass. The unit of
work is review-and-fixup of a PR- or milestone-named suite into a
subject-organised one, and then its rename; `S36` carries the
population and Ev's sequencing verdict. Read `S36`'s counts as a
proportion, not a pair of numbers.

## Why last

A PR-numbered name currently *carries signal*: it marks a suite not yet
combed. Renaming first converts a visible backlog into an invisible one.
Needs an owner and a slot, not just permission — the 2026-08-13
retirement licence has produced zero deletions. It is a cross-cutting
naming sweep over `crates/*/tests/` and the `review_*` modules in
`src/`, so it collides with every track and goes after the tracks empty.

## Re-homed to COMB (2026-09-11, the cut in `docs/WORK-TRACKS-2026-09.md` addendum 3)

COMB is code-quality's *Last, deliberately* group given a program of its
own, plus the roll-ups that no single track can hold. This row is here
because it is workspace-wide by construction and collides with every
fence.

Its class at the cut was **H** — per-suite comb-then-rename over
hundreds of files; collides with every fence. The class is a dispatch
estimate made by reading the row against the tree on 2026-09-11, not a
verdict on the finding, and a lane that finds it wrong says so in its
PR. The id, the `track:` letter where the row carries one, and the body
above are unchanged by the move.
