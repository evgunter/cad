---
id: sym
kind: program
title: SYM — the E12 symbolic identity tier
status: open
opened: 2026-09-13
area: kernel
prefix: sym/
tag: (SYM orchestrator)
ab_band: 4700-4799
paths: [crates/geom-core/src/sym.rs, crates/geom-core/src/sym/*, crates/geom-core/tests/m10_*]
keep_out: [OPENED 2026-09-13 at M10's exit sweep (docs/DOC-LEDGER.md sweep 13) on Ev's call that the tier's fourteen rows are a successor's opening slate rather than PROPS' inheritance, crates/geom-core/src/* is PROPS' glob and this program claims only sym.rs and sym/* inside it — the fence is written on both sides and PROPS' keep_out names this program, crates/geom-core/src/real.rs is PROPS' and the registered-identity door inside it (register_equal and SymRegistration and WITNESS_REL) is edited here by announced seam — the Real trait itself is SCALAR's subject and neither is taken from this program's slate, crates/geom-core/src/linalg/* is PROPS' — interval-self-dot-straddles-before-rule-a lands in vec.rs and is announced there because the fix is a value-channel one that PROPS' linalg lane may simply take, the driver and the analysis lane (editor-core analysis.rs and distribution.rs and drive.rs and measure.rs and mc.rs and stackup.rs) are PROPS' at M10's sweep — the tier is replayed from drive.rs through one replay_leaf door and a change to that door is announced there, crates/geom-core/tests/m10_* is also S-TCOST's and S-TINT's crates/*/tests/* territory by declaration — test-mechanism changes are announced to both and no row here is justified by a cpu-second, crates/profile/* and crates/sweep/* are S-BOOL's and BLEND's — the two constructor-registration rows that block this program's door consumers live on BLEND's slate and are not landed from here, tools/k-lint is INSTR's — the discharge vocabulary's fifth spelling is pinned across that workspace boundary and moves by announcement, docs/ERROR-DESIGN.md is a ratified design doc and E12 is cited here and never re-litigated]
priority: P1
---

The E12 symbolic identity tier — `geom_core::sym`, the hash-consed DAG
over the parameters whose lazy exact-rational normal form decides an
identically-zero margin `Zero` at any box width — as a program of its
own. M10 built it across four units (M10-7 … M10-10) and closed on
2026-09-13 with fourteen rows standing on it: the ceiling that bounds
each document today, the door's two unbuilt registrants, the cost
nobody has profiled inside, and the hygiene two reviews flagged. This
is that opening slate (`docs/DOC-LEDGER.md` sweep 13, Ev's call at the
sweep). Class H at its core with three E riders and one frontier that
is a design conversation before it is a unit. Charter and lanes:
`work/sym/plan.md`; narrative in `work/sym/log.md`.
