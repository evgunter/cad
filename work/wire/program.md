---
id: wire
kind: program
title: WIRE — the evaluation seat: wiring, the placement lift and the verb vocabulary
status: open
opened: 2026-09-11
area: api
prefix: wire/
tag: (WIRE orchestrator)
ab_band: 3700-3799
paths: [crates/editor-core/src/eval/mod.rs, crates/editor-core/src/eval/wire.rs, crates/editor-core/src/eval/anchor.rs, crates/editor-core/src/eval/schedule.rs, crates/editor-core/src/eval/slots.rs, crates/editor-core/src/placement.rs, crates/editor-core/src/product.rs, crates/editor-core/src/names/flush.rs, crates/editor-core/src/names/select.rs, crates/editor-core/src/names/table.rs, crates/verbs/src/*, crates/verbs/README.md]
keep_out: [crates/editor-core/src/names/role.rs and node.rs and program.rs and edit.rs and persist/* are DOCM's - D364 and S195 reach res_spec and res_target in program.rs and are announced there rather than landed from here, crates/editor-core/src/eval/parts.rs and eval/memo.rs are DOCM's by the same table, crates/profile/* is S-BOOL's glob - the two profile lift rows mint their door there by announced seam as EVAL and FILLET both did, crates/sweep/src/loft.rs is S-BOOL's and crates/sweep/src/* is BLEND's - the loft caller of the profile lift is announced to whichever owns the file it lands in, crates/editor-core/src/assembly.rs is DOCM's - product-gather edits product.rs and names/table.rs only, and PORT holds the two assembly.rs rows, crates/topo/src/source.rs birth records are TOPO's - the deferred verb-seat row names them and does not edit them]
---

The evaluation seat's successor. EVAL closed 2026-09-08
(`docs/DOC-LEDGER.md` sweep 9) leaving three rows in `work/issues/` as
"the successor's opening slate" and naming two already there; this is
that program, and it takes the seat's ground with them — the wiring, the
placement and profile lift doors, the names flush and select refusals,
and the verb vocabulary `crates/verbs` declares. Class M at its core,
four E openers and four H rows, one of them (`axis-flavoured-declarations`)
an `[ev]`-shaped design fork the 2026-09-03 survey already judged
correctly parked. Charter and unit order: `work/wire/plan.md`; narrative
in `work/wire/log.md`.
