---
id: suite
kind: program
title: SUITE — suites, fixtures and the helpers they copy
status: open
opened: 2026-09-11
area: infra
prefix: suite/
tag: (SUITE orchestrator)
ab_band: 4200-4299
paths: []
keep_out: [this program claims NO paths and the reason is the point - crates/*/tests/* and crates/test-utils/* are S-TCOST's and S-TINT's territory in every crate, so every unit here is announced to S-TINT and a row whose mechanism reaches into a crate's src is filed on that crate's owner, crates/topo/src/test_support_impl.rs and crates/sweep/src/test_support.rs are TOPO's and BLEND's - the shared-helper homes land there by announced seam, demos/tour/* is code-quality Track X's ground which that program still dispatches - D403 and the genus copies in demos are announced to it, crates/editor-core/tests/fixture/ migration is announced to DOCM and MSOLVE whose suites it moves, a test this program writes for another program's src change is that PR's test and not a row here - W's rule from code-quality plan.md binds unchanged]
---

The test-side rows S-TINT's and S-TCOST's own boards did not carry: 18
copied fixtures over six crates, eleven part-resolver stubs copied into
eleven editor-core suites, a `genus` helper now spelled eleven times, the
generic `cube<T: Real>` two rows have been describing from opposite
sides, and the differential probe `D114` wants. Every row is on S-TINT's
ground and every unit is announced there — this program is a slate, not
a second claim on `crates/*/tests/*`. Class M throughout with one H.
Charter and unit order: `work/suite/plan.md`.
