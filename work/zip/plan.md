# ZIP — the plan

The join, rest and merge lane: what the boolean hands back once the sections are found.

Opened 2026-09-20 by REACH's priority-seam cut (`work/README.md`,
Track size). Nothing dispatched yet.

## The slate

**30 budget points** of dispatchable work against a ceiling of 30.

| pri | item | cost | title |
|---|---|---|---|
| P0 | `boolean-rebuild-folds-an-in-band-second-order-into-conventional` | D | topo::boolean::ops folds an in-band tangent_second_order into the conventional posture, citing a tier-3 stance tier 3 does not take |
| P0 | `join-desync-on-the-star-fixture` | H | JoinDesync (every chord arc separates a loose scaffolding pair) on the star fixture in six member orders |
| P0 | `rest-zip-seam-chord-on-cylinder-wall` | H | The declared-REST zip leaves a straight seam chord where a cap rim cuts a bore wall mid-height; the merge door's refusal hid it |
| P0 | `subtract-of-a-hollow-operand-files-the-island-under-one-solid` | H | subtract(A, hollow B strictly inside A) files B's cavity as a second Outer shell of A's solid instead of a solid of its own |
| P0 | `two-parts-of-one-body-at-one-boolean-refuse-as-ray-exhausted` | D | Two Parts selecting one body at one boolean are DM5-distinct inputs carrying one Arc; the boolean refuses as Containment(RayExhausted), not as the same body twice |
| P1 | `descendant-chase-spends-its-budget-into-a-dropped-contact-record` | H | KeyView's live_vertex/live_face answer None on a spent budget, so a cycling fusion row drops a declared contact instead of refusing |
| P1 | `verbs-1031b-assigner-checker-divergence` | H | the winding assigner/checker divergence: merge_faces assigns arc-bounded roles validate check 6 cannot check |

## Order

`subtract-of-a-hollow-operand-files-the-island-under-one-solid` first,
because it is the one silent WRONG answer in the track and everything
else here is a refusal. Then `verbs-1031b-assigner-checker-divergence`
— the winding assigner and validator disagreeing is one fact recorded
twice, and settling which is right is likely to explain
`join-desync-on-the-star-fixture` too.

`rest-zip-seam-chord-on-cylinder-wall` and
`descendant-chase-spends-its-budget-into-a-dropped-contact-record` are
independent of both and can run in parallel.

## Review posture

OPEN, for this program's first dispatch. REACH inherited protocol v7
(`docs/MODEL-AB-LOG.md`, Ev 2026-09-19): the dual on triaged-in units
only, opus/opus outside it. This program's units are candidates where
the failure mode is a confident WRONG answer rather than a refusal —
the first orchestrator names which of its rows those are.
