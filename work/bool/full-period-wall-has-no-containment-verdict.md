---
id: full-period-wall-has-no-containment-verdict
kind: issue
title: Declared cylindrical Rest on a FULL-PERIOD wall still refuses: the chart door has no verdict there (lily wall probe 12)
status: open
opened: 2026-08-31
github: 1416
refs: [1032]
---

## From GitHub issue 1416

opened 2026-08-31, 1 comment.

The residue issue 1032's fix leaves behind, measured on the MATE-2 branch (`mate/2-cyl-rest`, head `c27ecb5a`) against `demos/tour/src/lily.rs` wall probe 12.

## What moved and what did not

Issue 1032's fix repairs the declared-cover rung for a shared carrier whose faces are SPLIT into arcs: an endpoint the cylinder chart trim places definitely `Out` of one wall face is now eventless there rather than a refusal, because the neighbouring face on the same carrier holds it. The shaft-in-a-bore class unions.

**Wall probe 12 does not flip.** Its refusal is bit-identical before and after:

```
CurvedPierceUnsupported { operand: B, face: FaceKey(3v1), edge: EdgeKey(4v1) }
```

## Why — measured

Traced at the arm, the refusing pair is:

- edge `4v1` of operand B: a LINE, a meridian of the stem foot's wall, `z ∈ [-0.92, 0]` at azimuth 210°;
- face `3v1` of operand A: the corm's bore wall, made by a **full revolve**, so its azimuth window is a full period;
- `covered = true` — the declaration is seen, exactly as it now is on the arc-split shapes;
- `curved_face_containment` answers **`None`** for BOTH endpoints — not `Out`.

`None` is that door's documented honest remainder, and the full-period azimuth window is one of the three cases it names for it (`crates/topo/src/boolean/contain.rs`: "the trim's cosine comparison is an equivalence only for a window narrower than a period, so at a full turn there is no angular test to run and the rectangle stops describing the face"). The MATE-2 fix deliberately does not widen the no-verdict case: `Placement::Undecided` keeps the frontier, because nothing at all is known about where the endpoint sits.

So the honest statement of the remaining wall is narrower than issue 1032's: a declared cylindrical `Rest` reaches the rest lane when the shared carrier's faces are **azimuth-split**, and does not when either side's wall is a **full-period** face.

## What would close it

A containment verdict for a full-period cylinder wall. Every azimuth is in the face, so membership reduces to the height window alone — the solid door already serves exactly this case (`point_on_wall_in_face`'s ray lane), and the face door refuses it only because its contract is the remainder. Giving the face door the same arm, with the height test standing alone, would let the meridian's endpoints be placed and the lily's mate through.

Wall probe 12 is the live pin; its note cites issue 1032 and should be re-pointed here when that closes.

## Comments

**2026-08-31** — orchestrator:

**Correction — the refusing meridian's azimuth is 240°, not 210°.** Review caught the arithmetic; the traced point is

```
px = (-0.030000000000000027, -0.051961524227066305, ·)   r = 0.06
```

and `atan2(-0.05196…, -0.03) = -120°`, i.e. **240°**. The description above says 210°, which is wrong and is the only number in it that is: everything else — edge `4v1` of operand B, a LINE meridian of the stem foot's wall spanning `z ∈ [-0.92, 0]`, `covered = true`, and `curved_face_containment` answering `None` at BOTH endpoints because the corm's bore is a full-period revolve face — is as measured and re-derived independently by both review lanes.

The azimuth is incidental to the diagnosis (the full-period window is what produces `None`, at any azimuth), but a wrong number in a measurement report is worth correcting rather than leaving for the next reader to trip over.

---
_Generated by [Claude Code](https://claude.ai/code)_

## Home

`work/bool/` — what closes it is a new arm on `curved_face_containment` in `crates/topo/src/boolean/contain.rs`, inside S-BOOL's territory glob `crates/topo/src/boolean/*` and its charter of containment doors that refuse legal inputs (the sibling gap on the same function is issue 1484).
