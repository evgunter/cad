---
id: bool
kind: program
title: S-BOOL — boolean reach and containment
status: open
opened: 2026-08-31
area: kernel
prefix: bool/
tag: (S-BOOL orchestrator)
ab_band: 1100-1199
paths: [crates/topo/src/boolean/*, crates/topo/src/splitting/*, crates/profile/*, crates/editor-core/src/resolve/vdiff.rs, crates/sweep/src/loft.rs]
keep_out: [the germ and pierce lanes (chord_join.rs and boolean/join.rs and splitting/* as the pierce reads them) and the declared-contact rungs (rest.rs and carrier_eq.rs and census.rs) and the operand-reach arms (sectors.rs and ops.rs's curved arm and boxes.rs) and geom-brep intersect.rs and implicit.rs are CURVED's since 2026-09-04 (VERBS' successor — the fence VERBS' keep_out cession stated in prose, now written on both sides per docs/WORK-TRACKS-2026-09.md), S-BOOL keeps the PATHS lattice and BOOL-4/5/6/7 and BOOL-9/10/12 and BOOL-Q, the operand-reach items and the two containment doors and the graft and boolean-declarations singles are on this slate until handed to CURVED by announcement on the away channel or at this program's exit, ssi* and pcurve_cache are Track Q ground behind PCURVE P-2, props/curved.rs is Track R's fence (BOOL-5 edits it by recorded seam), M10's Dual arms in product.rs and the AtRestPolicy seam are untouched by BOOL-7, crates/profile is SMELL track V fence ground (seam noted), the persist schema is contended ground — announce on the away channel before landing]
---

Operand gates and containment doors that refuse or mis-admit legal inputs —
`topo/boolean`, `splitting`, containment — plus the PATHS lattice work the
Q1 ruling chain produced (the straight continuation, the point-target
continuation, the declared arrival at the seam, the RawLoop demotion and the
`arc_continue` retirement), and SMELL track Q's topo rows. BOOL-1/2/3/8/11/13
are merged; BOOL-12 is under review, BOOL-9/10 are specced behind it, and
BOOL-4/5/6/7 and the Q-track lanes queue on the shared lane budget. Charter,
ratified ground, the three rulings and the unit list: `work/bool/plan.md`;
narrative in `work/bool/log.md`. Same orchestrator as S-MESH.
