---
id: vdoc
kind: program
title: VDOC — the viewer's prose, citations and censuses
status: open
opened: 2026-09-17
area: gui
prefix: vdoc/
tag: (VDOC orchestrator)
ab_band: 5500-5599
paths: [crates/viewer/README.md, crates/viewer/tests/*, crates/viewer/src/lib.rs]
keep_out: [OPENED 2026-09-17 in VIEW's re-scope as one of four successors on VIEW's ground and LAST of the four to dispatch - VIEW stays open until its exit walk and its paths cover every path above by declaration and VIEW's keep_out names this program in return, the three sibling successors are vnews and vgeom and vseam and this program claims none of their src files - a prose or citation or census defect lands wherever the sentence is and the sentence is usually in a file one of them owns, so a unit here that edits a doc comment or a header in crates/viewer/src outside lib.rs is announced to that owner in its PR body and takes the owner's word where the two disagree about what the code now does, and the reverse holds too - a sibling unit whose own diff moves the lines a row here cites repoints that row in its own PR per the VIEW register's a-diff-that-shifts-a-file-owns-the-citations-that-shift-broke, crates/viewer/tests/* is S-TCOST's and S-TINT's and Track W's by declaration and is the reason this program's territory warns on both - a test-MECHANISM change is announced to S-TINT and a roster line this program's own arrival reds is registered here per the census header's own instruction and never audited from here, crates/viewer/tests/* and README.md are also CHROME's by declaration and CHROME works tests/valid_range.rs and tests/combine_ops.rs and tests/tree_badges.rs by the carve-out of 2026-09-15 - those three files are announced before they are touched and CHROME's keep_out names VIEW and not yet this program with the row asking it to write its side on CHROME's slate, crates/viewer/GUI-DESIGN.md is in no open program's paths and its GQ clauses are ratified design - a citation INTO it is repointed here and a change to what a clause decides is an ev PR, crates/viewer/README.md is ratified design beside the code per CLAUDE.md so a repoint or a re-derived census lands here and a change to what a Where-in-the-code decision row DECIDES waits for Ev, other programs' item files are never edited from here - a stale citation on another program's slate is reported to its owner per implementer-discipline section 6 and the renamed-module row that arrived here is that report's carrier and not a licence to edit those rows]
priority: P4
---

One of four successors opened by VIEW's re-scope of 2026-09-17, on the
half of VIEW's slate whose subject is **claims the tree makes about
itself**: a citation, a census, a prose universal, a naming rule, and
the assertion that would have caught the difference. Twenty-two rows
arrived from `work/view/`, each by `git mv` with its body and its
history unchanged.

**This program dispatches LAST of the four.** Its spine is
`stale-file-citations-after-the-split` and every unit the other three
land invalidates more of it; the opening condition is in
`work/vdoc/plan.md` §Order and is not a note.

Charter and order: `work/vdoc/plan.md`; narrative in
`work/vdoc/log.md`. The lane register that binds every lane dispatched
here is `work/view/plan.md`'s, inherited by reference and not copied —
`work/vdoc/plan.md` §The register says why and what happens to it when
VIEW's directory goes.
