---
id: pipe
kind: program
title: PIPE — the topology pipeline, its shared cores and what a refusal there may say
status: open
opened: 2026-09-11
area: kernel
prefix: pipe/
tag: (PIPE orchestrator)
ab_band: 4500-4599
paths: []
keep_out: [this program claims NO paths - crates/topo/src/boolean/* and splitting/* are S-BOOL's and CURVED's and census.rs is CURVED's and validate.rs is TOPO's so every row here is on a live program's ground, the two extractions in topo-shared-cores and S5's unification move files S-BOOL and CURVED both have live units in - neither is dispatched without that program's announcement and the design half of S5 is a docs/DESIGN.md layering row which is Ev's, crates/mesh/src and crates/step-import/src consumers of the described-net class are S-MESH's and EXCH's - each arm is filed on its owner and the sweep itself is a routing list not a diff, S14's no-panic policy amends D9 and no lane resolves it by implementing, S79's three public API surfaces were explicitly scheduled as GitHub issues and not as rows - that disposition stands until Ev changes it]
---

`crates/topo`'s own unclaimed rows: the two engines (boolean and
splitting) whose shared cores are hosted in one half, `S5`'s duplicated
pipeline, the census arms that answer for a poisoned net or an
unreachable conic, the witness budget that exhausts under two caps with
one name, and the two questions about what a refusal in this layer may
say (`S14`'s no-panic policy, `S70`'s graft atomicity). Seven of ten
rows are `H` and three are rulings or ruling-gated. Charter and unit
order: `work/pipe/plan.md`.
