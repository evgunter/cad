---
id: D306
kind: unit
title: Correct offset_fit.rs's certificate-record note and collapse loop_area's two basis passes into ders1_in_span
status: closed
opened: 2026-09-02
track: R
refs: [1536]
branch: door/d306-fit-note-and-ders1
pr: 2394
closed: 2026-09-11
---

## What

**`offset_fit.rs:432–437`'s certificate-record note beside the fit door (filed by CERT-N1; the `topo/src/props.rs` members of the same class were rewritten by CERT-M2 — the lane refuses because no re-derivation exists at that scalar, never because no value can arrive)**: it should say the DOOR is `f64`-only and that the record travels with a scalar lift (`ApproxSurface::map_scalar` carries it as provenance the validator never trusts) — the note currently implies no lifted value exists. Beside it, `props/loop_area.rs:163–164` calls `eval_in_span` then `deriv_in_span` at one `(span, t)`: two basis passes for a point and a tangent that `NurbsCurve3::ders1_in_span` (CERT-N1's order-1 jet door, bit-identical to both, 336 ns vs 399 ns at degree 2) answers in ONE pass — one call, destructure the pair; the order-2 `ders_in_span` is NOT the door (it would discard `C″`).

## Was

CERT-N1 (PR 1536), filed.

## Finding

No live finding section; provenance only.

## Fence

Track R — the measuring consumers: `geom-brep` and `mesh/`. **Fence:** `crates/geom-brep/src/` less `ssi*`, `pcurve_cache.rs`, `nurbs_iso.rs` and `edge_nurbs.rs` (which are Q's), plus `crates/mesh/` — widened 2026-08-28 from `geom-brep/src/props/` only, so `patch_bound.rs`, `offset_meters.rs`, `nurbs_hull.rs` and the rest of the crate's `src/` are R's. **Block:** `D300`–`D319` / `S370`–`S389`.

## Re-homed to DOOR (2026-09-11, the cut in `docs/WORK-TRACKS-2026-09.md` addendum 3)

DOOR collects the rows whose fix is already written in the row — one PR
each, no design question left open. This row is here because a lane can
take it and land it without deciding anything first.

Its class at the cut was **E** — reword one note; collapse two calls
into existing bit-identical `ders1_in_span`. The class is a dispatch
estimate made by reading the row against the tree on 2026-09-11, not a
verdict on the finding, and a lane that finds it wrong says so in its
PR. The id, the `track:` letter where the row carries one, and the body
above are unchanged by the move.

## Closed (2026-09-11) — both halves landed

**The `loop_area` half was exactly as cited.**
`crates/geom-brep/src/props/loop_area.rs:165-166` called
`span.eval_in_span(t)` and `span.deriv_in_span(t)` at one `(span, t)`
inside `nurbs_vector_area`'s Gauss loop; it is now one
`span.ders1_in_span(t)` destructured into the pair. The row's warning
about `ders_in_span` was right and was not taken: the order-2 door
would compute and discard `C″`.

**Bit-identity held, and nothing moved.** `ders1_in_span`'s own claim
is that its point is `eval_in_span`'s bit for bit and its derivative is
`deriv_in_span`'s bit for bit; the claim is pinned by
`ders1_in_span_is_eval_and_deriv_bit_for_bit`
(`crates/geom/src/curves.rs:1145`), which was EXECUTED rather than read,
and falsified under a one-ulp perturbation of the door's point half to
confirm it can go red. Nothing moved: no golden, no committed
render cell and no k-lint baseline changed on the branch. Hosted run
**34647396039** on `e4fe4b73a` is green over 38 jobs — the full
code-tier matrix, twelve `test (…)` (both lanes x three eps rows x two
shards) and five `k-lint (gate, …)` unifications, nothing narrowed —
and every render lane reports `matches this render` with **zero**
neutral check runs on the head SHA, so no lane re-baselined. Locally the whole
of `geom-brep` is green (450 tests).

**No `geom-brep` row corroborates this beyond that.** An earlier draft
cited `r2_cert6_probes::r2_dome_gauge_silence_and_area_bit_invariance`
as "an area-bit test on the path this site feeds". It is not on this
path: that row drives `props::quad::nurbs_patch_face`, and
`crates/geom-brep/src/props/quad.rs` integrates a tensor net without a
single `*_in_span` call, while `nurbs_vector_area` is the curve
boundary integral reached from `props/curved.rs` and `props/mod.rs`.
The row is real and green; it is not evidence about this change. The
reviewer further established by execution that **nothing in `geom-brep`
goes red if the door's bit-identity breaks** — the pins are
`crates/geom/tests/span_bit_identity.rs` and `_ext.rs`, a captured
1001-value digest over all five doors at `f64`, `Dual64` and
`Interval`. So the new call site tests nothing; the claim it rests on
is pinned upstream, and only there. (The derivative half cannot break
at all: `deriv_in_span` literally IS `ders1_in_span(t).1`.)

**The `offset_fit.rs` half's citation never named the note — it had not
drifted.** The plan warned of drift; the file shows something different
and stronger. The row cited `:432-437`; the note is at
`crates/geom-brep/src/offset_fit.rs:647`, on the `pub use
geom::OffsetCertificate;` re-export beside the fit door, and it has
been there unchanged since at least 2026-08-15 (`git show` at that
date puts the same text at the same line), so `:432-437` never named
it. `:432-437` today is inside `OffsetFitError::BoundNotFinite`'s
fields.

The note said the record type has to sit below the surface enum, that
its derivation is this module's, and that every limb writes into it —
true, and read together an implication that an `OffsetCertificate` only
ever exists at the derivation's scalar. It now states the split the row
asked for: the DERIVATION is `f64`-only (every door in the module takes
and returns `NurbsSurface<f64>`, checked across all eleven `pub fn`s),
while the RECORD carries no scalar parameter and travels verbatim onto
a lifted surface through `ApproxSurface::map_scalar`
(`crates/geom/src/surfaces/approx.rs:316-322`), so it DOES reach
consumers at scalars this module never runs at.

**One correction to the row's wording, made at the site.** The row
says the record travels "as provenance the validator never trusts",
which is right at `f64` and incomplete elsewhere. At `f64` the
validator re-derives against the description and never consults the
stored copy. At a scalar with no re-derivation lane
(`PropsQuadLane::recertify_approx` answering `None`,
`crates/topo/src/props.rs:925`) tier 3 does not merely distrust the
carried record — it REFUSES the face,
`ValidationError::ApproxLaneUnsupported`
(`crates/topo/src/validate.rs:3323`). The note says that, which is also
the row's own parenthetical ("the lane refuses because no re-derivation
exists at that scalar, never because no value can arrive") stated where
a reader of the record meets it.

**A third production site, found in review, taken here.**
`crates/geom/src/curves/projection.rs:242` read
`let (c, c1, _) = span.ders_in_span(...)` — the order-2 door with `C″`
bound to `_`, which is the second wasteful consumer `ders1_in_span`'s
own doc anticipates ("or `ders_in_span` and discard `C″`",
`crates/geom/src/curves/nurbs.rs:494-497`). **The swap was not assumed
free.** The door is documented bit-identical to `eval_in_span` +
`deriv_in_span` and says nothing about matching `ders_in_span`'s first
two components, so the relation was pinned first and then executed:
`ders1_in_span_is_ders_in_spans_first_two_bit_for_bit`
(`crates/geom/src/curves.rs:1181`) is green on the same degree-4
fixture with weights spanning `1e-6`–`1e6`, and reds under a one-ulp
perturbation of the door. Only then was the site swapped. The other
`ders_in_span` call in the same file (`:210`) uses `c2` and stays.

Sweeping that shape across `crates/*/src` finds no other production
site discarding `C″`: `nurbs.rs:522` is `deriv2_in_span` (needs `.2`),
`nurbs.rs:1463`/`:1469` need all three components, and every remaining
hit is a dump, meter or pairing test that must keep calling the wide
door.

**Two rustdoc-visible copies of the corrected premise were swept with
it.** The note this row asked for is a `//` comment on a `pub use`, so
it renders nowhere, while `crates/geom/src/surfaces/approx.rs:210-213`
and `:308-311` are `///` docs a reader of `OffsetCertificate` actually
meets — and both still asserted that tier 3 "re-derives … on every call
and never consults the stored copy" with no scalar qualification. Both
now state the two mechanisms (re-derivation at `f64`, refusal where no
lane exists), so the invariant protects the code that did not already
know it. `offset_fit.rs:650-651`'s own sentence was also false as
written — "every door here takes and returns `NurbsSurface<f64>`", when
several return `OffsetCertificate`, `Surface<f64>` or
`Option<Point3<f64>>`; it now claims what is true and checkable, that
all eleven doors are monomorphic at `f64` and none is generic over
`T: Real`.

**Class estimate `E` was right.** Two files, no design question, and
the diff is four lines of code plus a note.

**Residue: one, filed and re-homed.**
`work/scalar/curve3-eval-and-deriv-at-one-t-run-two-basis-passes.md` —
the same defect one level up, at `Curve3::eval(t)` + `Curve3::deriv(t)`,
with no whole-curve order-1 jet door to collapse them into. Minting one
is a design question and it is the SAME question as `scalar`'s `S393`
(class `M`, "new door in `crates/geom/src/curves/nurbs.rs`"), so the
file lives on that slate rather than DOOR's.

Its census was corrected twice on the way there, both times against the
tree. `crates/geom/src/curves.rs:562-563` came OUT — those calls are in
`param_near`'s `Circle` arm and `:566` sends `Nurbs` to `None`, so the
pair can never reach the NURBS path. `crates/geom-brep/src/certify.rs:1743`
+ `:1805` and `crates/sweep/src/skin.rs:1120` + `:1160` went IN: the
first is the same parameter EXPRESSION sixty-two lines apart, the second
is routed through a helper — the two blind spots the first sweep
declared, each with a live instance, one of them inside this row's own
crate. Seven sites, not six.
