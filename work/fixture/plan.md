# FIXTURE — the plan

one body, box or cube spelled n times across the suites

Opened 2026-09-20 by TINT's priority-seam cut
(`work/README.md`, Track size). Nothing dispatched.

## The slate

**20 budget points** of dispatchable work against a ceiling of 30.

| pri | item | cost | title |
|---|---|---|---|
| P4 | `body-hash-census-misses-rename-only-duplicates` | E | the body-hash duplicate census misses rename-only twins |
| P4 | `cavity-module-doc-restates-a-census-that-is-now-wrong` | E | sweep/tests/common/cavity.rs restates the brick/prism census in its own module doc, and both equalities it asserts are false today |
| P4 | `census-answers-no-field-read-for-a-walk-that-reads-a-field` | E | the hand-written-impl census answers NoFieldRead for two shapes that do read a field, and its own doc says so |
| P4 | `editor-core-raw-twin-planes-unreconciled` | E | seat6/seat7's kernel-direct twins assert an equality nothing enforces: two plane spellings, hand-held |
| P4 | `geom-brep-inline-canonical-frame-surfaces` | E | geom-brep tests spell the canonical-frame elementary surfaces inline at 34 use sites |
| P4 | `landing-gathers-source-text-row-has-a-gate-now` | E | landing_gathers.rs's source-text row over product.rs's gather counter now duplicates a CI gate on the gatedness axis |
| P4 | `orient-module-prose-accumulation` | E | `sweep`'s orient.rs has no governor on prose accumulation |
| P4 | `reader-census-shared-disposition-survives-partial-reversion` | E | A Shared ledger row is checked by one substring, so a site that keeps the import and re-adds hand-rolled helpers stays green |
| P4 | `sweep-boolean-suite-brick-and-prism-copies` | E | sweep tests: the prism(pts, h) copy class outside the blend family (the brick half closed by S52) |
| P4 | `sweep-test-suites-cite-eight-deleted-suite-files` | E | sweep tests: sixteen tests→tests citations across thirteen suites, eight naming suite files that no longer exist |
| P4 | `the-per-impl-sight-anchor-is-a-suppression-list-that-shrinks` | E | the hand-written-impl census's only per-impl sight anchor is its suppression list, so it goes blind as the rows it names are repaired |
| P4 | `the-two-vertex-bulge-one-circle-fixture-has-eight-copies` | E | The two-vertex bulge-1 circle section is hand-written in eight sweep/profile test files, the newest added by a PR whose subject was retiring hand-spelling |
| P4 | `tint-plan-states-the-withdrawn-reviewer-independence-rule` | E | tint's plan states the withdrawn reviewer-independence rule beside the ruling that withdrew it |
| P4 | `topo-tests-cube-door-roster-is-a-hand-written-partition-claim` | E | cube_doors_agree.rs's door roster is hand-written; a new builder in common joins it silently as nothing |
| P4 | `topo-tests-mapped-cube-call-sites-that-are-brick-with-ceremony` | E | Nine mapped_cube call sites pass a diagonal affine map, which is brick spelled the long way |
| P4 | `topo-tests-notched-beams-fixture-is-verbatim-in-two-files` | E | The four-box notched_beams fixture is verbatim in crosslap_rest and m5_s1_rest_zip |
| P4 | `topo-tests-self-declared-fixture-copies-census` | E | A prose census of self-declared fixture copies in crates/topo/tests |
| P4 | `topo-tests-straddle-seat-hand-copies` | E | common::straddle_seat claims one shared builder and at least two suites hand-copy it, plus a five-way crossing-post profile |
| P4 | `topo-tests-unit-cube-has-fifty-spellings` | E | The unit cube is spelled 49 times across 19 suites, and three conventions for naming a nullary brick wrapper now coexist |
| P4 | `viewer-error-display-prose-bans-one-identifier-per-arm` | E | viewer's error_display prose helper bans one identifier per arm, so a sibling leak passes at 29 call sites |

## Order

**By file, not by row.** Every unit here is "give this shape one home
and convert its call sites", so the cheapest order is whichever suite
you are already editing. `topo-tests-unit-cube-has-fifty-spellings` is
the largest single population (49 spellings, 19 suites, three
conventions) and should be the one that DECIDES the convention; the
others follow it rather than each inventing one.

Take these as drive-bys wherever possible (`work/README.md`, "The
tracker is not comprehensive"). Dispatching twenty PRs for twenty
fixture moves would cost more than the duplication does.

## Review posture

OPEN, for this program's first dispatch. TINT inherits protocol v7
(`docs/MODEL-AB-LOG.md`, Ev 2026-09-19): the dual on triaged-in units
only, opus/opus outside it. Nobody has re-asked the triage question for
this slate, so the first orchestrator answers it here rather than
inheriting an answer.
