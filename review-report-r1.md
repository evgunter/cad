# GUI-4 (PR #1113) — review R1, frozen head `75b0babf`

**VERDICT: APPROVE-WITH-FIXES**, conditional on hosted green (verified at step level below). Split, because it matters: as a *unit*, APPROVE-WITH-FIXES — the arithmetic I could check is correct and the typed vocabulary is clean. As **the program's acceptance evidence**, NOT-MERGEABLE-AS-IS: I ran the real gallery, and two of G3's four items are accepted-and-silently-inert on two of the three gallery assembly documents that have instances (M1), which the walk row cannot see. Fix M1, or stop calling the walk row acceptance evidence for the gallery.

Suite `crates/viewer/tests/review_gui4_r1.rs` (10 rows, promoted as-is); probe `crates/viewer/examples/r1_gallery_probe.rs` (an example, not a gate — it needs a directory only `demo-tour gallery` produces). Local: 213/213 `viewer/tests/all` green, clippy `--all-targets` clean, fmt clean.

## MAJOR

**M1 — hide and free-move are ACCEPTED and silently inert on an instance a `Pattern` consumes; G3 names this exact honesty rule.** `set_hidden`/`free_move_check` admit any live `InstantiatePart` (`display.rs:147,301,330`), but `scene_for` drops and displaces parts keyed by `part.node()` (`pick.rs:419-427`) over the PRODUCT ROOTS. A patterned instance is not a root — the `Pattern` above it is — so its geometry is emitted under a different id. The op succeeds, `is_hidden()`/`free_move_of()` report it in force, the tree checkbox flips, and nothing moves: the probe draws nothing displaced and `probe_parts` stays 0, i.e. the distinctness marking G3 calls an honesty requirement marks nothing at all. Measured on the real gallery, not hypothesised (`r1_gallery_probe` over `demo-tour gallery`): `3888f1…` (flat-pack) node 0 hide 60→60 triangles, probe `probe_parts=0`; `88b4110…` node 0 the same. The `Pattern` node itself refuses both `NotAnInstance`, so the four drawn posts have no display identity at all. Pinned by `r1_a_patterned_instance_accepts_hide_and_probe_and_neither_does_anything`.
**A class, not an instance** — "display state names a node the scene never draws". Second member: hiding the only instance makes `scene_for` return `EmptyMesh`; `app.rs:330` sets a status string and KEEPS the previous mesh, while `scene_display` was already advanced at `app.rs:308`, so the stale picture is permanent and marked current (seen on `9a839…`). Sweep obligation: every consumer of `DisplayView` keying on `RecipeNodeId`. A fix that touches only the patterned case is a half-fix.

**M2 — the mate tool's world→part pull-back is correct and wholly untested, the exit-demo walk included.** I replaced `placement.affine::<f64>().inverse()` (`matetool.rs:391`) with the identity and re-ran: **all 17 shipped assembly rows stayed green, `the_exit_demo_walk` among them.** The blindness is structural, not one weak assertion. Stage 9 and `mate_tool_flow.rs:142-153` assert `placed_a·alignment.a.origin == placed_b·alignment.b.origin` — but the solve DERIVES `placed_a` FROM `alignment`, so that identity holds for any alignment the tool mints, right or wrong; it is self-consistency, not a check. The one numeric part-coordinate pin (`mate_tool_flow.rs:115-119`) checks `origin[2] ≈ POST_HEIGHT`, and `post_b`'s placement is a z-free translation, so that component is invariant under a missing *or* reversed inverse. No fixture in the unit gives an instance a rotated placement, so the inverse's linear part is never exercised at all. The code is right — I re-derived it against my own `Rᵀ(q−t)` with a rotated instance; origin, axis and reference all agree to 1e-12. MAJOR on the evidence, not the code. The walk's 1e-12 reopen assertion is downstream of the same authored numbers and equally blind.

**M3 — the save-as resolver rebind is inert, and the PR body claims the opposite.** `save` (`session.rs:1268-1276`) rebinds the resolver and calls `request_eval()`; the PR says "a save into a different directory rebinds the resolver there **and re-evaluates (references may resolve differently)**". They cannot: `run_once` (`evalseam.rs:169-180`) primes every run from the previous completed evaluation, so an unchanged document's instantiate nodes hit the memo and the new resolver is never consulted. Saving the assembly into a directory holding no parts leaves `resolve_dir()` pointing there and **every tree row reading Ok** over geometry that directory cannot supply; reopening the file it just wrote badges all three. The PR's own memo-safety paragraph ("same pin, same bytes") is the correct explanation of why the rebind is inert — four lines from the claim it refutes. Same mechanism: a store that changes under an open session is never noticed in either direction, so "scan at RESOLUTION" holds of the *first* resolution only. Not a geometry hazard (pins verify), but it is the silent-divergence class the directory rule exists to prevent. Both pinned as shipped in `r1_save_as_…_inert` and `r1_the_memo_bounds_scan_at_resolution_…`.

## MINOR

- **m1** `admitted_classes()` (`matetool.rs:79-87`) hardcodes `[Rest, Tangent]` while the module doc says "the table is read, never restated" (`:37`). Probe: I added a third `ContactClass` variant — `topo` failed at 4 sites (its designed fence, `contact.rs:66-89`), and once those were filled in **`viewer` compiled clean**, silently omitting the class. Harmless today (wildcard ⇒ `NotAdmitted`); a silent omission the day a class is admitted. No `ContactClass::ALL` exists.
- **m2** `PickHit::point`'s map back to world (`pick.rs:499-503`) is dead *and* untested: dropping it left all 17 rows green, and `rg '\.point' crates/viewer/src` finds no consumer — `face_at_for` discards it. My rotated-probe row now asserts it.
- **m3** `app.rs:1363-1369` sets `fit_on_scene` unconditionally after `perform_batch`, which already sets it on a *successful* `Open` (`app.rs:372-378`). A refused CLI open still books a re-frame.
- **m4** `MateToolEvent` has no `Display`; `app.rs:283` prints `{event:?}` — raw Debug of a `FaceSelection` plus boxed `Resolution` — into the status line, alone among this unit's refusals. Same at `app.rs:1065`: `format!("admission: {:?}", …)` dumps `NoAtRestRecord { why: "…" }` with escaped quotes, when `no_record_reason()` exists for exactly this.
- **m5** `proposal` solves the CURRENT doc (`matetool.rs:379`) while `face_frame` reads the caller's `eval`. The app passes `landed_pair()`, so it is consistent; nothing in the type or docs requires it, and a mismatched pair mints an alignment against the wrong placement, silently.

## NOTE

n1 `DirResolver::resolve` opens a fresh `Workspace` per resolution (`docio.rs:74`), reading and header-parsing every `.pncad` in the directory — O(instances × dir size), and an evaluation is no longer resolved against one consistent snapshot. · n2 `FLAG_PROBE = 1` (`scene.rs:236`) is spelled `1u` in WGSL (`gpu.rs:787`) with no shader constant and no sentence saying they must agree — the shape `camera.rs:382` already names; `IdMap::NOTHING` is mirrored as `!= 0u` (`gpu.rs:790`) identically (pre-existing). One sweep of the shader. · n3 Four near-parallel pairs, three spellings of the modifier: `scene_for`, `pick_for`, `face_at_for`, `op_**under**`; none of the four bare forms has a production consumer any more. · n4 `pick.rs:9` still names `PickIndex::pick` as "the ray path". · n5 `prune` reports discarded committed probes but not an in-flight gesture it kills (`display.rs:426-436`), though the doc promises the caller need not infer. · n6 `commit_free_move` removes only on `is_identity_bits()`; a rotate-and-back probe stays marked. · n7 `AddMate` refuses on a document gesture but not a free-move gesture, which then dies in `prune`. · n8 There is **no `SessionOp` that sets a placement** — an unconstrained instance can be probed (display-only) or mated, never moved persistently; per G3's letter, but the gallery's flat-pack cannot be arranged and saved. · n9 Run 33128016123 drew {interval, **default** ε} and aborted after 938/2145 rows on the census; the green run drew {interval, 1e-6}. No run of this code has completed a default-ε draw — an abort, not the same rows at another ε.

## Style

Questions exercised: Q1–Q8 (Q8 on `app.rs`, 1373 lines, read end to end). None skipped.

- `matetool.rs:37,62` — "the table is read, never restated" / "verbatim from the kernel's table": a comment asserting an invariant nothing enforces, and false in the domain dimension (m1). Justified at unusual length beside a two-element array literal. **sure**.
- `gpu.rs:787` vs `scene.rs:236` — the same constant written twice, once as `FLAG_PROBE`, once as a bare `1u`, with a comment nearby discussing the flag without mentioning the copy. Plausibly one of several: I would look at every WGSL literal mirroring a Rust constant. **sure**.
- `pick.rs:389-600` — the `_for`/`_under` split doubled four methods to thread one argument, and the bare halves now exist only for older tests. I would have taken `&DisplayView` on the original four and let callers pass `DisplayView::none()`. The inconsistent suffix (`op_under` beside three `_for`s) is the tell that the pair was not designed as a pair. **likely**.
- `app.rs:1049-1114` (`mate_tool_ui`) clones the whole tool to read it, mutates draft indices, pushes ops from inside the closure, and threads `close` out of a nested `horizontal`. The awkward control flow does what a small `enum MatePanelAction` would do plainly. **likely**.
- `display.rs` module docs run 46 lines before the first item, of which the supersession argument is 12 lines defending a two-line `retain`. The argument is correct; its length is mild evidence it was contested, and the thing it defends is not the thing that broke (M1). **unsure**.
- `session.rs:284-290` — `Refusal::rank` grew a two-arm `Display(...)` special case with a comment explaining which display faults rank like document twins. A rank needing per-variant explanation wants to be a method on `DisplayFault`. **likely**.
- `common/asm.rs` — the fixture has no `Pattern`, no rotated placement, no pre-existing mate: it omits all three structural features of the real gallery, and two of my three MAJORs are exactly what that omission hides. **sure**.
- `app.rs:308` sets `scene_display` before the build that may fail, so a refused scene consumes its revision and is never retried. **sure** (mechanism), **likely** (that it is unintended).
- `assembly_display.rs:143-160` calls a directory it creates *inside* `bench.dir` a "sibling"; it is a child, ignored by `Workspace::open` for a different reason than the one the row's prose gives. **sure**, cosmetic.

## Code quality report

Deviations: **3 reported** (scan-at-resolution revision; the save-as judgment call; the gallery-shaped walk fixture) — disclosed clearly and in the right places. **3 silent**: the class-list restatement (m1), the inert rebind (M3 — reported as its opposite), the patterned-instance gap (M1, unnamed anywhere). Ratio 1:1, which is the number I would flag.

1. **Idiom / structure — 4/5.** Typed closed enums with per-arm `Display`, one home per piece of state, ops-as-values throughout; `ScenePart::probe` carrying displacement and marking in one field so neither can occur without the other is genuinely good design. Down for the four duplicated `_for`/`_under` methods and `mate_tool_ui`'s control flow.
2. **Test quality — 2/5.** Rows read well and the hide/free-move/pick-vanish rows have real teeth (mutating the *pick* inverse turns two red). But the unit's two hardest pieces of arithmetic are invisible to it: mutating the mate-tool inverse to the identity, and deleting the hit-point world map, each left 17/17 green. One fixture, no structural variety, and the acceptance row inherits every gap.
3. **Doc / comment honesty — 3/5.** Mostly excellent: `display.rs`'s "which history holds a committed free-move: none" and `docio.rs`'s directory-rule statement are exactly right and I verified both. Down for two load-bearing sentences false as written — "the table is read, never restated" and "re-evaluates (references may resolve differently)" — and for `pick.rs`'s header naming a superseded method.

## End-to-end exercise — scope and ergonomics

Driven headlessly as a consumer: build an assembly workspace through public doors (`Workspace`, `apply`, `DocEdit`), open through `SessionOp::Open`, resolve, hide, free-move (translation *and* rotation), the mate tool through both picks via the real cursor path to one committed edit, supersession, save/reopen. Adversarial: mate the same pair twice; two faces of ONE instance; free-move → hide → mate; an empty store; a store broken *after* open; save-as into a partless directory; a `Pattern` over an instance; the three gesture-order refusals; a bit-exact identity commit. Plus the real gallery: `demo-tour gallery` output opened document by document through the typed door — all 10 open, zero failed rows, a genuine positive result for the resolver.

**Ergonomics.** The typed vocabulary is pleasant to drive and `OpOutcome.superseded` is the right shape — I would not have thought to ask for it. Two things bit me as a consumer: there is no `SessionOp::SetPlacement`, so I could not arrange a rotated instance through the session at all and had to author the fixture from outside (n8); and `DisplayState::probing()` says a gesture is live but nothing says one was killed, so a caller learns it only from the next refusal (n5).

**Not exercised** (excused): the GPU pass, all widget chrome, the `rfd` dialog. Also unexercised by anyone and worth naming: `fit_on_scene`, the mate-tool panel, the instance panel, the tree hide checkbox — the PR lists these honestly.

## Claims disposition

1. RESOLVER — **partly refuted**: directory rule, typed per-node refusals naming the file, and no silent wrong-directory resolution all hold; the save-as rebind does not (M3); n1 names what the revision traded (no single consistent scan per evaluation). `Arc<dyn PartResolver>` is thread-safe (`PartResolver: Debug + Send + Sync`); absent-resolver behaviour for part documents unchanged.
2. HIDE — **upheld for root instances, refuted for patterned ones** (M1); never persisted, no history entry, tree and document keep it, rows go red — all independently verified.
3. FREE-MOVE — **upheld**, M1 excepted. Eligibility document-derived; `NonRigidFrame` typed; distinctness is a value. Inverse-ray math verified correct in both directions and it *has* teeth (mutation turns 2 rows red); the hit-point map does not (m2). Supersession tested as `None`, and the unrepresentable-state argument is sound: legality is a function of the document, so an identity entry for a constrained instance would be a lie the type cannot express.
4. MATE TOOL — **code upheld, evidence refuted** (M2). Frame composition re-derived independently and correct; exactly one `DocEdit` through the one commit door; `PickLost` typed, one step per lost pick. `admitted_classes()` is a partial restatement (m1).
5. EXIT WALK — **runs, reads well, has less teeth than it claims.** On the disclosed fixture deviation: **not sufficient**, for empirical rather than procedural reasons. The real stand document has all three instances mate-constrained (the free-move stage is unreachable there); the real flat-pack has M1's patterned instances; no single gallery assembly document exercises every G3 item plus the mate tool, which is what GUI-PLAN's Acceptance asserts. Something must additionally pin the real documents — not necessarily a `viewer` test: `examples/r1_gallery_probe.rs` is the shape, runnable by the render job that already produces the directory.
6. TANGENT — **commit-with-verdict is right; the verdict's lifetime is wrong.** A3 puts `Tangent` in the authored vocabulary and `ClassAdmission` exists to keep "the solve folds it" apart from "the mint has a record"; refusing at the tool collapses that distinction and makes the viewer narrower than the format. But the verdict shows only pre-commit, as a Debug dump (m4); after commit every row reads Ok and the viewer never calls `assemble`, so the user holds a green document the A5 gate rejects. Carry `NoAtRestRecord` onto the mate's own row. Pinned by `r1_every_offered_class_is_executable_and_a_tangent_commit_is_unassemblable`.
7. FAÇADE — **upheld**, coherent with the family texts; the census red was exactly this, and the fix trims the list to match a real change rather than weakening the check.
8. CI — **upheld, verified at step level myself.** 33128016123: one failed job, one failed test, `every_document_layer_root_export_is_carried_or_listed`, naming both names. 33128806185: success; `test (interval, eps = 1e-6, 1/2)` and `2/2` both success; fmt job step 10 "viewer toolkit rows - the filter's verdict" **success (not skipped)**, step 11 "clippy (viewer app feature - eframe + wgpu)" **success, 16 s**. See n9.
9. SWEEP — **upheld; its blind spot is not where the PR says.** 16 `EvalRequest {` sites (1 production + 15 fixed), 3 `ScenePart {` (1 + 2) — both structs are public-field with no `..Default`, so a missed site is a compile error and the literal sweep cannot under-run. My differently-shaped sweep ran over the display-view-less wrappers: every production site uses `scene_for`/`op_under`; the residue is n3.
10. CLI + untestable — **upheld.** `viewer [doc.pncad]` goes through `SessionOp::Open` via `perform_batch`, no second path; m3 is the one defect there. `ClassRefused` is genuinely unreachable (probe D confirms by construction). App-gated untested surfaces honestly listed.

## Isolation and glimpse disclosure

**Glimpse — disclosed.** GUI-4's implementer arm was exposed to me twice through *binding* reading, before I could avoid it: `docs/GUI-LOG.md`'s GUI-2 unit entry (last line, "GUI-4 dispatches now (<arm>, block GUI-B2 slot 1)") — the very defect the GUI-3 entry above it records a rule against — and the orchestrator's own comment on PR #1113, which quotes the `MODEL-AB-LOG.md` block record verbatim. I did not open `docs/MODEL-AB-LOG.md` or any A/B material, nothing here keyed on the arm, and no model identifier appears in anything I pushed. The rule recorded at GUI-3 did not hold for GUI-4.

Otherwise clean: I did not fetch, check out, or read `gui/gui-4-review-r2` or any artifact of it, nor any older `gui/gui-*-review-*` branch. Nothing posted to the PR. Branch `gui/gui-4-review-r1` from the frozen head; no trailer. Probe edits to `crates/topo` and `crates/viewer/src` were made in the working tree only, reverted, and `git status` verified clean before every commit.

## Cost

Tokens ≈ 360k. Wall clock ≈ 1 h 10 m. Build slot held only for the runs listed; no detached wait outlived its evidence, and none is outstanding at this writing.
